-- 경합을 **제대로** 고치기 전 마지막 실행이 남긴 1건을 치운다 (2026-09-10, BACKLOG §M-4·F-2).
--
-- ---------------------------------------------------------------- 🔴 세 번째다. 그리고 마지막이다.
--
-- V61(55건) · V62(1건) · V63(1건) — 같은 DELETE 가 세 번 나온다. **중복처럼 보이지만 아니다:**
-- 각각이 «그 시점의 잔재» 를 치운 기록이고, 🔴 **세 번 나온 이유가 곧 이 파일의 내용이다.**
--
--   V61 → 쌓여 있던 55건을 치웠다. **그런데 같은 실행에서 1건이 새로 생겼다.**
--   V62 → 그 1건을 치우고, 테스트 정리를 «2초 예산 폴링» 으로 고쳤다.
--         ⚠ **그것도 부족했다** — 전수 부하에서 비동기 알림이 2초를 넘겨 또 1건이 샜다.
--   V63 → 이 1건. 그리고 원인을 **시계에서 «실행기 상태» 로** 바꿨다.
--
-- ---------------------------------------------------------------- 🔴 왜 이번엔 안 재발하나
--
-- 앞의 둘은 **«얼마나 기다리면 되나» 를 추측**했다. 추측은 부하에 따라 틀린다.
-- 지금은 이벤트 실행기(`eventExecutor`)를 **빈으로 꺼내** «큐가 비었고 도는 것도 없다» 를
-- **실제 상태로** 묻는다 — `AFTER_COMMIT` 은 정리보다 먼저 지났으므로 작업은 이미 큐에 있고,
-- 큐가 비면 **더 올 것이 없다.** ⚠ 예산(5초)은 남아 있지만 **매달리지 않기 위한 것**이지
-- 판정 기준이 아니다.
--
-- ⚠ 배운 것: 「비동기를 기다린다」를 **시간으로 쓰면 반드시 언젠가 틀린다.**
--   기다릴 것이 있으면 **그것의 상태를 물을 수 있게** 만들어야 한다(그래서 실행기를 빈으로 꺼냈다).

DELETE FROM notification n
 WHERE n.link LIKE '/products/%'
   AND NOT EXISTS (
       SELECT 1 FROM product p
        WHERE p.id = HEXTORAW(REPLACE(REGEXP_SUBSTR(n.link, '[0-9a-f-]{36}$'), '-', ''))
   );
