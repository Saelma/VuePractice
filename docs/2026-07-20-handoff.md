# 2026-07-20 작업 정리 (묶음 1·2·2.5·3 — 이벤트 실사용 / 리뷰 마감 / 프론트 노출 / 관리자 동선)

> 직전 작업일(2026-07-16)에 이커머스 도메인 루프(member→catalog→cart→order→review/inquiry)를 닫고
> 도메인 이벤트 3층 구조(§9)·Flyway(§10)까지 도입했다.
> 오늘은 ①**그 이벤트 구조가 실제로 값을 하는지 증명**(묶음 1 — 재고 부족·주문 취소)하고,
> 이어서 ②**리뷰 도메인을 마감**(묶음 2 — 구매인증 버그·포토 리뷰·평균별점)하고,
> ③**그 결과를 화면에 붙이고**(묶음 2.5 — 별점 표시·포토 리뷰 UI), ④**관리자 동선을 열었다**
> (묶음 3 — 주문 목록 API·화면). ③에서 버그가 2개 더 나왔다(§7).
> 작업 기록(handoff). 규칙·설계 원본은 커밋된 `CLAUDE.md` / `ARCHITECTURE.md` 참고.

---

## 0. 오늘의 커밋

**묶음 1 — 도메인 이벤트 실사용** (§2·§3)

| 커밋 | 내용 |
|---|---|
| `da11546` | feat: 재고 부족 도메인 이벤트 — catalog 발행 + notification 구독 |
| `399f548` | feat: 주문 취소 도메인 이벤트(OrderCancelledEvent) |
| `52cc35f` | docs: 핸드오프 + ARCHITECTURE 이벤트 항목 갱신 |

→ 여기까지 **main 머지·origin push·운영 배포 완료**.

**묶음 2 — 리뷰 도메인 마감** (§6)

| 커밋 | 내용 |
|---|---|
| `5ba922a` | docs: 쿼리 작성 기준(QueryDSL vs JPQL) 명문화 + existsPurchase 버그 등록 |
| `b576455` | fix: existsPurchase가 ORDERED만 봐서 결제·발송 고객이 리뷰를 못 쓰던 버그 |
| `14cf79b` | feat: 포토 리뷰 — review에 image_group_id 연결 (**V3**) |
| `87149c4` | feat: 상품 목록 평균별점 — 이벤트 기반 비정규화 (**V4**) |

**묶음 2.5 — 프론트 노출 + 버그 2건** (§7)

| 커밋 | 내용 |
|---|---|
| `748ef38` | docs: 묶음 4에 V1__init.sql 부재 항목 추가 |
| `09b6d8b` | docs: 다음 할 일 재정리 — 진행 순서 + 묶음 3 상세화 |
| `e4dc57e` | feat: 별점 표시 + 포토 리뷰 UI, **이미지 업로드 권한 ADMIN→로그인** |
| `391759c` | fix: **관리자가 본인 주문의 결제·취소 버튼을 못 보던 버그** |

→ **전부 main 머지·push·백엔드/프론트 배포 완료.** 운영 검증 잔여물도 정리 완료(§7-4).

**묶음 3 — 관리자 동선** (§8)

| 커밋 | 내용 |
|---|---|
| `848899d` | docs: 묶음 2.5 기록 + 묶음 4에 고아 이미지 정리 등록 |
| `7562bfe` | feat: 관리자 주문 목록 API (**V5** — 구매자 닉네임 스냅샷) |

→ **배포 미실시**(프론트 화면 커밋 포함). 스키마 V5는 통합 테스트 때 이미 적용됨.
  `myOrders`가 List → PageResponse로 **응답 계약이 바뀌었으므로 백엔드·프론트를 함께 배포**해야 한다
  (백엔드만 올리면 기존 주문 내역 화면이 빈 화면이 된다).

- 스키마 메모: V3·V4는 통합 테스트를 돌리는 순간 Flyway가 공유 espdb에 적용한다.
  둘 다 추가·nullable/DEFAULT라 **구버전 jar가 도는 운영에 영향 없었다**(적용 후 :8080 200 확인).
  배포 전까지 "스키마만 앞서 있고 기능은 안 보이는" 상태로 안전하게 유지됐다.

---

## 1. 왜 이 작업인가 — 문서 대조로 나온 결론

작업 후보를 뽑을 때 7/16 핸드오프만 보지 않고 **7/15 핸드오프 + ARCHITECTURE**까지 대조했더니,
7/16 문서만으로는 안 보이던 **미완 잔여물**이 드러났다.

- 7/15 §10-2 **"재고 부족 알림"** — 주문 상태 확장만 되고 이 항목은 이월된 채 7/16 문서에서 사라짐.
- 7/15 §8 image — 설계는 *"여러 도메인(상품·**공지·리뷰**)이 image_group_id만 두면 재사용"* 인데
  **실제 연결은 product뿐**. 7/16에 리뷰가 생겼는데 이미지가 없어 설계 의도가 아직 미증명.
- 7/16 §9 이벤트 3층 — Handler가 **로그 stub 하나뿐**이라
  *"Handler 추가만으로 fan-out, OrderService 불변"* 이라는 주장이 검증되지 않은 상태.

→ 7/15가 남긴 *재고 부족 알림*이 7/16이 만든 *이벤트 구조*의 첫 실사용처로 정확히 들어맞는다.
**구조를 만든 직후가 검증에 가장 싼 타이밍**이라 이걸 1순위로 잡았다.

> **교훈**: 핸드오프를 최신 것만 읽으면 "직전 문서에서 조용히 빠진 이월 항목"을 놓친다.
> 후보 뽑기 전 최소 2일치 + ARCHITECTURE를 대조할 것.

---

## 2. 재고 부족 이벤트 (`StockRunningLowEvent`) — catalog 소유

### 설계 판단 — 왜 order가 아니라 catalog가 발행하나
주문 때문에 재고가 준다고 해서 order가 발행 주체인 건 아니다. **재고는 catalog 소유**이고,
관리자 상품 수정 등 주문 외 경로로 줄어도 같은 알림이 나가야 한다.
→ 발행 주체를 catalog로 두면 order 도메인은 이 알림의 존재를 **전혀 모른다**.
부수효과로 `OrderService`는 한 줄도 안 바뀌었다(§5 검증 참고) — §9가 주장한 "OrderService 불변"의 더 강한 형태.

### 구성 (3층 규약 그대로)
| 파일 | 층 | 역할 |
|---|---|---|
| `domain/catalog/event/StockRunningLowEvent` | 이벤트 | `DomainEvent` implements. `(productId, productName, remainingStock, threshold)` |
| `domain/catalog/config/CatalogProperties` | 설정 | `catalog.low-stock-threshold`(기본 5). **0=품절도 포함** |
| `domain/notification/StockEventListener` | 어댑터 | `@Async` + `@TransactionalEventListener(AFTER_COMMIT)`, **위임만** |
| `domain/notification/StockAlertHandler` | 주체 | 실제 로직(로그 stub). 품절/부족 문구 분기 |

- 발행 지점: `ProductCommandService.decreaseStock` — 차감 성공 후 잔여재고가 임계치 **이하**면 발행.
- `AFTER_COMMIT`인 이유: 주문이 롤백되면 재고도 롤백되므로 "실제로 줄지 않은 재고" 오알림을 막는다.

### ⚠ 트랩 — 벌크 JPQL UPDATE는 1차 캐시를 갱신하지 않는다 (중요)
- `decreaseStock`은 `@Modifying @Query("update Product p set p.stock = p.stock - :qty …")` 벌크 UPDATE다.
  **영속성 컨텍스트에 올라와 있는 `Product` 엔티티를 갱신하지 않는다.**
- checkout은 `@Transactional`이라 `cartService.getCart` 단계에서 이미 `Product`가 1차 캐시에 올라와 있다.
  이 상태에서 `findById`로 잔여재고를 읽으면 **차감 전 값(stale)** 이 나와 오알림/미알림이 난다.
- **해결**: 엔티티 대신 **스칼라 프로젝션**으로 DB를 직접 읽는다.
  ```java
  @Query("select new com.glassvue.domain.catalog.repository.StockSnapshot(p.name, p.stock)"
       + " from Product p where p.id = :id")
  Optional<StockSnapshot> findStockSnapshot(@Param("id") UUID id);
  ```
  (`clearAutomatically = true`는 컨텍스트 전체를 날려 다른 엔티티까지 영향 → 채택 안 함.)
- **계열**: 7/16 §6의 *Oracle CHECK 제약 + ddl-auto=update* 트랩과 같은 뿌리 —
  **"ORM/도구가 알아서 해줄 것"이라는 가정이 어긋나는 지점**. 실측 없이는 안 잡힌다.

### 부수 변경 — `@ConfigurationPropertiesScan`
`CatalogProperties`를 등록하려면 도메인마다 `@EnableConfigurationProperties`용 `@Configuration`이 필요해진다.
→ 메인 클래스에 `@ConfigurationPropertiesScan` 한 줄로 대체(도메인별 프로퍼티 레코드 자동 등록).
기존 `JwtProperties`(SecurityConfig의 `@EnableConfigurationProperties`)와 충돌 없음 — 같은 빈 이름이라 멱등.

---

## 3. 주문 취소 이벤트 (`OrderCancelledEvent`) — order 소유

`OrderPlacedEvent`와 **대칭 구조**. 이번엔 새 이벤트라 `OrderService.cancel`에 publish 한 줄이 불가피하게 추가된다.

- `domain/order/event/OrderCancelledEvent` — record + `from(Order)`
- `OrderEventListener.onOrderCancelled` — 어댑터, 위임만
- `OrderNotificationHandler.handle(OrderCancelledEvent)` — 오버로드

### 설계 판단 — 재고 복원은 이벤트에 담지 않는다
복원은 **취소 처리의 일부**(동기적으로 반드시 성공해야 함)지 후처리가 아니다.
알림처럼 best-effort(`@Async`, 앱 다운 시 유실)로 흘려보내면 안 되는 작업이라 `cancel()` 안에 그대로 둔다.

---

## 4. 자동화 테스트

- **백엔드 127개** 전부 통과. 7/16의 95 → **+32** (묶음 1: 101, 묶음 2: 119, 묶음 2.5: 121, 묶음 3: 127).
- 묶음 3 신규(§8): `AdminOrderListIntegrationTest`(6) — 비로그인 401 / USER 403 / ADMIN 200,
  상태 필터, 페이징, **사용자 목록 스코프 우회 불가**.
- 묶음 2 신규(§6):
  - `OrderRepositoryIntegrationTest`(6) — `existsPurchase` **상태별** 검증(ORDERED/PAID/SHIPPED=true,
    CANCELLED=false) + 타인·타상품 배제. 회귀 방지용.
  - `ReviewCommandServiceTest` 보강(8) — 포토 리뷰 그룹 생성/교체/제거, 집계 이벤트 발행값(반올림 포함),
    실패 시 미발행
  - `ReviewEventListenerTest`(1) — 위임 검증 / `RatingSyncHandlerTest`(3) — 이벤트 값 그대로 반영,
    0 복귀, 상품 삭제됨(0행) 시 무예외
- **트랩**: 집계 이벤트가 모든 성공 경로에 끼어들어 `statsByProduct` 스텁이 필요해졌다.
  테스트마다 넣지 말고 `@BeforeEach` + `lenient()`로 기본값을 깔고 필요한 테스트만 덮어쓰는 게 깔끔하다.
- 묶음 1 신규·보강:
  - `ProductCommandServiceTest` — 임계치 **초과 시 미발행** / 임계치 도달 / 품절(0) / **차감 실패(OUT_OF_STOCK) 시 미발행**
  - `StockEventListenerTest` — 위임 검증
  - `OrderEventListenerTest` — 취소 이벤트 위임 검증
  - `OrderServiceTest` — 취소 성공 시 발행 / SHIPPED 취소 실패 시 미발행
- **트랩**: `CatalogProperties`는 record(=final)라 `@Mock` 대신 **실제 인스턴스**를 쓰고
  `@InjectMocks` 대신 생성자로 직접 조립하는 게 깔끔하다.
- 실행: `env -u DB_HOST ./gradlew test`(단위 81만, 38 skip) /
  `set -a; . /home/ecstel/work/.env; set +a; ./gradlew test`(통합 포함 127).
  통합이 up-to-date로 건너뛰면 `--rerun-tasks`. 집계 확인:
  ```bash
  grep -ho 'tests="[0-9]*" skipped="[0-9]*" failures="[0-9]*" errors="[0-9]*"' \
    build/test-results/test/*.xml | awk -F'"' '{t+=$2;s+=$4;f+=$6;e+=$8} END{printf "tests=%d skipped=%d failures=%d errors=%d\n",t,s,f,e}'
  ```
  (`grep -l "failure"` 류로 세면 `failures="0"` 속성에 걸려 전부 실패로 보인다.)

---

## 5. 검증 (실측, dev :8081)

운영(:8080) 무중단을 위해 포트만 바꿔 dev 인스턴스로 확인. **공유 espdb라 검증 데이터는 생성 후 즉시 삭제**(`ZZ` 접두사).

### 재고 이벤트 — 3케이스
| 케이스 | 재고 | 결과 |
|---|---|---|
| 대조군 | 100 → 99 | 주문 알림만, **재고 알림 없음** ✅ |
| 임계치 도달 | 6 → 5 | `[재고] 재고 부족(stub) — remaining=5 threshold=5` ✅ |
| 품절 | 5 → 0 | `[재고] 품절` ✅ |

### fan-out + 취소 (한 흐름)
```
[nio-8081-exec-9] Order created: 019f7d1c-e0b4…
[event-1] StockAlertHandler        : [재고] 재고 부족(stub) — remaining=5 threshold=5
[event-2] OrderNotificationHandler : [알림] 주문 확인 발송(stub) …
[nio-8081-exec-1] Order cancelled: 019f7d1c-e0b4…
[event-2] OrderNotificationHandler : [알림] 주문 취소 안내 발송(stub) …
```
- **fan-out 실증**: 주문 1건에 Handler 2개가 **서로 다른 이벤트 풀 스레드**(`event-1`/`event-2`)에서 병렬 반응.
- **요청 스레드 분리**: 처리 스레드가 `nio-8081-exec-*` → `event-*`로 이동 = `@Async` 동작 확인.
- 취소 후 재고 6 복원 확인.

### §9 주장의 증명
- `git show --stat da11546` 에 **`OrderService.java`가 없다** — 재고 알림이 늘었는데 order는 무수정.
- 새 리스너·핸들러 모두 기존 3층 규약을 그대로 따름 → 규약이 실제로 재사용 가능함을 확인.

---

## 6. 묶음 2 — 리뷰 도메인 마감

### 6-1. 🐞 `existsPurchase`가 `ORDERED`만 보던 버그 (`b576455`)

7/16에 주문 상태를 ORDERED→PAID→SHIPPED로 확장하면서 **같이 못 고친 흔적**.
`existsPurchase`는 `OrderService.hasPurchased` → `ReviewCommandService.create`의 리뷰 작성 자격
게이트(`REVIEW_NOT_PURCHASED`)에만 쓰이므로, **결제하고 배송까지 받은 고객이 리뷰를 못 쓰는** 상태였다.

- 수정: `status in (ORDERED, PAID, SHIPPED)` — **CANCELLED만 제외**.
- `<> CANCELLED`가 아니라 **명시적 열거**로 둔 이유: 나중에 추가될 상태(환불 등)가 자동으로
  "구매함"에 포함되지 않도록 **opt-in**으로 만들기 위함. 새 상태는 이 쿼리를 직접 고쳐야 한다.
- `OrderRepositoryIntegrationTest` 신설(6개) — 상태별 true/false + 타인·타상품 배제.
  기존 `OrderServiceTest.hasPurchased_delegates`는 **리포지토리 위임만** 검증해 이 버그를 못 잡았다.
  → **교훈**: "위임한다"만 검증하는 단위 테스트는 위임받은 쪽의 조건이 틀린 걸 절대 못 잡는다.
  쿼리 조건이 결과를 가르는 메서드는 통합 테스트로 막아야 한다.

> 발견 경위: 평균별점 작업 중 `OrderRepository`를 읽다 눈에 걸렸다. 요청받은 작업은 아니었지만
> 리뷰 도메인 마감의 전제라 같이 처리했다.

### 6-2. 포토 리뷰 — `image_group_id` (`14cf79b`, **V3**)

7/15 §8이 설계한 *"여러 도메인이 `image_group_id`만 두면 이미지 재사용"* 구조의 **두 번째 사용처**.
그동안 catalog(Product)만 쓰고 있어 설계 의도가 미증명 상태였다.

- `Review.imageGroupId`(RAW(16), nullable) — Product와 동일하게 **FK 없는 느슨한 UUID 참조**.
  image 도메인 엔티티를 직접 참조하지 않고 `ImageService` 공개 API로만 다룬다.
- 요청 DTO에 `imageIds`(최대 5장). **수정은 새 그룹으로 통째 교체**(`Product.update`와 같은 간단화,
  빈 목록이면 `createGroup`이 null을 반환해 이미지 제거).
- `ReviewResponse`는 `imageGroupId`를 감추고 `images`만 노출(`ProductResponse`와 같은 규약).
  **단일 인자 `from(Review)`는 제거** — 호출부가 이미지 조회를 빠뜨리고 조용히 빈 배열을 내보내지 못하게.
- 목록은 `findByGroups`로 **일괄 조회**(N+1 회피) — `ProductQueryService.search`와 동일한 패턴.
- `V3__add_review_image_group.sql` — `ddl-auto=validate`라 **마이그레이션 없이는 앱이 뜨지 않는다**.

### 6-3. 상품 목록 평균별점 — 이벤트 기반 비정규화 (`87149c4`, **V4**)

**처음 계획(A안)을 버리고 B안으로 바꾼 작업.** 판단 근거를 남겨둔다.

| 안 | 방식 | 문제 |
|---|---|---|
| A | `ProductQueryService` → `ReviewQueryService` 직접 호출 | 기존 `review → catalog`와 합쳐져 **catalog ↔ review 순환** |
| B ✅ | review가 이벤트로 집계를 밀어넣고 catalog가 저장 | 의존은 `review → catalog` 한쪽뿐 |

A안은 스프링 빈 순환은 아니라 앱은 정상 기동한다(`ReviewQueryService` ≠ `ReviewCommandService`).
하지만 **도메인 레벨 순환**이 생겨, MSA 전환 시 두 서비스가 서로를 동기 호출하는 구조가 된다
— CLAUDE.md의 *"domain 하위를 폴더째 분리 가능해야 함"* 이 깨진다.

**핵심 설계 — 집계값을 이벤트에 실어 보낸다.**

```
[review] create/update/delete
   └─ statsByProduct로 재계산 → publish ReviewRatingChangedEvent(productId, avg, count)
              │  AFTER_COMMIT + @Async
              ▼
[catalog] ReviewEventListener(위임만) → RatingSyncHandler(진짜 주체)
   └─ product.avg_rating / review_count 벌크 UPDATE + products:list 캐시 evict
```

- `productId`만 보내면 구독자(catalog)가 평균을 구하려고 **review를 되물어야 해서 순환이 되살아난다.**
  리뷰는 review 소유이니 **집계도 review가 계산해 넘기고** catalog는 받아 쓰기만 한다.
- 집계 쿼리는 JPQL이라 Hibernate가 실행 직전 **자동 플러시** → 방금 save/delete한 변경이 반영된 값이 나온다.
- **캐시 무효화를 Handler에 둔 이유**: `products:list`는 catalog 소유 캐시고 응답에 별점이 들어가므로
  값이 바뀌면 catalog가 스스로 비워야 한다. review는 이 캐시의 존재를 모른다.
- `ReviewStats.roundedAverage()`로 반올림을 한곳에 모음 — 상품 목록과 리뷰 상세가 같은 값을 쓰도록.

**V4 마이그레이션에서 놓치기 쉬운 것 — 백필.**
컬럼만 추가하면 기존 상품이 전부 별점 0이 되고, **해당 상품에 리뷰가 새로 달릴 때까지 영영 0으로 남는다**
(이벤트는 "변경 시점"에만 오므로). 그래서 V4에 기존 리뷰 집계 `UPDATE`를 함께 넣었다.
적용 후 검증: `지바` 비정규화 4.5/2 = 실제 집계 4.5/2 일치.

> Oracle 타입 메모: `DOUBLE PRECISION`으로 선언하면 Oracle이 `FLOAT`으로 만들고 Hibernate `validate`를 통과한다.

**실측(dev :8081, P6SPY)** — 리뷰 작성→수정→삭제에 따라
`update product set avg_rating=4.0 → 2.0 → 0.0`이 **`event-*` 스레드에서 각자 트랜잭션으로 커밋**.
요청 스레드(`nio-8081-exec-*`)는 리뷰만 저장하고 즉시 응답. catalog가 review를 되묻는 쿼리는 **0회**.

### 6-4. 리뷰 목록 캐시 — **보류**(넣지 않기로 결정)

7/16 §7-2에 남아 있던 항목이지만, 지금 넣지 않는 게 맞다고 판단했다.

- 리뷰 목록은 `products:list`·`notices:list`와 **성격이 다르다**. 상품/공지 목록은 모든 방문자가
  같은 첫 페이지를 보지만, 리뷰 목록은 **상품별로 갈라져** 상품 수만큼 캐시 키가 생긴다.
  TTL 60초에 히트율은 낮고 Redis 메모리만 쓴다.
- 정작 비쌌던 "상품 목록에서 상품마다 평균별점 계산"은 **§6-3의 비정규화로 이미 없앴다** —
  캐시로 가리려던 문제 자체가 사라졌다.
- 넣으면 무효화 대상만 하나 더 는다(리뷰 CUD → 리뷰 캐시 evict + 별점 이벤트 → 상품 캐시 evict).

→ **P6SPY로 리뷰 목록이 실제 병목으로 측정되면** 그때 넣는다. "현 단계에 실효 없는 도구는
로드맵으로 미룬다"는 기존 판단(Sentry 보류)과 같은 기준.

### 6-5. 부수 — ARCHITECTURE에 쿼리 작성 기준 명문화 (`5ba922a`)

"고정 쿼리인데 왜 QueryDSL을 안 썼나"라는 질문이 나와서, 암묵적이던 컨벤션을 문서화했다.
판단 기준은 **조건이 런타임에 바뀌는가** 하나 — 동적이면 QueryDSL, 고정이면 JPQL `@Query`.
함께 기록한 것: JPQL 생성자 표현식의 **문자열 클래스 경로** 약점(DTO 개명 시 컴파일은 통과하고
런타임에 실패), 그리고 §2의 벌크 UPDATE 트랩과 `clearAutomatically`를 쓰지 않는 이유.

---

## 7. 묶음 2.5 — 프론트 노출 + 그 과정에서 나온 버그 2건

백엔드는 배포됐는데 화면이 새 필드를 안 써서 **사용자 눈엔 아무것도 안 보이던** 상태를 해소.
그런데 실제로 붙여보니 **버그가 두 개 나왔다 — 둘 다 단위 테스트로는 잡히지 않는 종류**였다.

### 7-1. 공용 컴포넌트 2개 추출 (`e4dc57e`)

- **`StarRating.vue`** — 별점 UI가 `ProductReviews.vue`에만 이미 세 벌(입력·개별표시·평균요약)
  중복돼 있었고, 상품 목록·상세까지 붙이면 다섯 벌이 될 판이라 하나로 모았다.
  - **리뷰 0개면 "★ 0.0"이 아니라 "리뷰 없음"** 으로 표시한다. 비정규화 기본값이 0이라
    그냥 찍으면 "최악 평가"처럼 보인다. 값이 같아도 의미가 다르면 구분해야 한다.
- **`ImageUploader.vue`** — `ProductFormView`에 인라인이던 업로드 흐름을 추출(포토 리뷰가 두 번째 사용처).
  최대 장수 초과분은 **업로드 전에** 잘라낸다 — 올린 뒤 거절하면 서버에 고아 이미지가 남는다.

적용: `ProductListView`(별점 컬럼) · `ProductDetailView`(헤더 평균별점) ·
`ProductReviews`(사진 첨부 최대 5장 + 목록에 사진 표시).
**수정 폼은 기존 이미지를 들고 시작**한다 — 서버가 "보낸 목록으로 통째 교체"라 빠뜨리면 사진이 삭제된다.

### 7-2. 🐞 이미지 업로드가 ADMIN 전용이라 포토 리뷰가 아예 불가능했다 (`e4dc57e`)

백엔드·프론트를 다 만들고 E2E를 돌리자 `AUTH-403`. `SecurityConfig`에
`POST /api/images`가 `/api/products`·`/api/categories`와 한 줄로 묶여 `hasRole("ADMIN")`이었다.

- **상품 이미지가 유일한 용도였을 때 만든 규칙**이라 그때는 옳았다. 포토 리뷰를 만들면서
  아무도 이 줄을 보지 않았다 — 서비스 계층 단위 테스트로는 절대 안 잡히고
  **실제 HTTP 요청을 보내야만** 드러난다.
- `authenticated()`로 변경. 업로드된 이미지는 그룹에 붙기 전까진 무해하고, 실제 노출은
  리뷰 작성(구매 인증)·상품 등록(ADMIN)에서 걸린다. 크기 제한 5MB/10MB는 기존대로.
- `ImageUploadAuthIntegrationTest`(2) — USER 업로드 성공 / 비로그인 401.
  **권한 규칙은 조용히 되돌아가기 쉬운 곳**이라 테스트로 고정했다.
- 부작용: 고아 이미지가 쌓일 여지가 생겼다 → §8 묶음 4에 정리 항목 등록.

### 7-3. 🐞 관리자가 본인 주문의 결제·취소 버튼을 못 봤다 (`391759c`)

운영 검증 잔여 주문을 정리하려는데 **취소 버튼이 안 보여서** 발견.
`OrderDetailView`의 구매자 액션이 통째로 `v-if="!isAdmin"`으로 묶여 있었다.

- **"관리자는 구매하지 않는다"는 잘못된 전제.** 백엔드엔 이런 제약이 없다 —
  `pay`/`cancel`은 `findByIdAndMemberId`로 **본인 주문이면 role 무관하게** 허용한다.
  즉 백엔드는 소유 기준, 프론트는 역할 기준이라 어긋나 있었다.
- 수정: `v-if="isMine"`(소유 여부)로 변경 — 백엔드 규칙과 일치시킨다.
  관리자 발송 버튼은 그대로 role 기준(그건 실제로 역할 기반이 맞다).
- `OrderResponse`에 **`memberId` 추가** — 화면이 소유 여부를 판단할 근거가 아예 없었다.
  `OrderFlowIntegrationTest`에 응답 계약으로 고정(빠지면 버튼이 조용히 사라진다).

> **교훈**: 권한·소유 판단이 프론트와 백엔드 양쪽에 있으면 **기준이 갈리는 순간 버그**다.
> 프론트는 백엔드가 실제로 쓰는 기준(여기선 소유)을 그대로 따라야 한다.
> 그리고 오늘 나온 버그 3개가 전부 **E2E·실사용 시도에서** 나왔다 — 단위 테스트는 계층 안만 본다.

### 7-4. 운영 잔여 데이터 정리 완료

`재고 5 테스트` 상품 + 그 상품의 `ORDERED` 주문 3건.
앱의 정상 경로만 사용 — **주문 3건 취소(본인 계정) → 상품 삭제** 순서로 처리했다.
취소 시 재고가 3 → 6으로 복원되는 것까지 확인(취소-재고복원 동작의 운영 실증).
주문은 `CANCELLED` 이력으로 남는다(앱에 주문 삭제 기능 없음 — 의도된 설계).

---

## 8. 묶음 3 — 관리자 동선 (`7562bfe` 외)

**문제**: 관리자가 주문을 목록으로 볼 방법이 없었다. ADMIN이 쓸 수 있는 건 `ship`(발송)과
`get`(상세)뿐인데 **둘 다 주문 id를 이미 알아야** 호출된다 → 실제로 발송하려면 DB를 직접 뒤져야 했다.
이커머스 흐름(주문→결제→**발송**)에서 발송만 사람이 못 쓰는 상태(7/16 §7-4에서 이월).

### 8-1. 경로는 `/api/admin/orders`로 분리

| 안 | 결정 |
|---|---|
| 같은 경로에서 role 분기 | ✗ — 같은 URL이 호출자에 따라 다른 데이터를 주고, 구매자 정보를 넣으면 일반 사용자 응답에도 섞이거나 조건부 마스킹이 필요해진다 |
| `/api/admin/orders` 분리 | ✅ |

`SecurityConfig`에 **`/api/admin/**` 한 줄**로 막는다 — 관리 엔드포인트가 늘어도 개별 매처를
빠뜨릴 수 없다. §7-2의 이미지 업로드 권한 사고와 같은 계열을 구조로 예방하는 것.
매처 순서 주의: `/api/orders/**` 보다 **먼저** 와야 한다.

### 8-2. 구매자 닉네임은 **스냅샷** (V5)

관리자 목록은 "누구 주문인지"를 알아야 쓸모가 있는데 `OrderResponse`엔 `memberId`(UUID)뿐이었다.

- **조회 방식(member 배치 조회)을 버린 이유**: `MemberService.withdraw`가
  `memberRepository.delete()` — **하드 삭제**다. 탈퇴하면 회원 row가 사라져
  과거 주문의 구매자를 영영 알 수 없게 된다. 주문은 CS·배송 이력이라 시점 기록이 남아야 한다.
  `Review.author`가 같은 방식의 선례.
- **V5는 3단계**: `nullable로 추가 → 백필 → NOT NULL 확정`.
  기존 행이 있어서 바로 `NOT NULL`로 만들 수 없다. 탈퇴자는 `'(탈퇴한 회원)'`으로 채운다.
  (실제 백필 결과엔 탈퇴자가 없어 전부 실제 닉네임이 들어갔다.)

### 8-3. 스코프를 조건에 담아 리포지토리는 하나로

`OrderSearchCondition.memberId`는 **클라이언트 입력이 아니라 서버가 채우는 스코프**다
(`@Schema(hidden)` + `scopedTo()` / `forAll()`). 사용자 목록은 본인 id로 덮어쓰므로
`?memberId=...`를 넘겨 남의 주문을 보는 게 불가능하다 — 테스트로 고정했다.
덕분에 `search()` 하나로 사용자·관리자 목록을 모두 처리한다.

### 8-4. ⚠ N+1 — 컬렉션 fetch join은 페이징과 같이 못 쓴다

목록이 `Order.items`를 읽으므로 그냥 두면 주문마다 쿼리가 하나씩 더 나간다.
그렇다고 `fetch join`을 걸면 Hibernate가 **전체를 메모리로 올려 페이징**한다(HHH000104).
→ `@BatchSize(size = 100)`로 페이지 전체의 items를 **IN 쿼리 한 번**에 가져온다.

### 8-5. `myOrders` 페이징 전환 (계약 변경)

`List<OrderResponse>` 통째 반환 → `PageResponse`. 주문이 쌓이면 터지는 구조였다.
**프론트 `OrderListView.vue`도 같이 고쳐야 한다** — 안 고치면 목록이 빈 화면이 된다.

### 8-6. 관리자 화면 — 기본 필터를 PAID로

`OrderAdminView.vue`. 관리자가 이 화면에 오는 이유가 **"발송할 주문 찾기"** 라
기본 필터를 `PAID`(결제완료=발송 대기)로 둔다. 목록에서 바로 발송 버튼.
`CategoryAdminView`의 관리자 화면 패턴 + `ProductListView`의 `CustomStore` 서버 페이징을 따랐다.

### 8-7. 검증 (dev :8081)

| 항목 | 결과 |
|---|---|
| 권한 | 비로그인 401 / USER 403 / ADMIN 200 |
| 구매자·요약 | `김철수` / `ZZORD-검증` |
| 상태 필터 | PAID만 반환 |
| 페이징 | size=2 → content 2 |
| 발송 처리 | 200 → SHIPPED |
| 내 주문 | PageResponse 구조 확인 |
| 스코프 우회 | `?memberId=남의id` 넘겨도 본인 것만 |

### 8-8. ⚠ 검증 데이터 정리 — 절차상 문제를 하나 발견했다

E2E가 **한글 쿼리스트링을 URL 인코딩 없이** 보내 중간에 죽었고(`curl -G --data-urlencode` 필요),
정리 단계를 못 타서 주문이 남았다. 게다가 **발송(SHIPPED) 처리한 주문은 취소가 불가능**해
정상 경로로는 지울 방법이 아예 없었다(주문 삭제 API 없음).

- 결국 오늘 E2E로 쌓인 ZZ 주문 13건을 **DB에서 직접 삭제**했다(사용자 승인).
  안전장치로 *품목이 전부 ZZ인 주문만* 골랐고, 혼합 주문 0건을 먼저 확인했다.
- **다음부터 E2E 규칙**:
  ① `trap`으로 스크립트가 죽어도 정리가 돌게 할 것
  ② **되돌릴 수 없는 조작(발송)은 검증 스크립트에서 하지 말 것**
  ③ "상품은 삭제, 주문은 취소만" 절차는 **주문 레코드가 계속 쌓인다** — 검증 후 남는 주문을 매번 보고할 것
  ④ 한글 파라미터는 `curl -G --data-urlencode`

---

## 9. 다음 할 일

7/14~7/16 문서 + ARCHITECTURE 대조로 뽑은 **묶음** 단위. 오늘 묶음 1·2·2.5 완료.

1. ~~**묶음 1 — 이벤트가 실제로 값을 하게 만들기**~~ ✅ **완료**(§2·§3)
2. ~~**묶음 2 — 리뷰 도메인 마감**~~ ✅ **완료**(§6, 배포 완료). 리뷰 목록 캐시만 근거를 남기고 **의도적 보류**(§6-4)

2.5. ~~**묶음 2.5 — 프론트에 묶음 2 결과 노출**~~ ✅ **완료**(§7, 배포 완료)

   백엔드는 배포됐는데 **화면이 새 필드를 안 써서 사용자 눈엔 아무것도 안 보이는** 상태다.
   방금 만든 기능을 실제로 쓰이게 만드는 게 우선.
   - **별점 표시** — `ProductListView`·`ProductDetailView`에 `averageRating`·`reviewCount` 노출.
     작업량 적고 효과는 즉시 보임. 리뷰 0개(`0.0 / 0`)일 때 "별점 0점"이 아니라 **"리뷰 없음"** 으로
     보이게 처리할 것(비정규화 기본값이 0이라 구분이 안 되면 오해를 부른다).
   - **포토 리뷰 업로드 UI** — 리뷰 작성/수정 폼에 이미지 첨부(최대 5장).
     업로드는 기존 이미지 API로 id를 받은 뒤 `imageIds`로 전달하는 2단계
     (`ProductFormView`의 상품 이미지 업로드 흐름을 그대로 참고).
   - 리뷰 목록에 첨부 이미지 표시.

2.6. **운영 잔여 데이터 정리** — 배포 검증 때 만든 상품 `재고 5 테스트`(재고 3) 삭제.

3. ~~**묶음 3 — 관리자 동선**~~ ✅ **완료**(§8). 아래는 당시 계획 — 실제 결정은 §8 참조

   **문제**: 관리자가 주문을 **목록으로 볼 방법이 없다.** 현재 ADMIN이 쓸 수 있는 건
   `ship`(발송)과 `get`(상세)뿐인데 **둘 다 주문 id를 이미 알아야** 호출된다.
   → 실제로 발송 처리를 하려면 DB를 직접 뒤져 id를 복사해와야 한다.
   이커머스 흐름(주문→결제→**발송**)에서 발송만 사람이 못 쓰는 상태(7/16 §7-4).

   - **① `GET /api/orders` (ADMIN, 목록)**
     - 상태 필터(`ORDERED`/`PAID`/`SHIPPED`/`CANCELLED`) — 실무에선 "결제됐고 아직 발송 안 된 것"만
       보는 경우가 대부분이라 이게 핵심 기능
     - 페이징 + 정렬
     - 조건이 런타임에 갈리므로 **QueryDSL**(`OrderRepositoryCustom` + `Impl` 신설).
       ARCHITECTURE의 「쿼리 작성 기준」 그대로
     - **결정 필요**: 사용자용 내 주문 목록과 경로가 겹친다 → `/api/admin/orders`로 분리할지,
       같은 경로에서 role로 갈릴지
   - **② 관리자 주문 목록 화면** — `CategoryAdminView.vue`라는 관리자 화면 선례가 있으니 그 패턴을 따른다.
     목록에서 바로 발송 버튼 → `ship` 호출
   - **③ 곁다리: `myOrders`에 페이징이 없다** — `List<OrderResponse>`를 통째로 반환한다
     (`OrderService.myOrders` → `findByMemberIdOrderByCreatedAtDesc`). 주문이 쌓이면 그대로 터진다.
     ①에서 QueryDSL 리포지토리를 어차피 만드니 **같이 페이징으로 바꾸는 게 효율적**.
     단 프론트 `OrderListView.vue`의 응답 처리(`List` → `PageResponse`)도 함께 고쳐야 한다.
   - 참고: 백엔드 `get()`의 ADMIN 전체 조회는 **이미 구현돼 있다** — 목록만 없다.
4. **묶음 4 — 잔챙이 청소**
   - 🕳 **`V1__init.sql` 부재 — 빈 DB에서 처음부터 구축이 불가능하다**
     - 현재 `baseline-on-migrate: true` + `baseline-version: 1`이라, V1은 **실제 SQL 파일이 아니라
       "기존 espdb를 V1이라고 치자"는 마킹**뿐이다(`ddl-auto=update`로 만들어져 있던 스키마).
     - 그래서 기존 espdb에는 V2~V4가 잘 쌓이지만, **빈 DB에서는 V2의
       `CREATE INDEX ... ON review`가 테이블이 없어 실패한다.**
     - 지금은 DB가 하나뿐이라 안 아프지만, **개발 DB 분리 / 테스트 격리(Testcontainers 등) /
       서버 이전·재구축** 중 하나라도 하면 즉시 막힌다.
     - 해결: Hibernate `jakarta.persistence.schema-generation`으로 현재 스키마를 덤프해
       `V1__init.sql`로 추가(약 30분). 주의 — **기존 DB의 히스토리는 이미 V1을 "마킹"으로
       기록해 두었으므로**, 파일을 추가해도 기존 DB에서 재실행되지 않게 체크섬 처리를
       확인할 것(`flyway repair` 또는 baseline 유지 여부 판단 필요).
     - `db/migration/README.md`에도 이 공백이 이미 적혀 있다.
   - 🖼 **고아 이미지 정리 — 업로드했지만 어디에도 안 붙은 image row/파일**
     - 업로드는 2단계다(①`POST /api/images`로 올려 id를 받고 ②저장 시 `imageIds`로 전달).
       ①만 하고 ②를 안 하면 **어느 `image_group`에도 속하지 않은 row + 파일**이 남는다.
     - 지금까지는 업로드가 ADMIN 전용이라 사실상 안 쌓였다. **2026-07-20에 포토 리뷰 때문에
       일반 사용자에게 열면서**(§7-2) 쌓일 여지가 생겼다 — 사용자가 사진을 올렸다가
       리뷰 등록을 취소하면 그대로 고아가 된다.
     - 상품 수정도 같은 경로다 — `createGroup`이 **매번 새 그룹으로 교체**하므로
       이전 그룹에 붙어 있던 이미지들이 참조를 잃는다(row는 남고 group만 갈아탐).
     - 할 일: ①고아 판별 기준 확정(`image_group_id IS NULL` + `created_at`이 일정 시간 경과)
       ②정리 수단 — 배치(Spring Batch는 아직 미도입이라 `@Scheduled`로 충분) 또는 관리자용
       삭제 API ③**DB row와 실제 파일을 함께** 지울 것(`FileStorageService`에 삭제 경로 필요)
     - **테스트 픽스처**: 2026-07-20 배포 검증 때 만든 고아 이미지 1건을 **일부러 남겨두었다** —
       정리 기능을 만들 때 대상으로 쓸 것.
       `image.id = 019f7e48-8790-74a0-922d-b4b34ccd82d0`, 파일 `/uploads/f111cb16-de45-46df-a87f-a6ee9dd2d375.png`
       (1x1 PNG, 어느 그룹에도 안 붙음). 화면 어디에도 안 보이고 동작에 영향 없음.
   - 캐시 직렬화 deprecated API 정리(7/15 §9 — `GenericJackson2JsonRedisSerializer(mapper)`)
   - 닉네임/카테고리 정책 확정(7/15 §10-4)
   - HSTS 헤더 / self-signed → mkcert(7/16 §7-3)
5. (이후) Handler 추가 — 포인트 적립(`point` 도메인) 등. 지금은 알림 stub만 있음.
6. (보류 유지) PG 결제 = MSA 단계 + PG사 확정 시 / Sentry·관측 스택 / Docker·RabbitMQ

---

## 현재 상태 (참고)

7/16 대비 **바뀐 것만** 표기. 나머지는 `2026-07-16-handoff.md` 참조.

- **도메인 이벤트**(4종):
  - `OrderPlacedEvent`(checkout) → `OrderEventListener` → `OrderNotificationHandler`
  - **`OrderCancelledEvent`(cancel)** → 같은 리스너·핸들러(오버로드)
  - **`StockRunningLowEvent`(catalog, decreaseStock)** → `StockEventListener` → `StockAlertHandler`
  - **`ReviewRatingChangedEvent`(review, CUD)** → catalog `ReviewEventListener` → `RatingSyncHandler`
  - 전부 `@Async`(`event-*` 풀) + `AFTER_COMMIT`. 인프로세스라 **best-effort**(유실 금지는 아웃박스/RabbitMQ).
  - 도메인 의존은 **비순환 유지**: `review → catalog`, `review → order`, `order → catalog`, `cart → catalog`.
    catalog는 어느 도메인도 호출하지 않는다(이벤트만 받는다).
- **설정**: `catalog.low-stock-threshold: 5` 신규(`application.yml`). 메인 클래스에 `@ConfigurationPropertiesScan`.
- **테스트**: 백엔드 **127개**(조건부 `@EnabledIfEnvironmentVariable(DB_HOST)`). 프론트 15(Vitest, 변동 없음).
  `.vue` 컴포넌트 테스트는 여전히 0개(DevExtreme 마운트 제외 방침) — 오늘 버그 2개가
  프론트/계약 쪽에서 나온 걸 감안하면 **재고할 만한 지점**이다.
- **DB/스키마**: Flyway **V1(baseline) + V2 + V3 + V4**. V3·V4는 오늘 적용됨(공유 espdb).
  - V3: `review.image_group_id` RAW(16) nullable
  - V4: `product.avg_rating` FLOAT / `product.review_count` NUMBER, 둘 다 DEFAULT 0 NOT NULL + 기존 리뷰 백필
  - V5: `orders.buyer_nickname` VARCHAR2(50) NOT NULL(구매자 스냅샷) + `idx_orders_status_created`
- **캐시**: `products:list`, `notices:list` 2개(TTL 60초). 리뷰 목록 캐시는 **의도적 미도입**(§6-4).
  `products:list`는 이제 상품 CUD뿐 아니라 **리뷰 CUD(별점 변경)로도** 무효화된다(`RatingSyncHandler`).
- **보안 규칙 변경**: `POST /api/images`가 **ADMIN → 로그인(authenticated)** 으로 완화(§7-2).
  포토 리뷰의 전제. `ImageUploadAuthIntegrationTest`로 고정.
- **API 계약 변경**:
  - `OrderResponse`에 **`memberId` 추가**(§7-3) — 화면의 소유 판단 근거.
  - **`GET /api/orders`가 `List` → `PageResponse`**(§8-5). 프론트 동시 배포 필요.
  - **`GET /api/admin/orders` 신설**(ADMIN, 상태·구매자 필터·페이징).
- **보안 규칙 추가**: `/api/admin/**` → `hasRole("ADMIN")` 한 줄(§8-1). 매처 순서상 `/api/orders/**` 앞.
- **프론트**: 공용 컴포넌트 2개 신설(`StarRating.vue`·`ImageUploader.vue`).
  상품 목록·상세에 별점, 리뷰에 사진 첨부·표시. `OrderDetailView`는 `!isAdmin` → `isMine`.
- **git**: 브랜치 `daily-status-review`. 묶음 2.5(`391759c`)까지 배포 완료,
  **묶음 3(`848899d`~)은 미배포** — 백엔드·프론트 동시 배포 필요(응답 계약 변경).
- **운영 서비스**: `glassvue-backend` active :8080 UP, nginx HTTPS :443 200. dev 인스턴스는 검증 후 종료함.
- **운영 잔여물**: 상품·주문 **전부 정리 완료**. 오늘 E2E로 쌓인 ZZ 주문 13건은 DB에서 직접 삭제(§8-8).
  현재 ZZ 상품·주문·리뷰 **0건**, 고아 주문 0건 확인.
  단 배포 검증 때 만든 **고아 이미지 1건은 일부러 남겨두었다** — 묶음 4의 정리 기능 테스트용(§8).
