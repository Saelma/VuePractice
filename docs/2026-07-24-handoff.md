# 2026-07-24 작업 정리 (착수 시점 — 진행하며 채운다)

> **7/23 에서 넘어온 이월 미완은 0 건이다.** 어제 오전에 Oracle 자동기동(SELinux)을 잡고,
> 오후에 7/22 이월을 전부 털고 기능 5개(V13~V17)를 신설하며 미완을 비운 상태로 끝났다.
> 그래서 오늘은 **백로그에서 새로 뽑는 자리**다 — 이월을 이어받는 날이 아니다.
>
> 착수 전 대조: `WORKING-AGREEMENTS.md` → `2026-07-23-handoff.md` · `2026-07-22-handoff.md`
> → `ARCHITECTURE.md` → `BACKLOG.md` (CLAUDE.md 「작업 절차」, WORKING-AGREEMENTS §1-1).

## 0. 오늘의 커밋

| 커밋 | 내용 |
|---|---|
| `16875ba` | docs: esptest 스키마 비우기에 시퀀스 DROP 추가 — 테이블만 지우면 ORA-00955 |
| `cde3b1c` | feat: 배송지 주소록 (V18) — 컬럼 DROP은 V19로, 지금은 순수 추가만 |
| `0eccbdd` | docs: esptest 비우기에 sudo가 필요 없다 — 자기 스키마는 자기가 지운다 |
| `82de7b6` | docs: 배송지 주소록(V18) 핸드오프 §3 + ARCHITECTURE·백로그 갱신 |

전체 목록은 `git log --oneline --since="2026-07-24 00:00"` 로 본다.

> §4-4 대로 **작업을 멈출 때마다** `git log` 와 대조한다. 이 표는 구조상 자기 자신을 담은
> 커밋 이후를 못 적으므로, "이제 마지막"이라고 쓰지 않는다.

---

## 1. 착수 시점 상태 (실측)

7/23 문서의 「현재 상태」가 하루 뒤에도 그대로인지 **믿지 않고 확인**했다.
어제 운영이 두 번 내려갔던 날이라 더 그렇다(WORKING-AGREEMENTS §6-1: "지금 떠 있다"는
자동기동 성공의 증거가 아니다 — 다만 오늘은 재부팅이 없었으므로 자동기동 재검증은 대상이 아니다).

| 확인 | 명령 / 신호 | 결과 |
|---|---|---|
| 백엔드 | `systemctl is-active glassvue-backend` · `/actuator/health` | `active` · `{"status":"UP"}` |
| nginx | `curl -sk https://192.168.50.14/` | **200** |
| Oracle | `systemctl is-active oracledb_ESPDB-19c` · `ora_pmon` | `active` · pid 2590 |
| Redis | `systemctl is-active redis` | `active` |
| **배포된 코드** | `/v3/api-docs` 경로 37개 중 `/api/coupons/me`·`/api/admin/coupons`·`/api/admin/coupons/{couponId}/issue` | **존재** — V17 쿠폰 반영본이 맞다 |
| 배포된 번들 | `curl -sk https://192.168.50.14/ \| grep -oE 'index\.[a-f0-9]+\.js'` | `index.4e47c99c.js` — 7/23 §13-5 검증값과 **일치** |
| git | `main..daily-status-review` · `origin/..` · `status --short` | 전부 비어 있음 (main == 브랜치, 미푸시 0, 워킹트리 깨끗) |
| 디스크 | `df -h /` | 루트 **82%** (여유 4.9G) |
| `/tmp` 잔재 | `ls /tmp/*webp*binary` | **11개 32MB** (7/21 6개 · 7/22 5개) — 이월 1번 그대로 |

> **배포 확인을 상태코드가 아니라 OpenAPI 경로로 했다** — 7/23 §8-6 사고(없는 경로도 Security 필터에서
> 401이 나서 "엔드포인트 있음"으로 잘못 판단)에서 나온 규약이다(WORKING-AGREEMENTS §5).

---

## 2. 오늘 무엇을 할 것인가 — 판단 근거

이월이 0 이므로 `BACKLOG.md` 에서 뽑았다. 남은 후보는 이렇다.

| 후보 | 크기 | 왜 지금/왜 아닌가 |
|---|---|---|
| **B-5. 배송지 주소록** | 중 | 백로그의 「다음 추천」. **이미 만든 것이 미완**인 자리 — `member.ship_*` 5컬럼이라 배송지가 딱 하나다 |
| B-6. 위시리스트(찜) | 소~중 | 도메인 신설. 장바구니 패턴 재활용 가능하지만 "미완"이 아니라 **순수 신규** |
| C-8 상품 옵션 / C-9 반품·교환 | 대 | C-8 은 재고 구조 전체 파급, C-9 는 PG 없이는 반쪽 — 둘 다 지금이 아니다 |
| C-11. 관리자 매출 통계 | 중 | 관리자 API 가 주문·이미지뿐이라 자리는 비어 있다. B-5 다음 후보 |

→ **B-5 를 고른다.** 근거는 7/23 이 A 묶음을 비울 때 쓴 것과 같다 — **"이미 만든 것이 미완"이
새 기능보다 먼저**다. 배송지는 주문서·주문상세·회원정보에 이미 UI 가 있는데 저장할 자리가
하나뿐이라, 지금 상태가 "설계가 덜 끝난 채로 노출된" 자리다.

### 2-1. ⚠ 이 마이그레이션은 어제까지와 성격이 다르다 — 순수 추가가 아니다

V13~V17 은 **다섯 개 전부 순수 추가**였다(컬럼·테이블 ADD). B-5 는 다르다:
`member.ship_*` **기존 값을 새 테이블로 옮기고 원래 컬럼을 정리**해야 한다.

**위험**: `ddl-auto: validate` 이고 운영 jar 의 `Member` 엔티티가 `ship_*` 5개를 매핑한다.
통합 테스트를 돌리는 순간 Flyway 가 **공유 espdb 에 적용**되므로(WORKING-AGREEMENTS §5),
같은 마이그레이션에서 `ship_*` 를 `DROP` 하면 **구 jar 는 재기동이 불가능**해지고,
`MemberResponse`·`ShippingAddress` 조회는 그 즉시 `ORA-00904`(존재하지 않는 컬럼)로 깨진다.
7/21 의 닉네임 UNIQUE(V6) 사고와 같은 종류인데, **그때보다 훨씬 파괴적**이다 —
UNIQUE 는 신규 가입만 막았지만 컬럼 DROP 은 **기존 조회를 통째로** 깬다.

→ **expand / contract 2단계로 나눈다.**

```
V18 (오늘)  member_address 신설 + member.ship_* 값 복사   ← 순수 추가. 구 jar 무영향
            신 코드 배포 (읽기는 member_address 로)
V19 (나중)  member.ship_* DROP                            ← 신 코드가 운영에 완전히 올라간 뒤
```

`ship_*` 를 남겨두는 동안은 **두 곳에 같은 값이 있다**(이중 진실). 이건 감수한다 —
짧은 기간이고, 대안(한 번에 DROP)은 운영을 깨기 때문이다. 다만 **V18 이후 신 코드는
`member.ship_*` 에 쓰지 않는다** — 안 그러면 두 값이 갈라져 나중에 어느 쪽이 맞는지 알 수 없어진다.

⚠ `orders.ship_*` 는 **건드리지 않는다.** 그건 주문 시점 **스냅샷**이라 주소록과 무관하고,
주소록을 고쳐도 과거 주문의 배송지는 그대로여야 한다(7/23 까지 여섯 번 적용한 스냅샷 원칙).

**사용자 결정 (착수 전)**: DROP 은 **V19 로 분리**한다. 오늘은 V18(순수 추가)까지만.

### 2-1-1. 오늘 스코프 — 핵심 + 마이페이지 주소록 관리 화면

7/23 쿠폰(§13-1)에서 배운 대로 **핵심과 주변부를 갈라서** 정하고, 사용자가 어디까지 할지 골랐다.

| | 오늘 |
|---|---|
| **핵심** | `member_address` 테이블(V18) + 기존 값 이관 · 주소록 CRUD API · 기본배송지 지정 · 주문서에서 저장된 주소 선택 | ✅ |
| **주변부** | 마이페이지 **주소록 관리 화면**(별칭 편집 · 기본배송지 전환 UI) | ✅ **포함** |
| 스코프 밖 | `member.ship_*` DROP(→ V19) · 관리자용 주소 조회 · 도로명 주소 검색 API 연동 | ❌ |

### 2-2. 착수 전 워밍업 — esptest 비우기 스크립트가 시퀀스를 안 지운다

`db/migration/README.md` 의 "스키마만 비우기" 스크립트는 **테이블만 DROP** 한다.
7/23 V16 검증에서 `seq_order_no` 가 남아 **`CREATE SEQUENCE` 가 ORA-00955 로 실패**했고,
그때는 손으로 지웠는데 **README 에 반영을 안 했다**(7/23 §12-5 각주).

→ 오늘 V18 빈 DB 검증에서 **똑같이 걸린다.** 먼저 고치고 시작한다.
"재발 방지와 기존 잔재 정리는 별개"(§2-5)와 같은 결의 문제다 — 한 번 겪은 걸 문서에 안 넣으면
다음 사람이 같은 자리에서 멈춘다.

---

## 3. 기능 추가 — 배송지 주소록 (V18)

백로그 **B-5**. `member.ship_*` 5컬럼 = 회원당 배송지 하나였던 걸(V11) 별칭 붙은 주소록으로 늘렸다.

### 3-1. ⚠ 착수 전에 발견한 것 — 이관 대상 1건이 7/22 검증 잔재였다

회원 4명 중 배송지가 있는 사람은 **1명**이었는데, 그 값이 이랬다.

```
수령인=123 · 우편번호=06134 · 주소=123123123 · 상세=123
```

WORKING-AGREEMENTS §3-1 에 기록된 **그 사고의 흔적**이다 — 7/22 에 배송지 기능을 브라우저로
검증하며 본인 계정 + 실상품으로 주문했고 수령인에 `123` 을 넣었다. 그때 **주문은 화면에서 취소했고
재고도 복원**했지만, 회원의 기본 배송지에 남은 이 값은 **아무도 안 치웠다.**

> §2-5 "재발 방지와 기존 잔재 정리는 별개" 의 또 다른 실례다. 그때는 "주문" 만 잔재로 봤고
> **그 검증이 회원 프로필도 건드렸다는 걸 놓쳤다.** 검증이 만든 것을 셀 때 주문·상품만 보지 말 것.

**마이그레이션은 이 판단을 하지 않는다.** "값이 있으면 옮긴다" 만 하고 잔재 정리는 별건으로 뒀다 —
빈 DB·미래 환경에서도 같은 스크립트가 돌아야 하고, 데이터를 골라내는 마이그레이션은 재현이 안 된다.
마침 **오늘 만든 화면으로 바로 지울 수 있다**(이월 참조).

### 3-2. ⚠ 첫 번째 "순수 추가가 아닌" 마이그레이션 — expand / contract 로 나눴다

V13~V17 은 **다섯 개 전부 컬럼·테이블 ADD** 였다. B-5 는 기존 값 이관 + 컬럼 정리가 붙는다.

한 번에 `member.ship_*` 를 DROP 하면 어떻게 되는지가 착수 전 유일한 위험이었다:

```
통합 테스트 실행 → Flyway 가 공유 espdb 에 적용 → 컬럼 사라짐
  → 운영 구 jar 는 그 컬럼을 매핑 중 → 재기동 불가 + 회원·주문 조회가 ORA-00904
```

7/21 의 **닉네임 UNIQUE(V6) 사고와 같은 종류인데 훨씬 파괴적**이다. UNIQUE 는 신규 가입만 막았지만
컬럼 DROP 은 **이미 되던 조회를 통째로** 깬다.

→ 사용자 결정으로 **둘로 나눴다**:

| | 내용 | 구 jar 영향 |
|---|---|---|
| **V18 (오늘)** | `member_address` 신설 + `member.ship_*` 값 복사 | **없음** (순수 추가) |
| **V19 (나중)** | `member.ship_*` DROP | 신 코드가 운영에 올라간 뒤에만 안전 |

V19 는 이월이 아니라 **백로그 B-5-2** 에 뒀다 — 수명이 "며칠" 이 아니라 "배포가 안정된 뒤" 라는 조건부다.

### 3-3. 이중 진실을 규율이 아니라 **구조**로 막았다

V18~V19 사이엔 같은 주소가 두 곳에 있다. 여기서 흔한 실패는 *"신 코드는 옛 컬럼에 쓰지 않기로 하자"*
라고 **적어두고 끝내는 것**이다. 그러면 언젠가 누가 쓴다.

→ **`Member` 엔티티에서 `ship_*` 매핑 5개를 아예 걷어냈다.** 필드가 없으면 쓸 방법이 없다.
`ddl-auto=validate` 는 **매핑되지 않은 여분 컬럼을 문제 삼지 않으므로**(실측: V18 적용 후 기동 통과)
DB 에 컬럼이 남아 있어도 아무 문제가 없다.

부수효과로 **V19 는 코드 변경이 0** 이 됐다 — 순수 DDL 한 줄이다.

### 3-4. 기본 배송지가 하나뿐임을 DB 가 보장한다

Oracle 에는 부분 유니크 인덱스(`WHERE is_default = 1`)가 없다. 함수 기반 유니크 인덱스로 같은 효과를 낸다:

```sql
CREATE UNIQUE INDEX uq_member_address_default
    ON member_address (CASE WHEN is_default = 1 THEN member_id END);
```

`is_default=0` 인 행은 색인값이 NULL 이라 유니크 대상에서 빠지고, 같은 회원의 두 번째 기본 배송지만
ORA-00001 로 거부된다. 주문번호 유니크(V15)를 "앱 채번이 어긋났을 때의 최종 방어선" 으로 둔 것과 같은 성격이다.

**그 대가로 순서 제약이 생겼다.** "옛 기본 해제" 와 "새 기본 지정" 이 **DB 에 도달하는 순서**가 중요한데,
JPA 더티 체킹은 **flush 순서를 보장하지 않는다** — 같은 엔티티 타입이라 뒤집힐 수 있다.

```java
addressRepository.findByMemberIdAndIsDefaultTrue(memberId)
        .ifPresent(MemberAddress::unsetDefault);
addressRepository.flush();          // ← 이 줄이 없으면 ORA-00001
address.markDefault();
```

삭제도 같은 문제다 — 기본 배송지를 지우고 다른 주소를 승계시킬 때 **DELETE 가 먼저 나가야** 한다.
안 그러면 기본이 두 행이 되는 순간이 생긴다.

> 이 `flush()` 들은 **지우기 쉬운 줄**이다("불필요해 보이는 flush"). 그래서 코드 주석과 여기 양쪽에 적었다.

### 3-5. FK 를 진짜로 걸었다 — `member_coupon` 과 다른 이유

`member_coupon.member_id` 는 주석에 **"FK 아님(느슨한 참조) — 도메인 경계"** 라고 적혀 있다.
`member_address` 는 반대로 **진짜 FK + `ON DELETE CASCADE`** 다. 모순이 아니다:

| | 방향 | 판단 |
|---|---|---|
| `member_coupon` → member | **도메인 간**(coupon → member) | FK 를 걸면 MSA 분리 때 걸림돌. 느슨한 UUID |
| `member_address` → member | **도메인 안**(member → member) | 쪼개도 member 와 **함께** 움직인다. FK 가 맞다 |

그리고 CASCADE 는 선택이 아니었다 — `MemberService.withdraw` 가 member row 를 지우므로,
CASCADE 가 없으면 **주소가 있는 회원은 탈퇴 자체가 FK 위반으로 실패**한다.

### 3-6. 백필 PK 를 `SYS_GUID()` 로 하지 않았다

CLAUDE.md 는 PK 를 **UUIDv7 · 앱에서 생성**으로 못박는데, 백필 행은 앱을 거치지 않는다.
`SYS_GUID()` 는 v7 이 아니고(시간순이 아니다), **한 번 들어가면 나중에 구분할 방법도 없다.**

→ SQL 에서 **v7 레이아웃을 그대로 조립**했다.

```
[48비트 unix ms = 12 hex] [버전 '7'] [rand_a 3 hex] [variant 'A'] [rand_b 15 hex] = 32 hex
```

RFC 9562 의 variant 는 상위 2비트가 `10` 이어야 해서 17번째 hex 는 8·9·A·B 중 하나여야 한다 → `'A'` 고정.
**실측**: 생성값 `019F9175E3107575A1348689147A35E0` — 13번째 hex 가 `7`, 앞 12 hex 를 되읽으면 현재 UTC 와 일치.

> ⚠ 이 플랫폼의 `SYS_GUID()` 는 **순차적**이라 난수부의 무작위성이 약하다(연속 호출이 1~2 hex 만 다르다).
> 필요한 건 **서로 다른 값**뿐이고 PK 유니크가 최종 방어선이라 그대로 뒀다 — 다만 이걸 **난수원으로
> 믿으면 안 된다**(토큰·비밀값 용도로는 부적합).

### 3-7. API 계약을 유지한 채 저장 위치만 옮겼다

`MemberResponse.ship*` 5개 필드는 **그대로 뒀다.** 프론트의 주문서 자동 채움과 `hasAddress` 판정이
이 필드를 보고 있어서, 형태까지 바꾸면 **저장 구조 이전과 화면 개편이 한꺼번에 얽힌다.**
값의 **출처만** `member.ship_*` → 주소록의 기본 항목으로 바꿨다.

같은 이유로 옛 경로 `PATCH /api/members/me/shipping-address` 도 살렸다 — 주문서의
"이 주소를 기본 배송지로 저장" 체크가 계속 쓴다. 서버가 그 요청을 **기본 항목 upsert** 로 처리해
주소가 두 벌로 갈라지지 않는다(통합 테스트로 고정했다).

> 덕분에 **주문 API(`POST /api/orders`)는 한 글자도 안 바뀌었다.** 주문서는 원래 배송지를
> 본문으로 받고 있었고, 주소록은 그 폼을 **채우는 방법**이 하나 늘어난 것뿐이다.

### 3-8. 화면

- **마이페이지**: "기본 배송지" 폼 하나 → **주소록 카드**(목록 · 추가 · 수정 · 삭제 · 기본으로 지정).
  검증(`validateAddress`)과 입력 필드(`ShippingAddressFields`)는 주문서와 **그대로 공유**한다 —
  화면마다 검증을 따로 두면 한쪽만 통과하고 서버에서 400 을 받는다(7/22 에 정한 규칙).
- **주문서**: 저장된 주소를 **버튼으로 골라** 폼에 붓는다. 고른 뒤에도 폼은 고칠 수 있고,
  고친 값은 **이번 주문에만** 적용된다(주문엔 스냅샷, 주소록은 안 바뀜). 주소록에도 반영하려면 체크를 켠다.
- 기본 배송지가 바뀌면 `authState.user.ship*` 가 낡으므로 **`loadMe()` 로 다시 읽는다.**
  안 하면 화면엔 새 주소가 기본이라고 떠 있는데 **주문서 자동 채움은 옛 주소**가 된다.

### 3-9. 보안 — 이번엔 구멍이 없었다

7/23 쿠폰(§13-4)에서는 `SecurityConfig` 의 기본이 `anyRequest().permitAll()` 이라 매처를 빠뜨려
남의 쿠폰이 열릴 뻔했다. 이번엔 **`/api/members/**` 가 이미 `.authenticated()`** 라 새 경로가
자동으로 덮였다 — **경로를 모아 두면 권한을 빠뜨릴 수 없다**는 §2-4 규약이 두 번째로 값을 했다.

그래도 401 을 통합테스트 계약으로 고정했고, 남의 주소 접근은 **403 이 아니라 404** 로 답한다
(존재 여부를 알려주지 않는다 — 쿠폰과 같은 판단).

### 3-10. 검증 (실측)

| 확인 | 결과 |
|---|---|
| 백엔드 | **211개**(204 → +7: 첫 주소 자동 기본 · 기본 유일성 · 삭제 승계 · 소유 경계 404 · 401 · me 반영 · 옛 계약) · failures 0 · errors 0 · **skipped 0** |
| 프론트 | **64개**(58 → +6: `addressToForm` 3 · `addressSummary` 3) · 빌드 성공 |
| espdb 이관 | 1건. id `019F917F9F407575A15A8DA27594B2E0` — **13번째 hex = `7`**(UUIDv7 확인) |
| espdb 구조 | `UQ_MEMBER_ADDRESS_DEFAULT` UNIQUE · `FK_MEMBER_ADDRESS_MEMBER` **DELETE_RULE=CASCADE** · `CK_MEMBER_ADDRESS_DEFAULT`(이름 붙인 CHECK) |
| 컬럼 semantics | VARCHAR2 6개 전부 `CHAR_USED='C'` — `member.ship_*` 와 길이·semantics 동일(§2-2-1) |
| 구 jar 안전 | `member` 의 `SHIP%` 컬럼 **5개 그대로** · 운영 health `UP` 유지 |
| 번들 | `index.4e47c99c.js` → **`index.514b8879.js`** |

> **빈 DB(esptest) 검증 통과**: V1→V18 순차 적용(**18개, 8.6초**) + `validate` 통과(기동).
> `member_address` 이관 대상 **0건**(빈 DB라 정상), 인덱스·FK CASCADE·CHECK 전부 확인,
> `member.ship_*` 5컬럼도 그대로(V11 이 만들고 V18 이 안 지웠다), `SEQ_ORDER_NO` LAST_NUMBER=1.

### 3-12. 배포 (실측)

순서는 규약대로 **작업 브랜치 push → `main` ff 머지·push → 배포 스크립트**(WORKING-AGREEMENTS §5).
`git -C /home/ecstel/work log --oneline main..daily-status-review` 가 **비어 있는 것을 먼저 확인**했고,
`check-deploy-branch.sh` 도 조용했다(경고 없음). 백엔드 → 프론트 순으로 배포.

| 확인 | 신호 | 결과 |
|---|---|---|
| 백엔드 | OpenAPI 경로 수 | 37 → **40** |
| 주소록 API | `/v3/api-docs` | `/api/members/me/addresses`(get·post) · `/{addressId}`(put·delete) · `/{addressId}/default`(patch) |
| 프론트 | 배포된 번들 해시 | **`index.514b8879.js`** — 로컬 빌드와 일치 |
| 기동 | `/actuator/health` · `/api/products` | `UP` · 200 |
| 보안 | 미인증 `GET /api/members/me/addresses` | **401** |

> **배포 확인을 상태코드로 하지 않았다** — `/api/members/**` 는 인증이 필요해서 **없는 경로도 401** 이 난다.
> 7/23 §8-6 이 정확히 그 함정에 빠진 자리다. 그래서 **OpenAPI 에 경로와 메서드가 있는지**를 신호로 썼고,
> 401 은 "엔드포인트 존재 증거" 가 아니라 **권한 계약 확인**으로만 읽었다.

### 3-11. 곁가지 — esptest 비우기에 `sudo` 가 필요 없었다

`migration/README` 의 절차는 `sudo -iu oracle` + `sysdba` + `dba_tables` 였다. 그런데
**자기 스키마의 객체를 DROP 하는 데는 DBA 권한이 필요 없다** — `esptest` 로 직접 접속해
`user_tables`·`user_sequences` 를 돌면 끝이다. 오늘 그렇게 했다.

> **왜 고쳤나**: `sudo` 가 낀 절차는 **사람 손을 기다리게** 만들고, 기다리게 만드는 검증은 건너뛰게 된다.
> 빈 DB 검증은 마이그레이션마다 **매번** 돌려야 하는 절차라 특히 그렇다.

같은 자리에서 **시퀀스 DROP 누락**도 고쳤다(§2-2 에서 예고한 워밍업). 7/23 V16 검증에서
`seq_order_no` 가 살아남아 `CREATE SEQUENCE` 가 ORA-00955 로 실패했는데 README 에 반영이 안 돼 있었다.
오늘 실제로 시퀀스까지 지우고 돌려 **V15 가 깨끗이 재적용**되는 걸 확인했다.

---

## 현재 상태 (7/23 대비 바뀐 것만)

- **배송지 주소록 신설**(§3): 배송지가 회원당 하나 → **별칭 붙은 여러 개 + 기본 배송지**.
  마이페이지에 주소록 관리 화면, 주문서에 저장된 주소 선택. **주문 API 계약은 무변경.**
- **DB/스키마**: Flyway **V18**(`member_address` + FK CASCADE + 함수 기반 유니크 인덱스 + 값 이관 1건).
  espdb 적용 완료(3.6초) · esptest 빈 DB V1→V18 통과(8.6초).
  ⚠ **`member.ship_*` 5컬럼은 아직 DB 에 남아 있다** — 엔티티 매핑만 걷어냈고 DROP 은 V19(백로그 B-5-2).
- **테스트**: 백엔드 204 → **211** · 프론트 58 → **64**. 전부 통과, **skipped 0**.
- **검증 절차**: `migration/README` 의 esptest 비우기가 **sudo 불필요**로 바뀌었고(§3-11) 시퀀스도 지운다.
- **운영**: 정상. 백엔드 `active` / health `UP` / nginx `https:200` / Oracle `active` / Redis `active`.
  오늘 내려간 적 없다.
- **배포**: **완료**(§3-12). 백·프론트 모두. OpenAPI 경로 37 → **40**, 번들 `index.4e47c99c` →
  **`index.514b8879`**. `main` ff 머지 후 배포했고 브랜치 경고도 조용했다.
  **브라우저 검증은 남았다**(이월 1번).
- **디스크**: 루트 **82%**(여유 4.9G). `/tmp` WebP 잔재 11개 32MB — 7/21~22 것 그대로.

---

## 이월 (다음 문서로 반드시 옮길 것)

> 이 절은 규약이다(WORKING-AGREEMENTS §4-1). 다음 날 문서가 그대로 물려받아 시작한다. 처리했으면 그때 지운다.

### 미완 — 다음 세션이 이어받을 것

1. **주소록(§3) 브라우저 검증** — **배포는 끝났다**(§3-12). 화면 동작 확인만 남았다.
   - ⚠ **하드 새로고침(Ctrl+Shift+R) 부터** — 안 하면 브라우저가 옛 JS 번들을 물고 있어
     "기능이 안 먹는다" 로 오진한다(WORKING-AGREEMENTS §5, 7/21 사고).
   - 시나리오:
     ① 마이페이지 → 주소록에 **`123` 항목이 "기본 배송지" 별칭으로 옮겨와 있는지**(V18 이관 확인)
     ② `집`·`회사` 2개 추가 → `회사` 를 **기본으로** 전환 → `집` 의 뱃지가 사라지는지
     ③ 주문서 진입 → 주소 버튼이 뜨고 **기본이 선택된 채** 폼이 채워지는지 → 다른 주소 클릭 시 폼이 바뀌는지
     ④ 기본 배송지 **삭제** → 남은 주소가 자동으로 기본을 승계하는지
   - ⚠ 주문까지 넣어 볼 거면 `ZZP-` 상품을 만들어서 하고 수령인에 `ZZ` 를 남긴다(WORKING-AGREEMENTS §3-1).
     **오늘 그 규칙을 안 지킨 대가를 실제로 치렀다**(아래 2번).

2. **7/22 검증 잔재 `123` 정리** — 회원 기본 배송지에 `수령인=123 / 주소=123123123` 이 남아 있고,
   오늘 V18 이 그걸 주소록으로 **그대로 옮겼다**(별칭 "기본 배송지").
   - **배포 후 마이페이지 주소록 화면에서 지우거나 고치면 된다** — 오늘 만든 기능으로 처리 가능하다.
     DB 를 직접 UPDATE 하지 말 것(화면 경로로 하는 이유는 WORKING-AGREEMENTS §3-1 과 같다).
   - 남겨 두면 주문서 자동 채움이 계속 `123` 으로 채워진다.

3. **`member.ship_*` DROP (V19)** — 이월이 아니라 **백로그 B-5-2** 에 뒀다(조건부라 수명이 길다).
   조건: V18 을 담은 jar 가 운영에서 정상 동작 중일 것. 여기엔 **포인터만** 남긴다.

### 7/23 에서 넘어온 잔여 (전부 조건부 · 급하지 않음)

1. **`/tmp` WebP 잔재 11개 32MB** — 재발은 7/23 §5(전용 tmpdir)로 막았고, 이 11개는 7/21~22 잔재다.
   오늘 실측: `cwebp` 6개 · `dwebp` 5개, 루트 **82%**. 방치해도 `tmpfiles.d` 10일 규칙으로 지워진다
   (7/21 것은 곧 대상이 된다). 당길 거면 `rm -f /tmp/cwebp*binary /tmp/dwebp*binary` (실행 중 JVM 없을 때).
2. **문의 경로만으로 N+1 직접 판별**(7/23 §7-3) — 이미지 그룹 3개 이상인 ZZ 문의가 있어야 배치와
   N+1 이 갈린다. **결론이 바뀔 가능성은 낮다**(판별은 상품 목록이 했고 문의는 같은 코드를 탄다).
3. **공유기 DHCP 예약**(MAC `08:00:27:df:8e:fd` → `.14`) — IP 충돌을 겪으면 그때.
