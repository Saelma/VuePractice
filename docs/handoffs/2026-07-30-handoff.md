# 2026-07-30 작업 정리 (문서 개시 — 작업 착수 전)

> **7/29 핸드오프에서 이어서 진행한다.** 그 문서의 이월 **필수 미완은 0건**이고, 남은 것은
> 조건부 잔여 3건(`/tmp` WebP 잔재 · 운영자ID 계정 검증 적립 잔존 · 공유기 DHCP 예약)뿐이다.
>
> 착수 전 확인(2026-07-30, 실측): `main`(=`/home/ecstel/work`) HEAD == 작업 브랜치 `goblin` HEAD
> == `5d71455`, 워킹트리 **깨끗**. `git log main..goblin` **비어 있음**(WA §5 배포 전제 충족).
> 7/29 작업분(B-13·B-14·B-10 완성·태그라인·정렬/혜택 스트립·Mailpit·플래키 해소)은 전부 배포·검증 종결.
>
> 2일치 대조(§1-1): 7/28 이월 1건(쿠폰 권한 테스트)은 7/29 가 물려받아 해소했고, 7/27 발 조건부
> 잔여 4건 중 플래키 테스트도 7/29 에 해소됐다 — **조용히 빠진 항목 없음**.

---

## 0. 오늘의 커밋

| 해시 | 메시지 |
|---|---|
| (이 커밋) | docs: 07-30 핸드오프 개시 — 7/29 이월 인수 |

> ⚠ §4-4: 이 표는 **작업을 끝낸 뒤 다시 대조한다** — `git log --oneline --since="2026-07-30 00:00"`.
> 문서를 닫는 커밋과 그 뒤 후속 커밋이 구조적으로 빠지는 자리다.

---

## 1. 오늘 무엇을 할 것인가 — 후보와 근거

이월 필수 미완이 0건이라 **백로그에서 새로 꺼내는 날**이다. 7/29 의 6차 감사로 후보가 33개
생겼는데, 감으로 고르지 않고 **코드에서 다시 확인**한 뒤 우선순위를 잡았다.

### 1-1. 착수 전 실측 — 백로그 주장이 지금도 맞나

| 백로그 항목 | 실측 결과 |
|---|---|
| **H-1** audit 도메인 테스트 0건 | ✅ 맞다. 소스 **10개** / 테스트 **0개**. 감사를 건드리는 테스트는 `MemberAdminCommandServiceTest` 하나뿐이고 그건 **이벤트 발행만** 본다(`verify(eventPublisher)`) — 리스너·저장·조회·권한은 한 번도 실행된 적이 없다 |
| **E-2** 강등 시 토큰 무효화 | ✅ 맞고 **한 단계 더 나쁘다**. `suspend()` 는 `refreshTokenStore.delete` 를 부르는데 **`changeRole()` 은 그것조차 안 부른다**(실측) |
| **E-1** 로그인 시도 제한 | ✅ 없다(`grep` 0건) |
| **F-1** 탈퇴 연관 데이터 | ✅ 유효. `withdraw()` 는 refresh 삭제 + 자기 access 블랙리스트는 하지만 **연관 데이터 정리는 전무** |

> ⚠ **F-1 의 문장 하나를 정정한다.** 백로그엔 *"`withdraw()` 는 `memberRepository.delete(member)`
> 한 줄뿐"* 이라 적혀 있는데 실제로는 토큰 무효화 2줄이 앞에 있다. **주장의 핵심(연관 데이터 정리가
> 없다)은 그대로**이지만, 인용이 정확하지 않으면 다음에 읽는 사람이 코드를 다시 안 본다.

### 1-2. 고른 것 — H-1 → E-2 (사용자 확정)

| 순서 | 항목 | 크기 | 왜 이 순서인가 |
|---|---|---|---|
| ① | **H-1 감사 도메인 테스트** | 소 | **규약 부채라 기능보다 앞선다.** WA §2-4(새 관리자 엔드포인트엔 401/403/200 필수)가 `/api/admin/audit` 에서 빠져 있다 — **7/29 에 쿠폰에서 겪은 것과 같은 누락**이다. 게다가 감사 로그는 *"고장 나도 아무도 모르는"* 기능이라(쓰기만 하고 읽을 일이 없다) 검증 부재의 값이 특히 크다 |
| ② | **E-2 강등·정지 시 access 즉시 무효화** | 소 | 같은 **관리자 조작 자리**라 ①의 테스트 기반이 그대로 재사용된다. 지금은 **강등된 관리자가 최대 30분간 관리자 권한을 계속 쓴다** |

**미룬 후보와 이유**: E-1(로그인 시도 제한)·F-1(탈퇴 데이터 정리)·G-1(아이디 찾기)·B-16(관리자
대시보드). 셋 다 유효하지만 **E-2 가 더 뾰족하다** — 저것들은 "공격자가 시도해야" 문제가 되는데
E-2 는 **관리자가 정상적으로 강등을 눌렀는데도 권한이 남아 있는** 것이라 오작동에 가깝다.

---

## 이월 (다음 문서로 반드시 옮길 것)

> WORKING-AGREEMENTS §4-1. 다음 날 문서가 그대로 물려받아 시작한다. 처리했으면 지운다.

### 미완 — 다음 세션이 이어받을 것

(작업 진행에 따라 갱신)

### 7/29 에서 넘어온 잔여 (전부 조건부 · 급하지 않음)

1. **`/tmp` WebP 잔재** — tmpfiles.d 10일 규칙으로 자동 삭제. 당길 거면 `rm -f /tmp/cwebp*binary /tmp/dwebp*binary`.
2. **운영자ID 계정 검증 적립 영구 잔존**(SILVER·누적 204,880·잔액 97P) — 취소 불가가 설계라 그대로. 데모 때 감안.
3. **공유기 DHCP 예약**(MAC `08:00:27:df:8e:fd` → `.14`) — IP 충돌 겪으면 그때.
4. **8fter 의 미학(색·여백·타이포)** — 페이지 구조만 문서화됐고 시각 톤은 안 적었다. 사용자가 "무엇이
   좋았는지" 한 줄 주면 DESIGN.md §2 를 보강한다.
5. **디자인 더 갈 여지**: 회원 상세 레이아웃 카드화, 상태 뱃지 pill 강화(B-12 잔여).
