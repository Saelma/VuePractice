# 2026-08-19 핸드오프 — 기간 할인(타임세일) G-5 · V52 · 달력에 세일(B-27)

> ⚠ **08-14 이후 4일이 비었다**(08-15~08-18). 08-14 의 이월이 그대로 살아 있었고, 그 문서가
> *"08-19 에 배치가 도는 증거가 나온다"* 고 예고해 둔 날이 오늘이다.

---

## 0. 오늘의 커밋

| 해시 | 무엇 |
|---|---|
| `ed8e16a` | feat(catalog): 기간 할인(타임세일) — 정렬·필터까지 세일가로 (G-5, V52) |
| `aa37eb0` | test(catalog): 가격 필터 테스트의 간헐 실패를 구조로 없앤다 (G-5) |
| `9c339c6` | docs: 08-19 G-5 기간 할인 — 착수부터 변형 주입까지 · BACKLOG 완료 · DESIGN 가격 표시 규약 |
| `cdcd815` | docs: 08-19 §11 배포·브라우저 검증 종결 — 표본을 만들어서 밟았다 |
| `c9cdb8c` | docs: 08-19 §12 빈 DB 마이그레이션 검증 통과 (esptest) |
| `a023081` | feat(coupon): 프로모션 달력에 타임세일을 얹는다 (B-27 + G-5) |
| `9522d89` | docs: 08-19 §13 배치가 도는 증거 · §14 달력에 타임세일 · BACKLOG B-27 후속 완료 |
| `7c1141b` | docs: 08-19 §14-5 달력 배포·브라우저 검증 — 겹침을 눈으로 봤다 |

⚠ **이 표는 `check-handoff.sh` 가 한 번 잡았다**(문서 커밋 3건이 빠져 있었다). 스크립트 지시대로
**이어 붙이지 않고 `git log --reverse` 로 통째로 다시 만들었다** — 손으로 이어 붙이면 또 빠진다.

⚠ **문서 커밋은 이 표를 닫은 뒤에 한 번 더 대조한다**(WA §4-4).

---

## 1. 시작 — 이월이 지목한 것과 실측이 갈라졌다

핸드오프 3일치(08-12·13·14) + BACKLOG 를 대조했다. 08-14 이월이 남긴 것은 셋이었다:

1. **다음 기능은 정해 두지 않았다** — 후보 G-5 · G-4 · G-6/G-7
2. **화면 테스트는 막힌 것이 없다** — 다음 후보 `OrderAdminView`
3. **검증 상품 정리는 끝났다** — 🔴 **08-19 에 배치가 지운다**

사용자가 **G-5** 를 골랐다.

### 1-1. ⚠ 이월의 3번은 「손댈 것이 없다」가 맞았다 — 그리고 **아직 안 지워졌다**

09:17 실측: **넷 다 그대로 살아 있다.**

| 상품 | `deleted_at` | 유예 만료(KST) |
|---|---|---|
| `ZZ-테스트용입니다123123213123` (대조군) | 08-12 16:22 | **08-19 16:22 — 오늘** |
| `ZZ-옵션삭제검증` | 08-14 11:09 | 08-21 11:09 |
| `ZZ-재고알림테스트` · `ZZ-정가텟트` | 08-14 15:55 | 08-21 15:55 |

`purge-enabled: true` · 유예 **7일** · sweep **1시간 주기**(`fixedDelay`) · 백엔드 `active`.
→ **16:22~17:22 사이**에 대조군이 사라지면 그것이 08-12 §14-5 가 기다리던 **배치가 도는 증거**다.
⚠ **안 사라지면 그것도 증거다.**

⚠ **곁다리 — `deleted_at` 의 오프셋이 행마다 섞여 있다.** 08-12 행은 `+09:00`, 08-14 행들은 `+00:00`
으로 저장돼 있다. **절대 시각은 둘 다 문서와 일치하니 값은 맞고**, 원시 조회에서 헷갈리는 자리라 적어 둔다.
🔴 **원인은 안 찾았다** — 추측으로 적지 않는다.

---

## 2. 착수 전 결정 — 백로그가 「결정 필요」라 적은 자리는 실측으로 이미 답이 있었고, 없던 자리가 하나 나왔다

### 2-1. 🔴 돈은 통로가 하나다 — `ProductResponse`

배선을 읽었더니 장바구니(`CartService`)가 상품을 `ProductQueryService.findByIds` → `ProductResponse`
**로만** 읽고, 주문(`OrderService`)은 그 장바구니 줄을 복사한다. 배송비·쿠폰 최소금액도 전부
`cart.totalPrice()` 를 쓴다.

→ **유효 판매가를 한 자리에서 만들면 목록·상세·장바구니·주문·배송비·쿠폰이 전부 따라온다.**
여러 곳에 흩으면 **그 어긋남은 돈에서 난다**(08-10 B-25 가 같은 이야기).

### 2-2. ⚠ 캐시는 정할 것이 없었다

백로그가 *"TTL 로 버틸지 evict 할지 결정 필요"* 라 적어 둔 자리인데, `CacheConfig` 실측 **TTL 60초**다.
→ **TTL 로 버틴다.** 관리자 조작은 `@CacheEvict` 로 즉시 반영하고, **세일이 저절로 시작·종료하는 순간만**
최대 60초 늦다. evict 배선을 더 넣을 값어치가 없다.

### 2-3. 🔴 백로그에 없던 자리 — 정렬·가격필터가 `product.price` 컬럼을 본다

`ProductRepositoryImpl` 의 정렬 화이트리스트에 `price` 가 있고, `minPrice`/`maxPrice` 는
`@Cond(path="price")` 로 자동 처리돼 **컬럼을 그대로** 봤다. 세일가를 응답 조립에서만 얹었다면:

- 화면엔 8,000원인데 **「1만원 이하」 필터에 안 걸린다**
- **「가격 낮은 순」이 세일 전 순서**로 나온다

**08-13 §13-2 의 「화면에만 있었다」와 같은 종류**다. → **쿼리에서 계산한다**(사용자 결정).

### 2-4. 사용자와 확정한 셋

| 무엇 | 결정 | 안 고른 것과 그 대가 |
|---|---|---|
| 정렬·필터 | **쿼리에서 CASE 로 유효가 계산** | 「응답에서만」은 싸지만 화면과 조회가 어긋난다 / 「`sale_price` 컬럼 비정규화」는 갱신 배치가 필요하고 배치를 놓치면 **값이 거짓말을 한다** |
| 할인 방식 | **상품 단위 · 정률(%)** | 정액은 싸고 비싼 옵션에 같은 액수가 붙어 **체감 할인율이 제각각**이 된다 |
| 화면 표시 | **판매가 취소선 + 세일가**(정가는 숨김) | 정가 취소선은 할인폭이 커 보이지만 **정가 없는 상품은 세일인지가 안 보인다** |

### 2-5. 내가 정한 둘 (전례가 답을 갖고 있었다)

- **겹침** — G-8/V49 그대로. Oracle 유니크로 기간 겹침을 못 막아 **앱이 유일한 방어**라 테스트로 못 박고,
  조회는 `Optional` 이 아니라 **`List`** 로 받는다(뚫렸을 때 목록 전체가 500 나지 않게).
- **주문 스냅샷** — `price`=세일가 · `listPrice`=정가 둘만. B-7 토대가 이미 안전하다.

---

## 3. 🔴 착수 전 판단에 없던 덫 — 관리자 폼이 세일가를 원가로 굳힌다

`ProductResponse.price` 를 세일가로 바꾸기로 하고 프론트를 세었더니 `ProductFormView` 가
`form.price = p.price` 로 **응답의 판매가를 그대로 입력칸에 싣고 있었다.**

→ **세일 중에 상품을 수정하면 세일가가 원래 판매가로 저장되고**, 세일이 살아 있으면 **할인이 두 번 먹는다.**
컴파일도 테스트도 안 걸리고 화면은 멀쩡히 돈다.

### 3-1. 실측이 방향을 정했다 — **7 대 1**

08-13 §11-3(*"필드를 지우기 전에 그 이름을 저장소 전체에서 센다"*) 그대로 셌다.
`product.price` 를 읽는 자리 **8곳**:

| 성격 | 곳 | 수 |
|---|---|---|
| 고객에게 보이는 가격 | `ProductCard`·`ProductDetailView`(2)·`HomeView`·`WishlistView`·`CartView`·`recentlyViewed` | **7** |
| 관리자 입력칸 | `ProductFormView:40` | **1** |

→ **`price` = 세일가(청구액), `regularPrice` = 세일 전 판매가**, 폼만 `regularPrice` 를 읽는다.
🔴 **반대로 갔으면**(«`price` 는 원가, `salePrice` 를 새로 둔다») **7곳을 전부 `salePrice ?? price` 로
고쳐야 하고 한 곳만 빠져도 세일이 조용히 안 먹는다.** 위험을 여러 곳에 흩는 대신 **한 곳에 모은** 것이
이 선택이다.

⚠ 폼은 세일 중이라는 사실을 칸 아래에서 **말해 준다** — 안 그러면 관리자가 «내가 9,000 이라 적었는데
왜 7,200 으로 팔리지» 를 폼 어디에서도 확인 못 하고 **그 칸을 고쳐 맞추려 든다.**

---

## 4. 구현

- **V52** `product_discount`(상품·할인율 1~99·시작·종료). ⚠ **컬럼이 아니라 테이블인 이유 둘**:
  ①컬럼 한 벌이면 «지금 세일» 만 담겨 **다음 세일을 미리 등록할 수 없다** — 그게 이 기능의 목적이다
  ②덮어쓰면 끝난 세일이 흔적 없이 사라진다.
  🔴 **V48(`deleted_at`)이 컬럼으로 간 것과 갈리는 지점이 여기다** — 삭제 대기는 상품당 **하나뿐인 상태**고,
  할인은 **여러 개가 줄지어 서는 일정**이다.
- **적용 순서** `ROUND((price + priceDelta) × (100 - rate) / 100)` — 가격차를 **더한 뒤에** 할인율.
- **반올림** 정수 연산 `(x*(100-r)+50)/100` 으로 Oracle `ROUND(x,0)` 과 맞췄다.
  ⚠ SQL 과 Java 가 갈리면 **목록 가격과 장바구니 가격이 1원 어긋나고**, 그건 눈에 안 띄다가 결제에서 터진다.
- **정렬·필터** 상관 서브쿼리 + CASE. ⚠ **조인이 아니라 서브쿼리**인 이유: 조인하면 겹침이 뚫렸을 때
  **같은 상품이 목록에 두 줄로 조용히** 나온다 — 500 보다 나쁘다(아무 로그도 안 남는다).
- **관리자 API 는 `/api/admin/**` 아래** — 🔴 `SecurityConfig` 마지막 줄이 `.anyRequest().permitAll()`
  이라 `/api/products/**` 에 두고 매처를 한 줄 잊으면 **누구나 99% 세일을 걸 수 있다.**
  WA §2-4 가 관리 API 경로를 모아 두라고 한 이유가 정확히 이것이다.
  ⚠ B-25(관리자 주문 취소)는 반대로 갔는데, 그건 **형제 조작 넷이 이미 `/api/orders/...` 에 있었기** 때문이다.
  할인은 오늘 생긴 새 자원이라 그 논거가 없다.
- **날짜는 `LocalDate` 만 받고 경계는 서버가** 만든다(B-26 규약) · **종료일은 포함**.

### 4-1. 그 김에 이어 준 것 — 찜 목록

`WishlistItemResponse` 는 자기 DTO 를 쓰는데 `regularPrice`·`discountRate` 가 없었다.
가격 자체는 `ProductResponse` 를 타서 세일가로 맞지만 **취소선도 배지도 안 떠 「그냥 싼 상품」으로 보인다.**
🔴 그건 그 DTO 가 스냅샷 대신 «지금 값» 을 고른 이유(*"가격이 내렸는지 봐야 쓸모가 있다"*)와
**정면으로 어긋난다.** → 두 필드를 이어 줬다.

---

## 5. 테스트 — 31건

- **단위 14**(`ProductDiscountTest`): 반올림(딱 떨어짐 · 내림 · **정확히 0.5** · 1원 1% · 1%/99% 경계) ·
  기간 경계(시작 포함 · **종료 배타** · 1ms 전후 · 진행 중이면 예정이 아니다).
- **통합 17**(`ProductDiscountIntegrationTest`): 권한 401/403/200 · 곱 순서 · 세일 없음 ·
  예정/지난 세일 · 겹침 400 · **맞닿는 경계는 허용** · 역전 기간 400 · 할인율 0/100 400 ·
  기간 유지 수정 · 삭제 후 원가 복귀 · 다른 상품 경로 404 · 가격 필터 · 가격 정렬 · 목록 status.
- **뷰 14**(`ProductDiscountAdminView.test.js`).

### 5-1. 🔴 「0」이 두 뜻이었다 — 표본을 만들어서 밟았다

실측: 운영 옵션 **10개가 전부 `priceDelta = 0`** 이다. 그 데이터로는 **곱 순서를 뒤집어도 결과가 같다** —
초록이어도 아무것도 증명 못 한다(WA §3-3). → 테스트가 **`priceDelta = +2000` 옵션을 일부러 만들어**
`기본 10,000 → 8,000` 과 `L 12,000 → 9,600` 을 함께 단언한다.
⚠ 순서를 뒤집으면 L 이 `8,000 + 2,000 = 10,000` 이 되어 **두 기대값이 다르다** — 그래서 이 단언이 순서를 가른다.

### 5-2. ⚠ 내가 쓴 주석 하나가 사실이 아니어서 고쳤다

뷰 테스트에 *"0.5 경계에서 정수 연산과 `Math.round` 가 갈린다"* 고 적었는데 **JS `Math.round` 도 0.5 를
올린다** — 그 단언은 두 구현을 가르지 못한다. 정수 연산을 고른 이유는 **Oracle NUMBER 와 double 이
어디서 갈리는지 알 수 없어서**지, 이 테스트가 그 차이를 밟아서가 아니다.
→ 주석을 고쳤다. **안 밟은 것을 밟았다고 적지 않는다.**

---

## 6. 🔴 전수에서 6건이 깨졌다 — 내 변경이 아니었고, **테스트의 잠복 결함이었다**

`EventCouponIntegrationTest` 6건이 전부 `PathNotFoundException: No results for path: $['data']` 로 실패.

### 6-1. 원인 — **날짜가 지나야 드러나는 갈래**

- `ApiResponse` 에 **`@JsonInclude(NON_NULL)`** 이 붙어 있어 **열린 이벤트가 없으면 `data` 키가 통째로 빠진다.**
- 헬퍼 `openEventName` 은 *"없으면 만들고, 있으면 그걸 쓴다"* 고 적어 놓고
  `JsonPath.read(body, "$.data")` 로 읽는다 → **null 이 아니라 예외를 맞는다.**
  즉 **«없을 때를 위해» 쓴 갈래가 «없을 때» 터진다.**
- 실측: 열린 이벤트 **0건**(`5000` 08-13 마감 · `ZZ-이벤트쿠폰3` 08-14 마감, 둘 다 CLOSED).

🔴 **그동안 초록이었던 이유는 맞아서가 아니라 그 갈래를 안 밟아서다.** 08-13~14 에는 운영에 열린 이벤트가
늘 있어 **항상 첫 갈래로 흘렀고**, 오늘 둘의 발급 창이 다 지나자 처음으로 두 번째 갈래에 닿았다.
⚠ **코드를 아무리 봐도 안 보이는 종류다** — 달력이 넘어가야 드러난다.

### 6-2. 대응 — 08-14 §6-3 의 갈래로 「고친다」 쪽

*"테스트의 정의가 틀렸다 → 고친다 / 전제가 운영에 깨졌다 → 견디게 만든다"* 중 **전자**다.
헬퍼가 하겠다고 적은 일을 **실제로 못 하고 있었다.**
→ `bannerOrNull()` 로 모았다. ⚠ **같은 한 줄이 이 클래스에 네 곳 있었다** — 넷 다 «없으면 null 이겠지» 로
쓰였고 넷 다 같은 예외를 맞는다. 같은 사실이 네 곳에 흩어져 있으면 **한 곳만 고쳐진다.**

### 6-3. ⚠ 규약으로 올리지 않는다

**한 번의 관찰이다**(08-14 이월의 세는 기준과 같은 이유 — 한 번의 관찰을 여러 번으로 세지 않는다).
사용자와 확정. **또 나오면 그때 WA 로 올린다.**

---

## 7. 🔴 내가 쓴 테스트가 간헐적으로 실패했다 — **원인을 확정하지 못했고, 그렇게 적는다**

`priceFilterUsesSalePrice` 가 **3~4회 중 1~2회** 실패했다(원상복구 상태에서도).
구조가 *«같은 조회를 세일 전후로 두 번 불러 비교»* 였고 그 사이에 `products:list` 캐시 쓰기와
`@CacheEvict` 가 끼어 있었다.

**가설 하나를 세워 실제로 밟았고 기각됐다**: *"`save()` 한 할인이 flush 되지 않아 상관 서브쿼리에
안 보인다"* → 테스트에 `em.flush()` 를 넣고 4회 돌렸더니 **여전히 2회 실패**했다.

→ **추측으로 원인을 적지 않는다.** 대신 **구조를 바꿔 그 의존을 없앴다**: 대조군을 「세일 없는 다른 상품」
으로 옮겨 **한 번의 조회로 둘을 함께** 증명한다. 이후 **5회 연속 통과.**

⚠ **대조군의 값어치는 오히려 커졌다** — 같은 순간·같은 쿼리에서 «걸리는 것» 과 «안 걸리는 것» 을
나란히 본다(WA §3-3).
🔴 **원인이 남아 있다는 사실 자체를 이월에 남긴다** — 캐시가 얽힌 다른 테스트에서 또 날 수 있다.

---

## 8. 변형 주입 — 다섯 다 잡혔다 (커밋 뒤, WA §2-6)

| 변형 | 무엇을 망가뜨렸나 | 잡은 테스트 |
|---|---|---|
| **M1** | 곱 순서 뒤집기(할인 먼저, 가격차 나중) | 곱 순서 단언 |
| **M2** | 종료 경계를 배타 → 포함 | 경계 단위 테스트 |
| **M3** | 겹침 판정 `<,>` → `<=,>=` | 「맞닿는 것은 겹침이 아니다」 |
| **M4** | 정렬·필터에서 할인율 무시 | 가격 필터 · 가격 정렬 **둘** |
| **M5** | `NO_EXCLUDE` 를 `null` 로 | 겹침 거절 |

🔴 **M5 는 실측이 주석을 확인해 줬다** — 결과가 **200**(겹침이 통과됨)이었다. 즉 «NULL 비교는 UNKNOWN 이라
`d.id <> :excludeId` 가 항상 거짓이 되어 겹침 검사가 통째로 무력화된다» 는 주석의 주장이 **맞다.**

---

## 9. 곁다리 — `.ipt` 가 한 화면에 갇혀 있었다

기간 할인 화면에 네이티브 입력이 필요해 클래스를 찾다가, `.ipt` 가 **`CouponAdminView` 의
`<style scoped>` 안에만** 있는 것을 봤다. 🔴 **그런데 `StatsAdminView` 도 `class="ipt"` 를 쓴다** —
scoped 라 **거기서는 안 먹고, 그 화면의 입력칸은 스타일 없이 그려지고 있었다.**

⚠ **아무것도 안 깨지고 콘솔에도 안 남는다** — 08-13 §8 의 `border-ink-200`(정의 없는 토큰)과 같은 종류다.
**세 번째 화면이 같은 것을 필요로 할 때까지 안 보였다.**
→ `index.css` 로 올리고 scoped 사본을 지웠다(같은 사실이 두 곳에 있으면 한쪽만 고쳐진다).

🔴 **DESIGN §7 의 「관리자 기간 선택은 네이티브 date」 규약이 이 클래스를 전제로 한다** — 규약이 공용인데
스타일이 한 화면에 갇혀 있으면 **다음 화면이 또 같은 자리를 밟는다.**

---

## 10. 하루 총괄 (⚠ 이 절은 **집계**다 — 마감마다 다시 센다, WA §4-0)

- 커밋 **8**(기능 3 · 테스트 1 · 문서 4 — 이 마감 커밋 제외)
- 마이그레이션 **V52** — 새 테이블 하나. 백필 없음. 순수 추가라 구 jar 무해.
  ✅ **빈 DB 검증까지 통과**(§12).
- 백엔드 전수 **805** · skipped **0** · 실패 **0** (08-14 마감 **762** 대비 **+43**)
- 프론트 **228** · 테스트 파일 **28**
- 뷰 테스트 `views/` **5** · `components/` **7** (오늘 `views/` 에 하나 늘었다 — `ProductDiscountAdminView`)
- 변형 **5건 주입 · 5건 다 잡힘**
- 새 파일 **15**(백엔드 13 · 프론트 2 — 이 핸드오프 포함)
- 배포 **2회**(13:18 G-5 · 17:34 달력) — 둘 다 브라우저 검증까지 닫았다
- **기능 종결 2**: G-5 기간 할인(§1~§11) · B-27 후속 달력에 타임세일(§14)
- **곁다리로 고친 것 2**: `EventCouponIntegrationTest` 잠복 결함(§6) · `.ipt` 공용 승격(§9)
- **원인 미확정 3**: 가격 필터 테스트 간헐 실패(§7, flush 가설 **기각**) ·
  `deleted_at` 오프셋 혼재(§1-1) · 배치의 **CASCADE 미증명**(§13-2)
- 🔴 **배치가 도는 증거를 잡았다**(§13) — 16:23, 유예 만료 1분 30초 뒤
- 🔴 **겹침을 눈으로 봤다**(§14-5) — 검정·회색·테두리 세 줄이 `KIND_ORDER` 순서로 쌓였다

---

---

## 11. ✅ 배포 · 브라우저 검증 — **종결**

배포는 사용자가 직접 실행(`sudo`). 배포됐다는 말과 **새 코드가 돌고 있다**는 말은 다르므로 셋을 실측했다:

| 무엇 | 실측 |
|---|---|
| 백엔드 | **13:18:30** 기동 · 헬스 200 · 공개 목록 응답에 **`regularPrice` 가 실린다**(신 코드의 증거) |
| V52 | `flyway_schema_history` 에 `52 / add product discount / success=1` |
| 프론트 | nginx 가 주는 번들 **`index.dc0f9338.js`** = 레포 `dist` 와 동일 |

⚠ **캐시 flush 도 했다**(`products:list` 패턴 0건 확인). 응답에 필드 셋이 늘어 구 jar 가 쓴 값을
신 jar 가 읽는 창이 최대 60초 생기는 자리다(WA §2-7).

### 11-1. 밟은 것 — **표본을 만들어서 밟았다**

🔴 **`priceDelta ≠ 0` 인 옵션이 운영에 하나도 없어서**(실측 10개 전부 0) 검증용 상품을 새로 만들었다:
`ZZ-세일검증` · 판매가 **10,000** · 정가 비움 · 옵션 둘(「기본」 +0 / **「L」 +2,000**) · 재고 5씩.
⚠ **두 번째 옵션이 없으면 곱 순서를 뒤집어도 화면이 똑같다** — 08-14 §9-5 와 같은 이야기다.

| 스텝 | 무엇을 증명하나 | 결과 |
|---|---|---|
| 되읽기 문장 | 폼이 만들 것을 **금액까지** 말하나 | ✅ 「8월 19일부터 8월 19일까지 20% — 10,000원이 8,000원이 됩니다.」 |
| 🔴 **관리자 폼** | 판매가 칸이 **세일 전 값**인가 (§3 의 덫) | ✅ **10,000**(8,000 아님) + 「지금 20% 세일 중 — 고객에게는 8,000원」 |
| 고객 상세 | 판매가 취소선 + 세일가 + 종료일 | ✅ 10,000 취소선 · **8,000** · 20% · 「8월 19일까지」 |
| 🔴 **곱 순서** | 가격차를 더한 뒤 할인율인가 | ✅ 옵션 「L」 **9,600** = (10,000+2,000)×0.8 |
| 가격 필터 | 필터가 세일가를 보나 | ✅ 「9,000 이하」에 **나온다** |
| 🔴 **그 대조군** | 필터가 **실제로 거르고는 있나** | ✅ 「7,000 이하」에 **안 나온다**(8,000 > 7,000) |
| 정렬 | 「가격 낮은순」이 세일가 순인가 | ✅ |
| 장바구니 | 세일이 **청구로 이어지나** | ✅ 「기본」 8,000 · 「L」 9,600 |
| 겹침 | 앱이 유일한 방어 | ✅ 오늘~모레 30% → **「기간이 겹치는 할인이 이미 있습니다.」** |
| 🔴 **맞닿는 경계** | 연속 세일을 이어 붙일 수 있나 | ✅ **내일~모레는 통과**, 「예정」 배지 |

🔴 **「9,000 이하」와 「7,000 이하」를 나란히 밟은 것이 요점이다** — 앞엣것만 보면
«필터가 세일가를 본다» 와 «필터가 아예 안 걸린다» 가 **구분되지 않는다**(WA §3-3).

⚠ **자동 테스트로만 덮인 자리 없음.**

### 11-2. ⚠ 「정리했다」와 「남은 게 없다」는 다른 말이었다

사용자가 *"모두 진행하니까 잘 됐어"* 라고 했고 실제로 스텝은 다 통과했다. 그런데 **처분(7번)은
안 됐다** — 실측하니 상품이 살아 있고 할인 **2건**이 남아 있었다.

🔴 **나무랄 일이 아니라 적어 두면 되는 일이다**(08-14 §V51 검증 잔재와 같은 취급). 안 적으면
나중에 «이 세일 뭐지» 가 된다. ⚠ **그리고 물어보지 않고 «정리됐겠지» 로 문서를 닫았으면
조건부 잔여가 통째로 비어 있었을 것이다.**

⚠ 겹침 검증의 두 번째 할인이 30% 가 아니라 **20%** 로 등록됐다(할인율 칸을 그대로 두고 날짜만 바꾼 것).
**맞닿는 경계 통과를 증명하는 데는 할인율이 무관하므로 검증은 유효하다.**

### 11-3. 🔴 그래서 관측 기회가 하나 생겼다 — **08-20 00:00**

남은 할인 둘이 **경계를 맞대고 있다**:

| 할인 | 기간(KST) | 지금 |
|---|---|---|
| ① 20% | 08-19 00:00 ~ **08-20 00:00** | 진행 중 |
| ② 20% | **08-20 00:00** ~ 08-22 00:00 | 예정 |

→ **08-20 00:00 에 ①이 끝나고 ②가 곧바로 시작한다.** 그날 상품을 열어 보면:

- **「맞닿는 경계」가 실제로 이어지는가** — 세일가 8,000 이 **끊기지 않고** 유지되면 그렇다
  (한 순간이라도 10,000 이 보이면 경계에 틈이 있다는 뜻이다)
- **세일이 저절로 전이하는 순간** — 아무 조작도 없이 상태가 바뀐다
- ⚠ **캐시 60초 지연이 눈에 띄는지**(§2-2 에서 감수하기로 한 대가) — 실측으로 본 적이 아직 없다

🔴 **사용자 결정: 남긴다**(2026-08-19). 08-13 이 `ZZ-이벤트쿠폰3` 를 «내일 D-day 전이를 볼 표본» 으로
남긴 것과 **같은 모양**이다.

---

## 12. ✅ 빈 DB 마이그레이션 검증 (`esptest`) — **통과** (WA §2-2)

⚠ **기존 DB 에서 성공하는 것과 빈 DB 에서 처음부터 깔리는 것은 다른 문제다.** 오늘 배포는 이 검증
**없이** 나갔고(스키마 비우기가 자동 승인에서 막혀 있었다), 뒤늦게 사용자 승인으로 돌렸다.

### 12-1. 절차와 실측

| 단계 | 실측 |
|---|---|
| 지울 대상 확인 | `select user` → **`ESPTEST`**(운영 `ESP` 아님) · 테이블 **23** · 시퀀스 **1**(이전 검증 잔재) |
| 비우기 | 테이블 **0** · 시퀀스 **0** |
| 기동(8083) | 15:23:35 시작 → **15:24:56 `Started GlassvueBackendApplication`**(83.6초) |
| Flyway | 🔴 **`Successfully applied 52 migrations to schema "ESPTEST", now at version v52`** (13.68초) |
| 순서 | `1 - init` → `2 - add query indexes` → … → `51 - audit order admin actions` → **`52 - add product discount`** |
| `ddl-auto=validate` | ✅ **통과** — ⚠ 별도로 확인할 것이 없다. **validate 가 어긋나면 애초에 안 뜬다**(기동 자체가 증거다) |

⚠ **시퀀스를 함께 지운 것이 중요하다** — 테이블만 지우면 V15 가 만든 `seq_order_no` 가 살아남아
다음 검증에서 `CREATE SEQUENCE` 가 ORA-00955 로 실패한다(README 가 2026-07-23 사고로 적어 둔 자리).

### 12-2. 빈 DB 에서 `product_discount` 가 의도대로 생겼나

| 확인 | 실측 |
|---|---|
| 감사 컬럼 | `created_at`·`updated_at` **`TIMESTAMP(9) WITH TIME ZONE`** (V26 사고 회피) |
| 기간 컬럼 | `starts_at`·`ends_at` 도 같은 타입 · NOT NULL |
| CHECK | `CK_PRODUCT_DISCOUNT_RATE`(`rate BETWEEN 1 AND 99`) · `CK_PRODUCT_DISCOUNT_PERIOD`(`ends_at > starts_at`) |
| FK | `FK_PRODUCT_DISCOUNT_PRODUCT` |
| FK 인덱스 | `IX_PRODUCT_DISCOUNT_PRODUCT`(NONUNIQUE) — 성능이 아니라 **부모 삭제 시 자식 테이블 락**을 막으려고 만든 것 |
| 테이블 수 | 23 → **24** (딱 하나 늘었다) |

### 12-3. 자격증명 노출 확인 — **떠 있는 동안** 했다 (내려간 뒤엔 볼 수 없다)

- `/proc/<pid>/cmdline` 에 `password` **0건**. 인자는 `--server.port=8083 --spring.profiles.active=dev` **둘뿐**.
- `ps` 전수에서 비밀번호 값 **0건**.
- ⚠ **값이 사라진 게 아니라 옮겨간 것**이다 — 환경변수는 `/proc/<pid>/environ` 에 남고 그쪽은
  프로세스 소유자만 읽는다. 검증 전용 로컬 계정이라 여기까지로 둔다(README 의 판단 그대로).

### 12-4. 내렸다 — 그리고 운영이 멀쩡한지 봤다

- 8083 리스닝 **0** · 남은 프로세스 **없음**(⚠ 떠 있으면 다음 검증이 포트 충돌로 죽는다).
- 🔴 **운영 8080 헬스 200** — 검증이 공유 DB 를 쓰므로 **끝나고 운영을 확인하는 것이 규약이다**(WA §3).

---

## 13. 🔴 배치가 돌았다 — 08-12 §14-5 가 기다리던 증거 (16:23)

08-12 에 «배치가 도는지 확인할 방법이 없다» 고 적고 **대조군 상품을 일부러 남겨 뒀다.**
그 유예가 오늘 **16:22** 에 만료됐고, 지켜봤다.

```
16:23:38  [상품] 영구 삭제 — id=019ff4d6-… name=ZZ-테스트용입니다123123213123 (유예 경과)
16:23:41  [상품] 유예 경과 1건 영구 삭제 (기준: 2026-08-12T07:23:30.135Z 이전 삭제 · 유예 7일)
```

- **삭제 대기 4건 → 3건**(16:25:14 에 감지 — 2분 주기로 세고 있었다).
- 유예 만료로부터 **약 1분 30초** 뒤에 지웠다. sweep 이 1시간 주기라 최대 1시간 늦을 수 있었는데
  마침 타이밍이 좋았다(16:23:30 에 돈 회차가 잡았다).
- 🔴 **로그가 두 줄인 것이 설계의 증언이다** — 상품 한 줄 + 요약 한 줄.
  `ProductPurgeScheduler` 주석이 *"한 건씩 지운다. 하나가 터져도 나머지는 지워지고,
  **어느 상품에서 터졌는지 로그에 남는다**"* 라 한 그 모양 그대로 찍혔다.
  벌크 delete 였으면 «1건 삭제» 만 남고 **어느 상품인지는 영영 알 수 없었다.**

### 13-1. ⚠ 그런데 08-12 의 문장은 **절반만** 답해졌다

실측: 08-12 이후 지금까지 스케줄러 로그가 **오늘 두 줄뿐**이다.
🔴 **0건이면 로그를 안 남기는 것이 설계**이기 때문이다(`ProductPurgeScheduler` — *"대부분의 실행이
0이다"*). 그래서 여전히 **«돌고 있는데 지울 게 없다» 와 «안 돌고 있다» 는 구분되지 않는다.**

- ✅ 오늘 답해진 것: **«지울 게 생기면 실제로 지운다»**
- ❌ 아직 못 답하는 것: **«지금 이 순간 스케줄러가 살아 있나»**

⚠ 실용적으로는 앞엣것이 필요한 답이라 **여기서 멈춘다** — 헬스체크용 로그를 매시간 남기면
저널이 그걸로 채워지고, 그건 «미리 만들지 않는다» 에 걸린다.
🔴 **08-21 에 세 건이 한꺼번에 만료된다** — «여러 건을 한 회차에» 처리하는 갈래를 그때 볼 수 있다.

### 13-2. ⚠ CASCADE 를 «봤다» 고 적지 않는다

옵션·재고 이력이 함께 사라지는 것이 설계지만(FK CASCADE), **지우기 전 값을 안 세어 뒀다** —
지금 `product_variant` 11 · `stock_history` 36 인데 비교할 기준이 없다.
🔴 **그러니 «CASCADE 가 일했다» 는 오늘 증명되지 않았다.** 08-21 에 셋이 사라질 때
**미리 세어 두면** 그때 증명된다.

---

## 14. ✅ 프로모션 달력에 타임세일을 얹었다 (B-27 + G-5)

08-13 에 만든 달력은 **쿠폰만** 그렸다. 🔴 **오늘 G-5 가 들어오면서 비로소 이 화면이 원래 답하려던
질문에 답한다** — 그전까지 겹칠 것이 쿠폰뿐이라 «겹침» 이 사실상 «발급 창끼리» 하나였다.

⚠ **새로 보이게 된 것**: 쿠폰 발급 창과 상품 세일이 **같은 날에 있으면** 그 날은
**이미 깎인 세일가 위에 쿠폰이 또 붙는다**(마진이 두 번 깎인다). 그게 격자에서 눈으로 잡힌다.

### 14-1. 🔴 착수 전 결정 — 조립을 어디서 하나 (사용자 확정)

달력이 이제 **두 도메인**(쿠폰·상품)을 합친다. CLAUDE.md 는 도메인 간 직접 참조를 금지한다.

| 안 | 대가 |
|---|---|
| **✅ coupon 안에 `PromotionCalendarService` 신설** | 조립하는 자리가 이름을 갖는다. 상품은 공개 API 로만 읽어 catalog 를 폴더째 들어낼 수 있다 |
| `CouponService` 에 그냥 더한다 | 가장 싸지만 «쿠폰 도메인 서비스가 상품 세일을 조립한다» 가 된다 — **다음 사람이 발송·기획전을 얹을 때 또 쿠폰 서비스를 연다** |
| `promotion` 도메인 신설 | 이름은 가장 정직하지만 **화면 하나를 위해 도메인을 만드는 셈** |

⚠ **그래도 coupon 패키지 안이다** — 달력의 뿌리가 쿠폰이고 URL·화면도 거기 있다.
🔴 **발송·기획전까지 여기 모이면 그때가 `promotion` 도메인을 세울 때**라고 코드에 적어 뒀다.

### 14-2. 🔴 필드 이름이 어긋나 있었다 — `event` 는 «이벤트인가» 가 아니었다

`PromotionSpanResponse.event` 의 실제 뜻은 **«격자에 그릴 것인가»** 였다(상시 쿠폰은 스트립으로 뺀다).
세일은 **이벤트가 아닌데 격자에 그려야** 해서 이름이 처음으로 어긋났다. → **`gridded`** 로 바꿨다.

⚠ 함께 중립화한 것: `couponId` → `id`, `discountType`·`discountValue` → **`label`**(서버가 만든 문구).
🔴 **문구를 서버가 만드는 이유**: 쿠폰은 `DiscountType` enum 이고 세일은 %다 — **두 도메인의 표기
규칙**이라 화면이 하나의 함수로 다루려면 화면이 양쪽을 다 알아야 한다. **날짜를 서버가 잘라 주는 것과
같은 이유**다(달력은 조립물이다).
⚠ **프론트 `couponDiscountText` 는 그대로 남는다** — 세어 보니 **쿠폰 화면 일곱 곳**이 계속 쓴다.
🔴 «지우기 전에 이름을 저장소 전체에서 센다» 가 여기서도 답을 줬다(§3-1 과 같은 규율).

### 14-3. 색을 쓰지 않고 **채움 강도**로 갈랐다

셋이 됐는데 DESIGN 은 **무채색 원칙**(강조색은 CTA·상태에만)이다.

| 막대 | 그림 | 왜 |
|---|---|---|
| 발급 창 | **검정 채움** | 겹치면 **사고**다(서버가 등록 때 막는다) |
| 타임세일 | **회색 채움** | 겹칠 수 있지만 쿠폰과 겹치면 이중 할인 |
| 사용 기간 | 테두리만 | **겹치는 게 정상**이다 |

🔴 **세일에 빨강을 쓰면 «겹치면 안 되는» 발급 창보다 세일이 더 튀어 읽는 순서가 뒤집힌다.**
격자 안 정렬도 같은 기준으로 «겹치면 안 되는 것» 을 위로 올린다.

### 14-4. 테스트 6건 · ⚠ 도구를 하나 못 믿게 됐다

세일이 격자 막대로 온다 · 🔴 **종료일이 하루 밀리지 않는다**(배타 경계) · 하루짜리 세일 ·
다른 달은 안 온다 · 🔴 **삭제 대기 상품은 안 뜬다**(지우기 전 대조군 포함) · 쿠폰 막대 계약 유지.

🔴 **`JsonPath` 필터에 `.length()` 를 붙였더니 걸러진 개수가 아니라 전체 개수(10)가 나왔다**(실측).
일치하는 게 없으면 «No matching value» 로 터져서 **«0건이다» 를 단언할 수가 없었다.**
⚠ **공유 espdb 라 그 필터가 이 테스트의 전제인데**, 전제가 미덥지 않으면 초록이 아무것도 증명 못 한다.
→ 응답을 읽어 **Java 에서 거른다.** 애매한 도구 대신 확실한 쪽으로 옮겼다.

### 14-5. ✅ 배포 · 브라우저 검증 — 🔴 **겹침을 눈으로 봤다** (17:34 배포)

| 무엇 | 실측 |
|---|---|
| 백엔드 | **17:34:54** 기동 · 헬스 200 |
| 달력 경로 | 비로그인 **401** — 관리자 전용이 유지된다 |
| 프론트 | nginx 번들 **`index.86b4e738.js`** = 레포 `dist` 와 동일 |
| ⚠ 캐시 flush | **안 했다 — 필요 없다.** `products:list` 를 안 건드렸다(이번 변경은 달력 API 뿐) |

🔴 **검증하려고 사용자가 이벤트 쿠폰을 하나 만들었고, 그게 이 화면의 존재 이유를 실제로 만들었다.**

| 무엇 | 기간(KST) |
|---|---|
| 세일 ②(`ZZ-세일검증`) | 08-20 00:00 ~ **08-22 00:00** |
| 발급 창(`ZZ-발급마감쿠폰테스트`) | **08-20 00:00 ~ 08-21 23:59** |

→ **08-20~21 에 둘이 겹친다.** 그 칸에서 사용자가 본 것:

> *"세줄 딱 있더라 — 검정, 회색, 테두리만"*

🔴 **그 순서가 곧 `KIND_ORDER` 다**(발급 창 → 세일 → 사용 기간). 「겹치면 안 되는 것을 위로」가
화면에서 그대로 나왔다. ⚠ **정렬을 안 걸었으면 순서가 새로고침마다 바뀌었을 것**이고, 그러면
관리자는 «위에 있는 게 더 중요한 것» 이라는 읽기를 배울 수 없다.

⚠ **자동 테스트로만 덮인 자리 없음.** 다만 **겹침이 화면에서 어떻게 보이는가**는 이 한 번의 눈으로만
확인됐다(테스트는 «막대가 온다» 까지만 본다) — 이 화면에는 뷰 테스트가 없다.

---

## 이월 (다음 문서로 반드시 옮길 것)

> WORKING-AGREEMENTS §4-1. 다음 날 문서가 그대로 물려받아 시작한다. 처리했으면 지운다.

**다음 세션은 여기서 시작한다** — 순서는 이렇다:

1. 🔴 **08-20 00:00 에 세 가지가 한꺼번에 일어난다** — 오늘 검증이 만든 표본 덕에 관측 기회가
   커졌다(§14-5). 그 시각에:
   - 세일 ①(~08-20 00:00)이 끝나고 **②(08-20~08-22)가 곧바로 시작**한다
   - **`ZZ-발급마감쿠폰테스트` 의 발급 창이 열린다**(08-20 00:00 ~ 08-21 23:59)
   - 즉 **그 순간부터 세일과 발급 창이 겹친다**

   **볼 것**:
   ①세일가 **8,000 이 끊기지 않고 유지되는가**(한 순간이라도 10,000 이면 맞닿는 경계에 틈이 있다)
   ②아무 조작 없이 상태가 바뀌는가(달력 배지가 「예정」→「진행 중」)
   ③🔴 **캐시 60초 지연이 눈에 띄는가**(§2-2 에서 감수하기로 한 대가인데 **실측으로 본 적이 없다**)
   ④🔴 **이중 할인을 실제로 밟을 수 있다** — 그 쿠폰을 받아 `ZZ-세일검증` 을 사면
   **이미 깎인 8,000 위에 쿠폰이 또 붙는다.** 달력이 경고하던 바로 그것이라, 밟으면
   «달력이 옳았다» 가 실측이 된다. ⚠ 되돌릴 수 없는 조작(결제)이 얽히니 **밟을지는 그때 정한다.**

   ⚠ **08-22 00:00 이 지나면 세일 표본은 그냥 잔재다** — 그때 정리한다.
   ⚠ 쿠폰 사용 마감은 **09-02** 라 여유가 있다.
2. 🔴 **08-21 에 배치를 한 번 더 본다** — 남은 셋이 그날 만료된다(11:09 · 15:55 둘).
   오늘 못 본 것 둘을 그때 볼 수 있다:
   ①**여러 건을 한 회차에** 처리하는 갈래(오늘은 1건이었다)
   ②🔴 **CASCADE** — 오늘은 **지우기 전 값을 안 세어 둬서 증명되지 않았다**(§13-2).
   ⚠ **그날 아침에 `product_variant`·`stock_history` 수를 먼저 세어 둔다.** 안 세면 또 못 증명한다.
- 🔴 **`priceFilterUsesSalePrice` 의 간헐 실패 원인은 여전히 모른다**(§7). 구조를 바꿔 **회피**했을 뿐이다.
  flush 가설은 밟아서 기각했다. ⚠ **캐시가 얽힌 다른 테스트에서 또 날 수 있다** —
  다음에 «가끔 빨개지는 테스트» 를 만나면 **여기가 두 번째다.**
- ✅ **B-27 달력에 타임세일 — 오늘 닫았다**(§14, 배포·브라우저 검증까지). **코드로 막힌 것 없음.**
  ⚠ **남는 갈래 하나**: 이 화면에는 **뷰 테스트가 없다.** 겹침이 «어떻게 보이는가» 는 오늘 눈으로만
  확인됐고(테스트는 «막대가 온다» 까지만 본다), 🔴 **`KIND_ORDER` 정렬이 깨져도 아무것도 안 잡는다.**
  다음 뷰 테스트 후보로 둘 값어치가 있다(`OrderAdminView` 다음).
- ⚠ **감사 로그에 할인 조작을 안 붙였다.** 오늘 결정에 감사가 없었고, **상품 등록·수정 감사가 아직 미결**
  (V50 에서 «무엇이 바뀌었나를 detail 에 어떻게 적을지 따로 정해야 한다» 로 미뤄 둔 자리)인데
  **할인만 붙이면 비대칭**이 된다. 🔴 **세일은 돈을 바꾸는 관리자 조작**이라 언젠가는 붙어야 한다 —
  상품 등록·수정과 **함께** 정하는 것이 맞다.
- ⚠ **`AuditAction` 에 상품 등록·수정·쿠폰·할인이 없다**(08-14 이월에서 그대로 물려받는다.
  주문 넷은 08-14 §11 에서 닫혔다).
- ⚠ **`target_type` 열은 아직 없다**(08-14 이월). 오늘 G-5 도 이 조건을 안 건드렸다(감사를 안 붙였으므로).
- ⚠ **세일 시작·종료가 저절로 일어나는 순간은 캐시 때문에 최대 60초 늦다**(§2-2, 의도한 선택).
  실측으로 «늦는 것» 을 본 적은 없다 — **브라우저 검증에서 눈에 띄면 적는다.**
- ⚠ **`recentlyViewed`(최근 본 상품)의 세일 값은 낡을 수 있다** — localStorage 스냅샷이라 세일이 끝나도
  그대로다. 🔴 **다만 가격 자체도 원래 그랬다**(관리자가 판매가를 바꿔도 안 따라온다) — **세일이 새로
  만든 문제가 아니다.** 클릭하면 상세에서 진짜 값을 본다.
- ⚠ **`deleted_at` 오프셋이 행마다 섞여 있다**(§1-1). 값은 맞다. **원인 미상 — 추측으로 적지 않는다.**
- 🔴 **「화면이 유일한 방어」가 오늘 **세 번째**로 나왔다**(08-13 이월이 «세 번째가 나오면 WA 로 올린다» 고
  적어 둔 항목). 오늘 것은 **기간 겹침 금지** — Oracle 유니크로 못 막아 앱이 유일한 자리다.
  ⚠ **그런데 08-13 의 둘과 오늘 것은 같은 뿌리다**(둘 다 「기간 겹침을 DB 로 못 막는다」). 🔴 **하나를
  세 번으로 세지 않는다** — 08-13 §4(이벤트 발급 창)와 오늘은 **같은 관찰의 되풀이**이고, 08-13 §13-2
  (정가 > 판매가)만 다른 종류다. **즉 실제로는 두 종류에 두 번이다. 아직 올리지 않는다.**
- ✅ **「필드를 지우기 전에 이름을 센다」를 오늘 WA §2-10 으로 올렸다**(사용자 확정).
  08-13 이월이 «두 번째까지 와 있다 · 세 번째가 나오면 올린다» 고 조건을 걸어 뒀고, **오늘 두 번 더
  값을 해서** 조건이 성립했다 — §3-1(`product.price` **7 대 1**)과 §14-2(`couponDiscountText` **7곳**).
  🔴 **둘은 다른 작업(G-5 · B-27)이라 «한 번의 관찰을 여러 번으로 세는» 것이 아니다**
  (08-14 이월의 규율을 그대로 적용해 판단했다).
  ⚠ **범위를 넓혀 적었다** — «지우기» 만이 아니라 **«뜻을 바꾸기»** 까지다. 오늘 둘 다 지운 게 아니라
  뜻을 바꾼 경우였고, **그쪽이 더 위험하다**(이름이 그대로라 아무것도 안 걸린다).
- ⚠ **뷰 테스트는 `views/` 에 5개**(`CouponAdminView`·`ProductTrashAdminView`·`AuditLogAdminView`·
  `MemberDetailAdminView`·**`ProductDiscountAdminView`**), `components/` 에 7개.
  **다음 후보: `OrderAdminView`**(08-14 이월이 지목 — 발송·반품 승인, **되돌리기 어려운 조작이 남은
  마지막 자리**. 열어서 확인했다: 발송 패널·반품 승인/거절 `confirm`·취소 패널이 다 있다) ·
  `ReviewAdminView`·`InquiryAdminView`·`ProductFormView`.
- 🔴 **다음 기능 후보**(오늘 처리한 것은 뺐다 — WA §4-0 이 지목한 자리다):
  **G-4 부분 취소**(중 — ⚠ 어려운 곳이 UI 가 아니라 **정산 규칙**이다) ·
  **감사 확대: 상품 등록·수정 + 쿠폰 + 할인**(중 — 아래 항목, **셋을 함께 정해야 한다**) ·
  **G-6 등급 혜택 / G-7 최근 검색어**(소).
  ⚠ **뷰 테스트**는 기능이 아니지만 후보와 같은 줄에 둔다 — `OrderAdminView`(되돌리기 어려운 조작이
  남은 마지막 화면) · `PromotionCalendarAdminView`(오늘 만든 정렬 규칙을 아무것도 안 지킨다).
  ⚠ **F-2 는 아직 아니다** · **B-26 나머지는 «조건이 갖춰지면»** · **MSA 는 아직 아니다**(08-14 이월 그대로).
- ⚠ **08-14 이월의 나머지는 그대로 물려받는다** — 정가 칸 원인 미상 · 겹침 금지가 앱에만 있다 ·
  `SignupView` 의 `border-ink-200` · 배치 확인 수단 없음(오늘 §1-1 로 **일부 갚힌다**) ·
  막힌 것 셋(배송완료 리뷰 · 탈퇴 회원 관리자 조작 · 탈퇴 시 장바구니 정리) · F-2 는 아직 아니다 ·
  MSA 는 아직 아니다 · B-26 나머지는 «조건이 갖춰지면».

### 조건부 잔여

**08-14 §조건부 잔여를 그대로 물려받는다.** ⚠ 개수를 여기 적지 않는다 — 목록이 곧 개수다(WA §4-0).

#### 🔴 검증 상품 — **오늘 하나가 사라졌다** (마감 시점 실측)

| 상품 | 삭제 대기(KST) | 유예 만료 | 마감 시점 |
|---|---|---|---|
| ~~`ZZ-테스트용입니다123123213123`~~ (대조군) | 08-12 16:22 | 08-19 16:22 | ✅ **16:23:38 영구 삭제**(§13) |
| `ZZ-옵션삭제검증` | 08-14 11:09 | **08-21 11:09** | 살아 있음 |
| `ZZ-정가텟트` | 08-14 15:55 | **08-21 15:55** | 살아 있음 |
| `ZZ-재고알림테스트` | 08-14 15:55 | **08-21 15:55** | 살아 있음 |

⚠ **손댈 것이 없다.** 볼 것만 있다 — **08-21 에 셋이 한꺼번에** 만료된다.

🔴 **CASCADE 를 증명할 기준값을 지금 적어 둔다**(§13-2 가 오늘 못 한 것):

| 무엇 | 2026-08-19 마감 시점 |
|---|---|
| `product_variant` | **11** |
| `stock_history` | **36** |

→ 08-21 에 셋이 사라진 뒤 다시 세어 **줄어든 만큼**이 CASCADE 다.
⚠ **그 사이에 상품을 만들거나 지우면 기준이 흔들린다** — 그러면 그날 아침에 다시 세는 것이 먼저다.
⚠ 재고 이력은 상품과 함께 사라지고 **감사 줄은 남는다.**

⚠ **16번의 미결제 주문 둘**(`20260813-4185`·`20260813-4186`)은 **그대로다** — 오늘 안 건드렸다.
⚠ **V51 검증이 바꾼 옛 주문 둘**(`20260724-0249` `SHIPPED` · `20260727-0257` `RETURNED`)도 **그대로다**.

#### 🔴 오늘 브라우저 검증으로 생긴 것 — **남기기로 했다** (2026-08-19, 사용자 결정)

| 무엇 | 값 | 처분 |
|---|---|---|
| 상품 `ZZ-세일검증` | 15:13 생성 · 판매가 **10,000** · 옵션 둘(「기본」 +0 / **「L」 +2,000**) · 재고 5씩 · **마감 시점 살아 있는 유일한 `ZZ` 상품** | 🔴 **남긴다** |
| 할인 ① | **20%** · 08-19 00:00 ~ **08-20 00:00** (진행 중) | 🔴 **남긴다 — 전이를 볼 표본** |
| 할인 ② | **20%** · **08-20 00:00** ~ 08-22 00:00 (예정) | 🔴 **남긴다** |

🔴 **남기는 이유가 이 표본의 존재 이유다** — 둘이 **경계를 맞대고 있어** 08-20 00:00 에
«맞닿는 경계가 실제로 이어지는가» 를 운영에서 볼 수 있다(§11-3). 08-13 이 `ZZ-이벤트쿠폰3` 를
«내일 D-day 전이를 볼 표본» 으로 남긴 것과 **같은 모양**이다.

⚠ **이 상품은 `priceDelta ≠ 0` 인 옵션을 가진 유일한 상품이다**(운영의 나머지 10개는 전부 0).
곱 순서를 다시 밟을 일이 있으면 **여기가 유일한 대상**이다 — 지우면 다시 만들어야 한다.

⚠ **`ZZ-세일검증` 이 세일 중인 채로 고객 목록에 뜬다**(08-22 00:00 까지). 데모·발표에서 눈에 띈다.
🔴 **세일은 되돌릴 수 있는 조작이다** — 지우면 그 즉시 원가로 돌아간다(발송처럼 «지울 방법이
없어지는» 자리가 아니다). **08-22 이후에 정리한다.**

⚠ 겹침 검증의 두 번째 할인이 **30% 가 아니라 20%** 다(할인율 칸을 그대로 두고 날짜만 바꾼 것).
**맞닿는 경계 통과를 증명하는 데는 무관하다.**

#### 🔴 달력 검증으로 이벤트 쿠폰이 하나 생겼다 (2026-08-19 17:39)

| 무엇 | 값 |
|---|---|
| `ZZ-발급마감쿠폰테스트` | 발급 창 **08-20 00:00 ~ 08-21 23:59** · 사용 마감 **09-02** · 지금 **예정(UPCOMING)** |
| 발급분 | **0** |

🔴 **남긴다** — 이게 세일 ②와 **08-20~21 에 겹치는** 표본이고, 달력이 경고하는 「이중 할인」을
실제로 밟을 수 있는 유일한 대상이다(위 이월 1번 ④).
⚠ **`ZZ` 접두사가 있다**(WA §3-1) — 08-13 의 `5000` 이 접두사 없이 만들어져 «세는 쿼리로 못 찾는»
항목이 됐던 것과 다르다. **세는 쿼리로 찾힌다.**
⚠ **08-21 23:59 이 지나면 발급 창이 닫힌다** — 그 뒤엔 받을 수 없다. 밟으려면 그 전에 받아야 한다.
⚠ 이 쿠폰이 생기면서 **운영에 「열린 이벤트」가 다시 생긴다**(08-20 부터). 🔴 그건
`EventCouponIntegrationTest` 가 **첫 갈래로 흐른다**는 뜻이다 — 오늘 §6 에서 고친 «없을 때» 갈래는
**08-22 이후에나 다시 밟힌다.**

⚠ **장바구니에 담은 줄은 지우지 않았다** — 상품이 배치로 지워질 때 CASCADE 로 함께 정리된다.
🔴 통합 테스트가 만든 것은 남지 않는다(`@Transactional` 롤백 — 실측으로 확인).
