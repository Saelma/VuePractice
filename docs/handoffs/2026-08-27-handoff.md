# 2026-08-27 핸드오프 — I-7 을 닫았고, **이월이 시킨 「코드부터 열어라」가 실제로 걸러 냈다**

> §I-7(「부분」이 주문 목록에서 안 보인다)을 고쳤다.
>
> 🔴 **오늘의 요점은 셋이다.**
> ① **아침 기준값 재계수가 처음으로 «전부 일치» 로 끝났다**(§1) — WA §3-5 가 생긴 다음 날이고,
>    규약이 «다르면 정보, 같으면 확인» 이라고 한 그 «같은 쪽» 을 처음 밟았다.
> ② 🔴 **I-7 에 적힌 항목 셋 중 하나가 «적힌 시점에 이미 거짓» 이었다**(§2) — 08-25 에 I-7 을
>    쓰면서 같은 날 자기 커밋(`bd6a741`)이 고친 것을 못 본 것이다. **착수 전에 코드를 연 덕에 걸렀다.**
> ③ 🔴 **또 하나는 «막힌 곳이 어디인가» 가 뒤집혔다** — 고객 목록은 서버가 진작부터 값을 다
>    내려주고 있었고 **화면만 안 그렸다.** 한 항목에 묶여 있어 «둘 다 서버가 안 준다» 로 읽혔다.

---

## 0. 오늘의 커밋

| 해시 | 무엇 |
|---|---|
| `ba4cd5e` | fix(order): 「부분」이 주문 목록에서 안 보이던 자리를 고친다 (BACKLOG I-7) |
| `f57f70f` | docs: I-7 을 완료로 옮기고 08-27 핸드오프를 쓴다 |
| `cd40c2c` | fix(order): 반품 승인 문구를 한 곳으로 모은다 — 상세가 수량을 안 말했다 (§I-7 후속) |
| `9d1f8f9` | docs: 08-27 이월을 push·배포 완료 상태로 맞춘다 |
| `a82c0a2` | docs: I-9 결정 표를 만들고 I-15 를 신설한다 |
| `685cee1` | feat(order): 반품 요청에 7일 기한을 건다 (BACKLOG I-9) |
| `2b9c732` | docs: I-9 을 완료로 옮기고 08-27 §10~§12 를 쓴다 |
| `4799504` | docs: ZZ-123123 관측 결과와 «배치가 밀린 진짜 이유» 를 적는다 |
| `c17246f` | fix(audit): 원장 detail 절단을 감사 도메인 한 곳으로 모은다 (BACKLOG I-13) |
| `5849bf2` | docs: I-9·I-13 배포·검증과 08-27 마감 기준값을 적는다 |
| `3d51f62` | docs: 매출을 다시 세고 「줄었을 것」이라는 내 예상을 바로잡는다 |
| `811b0a9` | feat(order): 관리자 대행 반품 요청을 만든다 (BACKLOG I-15 · V60) |

---

## 1. 아침 기준값 재계수 — **전부 일치** (WA §3-5)

어제 신설한 규약의 첫 적용이다. 09:32 실측.

| | 08-26 마감값 | 08-27 아침 | |
|---|---|---|---|
| `product_variant` / `stock_history` | 10 / 78 | **10 / 78** | ✅ |
| product alive / trash | 7 / 1 | **7 / 1** | ✅ |
| `point_history` / `notification` / `orders` | 50 / 186 / 80 | **50 / 186 / 80** | ✅ |
| 김기현팀 적립금 / 누적구매 / 등급 | 179,646 / 153,978 / SILVER | **동일** | ✅ |
| 김기현팀4 | 0 / 0 / BRONZE | **동일** | ✅ |

⚠ **이것이 규약의 «같은 쪽» 이다** — WA §3-5 는 *«같으면 «안 움직였다» 가 실측으로 확인된 것»* 이라
적어 뒀다. 어제(08-26)는 **다른 쪽**을 밟아 값 셋이 뒤집혔다. 하루 만에 양쪽이 다 나온 셈이라,
규약이 «틀린 문서를 잡는 장치» 만이 아니라 **«안 움직였음을 말할 수 있게 하는 장치»** 이기도 하다는
것이 드러났다. 🔴 **재계수 없이는 «안 움직였다» 도 주장할 수 없다.**

### 1-1. 이월의 낡은 문장 하나를 지웠다

08-26 이월 **7번**이 *「프론트 재배포가 남아 있다」* 라고 적고 있었다. **거짓이다** —
프론트 런타임의 마지막 변경은 `04cf17d`(StatsAdminView 문구)이고 08-26 §6-3 이 배포·번들 해시
확인까지 적어 뒀다. 그 뒤 프론트를 건드린 커밋은 `32f0e95` 하나인데 `order.test.js` **테스트 파일만**
바꿨다.

🔴 **08-25 이월에서 그대로 물려받은 문장이 하루를 더 살아남았다.** 08-26 이 «이월의 낡은 문장 넷을
실제와 맞춘다»(`2d88f27`)를 하고도 이건 안 걸렸다. ⚠ **이월 대조는 «오늘 새로 쓴 항목» 보다
«어제에서 물려받은 항목» 에서 더 자주 틀린다** — 새로 쓴 것은 방금 본 것이고, 물려받은 것은
아무도 다시 안 본다.

---

## 2. 🔴 I-7 의 세 항목을 착수 전에 하나씩 열어 봤다 — **셋 중 하나가 이미 거짓**

08-26 이월 2번이 *«남은 넷도 같은 방식으로 코드를 열어 확인한 뒤 착수한다»* 라고 적어 둔 그대로 했다.

| I-7 이 주장한 것 | 지금도 참인가 |
|---|---|
| ① `AdminOrderResponse` 에 부분 필드가 없다 | ✅ **참** |
| ② 목록 승인 confirm 이 «결제금액을 …» (전량 시절 문구) | ❌ 🔴 **거짓 — 적힌 시점에 이미 고쳐져 있었다** |
| ③ 고객 목록이 원본 수량·합계만 그린다 | ✅ **참 — 단 성질이 다르다**(§2-2) |

### 2-1. ②가 «적힌 시점에 이미 거짓» 이었던 경위

`bd6a741`(2026-08-25 13:30, *fix(stats): 매출이 부분 반품을 안 빼던 자리를 고친다 — I-1*)이
같은 커밋에서 `OrderAdminView.vue` 의 confirm 문구를 고쳤다:

```
- 재고 복원 + 결제금액을 적립금으로 환불합니다.
+ 요청된 품목의 재고가 복원되고 그 몫이 적립금으로 환불됩니다.
```

I-7 은 **같은 날 그 뒤에 작성됐다.** 즉 «I-1 을 고치면서 곁다리로 고친 자리» 를 «I-7 의 할 일» 로
다시 적은 것이다. ⚠ **커밋이 항목 하나만 건드리지 않을 때 생기는 어긋남**이다 — I-1 커밋 메시지는
매출 식 이야기만 하고, 문구 수정은 본문에 안 적혀 있다.

🔴 **그래서 「코드부터 열어라」가 값을 했다.** 안 열었으면 **이미 고쳐진 것을 고치고**, 「고쳤다」고
핸드오프에 적었을 것이다.

### 2-2. ③은 «막힌 곳» 이 서버가 아니라 화면이었다

I-7 이 ①과 ③을 한 항목에 묶어 놔서 *«관리자도 고객도 서버가 부분 필드를 안 준다»* 로 읽힌다.
실제로는 **고객 쪽은 서버가 진작부터 다 주고 있었다** — `OrderService.myOrders()` 가
`AdminOrderResponse` 같은 요약본이 아니라 **full `OrderResponse`** 를 `page.map` 한다. 즉
`cancelledQuantity`·`returnedQuantity`·`remainingQuantity` 가 **목록 응답에 이미 들어 있었고**,
`OrderListView.vue` 가 `item.quantity`·`item.lineTotal` 만 그리고 있었다.

⚠ **한 항목에 성질이 다른 둘을 묶으면 크기 추정과 착수 순서가 같이 틀어진다.** 관리자 쪽은
DTO 확장(서버)이고 고객 쪽은 **화면만**이다.

---

## 3. 고친 방식

### 3-1. 관리자 목록 — 서버가 부분 수량 **다섯**을 싣는다

`AdminOrderResponse` 에 `totalQuantity` · `cancelledQuantity` · `returnedQuantity` ·
`returnRequestedQuantity` · `remainingQuantity`.

- ⚠ **합은 `OrderItem` 의 메서드로만 낸다** — `sum(o, OrderItem::remainingQuantity)` 처럼
  **메서드 참조를 받는 private 헬퍼** 하나를 두고, DTO 안에서 `quantity - cancelled - returned` 를
  **다시 쓰지 않는다.** 그 식은 이미 셋이다(엔티티 · `ITEM_SALES` SQL · 화면).
- 🔴 **금액은 일부러 안 실었다.** 관리자가 목록에서 묻는 것은 «몇 개가 돌아오나» 지 «얼마가
  나가나» 가 아니다. 금액을 실으면 「남은 금액」식의 **넷째 사본**이 생긴다.
- ⚠ `itemCount`(품목 **종류** 수)와 `totalQuantity`(**수량** 합)는 다르다. 「1종 1개」 주문만
  테스트하면 **영영 안 갈리므로** 「2종 5개」로 보는 테스트를 따로 뒀다.

### 3-2. 승인 confirm 이 «몇 개» 를 말한다

`요청된 품목(3개 중 1개)의 재고가 복원되고 그 몫이 적립금으로 환불됩니다.`

🔴 **처음에 «요청된 품목» 을 떨어뜨렸다가 08-25 에 붙여 둔 테스트(§I-2)가 잡았다.**
같이 드러난 것 하나 — 수량 칸이 없는 응답에서 문구가 **«undefined개 전부»** 가 됐다.
→ **수량이 없으면 원래 문구로 돌아가게** 고치고 회귀 테스트를 붙였다.
⚠ **화면이 서버 필드에 매달리면 안 된다** — 필드가 늦게 오거나 옛 응답이 섞여도 문구는 성립해야 한다.

### 3-3. 고객 목록 — 표시를 **컴포넌트로 뽑아** 상세와 같은 것을 쓴다

`components/OrderItemPartialNote.vue` (취소 · 반품 · 요청 세 줄). 상세(`OrderDetailView`)도 이걸 부른다.

🔴 **베껴 쓰지 않은 이유가 I-7 본문에 적혀 있다** — 고치려는 병이 *「목록과 상세가 같은 주문에 다른
숫자를 말한다」* 인데, **사본을 하나 더 만들면 그 병이 그대로 재발한다.** 이 저장소가 그걸로 두 번
데었다(§I-1 매출 식 넷 · §I-2 목록 문구).

합계 자리도 상세와 같은 규칙으로 바꿨다:

| | 고치기 전 | 고친 뒤 |
|---|---|---|
| 라벨 | 「합계」 고정 | 「결제 금액」 / 부분이 있으면 **「남은 결제 금액」** |
| 값 | `totalPrice` (주문 시점 상품합계) | `payAmount` (**서버가 이미 뺄셈을 한 값**) |

⚠ `totalPrice` 는 **부분 취소·반품이 있어도 안 줄어든다.** 그래서 목록은 원래 금액을, 상세는 남은
금액을 말하는 상태였다. 🔴 **화면에서 뺄셈을 하지 않았다** — `payAmount` 를 그대로 쓴다.

---

## 4. 되돌려서 확인 — **다섯 다 의도한 테스트가 잡았다**

| 무엇을 되돌렸나 | 잡은 것 |
|---|---|
| `remainingQuantity` 합을 **원본**(`getQuantity`)으로 | 백엔드 1건 |
| **요청과 반품을 한 칸으로 섞음**(`getReturnRequestedQuantity` → `getReturnedQuantity`) | 백엔드 1건 |
| 고객 목록에서 흔적 컴포넌트 제거 | 프론트 3건 |
| 합계를 옛 모습(`totalPrice`/「합계」)으로 | 프론트 2건 |
| 관리자 목록에서 요청 수량을 **반품 줄에 합산** | 프론트 2건 |

**전수**: 백엔드 주문 도메인 **190건 통과**(`AdminOrderListIntegrationTest` 10건 — 새것 둘 포함) ·
프론트 **310건 통과**(32파일 — 새 `OrderListView.test.js` 6건 포함).

### 4-1. ⚠ 내가 낸 실수 하나

새 `OrderListView.test.js` 에서 `w.text()` 로 **화면 전체**를 보고 *«멀쩡한 주문엔 취소됨이 없다»* 를
단언했다가 빨개졌다 — **상태 탭의 라벨**(「취소됨」·「반품완료」)이 걸린 것이다.
→ 주문 카드(`li.card`) 안으로 범위를 좁혔다.
🔴 **목록 화면의 텍스트 단언은 탭·필터를 먼저 뺀다.** 상세 화면 테스트에는 없던 함정이다
(상세엔 상태 탭이 없다).

---

## 5. DB 는 안 움직였다

`@Transactional` 롤백이라 예상대로지만 WA §3-5 대로 **테스트 뒤 그 자리에서 다시 셌다**(09:58):
`product_variant` **10** · `stock_history` **78** · product **7 / 1** · `point_history` **50** ·
`notification` **186** · `orders` **80** — 아침과 **전부 같다**. 표식(`ZZADMLIST`) 잔재 **0건**.

---

## 6. 🔴 아직 안 한 것 — `ZZ-123123` 영구 삭제 관측 (오늘 13:16 이후)

⚠ **시각을 어제 문서보다 정확히 낼 수 있게 됐다.**

- `deleted_at` = **2026-08-20 04:16:55 UTC** (= **13:16:55 KST**) · `purge-grace-days: 7` ·
  `purge-enabled: true` → **임계는 오늘 13:16 KST** 가 맞다.
- 🔴 **그런데 배치가 도는 시각은 13:16 이 아니다.** `ProductPurgeScheduler` 는
  `fixedDelay 1h` + `initialDelay 5m` 이라 **«매시 정각» 이 아니라 «기동 시각 + 5분부터 1시간마다»** 다.
  백엔드가 **오늘 09:29:27 에 기동**했으므로 틱은 09:34 → 10:34 → 11:34 → 12:34 → **13:34** 이고,
  임계를 넘는 **첫 틱은 13:34경**이다.
  ⚠ **재기동하면 이 시각이 다시 밀린다** — 오늘 배포를 하면 배포 시각 + 5분부터 다시 센다.
- **예측값**(오늘 아침 실측 기준, §1 에서 안 움직였음이 확인됨):
  `product_variant` **10 → 8** · `stock_history` **78 → 75** · product alive/trash **7 / 1 → 7 / 0**.

---

## 7. 🔴 배포 뒤 **사용자가 잡았다** — 상세 화면의 승인 문구엔 수량이 없었다

배포하고 브라우저로 밟은 사용자가 *«confirm 창에서 (수량이) 안 뜨는데?»* 라고 했다.
**나머지(고객 목록의 「남은 결제 금액」)는 맞다고 했다** — 그 둘이 갈린 것이 단서였다.

### 7-1. 가른 순서 (TROUBLESHOOTING 「배포했는데 화면이 그대로」의 절차)

| 의심 | 실측 | 판정 |
|---|---|---|
| ② 백엔드를 안 올렸나 | 배포된 jar(10:11)의 `AdminOrderResponse.class` 에 새 필드 **다섯 다 있음** | ✅ 배포됨 |
| ② 프론트를 안 올렸나 | 배포된 번들에 `요청된 품목(${e.totalQuantity}개 중 ${a}개)` **그대로 있음** | ✅ 배포됨 |
| ① 값이 없나 | `20260827-6413` = `RETURN_REQUESTED` · qty **8** · req **5** | ✅ 값 있음 |
| ③ 브라우저 캐시인가 | 「남은 결제 금액」과 승인 문구가 **같은 청크**(`index.b5b9d59f.js`)다 — 한쪽이 보이면 둘 다 새 코드다 | ✅ 아님 |

🔴 **셋 다 정상이면 남는 것은 «다른 화면» 뿐이다.** `approveReturn` 을 부르는 곳을 세니 **둘**이었다 —
`OrderAdminView`(목록, 오늘 고침)와 **`OrderDetailView`(상세, 안 고침)**.

### 7-2. 🔴 I-7 을 고치다가 I-7 의 병을 다시 냈다

I-7 이 고치려던 것이 *「목록과 상세가 같은 주문에 다른 말을 한다」* 인데,
**목록의 문구에만 수량을 넣어 정확히 그 상태를 만들었다.** WA §1-2-1 「짝 중 한쪽만 고쳐진다」다.

⚠ **오늘 세 번째다** — ①I-2 가 08-25 에 상세만 고치고 목록을 안 열었고, ②I-7 이 그걸 항목으로
적으면서 ②-1 이미 고쳐진 것을 다시 적었으며(§2-1), ③오늘 내가 반대 방향으로 똑같이 했다.

**고친 방식**: 문구를 `api/order.js` 의 **`returnApproveConfirm()` 하나**로 옮겼다.
⚠ **두 화면이 가진 모양이 다르다** — 목록은 서버가 낸 **합**, 상세는 **품목 배열**. 둘 다 받는다.
🔴 **문구를 화면에 두는 한 또 갈린다** — 그게 이번에 실증됐다.

**테스트를 두 겹으로 뒀다**:
- `api/order.test.js` — **문구 규칙**(같은 주문이면 두 모양이 같은 말 · 전량이면 「몇 개 중」을 안 씀 ·
  수량이 없으면 원래 문구 · «undefined»·«NaN» 금지).
- `OrderDetailView.test.js` — **배선**(화면이 그 함수를 «실제로 부르는가»). ⚠ 이 테스트가 없으면
  누가 문구를 다시 인라인으로 적어 놔도 초록이다. 🔴 그래서 이 파일의 모듈 목에서
  `returnApproveConfirm` 만 **진짜를 쓴다**.
- **되돌려서 확인**: 상세를 오늘 아침 상태(고정 문구)로 되돌리니 배선 테스트가 빨개졌다.

---

## 8. 🔴 «가끔 빨개지는 테스트» 를 하나 만났고, **이번엔 원인을 알고 고쳤다**

§7 을 고치고 전체 스위트를 돌리니 **2건 실패 → 다시 돌리니 1건 실패 → 또 돌리니 전부 통과**.
🔴 **실패하는 테스트가 매번 달랐다**(승인 문구 → 거절 사유 → …). 그래서 처음엔 방금 만진 코드를
의심했다 — **두 번 오진했다.**

### 8-1. 원인

`OrderAdminView.test.js` 의 `open()` 이 `await flushPromises()` 를 **두 번** 부르고 그리드가
그려졌다고 가정했다. 🔴 **«두 번이면 되겠지» 는 추측이다** — `CustomStore.load` → devextreme 내부 →
Vue 렌더가 프로미스를 몇 개 거치는지는 우리 사정이 아니다.

전체 스위트로 부하가 걸리면 두 번이 모자라 **행이 아직 없고**, `btn(w, '거절')` 이 `undefined` 라
`Cannot read properties of undefined (reading 'trigger')` 로 죽는다.
⚠ **에러 모양이 「테스트 코드 버그」처럼 보여서** 원인이 타이밍이라는 게 안 보인다.

⚠ **08-25 에 심긴 잠재 결함이고, 오늘 내가 이 파일에 테스트를 넷 더하면서 드러났다** —
파일이 무거워져 두 번으로 모자라는 창이 커졌다.

### 8-2. 고친 방식과 **판정법**

횟수를 **조건**으로 바꿨다 — 「상세」 버튼(상태와 무관하게 모든 행에 있는 유일한 버튼)이 나올 때까지
`flushPromises()` + `setTimeout(0)` 를 돌리고, 못 찾으면 **명시적으로 던진다**(조용히 통과시키지 않는다).

🔴 **재현 조건을 찾은 것이 이 절의 값이다** — 단독 실행으로는 **절대 안 잡힌다**(15회 넘게 초록).
**파일 하나를 고쳐 캐시를 무효화한 직후 전체 스위트**를 돌리면 잘 난다.
**고친 뒤**: 그 재현 조건 **3회** + 평범한 전체 실행 **5회**, 총 8회 전부 초록(317건).

⚠ **이 저장소의 두 번째 «가끔 빨개지는 테스트» 다**(첫째는 `priceFilterUsesSalePrice`, 08-19 §7).
🔴 **다만 이쪽은 원인을 알고 고쳤다** — 첫째는 구조를 바꿔 **회피**했을 뿐이다.
→ TROUBLESHOOTING 에 증상·판정법을 올렸다.

---

## 9. I-9 결정 표 — **코드는 아직 한 줄도 안 썼다**

이월이 *«I-9 은 결정 표부터, 코드보다 「N일」이 본체다»* 라고 적어 둔 대로 **표만** 만들었다.
표 자체는 BACKLOG §I-9 에 있다 — 여기엔 **경위와 실측**만 남긴다.

### 9-1. 실측이 결정을 바꾼 자리

착수 전에 DB 를 세어 보니 **`DELIVERED` 6건 중 5건이 이미 7일을 넘겼다**(33일 × 4 · 29일 × 1).
🔴 그중 하나가 **`20260724-0166`** — 「조건부 잔여」가 **지우지 말라**고 지정한
*「부분 반품이 살아 있는 유일한 주문」* 이다.

⚠ **«소급 적용할까» 가 추상적인 물음이 아니라 «표본 하나를 잃을까» 가 됐다.**
숫자를 안 세고 물었으면 «당연히 소급» 으로 지나갔을 자리다.
→ 소급으로 정하되 **되살리는 법(«`delivered_at` 을 손으로 당긴다»)을 표에 적어 뒀다.**

### 9-2. 🔴 결정 2 는 «바꾸는 것» 이 아니라 «명문화 + 가드» 였다

「기산점을 최초 배송완료로 고정」을 정하고 코드를 열어 보니 **이미 그렇게 돌고 있었다** —
`deliveredAt` 을 쓰는 곳은 `deliver()`(`Order.java:514`) **하나뿐**이고, 반품 승인(:733)과
거절(:755)은 **상태만 직접 대입**한다.

⚠ **그래서 이 결정은 공짜다.** 다만 **깨지기 쉽다** — 누가 저 두 줄을 «중복이네» 하며 `deliver()`
호출로 정리하면 **기한이 조용히 갱신되고 아무도 모른다.** 착수할 때 **가드 테스트가 먼저**다.

### 9-3. 결정 4 에서 항목 하나가 갈라져 나왔다 — **I-15 신설**

관리자에겐 **반품 «요청» 경로가 아예 없다**(`/return-request` 는 `user.id()` 로 소유를 본다).
기한을 걸면 **넘긴 건을 구제할 자리가 없어진다.** I-9 을 크기 소로 유지하려고 **갈라 뒀다** —
`admin-cancel` 과 같은 모양이 될 것이고, **§I-6 의 «대행하면 행위자가 안 남는다» 가 그대로 온다.**

---

## 10. I-9 구현 — **컴파일러에게 일을 시켰다**

결정 표(§9)대로 구현했다. 방식은 BACKLOG §I-9 「고친 방식」에 있고, 여기엔 **경위**만 남긴다.

### 10-1. 🔴 시그니처를 바꾼 것이 이 항목의 핵심 판단이다

`isReturnRequestable()` 을 그대로 두고 서비스에서만 기한을 보는 길이 더 쉬웠다.
**안 그랬다** — 무인자 판이 남아 있으면 «기한을 안 보는 질문» 을 누구나 부를 수 있고,
그게 **WA §1-2-1 의 「짝 중 한쪽」** 이다. 오늘만 그 모양을 **세 번** 봤다(§2-1 · §7-2).

→ `isReturnRequestable(int graceDays, Instant now)` 로 바꿔 **컴파일러가 모든 호출자를 열게** 했다.
✅ **실제로 기존 호출자 둘이 컴파일 에러로 잡혔다.** 사람이 grep 으로 찾는 것과 달리 **놓칠 수 없다.**

⚠ `now` 를 인자로 받은 것도 같은 종류의 판단이다 — 안에서 `Instant.now()` 를 부르면
**경계를 밟을 방법이 없다**(7일을 실제로 기다릴 수는 없다). 그래서 마감 «직전·정각·직후» 를
밀리초로 밟는 테스트가 가능해졌다.

### 10-2. ⚠ 내가 낸 실수 — 400 이 났는데 **다른 이유로** 났다

기한 초과 HTTP 테스트에서 본문을 `{"reason":"변심","items":[]}` 로 보냈다.
**빈 목록이라 `@Valid` 가 먼저 `COMMON-400` 을 냈고, 내 가드까지 오지도 않았다.**

🔴 **「400 이 났으니 됐다」로 넘어갔으면 엉뚱한 것을 검증할 뻔했다** — 상태코드만 보고
`$.error.code` 를 안 단언했으면 **영영 안 드러난다.** WA §3-3 의 «0 이라는 답에는 이유가 둘» 이
상태코드 판으로 나온 것이다.

### 10-3. 드리프트 가드를 **일부러 안 뒀다**

단위 테스트가 `new OrderProperties(7)` 을 쓴다. 처음엔 «yml 과 갈리면 안 된다» 는 주석을 달고
가드를 만들려 했는데, 세어 보니 **이미 지켜지고 있었다** — 단위 테스트는 경계를 안 밟으므로
값이 0 보다 크기만 하면 되고, **「7」을 실제로 지키는 곳은 `OrderReturnIntegrationTest`** 다
(진짜 설정을 로드한 채 8일 전·6일 전으로 당겨 밟는다. yml 을 바꾸면 거기가 빨개진다).
→ **주석을 사실에 맞게 고치고 가드는 안 만들었다.** 같은 것을 두 곳에서 지키면 한쪽이 낡는다.
⚠ 🔴 **08-26 의 「전례를 세어 보니 이미 규약이 있었다」와 같은 자리다**(WA §3-5 경위, §15-1·§15-3).

---

## 11. ✅ `ZZ-123123` 관측 완료 — **값은 맞혔고 시각은 두 번 틀렸다**

### 11-1. 결과 — 예측값 셋이 정확히 맞았다

**14:00:36 삭제됨**(로그 확인). 13:14 기준으로 다시 낸 예측(§12)과 한 자리도 안 어긋난다.

| | 13:14 실측 | 예측 | 삭제 후 실측 |
|---|---|---|---|
| `product_variant` | 10 | 8 | **8** ✅ |
| `stock_history` | 82 | 79 | **79** ✅ |
| product alive / trash | 7 / 1 | 7 / 0 | **7 / 0** ✅ |

🔴 **CASCADE 가 옵션 2 · 이력 3 을 함께 지웠다** — 「무엇이 딸려 지워지나」는 이 뺄셈이 유일한 근거였고,
그래서 **직전에 한 번, 직후에 한 번** 센 것이 값을 했다.

### 11-2. 🔴 시각 예측은 두 번 틀렸고, **두 번째 원인은 내 산수가 아니었다**

| 시도 | 예측 | 실제 | 왜 틀렸나 |
|---|---|---|---|
| 아침(§6) | 13:34 | — | 기동 시각을 **09:29:27** 로 잡았는데, 그 뒤 I-7 배포로 **10:11:42 에 재기동**했다. 나는 그 시각을 §7-1 에서 **확인해 놓고도** 틱을 다시 안 냈다 |
| 13:38 | 14:16:42 | **14:00:36** | 🔴 **전제 자체가 깨졌다** — 아래 |

**로그가 답을 줬다**:

```
10:12:59  Started GlassvueBackendApplication in 69.114 seconds
13:00:10  WARN HikariPool-1 - Thread starvation or clock leap detected
                              (housekeeper delta=1h24m39s342ms)
14:00:36  [상품] 영구 삭제 — ZZ-123123 (유예 경과)
14:00:37  기준: 2026-08-20T05:00:24.997Z 이전 삭제 · 유예 7일
```

🔴 **VM 이 약 1시간 24분 멈춰 있었다.** Hikari 하우스키퍼는 **30초**마다 도는데 **1h24m39s** 만에
깨어났다 — 그동안 스케줄러 타임라인이 통째로 밀렸다. 그래서 실제 스윕은 `:16` 이 아니라
**`:00:2x`** 에 돈다(13:00:24 → 14:00:24).

- 13:00:24 스윕은 임계(13:16:55)보다 **16분 반** 먼저였다 — 내가 적었던 «13초» 는 **틀린 이야기다.**
- 14:00:24 스윕이 임계를 넘겨 지웠다.

⚠ 🔴 **교훈이 갈린다.** 첫 번째 실패는 «파생값을 다시 안 냈다» 이고(오늘 세 번째다 — §12 · 어제 §6),
**두 번째는 «기동 시각 + 간격 = 벽시계 시각» 이라는 전제가 물리적으로 깨진 것**이다.
🔴 **`@Scheduled(fixedDelay)` 는 벽시계를 약속하지 않는다** — VM 이 멈추면 그만큼 밀리고,
**멈췄다는 사실은 Hikari 경고에만 남는다.** → TROUBLESHOOTING 에 증상·판정법을 올렸다.

---

## 12. 🔴 브라우저 검증이 값을 흔들었다 — 그래서 CASCADE 예측을 다시 냈다

관측 직전(13:14)에 세니 **아침과 달라져 있었다** — 당신이 I-7 을 브라우저로 밟은 결과다.

| | 아침(09:58) | 관측 직전(13:14) | |
|---|---|---|---|
| `stock_history` | 78 | **82** | +4 |
| `point_history` | 50 | **55** | +5 |
| `notification` | 186 | **202** | +16 |
| `orders` | 80 | **82** | +2 |
| `product_variant` · product | 10 · 7/1 | **10 · 7/1** | 그대로 |

🔴 **그래서 §6 의 CASCADE 예측(78 → 75)이 낡았다.** 다시 냈다 —
**`stock_history` 82 → 79** · `product_variant` **10 → 8** · product **7/1 → 7/0**
(ZZ-123123 의 옵션 **2** · 이력 **3** 을 13:14 에 다시 실측했다).

---

## 13. I-13 — **본문 한 줄이 틀려 있었고, 그게 결정을 바꿨다**

결정 셋과 고친 방식은 BACKLOG §I-13 에 있다. 여기엔 **경위**만.

### 13-1. 🔴 「주문 도메인 한정」이 아니었다

I-13 본문은 *「당장은 `OrderService.fitDetail` 로 막았다(주문 도메인 한정)」* 이라고 적었다.
착수 전에 세어 보니 **`ProductCommandService:340` 에도 있었다** — 그리고 **방식이 달랐다**:

| 어디 | 어떻게 |
|---|---|
| `OrderService.fitDetail` | 1000자로 자르고 **«…(잘림)» 을 붙인다** |
| `ProductCommandService` | 1000자로 **조용히** 자른다 |

🔴 **그래서 후보 ①(전역 절단)의 성격이 바뀌었다** — «새 동작을 더한다» 가 아니라 **«갈린 둘을 합친다»** 다.
본문이 우려한 *「조용히 잘림이 늘어난다」* 도 **이미 하나 있었다.** 안 세었으면 그 우려를 근거로
①을 피했을 수도 있다.

⚠ **오늘 세 번째로 「문서가 코드보다 낡았다」** — I-7 의 항목 ②(§2-1) · I-7 의 항목 ③(§2-2) · 이것.
🔴 **세 번 다 «착수 전에 코드를 연다» 가 잡았다.**

### 13-2. 관문이 하나라서 «크기 소~중» 이 «소» 가 됐다

원장에 쓰는 길은 `AdminAuditCommandService.record()` 하나이고 `AdminAuditLog.builder()` 를 부르는
곳도 **그 한 줄뿐**이다 — 9개 도메인 · 12개 발행 지점이 전부 거기로 모인다.
**그래서 ①이 3줄이었다.** ⚠ 이걸 모르면 «9개 도메인을 다 열어야 한다» 로 크기를 잡게 된다.

### 13-3. 🔴 후보 ②의 길이 막혀 있다는 것도 이번에 알았다

`MAX_STRING_SIZE = STANDARD` 라 `VARCHAR2` 는 **4000바이트**가 천장이고,
`1000 CHAR` × 최대 4바이트 = 정확히 4000 — **이미 붙어 있다.** 「컬럼을 좀 늘리자」는 **불가능**하고
CLOB 밖에 없다(또는 되돌릴 수 없는 DB 전역 `EXTENDED` 전환).
⚠ **후보를 적을 때는 그럴듯했는데 실측하니 하나가 사라졌다.**

### 13-4. 관문으로 모으면서 **덤으로 얻은 것** — 서로게이트

`substring` 이 이모지 한가운데를 자르면 **짝 없는 서로게이트**가 남아 Oracle 이 거부하거나 깨진
글자가 저장된다. 🔴 **옛 두 구현엔 둘 다 이 처리가 없었다.**
⚠ 사본이 흩어져 있으면 **이런 개선이 한 곳에만 들어가고 나머지는 낡는다** — 모아 둔 값이 여기서 났다.

### 13-5. ⚠ 테스트를 두 겹으로 둔 이유

단위 테스트는 **자바 문자열이 1000자 안인가**만 본다. 🔴 **컬럼엔 천장이 하나 더 있다** — 4000바이트.
한글은 UTF-8 에서 3바이트라 1000자면 3000바이트로 통과하지만, **«1000자» 를 통과해도 바이트에서
걸릴 수 있다는 사실은 DB 에 실제로 넣어 봐야 드러난다.** 그래서 통합 테스트를 **한글로** 채웠다 —
🔴 **ASCII 로 채우면 그 천장을 영영 안 밟는다.**

⚠ **`OrderServiceTest` 의 옛 절단 테스트는 «지키는 것» 이 뒤집혔다** — 「짧게 만들어 보냈나」가 아니라
**「긴 것을 깎지 않고 넘겼나」**. 거기서 미리 자르면 **두 곳에서 자르게 되고**, 감사 쪽 규칙을 바꿔도
주문만 옛 규칙을 따른다.

---

## 14. I-9 · I-13 배포·검증 — **원장에서 순서가 갈리는 것이 보인다**

✅ **백엔드·프론트 배포**(14:24 기동, PID 78417). 배포본 확인:
`OrderProperties.returnGraceDays` · `AdminAuditLog.DETAIL_MAX`/`fit()` · `return-grace-days: 7`
셋 다 배포된 클래스·yml 에 있다.

### 14-1. 🔴 I-13 의 증거가 운영 데이터에 남았다

```
20260827-6413 / 사유: 5665 / 몽쉘 5개 반품 / 환불 34000원   ← 배포 후 (사유가 앞)
20260827-6412 / 몽쉘 1개 반품 / 환불 6800원 / 사유: 13      ← 배포 전 (사유가 뒤)
20260826-6187 / 몽쉘 1개 반품 / 환불 6800원 / 사유: 2133    ← 배포 전
```

⚠ **한 테이블 안에 두 형식이 섞여 있다** — §I-13 결정 3 이 «읽는 쪽이 파싱하지 않는 칸이라
마이그레이션은 안 한다» 고 정한 그대로다. 🔴 **다만 「detail 부분일치 검색」(§I-11)을 만들 때는
이 사실을 알고 있어야 한다** — 주문번호는 여전히 맨 앞이라 그 최소안 자체는 그대로 성립한다.

### 14-2. 불변식이 닫힌다

반품 승인 한 건(`20260827-6413`, 8개 중 5개)으로 값이 이렇게 움직였다:

| | 배포 직전 | 검증 후 | 차이 |
|---|---|---|---|
| 김기현팀 누적구매 | 208,378 | **174,378** | **−34,000** = 환불액과 **정확히 같다** |
| 김기현팀 적립금 | 194,334 | **227,654** | **+33,320** = 34,000 − **680**(회수된 적립 2%) |

✅ **`SUM(point_history) == 잔액`** 성립(227,654). 산수가 닫힌다.

### 14-3. ⚠ 표본 하나를 잃었다 — **알고 한 결정이다**

`20260724-0166`(34일 경과)의 반품 요청 경로가 **소급 적용으로 막혔다**.
🔴 **동작 자체는 안 잃었다** — 「부분 반품 뒤 다시 요청할 수 있나」는 08-26 §I-8 이 채운
HTTP 관통 테스트 셋이 덮고 있다(3개 중 1개 · 두 회차로 수렴 · 2회차가 1회차 사유를 안 덮는다).
⚠ **그 표본은 애초에 옵션이 삭제돼 있어 재고 검증은 못 하는 상태였다.**
🔴 **다시 필요해지면 새로 만드는 편이 낫다** — 새 것은 기한 안이라 계속 쓸 수 있고 옵션이 살아 있어
**재고까지 검증된다.** 「지우지 말 것」은 **지우지 말라는 뜻이지 대체 불가라는 뜻이 아니다.**

---

## 15. 매출 재계수 — **«줄었을 것» 이라는 내 예상이 틀렸다**

**321,018원 · 매출 상태 16건.** 08-26 마감값 **300,618** 에서 **+20,400**.

⚠ 나는 이월에 *«반품이 여러 건 승인됐으므로 줄었을 것이다»* 라고 적었다. **반대였다.**

| 주문 | 무슨 일 | 매출 기여 |
|---|---|---|
| `20260827-6413` | 상품합계 54,400 중 34,000 반품 · 🔴 **`DELIVERED` 로 남는다** | **+20,400** |
| `20260827-6412` | 전량 반품 → `RETURNED` | 0 (들어왔다 나갔다) |
| `20260826-6187` | 어제 `ORDERED`(미결제) → 오늘 결제·배송·전량반품 → `RETURNED` | 0 (어제도 오늘도 매출 상태가 아니다) |

`300,618 + 20,400 = 321,018` — **한 자리도 안 어긋난다.**

🔴 **내가 빠뜨린 것은 «부분 반품은 주문을 매출 상태에 남긴다» 다** — 전량이 빠져야
`RETURNED` 로 떨어진다. **그게 정확히 §I-1 이 물렸던 자리인데**(매출이 부분 반품을 안 빼서
부풀려졌다) 이번엔 **반대 방향으로 같은 것을 놓쳤다.**
⚠ **«반품 = 매출 감소» 라는 직관이 부분 반품 이후로는 틀린다.**

⚠ **식은 손으로 안 썼다** — `ITEM_SALES` 와 `isRevenue()` 를 **소스 파일에서 읽어** 쿼리를 조립했다.
🔴 손으로 옮겨 적었으면 그게 **다섯 번째 사본**이고, §I-1 이 그 사본 넷 때문에 났다.

---

## 16. I-15 — **오늘 만든 구멍을 오늘 메웠다**

I-9 이 7일 기한을 걸면서 «기한 넘긴 건을 구제할 자리» 가 사라졌다. 그 자리를 만들었다.
결정 셋과 고친 방식은 BACKLOG §I-15 에 있고, 여기엔 **경위**만.

### 16-1. 🔴 테스트가 «진짜 버그» 를 잡았다 — 하마터면 고객 반품이 전부 막힐 뻔했다

폼을 하나로 쓰고 `openReturnForm(byAdmin)` 로 갈랐는데, 템플릿에 **`@click="openReturnForm"`** 로
썼다. 🔴 **Vue 는 DOM 이벤트를 첫 인자로 넘긴다** — `byAdmin` 이 MouseEvent(truthy)가 되어
**고객이 「반품 요청」을 누르면 대행 API 로 나가고 403** 이 된다.

⚠ **기존 테스트**(「요청 본문에 고른 품목·수량이 실린다」)가 먼저 빨개졌다 — I-15 와 무관한,
G-10 때 붙여 둔 테스트다. 🔴 **새 기능이 옛 기능을 부순 것을 옛 테스트가 잡았다.**
→ `openReturnForm()` 으로 고치고 `byAdmin === true` 로 눕혀 **두 번째 방어선**을 뒀다.
그리고 **그 실수 자체를 이름으로 가진 회귀 테스트**를 붙였다.

### 16-2. ⚠ 08-10 의 드리프트 가드가 또 값을 했다

새 `AuditAction` 을 만들고 **프론트 라벨을 안 넣었더니** `audit.test.js` 가 빨개졌다
(«라벨 키 집합이 enum 과 정확히 같다»). 안 잡혔으면 감사 화면 필터에서 **못 고르고 날문자로** 떴다.
🔴 **오늘 §I-8 이 그 패턴을 주문 상태 라벨로 옮겨 심었는데**(08-26), 원본 쪽이 오늘 또 잡았다.

### 16-3. ⚠ 이름 길이가 «정확히» 맞았다

`action` 열은 `VARCHAR2(20 CHAR)` 이고 `ORDER_RETURN_REQUEST` 는 **정확히 20자**다
(`ORDER_RETURN_APPROVE` 와 같다). 🔴 **한 글자만 길었어도 컬럼을 함께 넓혀야 했다** —
착수 전에 세어 봐서 알았다. **다음에 action 을 더할 때 이름 길이부터 센다.**

### 16-4. 이벤트를 «플래그» 로 안 가른 이유

기존 `OrderReturnRequestedEvent` 에 «누가 했나» 플래그를 다는 길이 더 짧았다. **안 했다** —
그러면 **구독자마다 «이번엔 내 차례인가» 를 판단**해야 하고, 그 판단이 틀리면 알림이 두 번 뜨거나
아예 안 뜬다. ⚠ **부분 취소 알림이 `orderFullyCancelled` 한 칸에 걸려 있는 것이 이미 그 모양**이고
(조건부 잔여에 «그 칸이 틀리면» 이라고 적혀 있다), **하나로 족하다.**

**전수**: 백엔드 **1000건** · 프론트 **327건** 전부 통과.

---

## 17. I-15 배포·검증 — 🔴 **오늘의 고리가 닫혔다**

✅ **백엔드·프론트 배포**(15:19 기동). 배포본에 `ORDER_RETURN_REQUEST` · `V60` · 프론트 번들에
「반품 대행 접수」·「반품 요청(대행)」 확인.
⚠ **V60 은 배포 전에 이미 적용돼 있었다**(`success=1`) — 전수 테스트가 컨텍스트를 띄우며 Flyway 를
태웠기 때문이다. 배포 때는 「이미 적용됨」으로 지나갔다.

### 17-1. 증거가 «그 표본» 에 찍혔다

```
20260724-0166 / 사유: 123123 / 대행 접수 (🔴 기한 경과 — 7일 지남)
```

🔴 **§14-3 에서 «표본 하나를 잃었다» 고 적은 바로 그 주문이다.** I-9 이 34일 지난 이 주문을
막았고, 몇 시간 뒤 I-15 가 **되살렸다** — 같은 날 만든 구멍을 같은 날 메운 것이 <b>운영 데이터에
한 줄로 남았다.</b>
⚠ **표본이 «다른 방식으로» 살아 있다** — 이제 이 주문은 「기한 지난 주문을 대행으로 밟는 자리」다.

### 17-2. 마감 계수

**매출 321,018 · 16건 — 오후와 같다.** ✅ 대행 접수는 아직 **요청** 단계라 돈이 안 움직이는 것이
맞다(승인해야 움직인다). 🔴 **«알림 4건 늘고 매출은 그대로» 가 그 사실의 실측이다.**

불변식 **넷 다 성립**(`SUM(point_history) == 잔액`).

---

## 18. 마감 — 규약 하나를 올렸고, **올리기 전에 세어서 좁혔다**

### 18-1. WA §1-3-1 신설 (2026-08-27)

*«백로그 항목이 «없다/막혀 있다» 고 하면 그 자리를 열어 본다 — 곁다리 수정은 커밋 메시지에 안 남는다»*

🔴 **처음엔 «착수 전에 코드를 연다» 를 새 규약으로 올리려 했다.** 세어 보니 **§1-3 이 이미 대부분을
덮고 있었다**(*"문서의 「결론」이 맞아도 「이유」는 틀릴 수 있다 — 이유까지 실측한다"*).
→ **하위 절로 좁혀** 오늘 새로 드러난 기전 하나만 적었다: **곁다리 수정은 커밋 메시지에 안 남는다.**

⚠ 🔴 **08-26 과 같은 결말이다** — 그날도 규약 셋을 올리려다 **세어 보니 둘은 이미 있었다**(§15-1·§15-3).
**규약을 올리기 전에 «이미 있나» 부터 센다** 가 두 번 연속으로 값을 했다.

**근거로 센 것** — 오늘 다섯(§2-1 · §2-2 · §13-1 · §13-3 · §10-1) + 선행 둘(08-25 · 08-26).
세 문서에 걸쳐 있어 «오늘 하루의 우연» 이 아니다.

### 18-2. 마감 계수 — **§17 과 한 자리도 안 다르다**

16:04 재계수: `product_variant` **8** · `stock_history` **80** · `point_history` **56** ·
`notification` **207** · `orders` **82** · `admin_audit_log` **84** · 매출 **321,018 · 16건**.
✅ `orders.updated_at` 최댓값이 **06:34:06 UTC**(= 15:34 KST)에 멈춰 있다 —
**I-15 검증 뒤로 DB 가 안 움직였다는 실측**이다.

⚠ WA §3-5 가 *«마감에 한 번 더 센다 — 그게 다음 날의 대조 대상»* 이라고 한 그 계수다.
🔴 **08-26 이 여기서 틀렸다** — 검증이 끝나기 전 값을 「마감값」이라 적어 다음 날 아침에 어긋났다.
그래서 오늘은 **닫고 나서** 셌다.

---

## 이월 (다음 문서로 반드시 옮길 것)

> WORKING-AGREEMENTS §4-1. 다음 날 문서가 그대로 물려받아 시작한다. 처리했으면 지운다.
> ⚠ 🔴 **물려받을 때 «아직 참인가» 를 확인한다** — 오늘 그 대조가 I-7 의 항목 하나를 거짓으로
> 지목했고(§2-1), 이월의 낡은 문장 하나도 지웠다(§1-1).

### 다음 세션에서 시작할 것

1. ✅ **`ZZ-123123` 관측은 끝났다**(§11) — 14:00:36 삭제, **예측값 셋이 정확히 맞았다**
   (`product_variant` **8** · `stock_history` **79** · product **7 / 0**).
   🔴 **표본이 사라졌다** — 「삭제 대기 상품」을 다시 밟으려면 **새로 만들어 지워야** 한다.
   ⚠ 🔴 **VM 이 오늘 ~1시간 24분 멈췄다**(Hikari 경고). 배치 시각을 예측할 일이 또 생기면
   **`@Scheduled` 는 벽시계를 약속하지 않는다** 는 것을 먼저 본다 — 판정법은 TROUBLESHOOTING 에 있다.

2. **§I-7 은 1차 배포·검증까지 갔고, 검증이 빠진 자리 하나를 찾아냈다**(§7).
   ✅ 백엔드·프론트 **배포됨**(10:11) · 브라우저로 밟음 · 🔴 **사용자가 상세 화면의 승인 문구를 잡았다.**
   ✅ **그 수정(문구 통합 + 간헐 실패 수정)도 프론트 재배포·브라우저 확인까지 끝났다**
   (백엔드는 안 건드렸으므로 재배포 안 함).
   ⚠ 🔴 **여기 적혀 있던 «13:34 예측은 그대로 유효하다» 는 틀렸다** — 실제 삭제는 **14:00:36** 이었고
   원인은 프론트 재배포가 아니라 **VM 이 ~1시간 24분 멈춘 것**이었다(§11-2).
   ✅ **push 했다** — `5ca72c7..cd40c2c` 로 origin 과 동기화됐다.
   ⚠ **push 와 배포는 다른 일이다**(08-26 이월 7번이 그 반대 방향으로 틀렸던 자리) —
   여기서는 **둘 다 끝났다.**

3. ✅ **§I-9 · §I-13 은 배포·브라우저 검증까지 끝났다**(§14). **남은 것이 없다.**
   ⚠ 🔴 **`20260724-0166` 표본은 §14-3 에서 «막혔다» 고 적었는데, §17-1 에서 되살아났다** —
   I-15 대행 접수로 밟았다. **지금은 `RETURN_REQUESTED` 이고 「기한 지난 주문을 대행으로 밟는 자리」다.**
   ⚠ **`delivered_at` 을 손으로 당길 이유가 없어졌다.**

4. **§I 의 남은 항목** — ✅ **I-7 · I-9 · I-13 · I-15 를 오늘 닫았다.**
   🔴 **§I 에서 남은 것은 I-11(보류) 하나뿐이다** — 그리고 그 안의 둘(동시성 · 잔돈 음수)은
   **재현 테스트를 먼저** 만든 뒤 판단한다. 나머지는 «만들지 않기로 정한 것» 이거나 실효가 없다.
   ✅ **I-15 도 배포·브라우저 검증까지 끝났다**(§17) — V60 포함. **오늘 연 넷은 배포가 남은 것이 없다.**

5. ⚠ **아침에 기준값을 다시 센다.** **마감값 (2026-08-27, I-15 배포·검증 후 — 오늘의 마지막 계수)**:
   `product_variant` **8** · `stock_history` **80** · product alive / trash **7 / 0** ·
   `point_history` **56** · `notification` **207** · `orders` **82** ·
   `admin_audit_log` **84행 · 최장 detail 64자** · **매출(상품) 321,018 · 매출 상태 16건**.
   **김기현팀**: 적립금 **227,654** · 누적구매 **174,378** · **SILVER**.
   **김기현팀2**: 150 / 15,000 · BRONZE · **김기현팀3·4**: 0 / 0 · BRONZE.
   ✅ **불변식 넷 다 성립**(`SUM(point_history) == 잔액`).
   ⚠ 이력은 `product_id` 로 센다.
   🔴 **오늘 값이 하루에 네 번 흔들렸다** — 아침 09:58(78/50/186/80) → 13:14(82/55/202/82) →
   CASCADE → I-9·I-13 검증(80/56/203/82) → I-15 검증(80/56/**207**/82).
   **아침 값을 근거로 쓰면 안 된다.**
   🔴 **매출은 08-26 의 300,618 에서 «늘었다»(+20,400)** — 내가 «반품이 승인됐으니 줄었을 것» 이라
   적었는데 **틀렸다**(§15). **부분 반품은 주문을 매출 상태에 남긴다.**
   ⚠ **I-15 검증은 알림만 4건 늘리고 매출을 안 건드렸다** — 대행 «요청» 은 돈을 안 움직인다(§17-2).

6. 🔴 **`sold_count` 의 옛 이력은 그대로 남아 있다** — 08-25 이전 부분 취소가 판매량을 안 되돌린 몫
   (지바 12 vs TOP 5 · 몽쉘 10개 8 vs 1). **보정할지는 여전히 안 정했다.**
   ⚠ 정한다면 «로컬이라 그냥 둔다» 로 끝난 08-24 의 어긋난 값 셋과 **같은 성질의 결정**이다.

7. ✅ **전례를 세고 규약 하나를 올렸다 — WA §1-3-1**(2026-08-27). 경위는 §18.
   *«백로그 항목이 «없다/막혀 있다» 고 하면 그 자리를 열어 본다 — 곁다리 수정은 커밋 메시지에 안 남는다»*
   🔴 **새 규약을 통째로 올리려다 세어 보니 §1-3 이 이미 덮고 있어 하위 절로 좁혔다** —
   08-26 과 같은 결말이다(그날도 셋 중 둘이 이미 있었다).

8. 🔴 **다음 기능 후보 — 이제 «기능 차례» 다.** §I 축이 닫혔으므로(위 4번) 우선순위가 바뀐다.
   **후보는 사실상 둘이고, 2026-08-27 에 코드로 대조해 «지금도 참» 임을 확인했다**(WA §1-3-1):
   - **G-6 등급 혜택 확장**(소) — `MemberGrade` 는 `(minPurchase, earnPercent)` 뿐이고 혜택이
     **적립률 하나**다. `ShippingPolicy`(global)는 등급을 모르고 호출자가 **3곳**뿐이다
     (`CartService` 2 · `OrderService` 1). ✅ **도메인 경계는 안 걸린다** — `PointService.accountOf()`
     가 공개 API 이고 `OrderService` 는 이미 `pointService` 를 쓴다. 다만 BACKLOG 경고대로
     **`ShippingPolicy` 에 등급을 넘기지 말고 «적용할 기준 금액» 을 넘긴다.**
   - **G-7 최근 검색어**(소) — 헤더에 전역 검색이 **있고**(`App.vue`, `?name=`) 최근 검색어 코드는
     **0건**이다. localStorage 로 충분. ⚠ **인기 검색어는 트래픽이 없어 성립하지 않는다.**

   ⚠ **뷰 테스트가 반쪽이다**: `OrderAdminView` 는 반품 승인·거절 + 부분 수량 자리만 있고
   **발송·배송완료·대행 취소·필터는 여전히 비어 있다.** 🔴 오늘 그 파일의 간헐 실패를 고치며
   `untilRendered()` 를 만들어 뒀으므로 **지금이 채우기 가장 싼 때다**(§8-2).
   ⚠ **마케팅 발송은 후보가 아니다** · **F-2 는 아직 아니다** · **B-26 나머지는 «조건이 갖춰지면»** ·
   **MSA 는 아직 아니다.**

### 조건부 잔여

**08-26 §조건부 잔여를 그대로 물려받는다.** ⚠ 개수를 여기 적지 않는다 — 목록이 곧 개수다.

#### 오늘 더해진 것

- ⚠ **`OrderItemPartialNote` 가 상세·고객목록 **두 화면**의 공용이 됐다** — 🔴 **고치면 두 곳이 함께
  움직인다.** 그게 만든 이유지만, **한쪽 사정으로 고치면 다른 쪽이 조용히 바뀐다.**
  ⚠ **관리자 목록은 이걸 못 쓴다**(그리드 한 줄이 주문 하나라 품목을 펼칠 자리가 없다) —
  즉 **표시 규칙이 여전히 두 벌**이다. 어긋나면 잡히도록 양쪽에 테스트를 뒀다.
- ⚠ **`AdminOrderResponse` 는 수량만 싣고 금액은 안 싣는다**(§3-1). 🔴 **누가 «목록에도 남은 금액을»
  이라고 하면 「남은 금액」식의 넷째 사본이 생긴다** — 그때는 `remainingItemsTotal()` 을 **엔티티에서
  불러** 싣지, 화면이나 DTO 에서 빼지 않는다.
- ⚠ **고객 목록의 합계가 `payAmount`(배송비 포함) 로 바뀌었다** — 이전엔 `totalPrice`(상품합계,
  배송비 제외)였다. 🔴 **같은 주문의 목록 숫자가 어제와 달라 보인다** — 상세와 맞춘 결과지 회귀가 아니다.
- ⚠ **`OrderListView` 에 테스트가 오늘 처음 생겼다**(6건). 부분 표시와 합계만 본다 —
  **상태 탭·페이징·빈 목록은 여전히 안 덮였다.**

#### ⚠ 그대로 물려받는 것

- 🔴 **`ZZ-123123`** — 유예 만료 **오늘 13:16 KST**, 배치 틱은 **13:34경**(§6).
- 🔴 **08-24 검증이 남긴 어긋난 값 셋** — `김기현팀` 적립금 **+1,428** · 지바 재고 **+1** ·
  반팔티 재고 **+1**. 「로컬이라 그냥 둔다」 결정. ⚠ 되돌리고 싶어지면 **적립금은 원장에 «보정»
  줄을 남기는 쪽**이 맞다(잔액만 고치면 `SUM(history) == 잔액` 이 깨진다).
- 🔴 **오늘 더해진 것 — I-9·I-13·I-15 가 만든 자리들**:
  - **`20260724-0166`** 은 이제 **`RETURN_REQUESTED`**(I-15 대행 접수, 기한 34일 경과).
    🔴 **「기한 지난 주문을 대행으로 밟은 유일한 표본」이다** — 승인·거절 어느 쪽으로 밟아도 되지만,
    **승인하면 그 성격이 사라진다.** 원장에 `ORDER_RETURN_REQUEST` 줄이 증거로 남아 있다.
  - ⚠ **원장에 detail 형식이 «두 벌» 이다**(§14-1) — 08-27 배포 후 건은 「사유가 앞」, 그 전 건은 뒤.
    **마이그레이션 안 한다는 결정**이고, 「detail 부분일치 검색」(§I-11)을 만들 때 알고 있어야 한다.
  - ⚠ **`OrderProperties.returnGraceDays` 가 정책값 둘째다**(`catalog.purge-grace-days` 에 이어).
    🔴 **「7」을 실제로 지키는 곳은 `OrderReturnIntegrationTest` 하나**다(8일 전·6일 전으로 당겨 밟는다) —
    yml 을 바꾸면 **거기가 빨개진다.** 단위 테스트의 `new OrderProperties(7)` 은 경계를 안 밟는다.
  - ⚠ **`AdminAuditLog.fit()` 이 원장 절단의 유일한 자리다** — 도메인 사본 둘을 걷어냈다.
    🔴 **여기가 무너지면 9개 도메인이 한꺼번에 무방비**가 된다.
  - ⚠ **`admin-return-request` 만 기한을 안 본다.** 고객이 탈 수 있으면 **기한이 없어진다** —
    그래서 자기 주문이어도 403 이다.
  - ⚠ **`OrderDetailView` 의 반품 폼이 «둘» 을 겸한다**(본인/대행). 🔴 `openReturnForm()` 을
    **괄호 없이** 쓰면 DOM 이벤트가 인자로 들어가 **고객 반품이 전부 대행 경로로 샌다**(§16-1).

- **살아 있는 표본** — `20260724-0166`(🔴 **위 참조 — 이제 `RETURN_REQUESTED` 다**) ·
  `20260727-0253`(부분 취소) · `20260825-5633`(반품 거절) · `20260724-0246` · `20260825-5484` ·
  `20260826-5930`(부분 취소 + `priceDelta ≠ 0` 세일 옵션) · `20260826-5931`(§I-6 증거물) ·
  `20260826-6117`(§I-12) · `20260826-6119`(§I-10) · `20260826-6187`(§I-14). **지우지 말 것.**
  ⚠ **`20260826-6118` 은 잔재**이고 표본 값이 없다.
- ⚠ **`20260826-5931` 은 §I-12 «이전» 코드가 만든 알림을 들고 있다**(「0원 주문이 취소되었습니다」).
  **버그의 증거물로는 값이 있지만, 고친 동작의 예로 읽으면 안 된다.**
- **세일 표본 `ZZ-세일검증`** — 세일 둘 다 `ENDED`. 🔴 `priceDelta ≠ 0` 인 옵션을 가진 **유일한
  상품**이다. **정리는 아직 안 했다.**
- **G-9 표본 `20260820-5108`**(`ORDERED`) — 품목이 하나뿐이라 부분 취소가 곧 전체 취소가 되어
  표본이 사라진다.
- **16번의 미결제 주문 둘**(`20260813-4185`·`20260813-4186`) · **V51 검증이 바꾼 옛 주문 둘**
  (`20260724-0249` `SHIPPED` · `20260727-0257` `RETURNED`) · **계정 `김기현팀4`**(USER) ·
  **08-24 검증 주문 셋**(`20260824-5295`·`5296`·`5297` — `5296` 은 **사고의 증거물**이다).
- ⚠ **`returnReason` 은 여전히 한 칸이라 회차마다 덮인다** · **`order_item.returned_at` 도 마지막 시각만**.
- ⚠ **매출 식이 상수 한 곳(`OrderStatsRepository.ITEM_SALES`)에 있다.** 🔴 «식이 옳은가» 는
  `partialReturnReducesRevenue` 하나가 지킨다 — **그 테스트를 지우면 아무도 안 지킨다.**
- ⚠ **같은 「남은 것」식이 셋이다** — `ITEM_SALES` SQL · `OrderItem.remainingQuantity()`·`remainingAmount()`.
  **셋 중 하나를 고치면 나머지 둘을 연다.**
  🔴 **매출을 손으로 세지 않는다** — 2026-08-27 에 `ITEM_SALES` 와 `isRevenue()` 를 **소스에서 읽어**
  쿼리를 조립했다(§15). 옮겨 적었으면 **넷째 사본**이고, §I-1 이 그 사본 넷 때문에 났다.
- ⚠ **부분 취소 알림은 «전량이 빠지는 경우» 를 건너뛴다** — 그 판단이 이벤트의 `orderFullyCancelled`
  한 칸에 걸려 있다.
- ⚠ **배분식이 서버와 화면 두 벌이다.** 뷰 테스트가 서버 테스트와 **글자 그대로 같은 숫자**를 단언한다
  (12,858 · 17,143 · 8,333 · 2,142 · 2,857).
- 🔴 **`priceFilterUsesSalePrice` 의 간헐 실패 원인은 여전히 모른다**(08-19 §7). 구조를 바꿔 **회피**했을 뿐이다.
- ⚠ **「캐시 60초 지연」은 끝내 실측하지 못했다.** · ⚠ **`COUPON_WELCOME_SET` 은 아직 브라우저로 안 밟았다.**
- ⚠ **`recordAdmin` 은 `actor == null` 을 견디는데 감사는 못 견딘다**(08-20 §7-7). 그대로 뒀다.
- ⚠ **`recentlyViewed` 의 세일 값은 낡을 수 있다** — localStorage 스냅샷이다.
- ⚠ **`deleted_at` 오프셋이 행마다 섞여 있다**(08-19 §1-1). 값은 맞다. **원인 미상.**
  🔴 **오늘 이게 값을 했다** — `ZZ-123123` 의 `deleted_at` 이 UTC 로 읽혀 «04:16» 이었고, 그래서
  이월의 «13:16» 과 9시간 어긋나 보였다. **둘은 같은 시각이다**(§6).
- 🔴 **「화면이 유일한 방어」는 여전히 «두 종류에 두 번» 이다.** 오늘 새로 난 것 없다. **아직 안 올린다.**
- ⚠ **08-21 이월의 나머지는 그대로 물려받는다** — 정가 칸 원인 미상 · 겹침 금지가 앱에만 있다 ·
  `SignupView` 의 `border-ink-200` · 막힌 것 셋(배송완료 리뷰 · 탈퇴 회원 관리자 조작 ·
  탈퇴 시 장바구니 정리).
