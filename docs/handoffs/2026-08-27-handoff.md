# 2026-08-27 핸드오프 — I-7 을 닫았고, **이월이 시킨 「코드부터 열어라」가 실제로 걸러 냈다**

> §I-7(「부분」이 주문 목록에서 안 보인다)을 고쳤다.
>
> 🔴 **오늘의 요점은 셋이다.**
> ① **아침 기준값 재계수가 처음으로 «전부 일치» 로 끝났다**(§1) — WA §3-5 가 생긴 다음 날이고,
>    규약이 «다르면 정보, 같으면 확인» 이라고 한 그 «같은 쪽» 을 처음 밟았다.
> ② 🔴 **I-7 에 적힌 항목 셋 중 하나가 «적힌 시점에 이미 거짓» 이었다**(§2) — 08-25 에 I-7 을
>    쓰면서 같은 날 자기 커밋(`bd6a741`)이 고친 것을 못 본 것이다. **착수 전에 코드를 연 덕에 걸렀다.**
> ③ 🔴 **또 하나는 «막힌 곳이 어디인가» 가 뒤집혔다** — 고객 목록은 서버가 진작부터 값을 다
>    내려주고 있었고 **화면만 안 그렸다.** 한 항목에 묶여 있어 «둘 다 서버가 안 준다» 로 읽혔다.

---

## 0. 오늘의 커밋

| 해시 | 무엇 |
|---|---|
| `ba4cd5e` | fix(order): 「부분」이 주문 목록에서 안 보이던 자리를 고친다 (BACKLOG I-7) |

---

## 1. 아침 기준값 재계수 — **전부 일치** (WA §3-5)

어제 신설한 규약의 첫 적용이다. 09:32 실측.

| | 08-26 마감값 | 08-27 아침 | |
|---|---|---|---|
| `product_variant` / `stock_history` | 10 / 78 | **10 / 78** | ✅ |
| product alive / trash | 7 / 1 | **7 / 1** | ✅ |
| `point_history` / `notification` / `orders` | 50 / 186 / 80 | **50 / 186 / 80** | ✅ |
| 김기현팀 적립금 / 누적구매 / 등급 | 179,646 / 153,978 / SILVER | **동일** | ✅ |
| 김기현팀4 | 0 / 0 / BRONZE | **동일** | ✅ |

⚠ **이것이 규약의 «같은 쪽» 이다** — WA §3-5 는 *«같으면 «안 움직였다» 가 실측으로 확인된 것»* 이라
적어 뒀다. 어제(08-26)는 **다른 쪽**을 밟아 값 셋이 뒤집혔다. 하루 만에 양쪽이 다 나온 셈이라,
규약이 «틀린 문서를 잡는 장치» 만이 아니라 **«안 움직였음을 말할 수 있게 하는 장치»** 이기도 하다는
것이 드러났다. 🔴 **재계수 없이는 «안 움직였다» 도 주장할 수 없다.**

### 1-1. 이월의 낡은 문장 하나를 지웠다

08-26 이월 **7번**이 *「프론트 재배포가 남아 있다」* 라고 적고 있었다. **거짓이다** —
프론트 런타임의 마지막 변경은 `04cf17d`(StatsAdminView 문구)이고 08-26 §6-3 이 배포·번들 해시
확인까지 적어 뒀다. 그 뒤 프론트를 건드린 커밋은 `32f0e95` 하나인데 `order.test.js` **테스트 파일만**
바꿨다.

🔴 **08-25 이월에서 그대로 물려받은 문장이 하루를 더 살아남았다.** 08-26 이 «이월의 낡은 문장 넷을
실제와 맞춘다»(`2d88f27`)를 하고도 이건 안 걸렸다. ⚠ **이월 대조는 «오늘 새로 쓴 항목» 보다
«어제에서 물려받은 항목» 에서 더 자주 틀린다** — 새로 쓴 것은 방금 본 것이고, 물려받은 것은
아무도 다시 안 본다.

---

## 2. 🔴 I-7 의 세 항목을 착수 전에 하나씩 열어 봤다 — **셋 중 하나가 이미 거짓**

08-26 이월 2번이 *«남은 넷도 같은 방식으로 코드를 열어 확인한 뒤 착수한다»* 라고 적어 둔 그대로 했다.

| I-7 이 주장한 것 | 지금도 참인가 |
|---|---|
| ① `AdminOrderResponse` 에 부분 필드가 없다 | ✅ **참** |
| ② 목록 승인 confirm 이 «결제금액을 …» (전량 시절 문구) | ❌ 🔴 **거짓 — 적힌 시점에 이미 고쳐져 있었다** |
| ③ 고객 목록이 원본 수량·합계만 그린다 | ✅ **참 — 단 성질이 다르다**(§2-2) |

### 2-1. ②가 «적힌 시점에 이미 거짓» 이었던 경위

`bd6a741`(2026-08-25 13:30, *fix(stats): 매출이 부분 반품을 안 빼던 자리를 고친다 — I-1*)이
같은 커밋에서 `OrderAdminView.vue` 의 confirm 문구를 고쳤다:

```
- 재고 복원 + 결제금액을 적립금으로 환불합니다.
+ 요청된 품목의 재고가 복원되고 그 몫이 적립금으로 환불됩니다.
```

I-7 은 **같은 날 그 뒤에 작성됐다.** 즉 «I-1 을 고치면서 곁다리로 고친 자리» 를 «I-7 의 할 일» 로
다시 적은 것이다. ⚠ **커밋이 항목 하나만 건드리지 않을 때 생기는 어긋남**이다 — I-1 커밋 메시지는
매출 식 이야기만 하고, 문구 수정은 본문에 안 적혀 있다.

🔴 **그래서 「코드부터 열어라」가 값을 했다.** 안 열었으면 **이미 고쳐진 것을 고치고**, 「고쳤다」고
핸드오프에 적었을 것이다.

### 2-2. ③은 «막힌 곳» 이 서버가 아니라 화면이었다

I-7 이 ①과 ③을 한 항목에 묶어 놔서 *«관리자도 고객도 서버가 부분 필드를 안 준다»* 로 읽힌다.
실제로는 **고객 쪽은 서버가 진작부터 다 주고 있었다** — `OrderService.myOrders()` 가
`AdminOrderResponse` 같은 요약본이 아니라 **full `OrderResponse`** 를 `page.map` 한다. 즉
`cancelledQuantity`·`returnedQuantity`·`remainingQuantity` 가 **목록 응답에 이미 들어 있었고**,
`OrderListView.vue` 가 `item.quantity`·`item.lineTotal` 만 그리고 있었다.

⚠ **한 항목에 성질이 다른 둘을 묶으면 크기 추정과 착수 순서가 같이 틀어진다.** 관리자 쪽은
DTO 확장(서버)이고 고객 쪽은 **화면만**이다.

---

## 3. 고친 방식

### 3-1. 관리자 목록 — 서버가 부분 수량 **다섯**을 싣는다

`AdminOrderResponse` 에 `totalQuantity` · `cancelledQuantity` · `returnedQuantity` ·
`returnRequestedQuantity` · `remainingQuantity`.

- ⚠ **합은 `OrderItem` 의 메서드로만 낸다** — `sum(o, OrderItem::remainingQuantity)` 처럼
  **메서드 참조를 받는 private 헬퍼** 하나를 두고, DTO 안에서 `quantity - cancelled - returned` 를
  **다시 쓰지 않는다.** 그 식은 이미 셋이다(엔티티 · `ITEM_SALES` SQL · 화면).
- 🔴 **금액은 일부러 안 실었다.** 관리자가 목록에서 묻는 것은 «몇 개가 돌아오나» 지 «얼마가
  나가나» 가 아니다. 금액을 실으면 「남은 금액」식의 **넷째 사본**이 생긴다.
- ⚠ `itemCount`(품목 **종류** 수)와 `totalQuantity`(**수량** 합)는 다르다. 「1종 1개」 주문만
  테스트하면 **영영 안 갈리므로** 「2종 5개」로 보는 테스트를 따로 뒀다.

### 3-2. 승인 confirm 이 «몇 개» 를 말한다

`요청된 품목(3개 중 1개)의 재고가 복원되고 그 몫이 적립금으로 환불됩니다.`

🔴 **처음에 «요청된 품목» 을 떨어뜨렸다가 08-25 에 붙여 둔 테스트(§I-2)가 잡았다.**
같이 드러난 것 하나 — 수량 칸이 없는 응답에서 문구가 **«undefined개 전부»** 가 됐다.
→ **수량이 없으면 원래 문구로 돌아가게** 고치고 회귀 테스트를 붙였다.
⚠ **화면이 서버 필드에 매달리면 안 된다** — 필드가 늦게 오거나 옛 응답이 섞여도 문구는 성립해야 한다.

### 3-3. 고객 목록 — 표시를 **컴포넌트로 뽑아** 상세와 같은 것을 쓴다

`components/OrderItemPartialNote.vue` (취소 · 반품 · 요청 세 줄). 상세(`OrderDetailView`)도 이걸 부른다.

🔴 **베껴 쓰지 않은 이유가 I-7 본문에 적혀 있다** — 고치려는 병이 *「목록과 상세가 같은 주문에 다른
숫자를 말한다」* 인데, **사본을 하나 더 만들면 그 병이 그대로 재발한다.** 이 저장소가 그걸로 두 번
데었다(§I-1 매출 식 넷 · §I-2 목록 문구).

합계 자리도 상세와 같은 규칙으로 바꿨다:

| | 고치기 전 | 고친 뒤 |
|---|---|---|
| 라벨 | 「합계」 고정 | 「결제 금액」 / 부분이 있으면 **「남은 결제 금액」** |
| 값 | `totalPrice` (주문 시점 상품합계) | `payAmount` (**서버가 이미 뺄셈을 한 값**) |

⚠ `totalPrice` 는 **부분 취소·반품이 있어도 안 줄어든다.** 그래서 목록은 원래 금액을, 상세는 남은
금액을 말하는 상태였다. 🔴 **화면에서 뺄셈을 하지 않았다** — `payAmount` 를 그대로 쓴다.

---

## 4. 되돌려서 확인 — **다섯 다 의도한 테스트가 잡았다**

| 무엇을 되돌렸나 | 잡은 것 |
|---|---|
| `remainingQuantity` 합을 **원본**(`getQuantity`)으로 | 백엔드 1건 |
| **요청과 반품을 한 칸으로 섞음**(`getReturnRequestedQuantity` → `getReturnedQuantity`) | 백엔드 1건 |
| 고객 목록에서 흔적 컴포넌트 제거 | 프론트 3건 |
| 합계를 옛 모습(`totalPrice`/「합계」)으로 | 프론트 2건 |
| 관리자 목록에서 요청 수량을 **반품 줄에 합산** | 프론트 2건 |

**전수**: 백엔드 주문 도메인 **190건 통과**(`AdminOrderListIntegrationTest` 10건 — 새것 둘 포함) ·
프론트 **310건 통과**(32파일 — 새 `OrderListView.test.js` 6건 포함).

### 4-1. ⚠ 내가 낸 실수 하나

새 `OrderListView.test.js` 에서 `w.text()` 로 **화면 전체**를 보고 *«멀쩡한 주문엔 취소됨이 없다»* 를
단언했다가 빨개졌다 — **상태 탭의 라벨**(「취소됨」·「반품완료」)이 걸린 것이다.
→ 주문 카드(`li.card`) 안으로 범위를 좁혔다.
🔴 **목록 화면의 텍스트 단언은 탭·필터를 먼저 뺀다.** 상세 화면 테스트에는 없던 함정이다
(상세엔 상태 탭이 없다).

---

## 5. DB 는 안 움직였다

`@Transactional` 롤백이라 예상대로지만 WA §3-5 대로 **테스트 뒤 그 자리에서 다시 셌다**(09:58):
`product_variant` **10** · `stock_history` **78** · product **7 / 1** · `point_history` **50** ·
`notification` **186** · `orders` **80** — 아침과 **전부 같다**. 표식(`ZZADMLIST`) 잔재 **0건**.

---

## 6. 🔴 아직 안 한 것 — `ZZ-123123` 영구 삭제 관측 (오늘 13:16 이후)

⚠ **시각을 어제 문서보다 정확히 낼 수 있게 됐다.**

- `deleted_at` = **2026-08-20 04:16:55 UTC** (= **13:16:55 KST**) · `purge-grace-days: 7` ·
  `purge-enabled: true` → **임계는 오늘 13:16 KST** 가 맞다.
- 🔴 **그런데 배치가 도는 시각은 13:16 이 아니다.** `ProductPurgeScheduler` 는
  `fixedDelay 1h` + `initialDelay 5m` 이라 **«매시 정각» 이 아니라 «기동 시각 + 5분부터 1시간마다»** 다.
  백엔드가 **오늘 09:29:27 에 기동**했으므로 틱은 09:34 → 10:34 → 11:34 → 12:34 → **13:34** 이고,
  임계를 넘는 **첫 틱은 13:34경**이다.
  ⚠ **재기동하면 이 시각이 다시 밀린다** — 오늘 배포를 하면 배포 시각 + 5분부터 다시 센다.
- **예측값**(오늘 아침 실측 기준, §1 에서 안 움직였음이 확인됨):
  `product_variant` **10 → 8** · `stock_history` **78 → 75** · product alive/trash **7 / 1 → 7 / 0**.

---

## 이월 (다음 문서로 반드시 옮길 것)

> WORKING-AGREEMENTS §4-1. 다음 날 문서가 그대로 물려받아 시작한다. 처리했으면 지운다.
> ⚠ 🔴 **물려받을 때 «아직 참인가» 를 확인한다** — 오늘 그 대조가 I-7 의 항목 하나를 거짓으로
> 지목했고(§2-1), 이월의 낡은 문장 하나도 지웠다(§1-1).

### 다음 세션에서 시작할 것

1. 🔴 **`ZZ-123123` 관측이 오늘 안에 끝났는지부터 본다**(§6). 안 했으면 **표본이 이미 사라졌다** —
   그때는 `product_variant`·`stock_history`·product 를 세어 **예측값(8 / 75 / 7·0)과 맞는지**로
   사후 확인한다. ⚠ **CASCADE 가 무엇을 지웠는지는 그 뺄셈이 유일한 근거다.**

2. **§I-7 은 코드·테스트까지 끝났고 배포는 안 했다.** 🔴 **배포·검증이 남아 있다** —
   백엔드(`AdminOrderResponse` 필드 다섯) · 프론트(목록 두 화면 + 새 컴포넌트) **둘 다**.
   ⚠ **push 도 안 했다.**

3. **§I 의 남은 항목** — ✅ **I-7 은 오늘 닫았다**(§2·§3) ·
   **I-9**(반품 기한 — 🔴 **결정 표부터**, 코드보다 «N일» 이 본체다) ·
   **I-13**(원장 detail 1000자 — 주문 도메인만 막아 뒀고 **주문 밖과 근본 해법은 감사 도메인의 결정**).
   ⚠ **I-11 의 보류 항목**(동시성 · 잔돈 음수)은 **재현 테스트를 먼저** 만든 뒤 판단한다.
   ⚠ 🔴 **남은 셋은 전부 «코드보다 결정이 먼저» 다** — I-9 는 기한 일수, I-13 은 감사 도메인 설계,
   I-11 은 재현 조건. **손을 먼저 대면 되돌린다.**

4. 🔴 **`sold_count` 의 옛 이력은 그대로 남아 있다** — 08-25 이전 부분 취소가 판매량을 안 되돌린 몫
   (지바 12 vs TOP 5 · 몽쉘 10개 8 vs 1). **보정할지는 여전히 안 정했다.**
   ⚠ 정한다면 «로컬이라 그냥 둔다» 로 끝난 08-24 의 어긋난 값 셋과 **같은 성질의 결정**이다.

5. ⚠ **아침에 기준값을 다시 센다.** **마감값 (2026-08-27 09:58 기준)**:
   `product_variant` **10** · `stock_history` **78** · product alive / trash **7 / 1** ·
   `point_history` **50** · `notification` **186** · `orders` **80** · **매출(상품) 300,618**.
   **김기현팀**: 적립금 **179,646** · 누적구매 **153,978** · **SILVER**.
   **김기현팀4**: **0 / 0 · BRONZE**.
   ⚠ 이력은 `product_id` 로 센다.
   🔴 **이 값은 «배포 전» 값이다** — 배포·브라우저 검증을 하면 그 자리에서 다시 세야 한다(WA §3-5).
   ⚠ **매출 300,618 은 08-26 마감값을 그대로 옮긴 것이고 오늘 다시 세지 않았다** — 데이터가 안
   움직였으므로 참일 것이나, **실측한 것은 아니다.**

6. 🔴 **다음 기능 후보** — **G-6 등급 혜택 / G-7 최근 검색어**(소) 외에는 **§I 의 남은 항목이
   우선**이다(위 3번). ⚠ **뷰 테스트**: `OrderAdminView` 는 반품 승인·거절 + **오늘 더한 부분 수량
   자리**만 있고 **발송·배송완료·대행 취소·필터는 여전히 비어 있다.**
   ⚠ **마케팅 발송은 후보가 아니다** · **F-2 는 아직 아니다** · **B-26 나머지는 «조건이 갖춰지면»** ·
   **MSA 는 아직 아니다.**

### 조건부 잔여

**08-26 §조건부 잔여를 그대로 물려받는다.** ⚠ 개수를 여기 적지 않는다 — 목록이 곧 개수다.

#### 오늘 더해진 것

- ⚠ **`OrderItemPartialNote` 가 상세·고객목록 **두 화면**의 공용이 됐다** — 🔴 **고치면 두 곳이 함께
  움직인다.** 그게 만든 이유지만, **한쪽 사정으로 고치면 다른 쪽이 조용히 바뀐다.**
  ⚠ **관리자 목록은 이걸 못 쓴다**(그리드 한 줄이 주문 하나라 품목을 펼칠 자리가 없다) —
  즉 **표시 규칙이 여전히 두 벌**이다. 어긋나면 잡히도록 양쪽에 테스트를 뒀다.
- ⚠ **`AdminOrderResponse` 는 수량만 싣고 금액은 안 싣는다**(§3-1). 🔴 **누가 «목록에도 남은 금액을»
  이라고 하면 「남은 금액」식의 넷째 사본이 생긴다** — 그때는 `remainingItemsTotal()` 을 **엔티티에서
  불러** 싣지, 화면이나 DTO 에서 빼지 않는다.
- ⚠ **고객 목록의 합계가 `payAmount`(배송비 포함) 로 바뀌었다** — 이전엔 `totalPrice`(상품합계,
  배송비 제외)였다. 🔴 **같은 주문의 목록 숫자가 어제와 달라 보인다** — 상세와 맞춘 결과지 회귀가 아니다.
- ⚠ **`OrderListView` 에 테스트가 오늘 처음 생겼다**(6건). 부분 표시와 합계만 본다 —
  **상태 탭·페이징·빈 목록은 여전히 안 덮였다.**

#### ⚠ 그대로 물려받는 것

- 🔴 **`ZZ-123123`** — 유예 만료 **오늘 13:16 KST**, 배치 틱은 **13:34경**(§6).
- 🔴 **08-24 검증이 남긴 어긋난 값 셋** — `김기현팀` 적립금 **+1,428** · 지바 재고 **+1** ·
  반팔티 재고 **+1**. 「로컬이라 그냥 둔다」 결정. ⚠ 되돌리고 싶어지면 **적립금은 원장에 «보정»
  줄을 남기는 쪽**이 맞다(잔액만 고치면 `SUM(history) == 잔액` 이 깨진다).
- **살아 있는 표본** — `20260724-0166`(부분 반품이 살아 있는 유일한 주문) ·
  `20260727-0253`(부분 취소) · `20260825-5633`(반품 거절) · `20260724-0246` · `20260825-5484` ·
  `20260826-5930`(부분 취소 + `priceDelta ≠ 0` 세일 옵션) · `20260826-5931`(§I-6 증거물) ·
  `20260826-6117`(§I-12) · `20260826-6119`(§I-10) · `20260826-6187`(§I-14). **지우지 말 것.**
  ⚠ **`20260826-6118` 은 잔재**이고 표본 값이 없다.
- ⚠ **`20260826-5931` 은 §I-12 «이전» 코드가 만든 알림을 들고 있다**(「0원 주문이 취소되었습니다」).
  **버그의 증거물로는 값이 있지만, 고친 동작의 예로 읽으면 안 된다.**
- **세일 표본 `ZZ-세일검증`** — 세일 둘 다 `ENDED`. 🔴 `priceDelta ≠ 0` 인 옵션을 가진 **유일한
  상품**이다. **정리는 아직 안 했다.**
- **G-9 표본 `20260820-5108`**(`ORDERED`) — 품목이 하나뿐이라 부분 취소가 곧 전체 취소가 되어
  표본이 사라진다.
- **16번의 미결제 주문 둘**(`20260813-4185`·`20260813-4186`) · **V51 검증이 바꾼 옛 주문 둘**
  (`20260724-0249` `SHIPPED` · `20260727-0257` `RETURNED`) · **계정 `김기현팀4`**(USER) ·
  **08-24 검증 주문 셋**(`20260824-5295`·`5296`·`5297` — `5296` 은 **사고의 증거물**이다).
- ⚠ **`returnReason` 은 여전히 한 칸이라 회차마다 덮인다** · **`order_item.returned_at` 도 마지막 시각만**.
- ⚠ **매출 식이 상수 한 곳(`OrderStatsRepository.ITEM_SALES`)에 있다.** 🔴 «식이 옳은가» 는
  `partialReturnReducesRevenue` 하나가 지킨다 — **그 테스트를 지우면 아무도 안 지킨다.**
- ⚠ **같은 「남은 것」식이 셋이다** — `ITEM_SALES` SQL · `OrderItem.remainingQuantity()`·`remainingAmount()`.
  **셋 중 하나를 고치면 나머지 둘을 연다.**
- ⚠ **부분 취소 알림은 «전량이 빠지는 경우» 를 건너뛴다** — 그 판단이 이벤트의 `orderFullyCancelled`
  한 칸에 걸려 있다.
- ⚠ **배분식이 서버와 화면 두 벌이다.** 뷰 테스트가 서버 테스트와 **글자 그대로 같은 숫자**를 단언한다
  (12,858 · 17,143 · 8,333 · 2,142 · 2,857).
- 🔴 **`priceFilterUsesSalePrice` 의 간헐 실패 원인은 여전히 모른다**(08-19 §7). 구조를 바꿔 **회피**했을 뿐이다.
- ⚠ **「캐시 60초 지연」은 끝내 실측하지 못했다.** · ⚠ **`COUPON_WELCOME_SET` 은 아직 브라우저로 안 밟았다.**
- ⚠ **`recordAdmin` 은 `actor == null` 을 견디는데 감사는 못 견딘다**(08-20 §7-7). 그대로 뒀다.
- ⚠ **`recentlyViewed` 의 세일 값은 낡을 수 있다** — localStorage 스냅샷이다.
- ⚠ **`deleted_at` 오프셋이 행마다 섞여 있다**(08-19 §1-1). 값은 맞다. **원인 미상.**
  🔴 **오늘 이게 값을 했다** — `ZZ-123123` 의 `deleted_at` 이 UTC 로 읽혀 «04:16» 이었고, 그래서
  이월의 «13:16» 과 9시간 어긋나 보였다. **둘은 같은 시각이다**(§6).
- 🔴 **「화면이 유일한 방어」는 여전히 «두 종류에 두 번» 이다.** 오늘 새로 난 것 없다. **아직 안 올린다.**
- ⚠ **08-21 이월의 나머지는 그대로 물려받는다** — 정가 칸 원인 미상 · 겹침 금지가 앱에만 있다 ·
  `SignupView` 의 `border-ink-200` · 막힌 것 셋(배송완료 리뷰 · 탈퇴 회원 관리자 조작 ·
  탈퇴 시 장바구니 정리).
