# 2026-08-10 핸드오프 — 착수 전 실측 (디스크 확장 완료 · 취소 적립금 환불 검증 대기)

> WORKING-AGREEMENTS §4-0-1: **배포할 때마다 닫는다.** 하루 끝에 몰지 않는다.
> 보조 기준은 시간이 아니라 **커밋 5개**.

---

## 0. 오늘의 커밋

| 커밋 | 내용 |
|---|---|
| `d7a861b` | docs: 08-10 핸드오프 개시 — 디스크 확장 종결 · BACKLOG F-6 완료 이관 |
| `869dbd5` | docs: 08-10 §0 커밋 표에 개시 커밋 추가 (WA §4-4) |
| `14deb5a` | docs: 08-10 §5 취소 적립금 환불 검증 종결 · §6 08-07 「0건」 오판정의 원인 |
| `d9b24c0` | docs: 08-10 §0 커밋 표 재작성 (WA §4-4) |
| `84a8ea3` | docs: 08-10 §5-2-1 대조군 — 화면은 닫히고 원장은 남았다 |
| `120cecc` | docs: 08-10 §0 커밋 표 재작성 (WA §4-4) |
| `964fa04` | docs: 08-10 §5-2-1 대조군 원장까지 종결 — 양성 증거가 붙어서야 0행이 뜻을 갖는다 |

> ⚠ 하루를 닫을 때 **이어 붙이지 말고 통째로 다시 만든다**(WA §4-4):
> `git log --reverse --since="2026-08-10 00:00" --pretty='| \`%h\` | %s |'`
> ⚠ 이 표는 **자기 자신을 담은 커밋 이후를 못 적는다**. 배포 전에 `check-handoff.sh` 가 한 번 더 센다.

---

## 1. 오늘 무엇을 할 것인가 — 후보와 근거

**08-07(금) 이월을 그대로 물려받고 시작한다.** 그 문서의 🔴 는 둘이었다 —
①취소 적립금 환불의 **브라우저 검증 미완** ②**디스크 확장 절반**.
주말(08-08·08-09) 커밋은 **0건**이다(`git log --since="2026-08-08 00:00"` 빈 출력).

| 후보 | 크기 | 근거 | 오늘 |
|---|---|---|---|
| **디스크 확장 마무리** | 소 | 08-07 §10 이 절반에서 끊겼다. **착수 전 정리**라 작업 항목이 아니다 | **← 먼저 (§2 에서 종결)** |
| **취소 적립금 환불 브라우저 검증** | 소 | 배포는 나갔는데 **아무도 안 밟았다**(08-07 §9-2). 닫기 전에는 «완료» 가 아니다 | **← 고름** |
| B-25 관리자 주문 취소 | 중 | 위 검증에서 **사용자 취소 흐름을 눈으로 볼 참**이라 짝이 맞다(WA §1-2-1) | 다음 후보 |
| B-18 남은 것(신고·문의 숨김·감사) | 소~중 | 8/06 에 *"이번엔 안 붙인다"* 로 갈라 뒀다 | 아님 |
| 관리자 재고 목록 화면 | 소 | `stock_history` 가 아직 얇다 | 아님 |

---

## 2. 디스크 — VM 가상 디스크 확장 **종결** (BACKLOG `F-6`)

08-07 §10 이 *"절반 끝났다"* 로 끊겼던 자리다. **오늘 나머지를 다 밟았다.**

### 2-1. 이어받은 상태가 문서와 달랐다

문서(08-07 이월)는 `sda2` **29G** 를 물려줬는데 오늘 아침 실측은 **49G** 였다 —
`parted resizepart` + 재부팅이 이미 되어 있었다(`uptime -s` = **2026-08-10 07:55:14**).
⚠ **이월을 그대로 믿고 `parted` 를 또 제안했으면 이미 끝난 조작을 되풀이할 뻔했다.**
이월은 «어제까지의 상태» 지 «지금의 상태» 가 아니다 — **손대기 전에 다시 잰다.**

### 2-2. 남은 LVM 3단계 (사용자가 실행, CLAUDE.md — sudo 는 제안만)

```
sudo pvresize /dev/sda2
sudo lvextend -l +100%FREE /dev/mapper/rl-root
sudo xfs_growfs /
```

### 2-3. 실측 — 전후

| 항목 | 오늘 아침 | 지금 |
|---|---|---|
| `sda` | 50G | 50G |
| `sda2` | 49G | 49G |
| `rl-root` | **26G** | **46G** |
| `/` 크기 · 사용 · 여유 | 26G · 23G · **3.9G** | 46G · 23G · **24G** |
| 사용률 | **86%** | **49%** |

`sda` 50G = `sda1` 1G + `sda2` 49G 로 **남는 공간이 없다** — 여기서 더 늘리려면
**다시 호스트에서 VDI 를 키우는 것부터** 시작해야 한다(그 순서와 스냅샷 함정은 08-07 §10-2).

### 2-4. 캐시 1.9G 는 **안 지웠다** — 판단이 바뀌었다

이월에 *"sudo 캐시 정리 1.9G 도 아직 안 했다"* 가 있었지만, 실측하니 그대로다:
`/var/cache/PackageKit` **1.6G** · `/var/cache/dnf` **282M** · `~/.npm/_cacache` **108M**.

→ **지우지 않는다.** 08-07 §10 이 *"이건 또 찬다"* 로 이미 적었듯 근본은 PackageKit 의
선다운로드 설정이라 **지워도 되돌아온다.** 여유가 3.9G 였을 땐 1.9G 가 절반이었지만
24G 인 지금은 **8%** 다 — **같은 조작의 값어치가 여유에 따라 달라진다.**
⚠ `~/.npm/_cacache` 가 pnpm 만 쓰는데도 되살아나는 **원인은 여전히 모른다**(08-07 §10). 추측으로 적지 않는다.

---

## 3. 착수 전 실측 — 코드·배포 쪽

| 확인 | 실측 |
|---|---|
| 워크트리 | `/home/ecstel/work` = `main` · `/home/ecstel/work-goblin` = `order-cancel-point-refund` — **둘 다 같은 커밋** |
| 취소 환불 커밋 | `git log main..order-cancel-point-refund` **빈 출력** = `c724ae6` 은 main 에 들어가 있다 |
| 주말 커밋 | 0건 |
| 백엔드 서비스 | `glassvue-backend.service` **active running** |
| Oracle | `oracledb_ESPDB-19c.service` **active running** |
| 취소 환불 로그 | ⚠ **못 쟀다** — `journalctl` 이 sudo 를 요구한다. **0건이라고 쓰지 않는다**(WA §3-3·§4-3-1) |

⚠ 마지막 줄이 요점이다. 08-07 §9-2 는 `Point refunded (cancel)` **0건**을 근거로 *"아직 아무도 안 밟았다"*
라고 판정했는데, 오늘 그 값을 **다시 얻지 못했다.** 「0」과 「못 쟀다」는 다르다(WA §3-3).

🔴 **그리고 이 칸은 끝내 로그로 채우지 않았다 — 채울 수 없는 자리였다.** 오늘 07:55 에 재부팅했고
저널은 휘발성이라 08-07 로그는 **애초에 없다**. 대신 **DB 원장으로 판정했고, 그게 더 나았다**(§5·§6).
⚠ *"sudo 가 없어서 못 쟀다"* 가 아니라 **«그 수단으로는 원래 못 잰다»** 였다 — 수단을 못 쓰는 것과
수단이 답을 갖고 있지 않은 것은 다르다.

---

## 4. 작업 순서

1. ✅ 디스크 마무리 (§2) — **종결**
2. ✅ **취소 적립금 환불 브라우저 검증** (§5) — **종결**. 곁가지로 08-07 오판정을 찾았다(§6)
3. ✅ **B-25 관리자 주문 취소** (§7) — 구현·테스트·변형·빈 DB 검증까지. **배포·브라우저 검증은 남았다**

---

## 5. 취소 적립금 환불 — 브라우저 검증 **종결** (이월의 🔴)

08-07 §9-2 가 *"배포는 나갔고 검증은 안 됐다"* 로 끊었던 자리다. **오늘 닫았다.**

### 5-1. 실측 — 적립금 원장 (KST)

⚠ **로그가 아니라 DB 원장으로 판정했다** — 이유는 §6.

**`20260810-2651`** · 09:26 · `CANCELLED` · `used_point` **500** · 수령인 `ZZ-대몽쉘`

| 시각 | 유형 | 금액 | 잔액 | reason |
|---|---|---|---|---|
| (주문 직전) | — | — | **36,457** | 기준값 |
| 09:26:10 | `USE` | −500 | 35,957 | 주문 사용 |
| 09:26:13 | `REFUND` | **+500** | **36,457** | **주문 취소 환불 500** |

**잔액이 기준값으로 정확히 복귀했다.** 이월 4스텝 중 ①②④ 가 이 표로 닫히고,
③(취소 폼의 *"사용한 적립금 500원은 …돌려드립니다"*)은 **사용자가 화면에서 확인**했다.

⚠ `reason` 의 `"주문 취소 환불 500"` 은 **새 코드에만 있는 문자열**이다
(`refundCancelledOrder` 의 `"주문 취소 환불 " + usedPoint`). 반품 쪽은 `"반품 환불 … (적립 … 회수)"` 라
**원장만 보고도 어느 경로가 돌았는지 갈린다** — `PointType` 에 `CANCEL` 을 안 만든 판단이
여기서 값을 했다(취소인지 반품인지는 `order_id` 와 reason 으로 되짚어진다).

### 5-2. 곁가지 — **반품을 취소로 착각할 뻔했다**

검증 도중 사용자가 *"원금 − 적립금까지만 환불됐다"* 고 물어 왔다. 확인해 보니 그 주문
(**`20260810-2650`**)은 `CANCELLED` 가 아니라 **`RETURNED`** 였다 — 밟힌 것은 오늘 검증 대상인
`cancel()` 이 아니라 `approveReturn()` 이었다.

| 시각 | 유형 | 금액 | 잔액 |
|---|---|---|---|
| (직전) | — | — | 30,657 |
| 09:14:31 | `USE` 주문 사용 | −1,000 | 29,657 |
| 09:14:38 | `EARN` SILVER 2% | +116 | 29,773 |
| 09:14:43 | `REFUND` 반품 환불 6800 (적립 116 회수) | **+6,684** | **36,457** |

**6,684 = 6,800 − 116 이 맞다** — 배송완료에 이미 준 적립을 회수하는 것이 반품 설계(C-9)다.
⚠ 그리고 **쓴 적립금 1,000P 는 안 사라졌다**: 순변동 **+5,800**(= 6,800 − 1,000). 결제 연동이 없어
반품 환불은 **전액 적립금으로** 나가므로, 현금으로 낸 몫이 적립금이 되어 **잔액이 오히려 는다.**
⚠ 배송비 3,000 은 안 돌아온다 — `refundableAmount()` 가 **상품합계 − 쿠폰**이고
`approveReturn()` 주석에 그렇게 적혀 있다. **의도된 것이다.**

→ 교훈은 §6 이 아니라 여기다: **«환불이 이상하다» 는 물음에 금액부터 계산하면 안 된다.**
   **어느 경로가 돌았는지**(주문 상태)를 먼저 봐야 한다 — 취소와 반품은 환불 공식이 다르다.

### 5-2-1. 대조군 — **적립금을 안 쓴 주문의 취소 폼** (화면만 닫혔다)

**`20260810-2652`** · `used_point` **0** · 09:35 · 취소 폼 실측:

> 취소하면 **재고가 복원**됩니다. 되돌릴 수 없습니다.

**적립금 줄이 없다** — `v-if="order.usedPoint > 0"` 대로다. «0P 환불» 같은 줄을 안 만든다는 설계가
화면에서 확인됐다(반대쪽은 §5-1 에서 *"사용한 적립금 500원은 …"* 이 떴다).

#### 원장 쪽 — **09:36:19 에 실제로 취소해서 닫았다**

처음엔 이 주문이 `ORDERED` 인 채였다(폼만 열어 봤다). 그 상태의 `point_history` **0행**은
*"0원 이력이 안 생겼다"* 가 아니라 **«취소를 안 해서 0»** 이라 근거가 못 됐다 —
WA §3-3 의 구분이 **오늘 두 번째로** 걸린 자리다(§6 이 첫 번째). 그래서 실제로 취소했다:

| 확인 | 실측 |
|---|---|
| 주문 | `CANCELLED` · `cancelled_at` **09:36:19** · `used_point` **0** |
| **음성 증거** | 09:26 이후 `point_history` **0행** — 0원 이력이 안 생겼다 |
| **양성 증거** | `stock_history` **`CANCEL` +1 → `stock_after` 94** @ **09:36:19** |
| 잔액 | **36,457** 그대로 · 누적 242,880 · SILVER (전부 불변) |

**양성 증거가 붙어서야 0행이 뜻을 갖는다.** 재고 복원 줄이 `cancelled_at` 과 **같은 초**에 남았으므로
`cancel()` 이 돌았음이 증명되고, **그 위에서의 0행**이라야 *"적립금을 안 쓴 취소는 원장을 안 건드린다"*
가 된다. WA §3-3 의 «없어야 할 것이 없다 **+** 있어야 할 것이 있다» 를 둘 다 갖춘 판정이다.

⚠ **재고 이력이 이 판정을 가능하게 했다.** `stock_history` 는 B-19 로 «왜 돌아왔는지» 를 구분하려고
만든 것인데(`ORDER` / `CANCEL` / `RETURN`), **다른 도메인의 0을 증명하는 양성 증거**로 쓰였다.
원장은 자기 도메인 밖에서도 값을 한다.

### 5-3. 검증 잔재

오늘 만든 검증 주문은 전부 `ship_recipient` = **`ZZ-대몽쉘`** 이라 **세는 쿼리에 잡힌다**(WA §3-1 준수).
조건부 잔여 7번에 합류하며, 7번이 이미 *"건수를 적지 않는다"* 로 되어 있어 **여기에도 개수를 남기지
않는다**(WA §4-0-2).

> ⚠ **여기 처음엔 «3건(2649·2650·2651)» 이라고 적었다가 20분 만에 낡았다** — 대조군 `2652` 가
> 늘었다. §4-0-2 가 말한 그대로다(*"검증할 때마다 늘어나는데 문장은 처음 쓴 상태로 남는다"*).
> **규약을 인용한 문단 바로 옆에서 규약을 어겼다** — 세지 않는 것이 규약인 자리에 개수를 적으면,
> 그게 근거처럼 보여서 다음 사람이 다시 세지 않는다.

⚠ **`20260810-2652` 는 한때 살아 있는 주문(`ORDERED`)이었다** — 09:36 에 취소해 `CANCELLED` 가 됐다.
잔재의 성격이 다른 것들과 같아졌다(§5-2-1).

---

## 6. 🔴 08-07 §9-2 의 「0건」은 **틀린 판정이었다** — 재부팅이 저널을 지웠다

08-07 §9-2 는 *"로그 전수 `Point refunded (cancel)` **0건** — 아직 아무도 안 밟았다"* 라고 적었다.
**그런데 DB 원장에 그날의 취소 환불이 남아 있다**:

| 시각(KST) | 유형 | 금액 | 잔액 | reason | 주문 |
|---|---|---|---|---|---|
| 08-07 17:05:23 | `USE` | −150 | 15,507 | 주문 사용 | `20260807-2648` (CANCELLED) |
| 08-07 17:05:30 | `REFUND` | **+150** | 15,657 | **주문 취소 환불 150** | 〃 |

**배포 4분 뒤에 이미 돌았다**(배포 17:00–17:01, 08-07 §9-1). 즉 그 「0건」은 *"안 밟아서 0"* 이 아니라
**«밟았는데 0으로 보인» 것**이다.

### 6-1. 왜 0으로 보였나 — **같은 문서의 다른 절에 답이 있었다**

08-07 §10-2 가 디스크 작업 중 `uptime -s` = **17:26** 을 적어 뒀다. 그리고 §9-2 의 로그 조회는
문서 커밋 시각으로 보아 **17:52**(커밋 `dd321a4`)다.

```
17:05 취소 환불 실행  →  17:26 재부팅(디스크 작업)  →  17:52 journalctl 조회 = 0건
```

**WA §3-3-1 이 적어 둔 그대로다** — *"이 VM 의 `journalctl` 은 휘발성이라 `--list-boots` 에 현재
부팅 하나만 남는다."* 규약은 8/05 에 이미 있었는데 **이틀 만에 같은 자리에서 또 밟혔다.**

⚠ **왜 규약이 있는데도 못 막았나**: 8/05 사고는 *"어제 로그를 오늘 다시 보려 했다"* 라 **날짜가 달랐다.**
이번엔 **같은 날 오후**라 «어제 로그» 라는 신호가 없었다 — 부팅 경계는 **날짜 경계와 다른 자리**에 있고,
그 경계를 넘은 사실이 **같은 문서의 다른 절(§10)에 적혀 있었는데도** §9 를 쓸 때 대조되지 않았다.

### 6-2. 무엇을 바꾸나

- **돈이 움직이는 경로는 로그가 아니라 원장(DB)으로 판정한다.** 로그는 재부팅에 사라지지만
  `point_history` 는 남는다 — 그리고 원장은 **금액·잔액·순서까지** 준다(§5-1 표가 로그 한 줄보다 낫다).
- WA §3-3-1 에 **부팅 경계 확인**을 덧붙였다(같은 날이어도 갈린다).
- 08-07 §9-2 는 **고치지 않고 정정 주석만 달았다**(WA §4-5 — 이미 푸시된 기록은 되돌리지 말고
  다음 커밋에서 채운다). 그날 실제로 본 것은 «0건» 이 맞고, **틀린 것은 거기서 끌어낸 결론**이다.

---

## 7. B-25 관리자 주문 취소 — 착수 전 실측 · 결정 · 구현 (커밋 `ec8c7e5`)

### 7-1. 실측 — **결론은 맞고 이유는 낡아 있었다**

| 항목 | 백로그(08-04) | 실측(08-10) |
|---|---|---|
| `/api/admin/orders` | GET 3개뿐 | ✅ 그대로 |
| `OrderService.cancel` | 본인만 | ✅ `findByIdAndMemberId` 그대로 |
| `isCancellable()` | — | `ORDERED \|\| PAID` |
| 취소자 컬럼 | 없음 | ✅ `cancel_reason` 만 있고 행위자 없음 |

**바뀐 것 둘** — 둘 다 백로그 문장을 그대로 믿었으면 틀렸을 자리다(WA §1-3):

🔴 **①「관리자 조작 API 가 없다」는 인상이 틀렸다.** 관리자 주문 조작은 `/api/admin/orders` 가 아니라
**`/api/orders/{id}/…` 에 이미 넷** 있다(`ship`·`deliver`·`return-approve`·`return-reject`, SecurityConfig
매처로 ADMIN). **`AdminOrderController` 는 조회 전용이 규약**이었다.
→ B-25 는 «새 자리를 만드는 일» 이 아니라 **있는 줄에 하나 더 얹는 일**이다. 화면도 같다 —
`OrderAdminView.vue` 에 발송·배송완료·반품승인·거절·상세가 나란히 있고 **취소만 비어 있었다.**

🔴 **②결정 셋 중 하나가 이미 풀려 있었다.** 백로그의 *"②환불이 따라오나"* 는 **08-07 에 답이 나왔다** —
`cancel()` 이 `refundCancelledOrder` 를 부르게 되면서다. 관리자 취소가 **같은 `cancel` 경로를 타기만
하면** 환불이 자동으로 따라온다.
⚠ **백로그 항목은 쓰인 날의 세계를 담는다.** 3일 사이에 우리가 그 전제를 바꿔 놨는데 문장은 그대로였다.

**곁가지 확인**: 알림은 이미 붙는다(`OrderCancelledEvent` → *"주문이 취소되었어요"*).
감사는 `AdminActionEvent.targetId` 가 **대상 회원 id** 인데 **주문자가 곧 대상 회원**이라 모양이 맞는다 —
B-18 에서 리뷰 숨김에 감사를 못 붙였던 이유(대상이 회원이 아니다)가 **여기엔 해당하지 않는다.**

### 7-2. 착수 전 결정 (사용자와 확정, 2026-08-10)

| 결정 | 확정 | 버린 안과 이유 |
|---|---|---|
| 허용 상태 | **ORDERED·PAID (본인과 동일)** | SHIPPED 개방 — **돌아오지도 않은 물건으로 재고가 복원된다.** 회수 확인 단계가 따라와야 하고, 그 자리는 이미 반품이 맡는다 |
| 취소자 기록 | **컬럼 + 감사 둘 다** | 감사만 — 조회가 `SUPER_ADMIN` 전용이라 **일반 ADMIN 이 취소자를 못 본다** / 컬럼만 — 회원 정지·역할변경·삭제는 다 남는데 **주문 취소만 감사에서 빠진다** |
| 사유 | **필수**(본인은 선택) | 선택 — *"누가 취소했는지는 아는데 왜인지는 모르는"* 주문이 남는다. 발송 처리가 운송장을 필수로 받는 것과 같은 판단 |

### 7-3. 구현에서 실제로 판단이 갈린 자리

- **경로를 갈랐다**(`/admin-cancel` ≠ `/cancel`). 같은 경로에 역할로 분기하면 관리자가 본인 취소
  경로를 타 `cancelledBy` 가 비고, **그 주문은 「본인이 취소했다」로 거짓말을 한다**(NULL 이 본인이라는 뜻이므로).
- **취소 본체는 `applyCancellation` 하나로 공유**한다(재고 → 환불 → 이벤트). 갈라 두면 한쪽만 고쳐지고
  **그 어긋남은 돈에서 난다** — 08-07 이 정확히 그 모양이었다(WA §1-2-1).
- **`cancelByAdmin` 은 `cancel` 을 부르고 행위자를 덧칠하는 식이 아니라 한 메서드**다.
  두 줄로 나뉘면 «한쪽만 부른 호출부» 가 생기고, 그 행이 위의 거짓말을 한다.
- **플래그를 안 뒀다**(`is_admin_cancelled` 같은 것). 플래그와 id 가 어긋난 행은 **앱이 멀쩡히 돌면서**
  화면에만 틀리게 나온다 — G-3 의 `product_id`·`inquiry_type` 쌍에서 겪은 모양이다.
- **고객 화면에도 띄운다** — *"고객센터에서 대신 취소했어요(닉네임)"*. 안 띄우면 화면이 「취소됨」만
  말하고 주체를 말하지 않는데, **그때 기본값처럼 읽히는 주체는 본인**이다.
  ⚠ 08-07 취소 폼이 적립금 환불을 «말하지 않아서» 안 하는 줄 알았던 것의 **반대 방향**이다 —
  그때는 침묵이 기능 부재를 가렸고, 여기선 침묵이 행위자를 가린다.
- **새 에러코드를 안 만들었다** — 발송 후 거절은 본인 취소와 이유가 같아 `ORDER-400C` 를 그대로 쓴다.

### 7-4. 테스트 · 변형 주입

**백엔드 657**(645 → **+12**, skipped 0) · **프론트 153**(151 → +2).

| 변형 | 무엇을 망가뜨렸나 | 잡혔나 |
|---|---|---|
| M1 | 감사 이벤트를 안 낸다 | ✅ 감사 원장 테스트 |
| M2 | `applyCancellation` 에서 적립금 환불 제거 | ✅ 적립금 환불 테스트 |
| M3 | `cancelByAdmin` → `cancel`(취소자 안 남김) | ✅ 취소자 기록 테스트 |
| M4 | 관리자 경로의 상태 가드 제거 | ✅ 발송 후 400 테스트 |
| M5 | 감사 `target_login` 에 loginId 대신 닉네임 | ✅ 감사 원장 테스트 |
| M6 | **엔티티의 사유 가드 제거** | 🔴 **안 잡혔다** |

🔴 **M6 이 요점이다.** HTTP 로 오면 `AdminOrderCancelRequest` 의 `@NotBlank` 가 먼저 400 을 내서
**엔티티의 가드 줄에 닿지 않는다** — 통합 테스트로는 **영원히 못 덮는 자리**였다.
→ 가드를 지우지 않고 **단위 테스트로 덮은 뒤 M6 을 재주입해 실제로 빨개지는 것까지 확인**했다.
남긴 이유: `cancelByAdmin` 은 `cancelledBy` 를 채우는 **유일한 경로**라, DTO 를 안 거치는 호출부가
생기면 «누가 취소했는지는 아는데 왜인지는 모르는» 행을 막을 것이 없다(WA §2-4-2 와 같은 판단).

⚠ **곁가지로 기존 테스트의 스텁이 틀린 것이 드러났다.** `OrderServiceTest` 의 취소 테스트가
`findByIdAndMemberId(orderId)` 에 **id 가 `orderId` 가 아닌** 주문을 물려주고 있었다 —
id 를 앱에서 만들므로(`BaseTimeEntity`) `Order.create` 는 이미 자기 id 를 갖는다. **운영에서 나올 수
없는 상태**라 조회 키를 주문의 실제 id 로 맞췄다. 리팩터가 재고 이력에 넘기는 값을 «요청 id» 에서
«엔티티 id» 로 바꾸지 않았으면 **드러나지 않았을 것**이다.

### 7-5. 빈 DB 마이그레이션 검증 (WA §2-2) — **통과**

| 확인 | 실측 |
|---|---|
| 적용 | `Successfully applied **43** migrations to schema "ESPTEST", now at version **v43**` (15.4s) |
| 기동 | `Started GlassvueBackendApplication in 78.107 seconds` = `ddl-auto=validate` 통과 |
| `cancelled_by` | `RAW(16)` · nullable |
| `cancelled_by_name` | `VARCHAR2` · `DATA_LENGTH 200` / `CHAR_LENGTH 50` / **`CHAR_USED = C`** |
| 감사 CHECK | `action IN (… 'MEMBER_DELETE', **'ORDER_CANCEL'**)` |

⚠ `CHAR_USED = C` 를 **눈으로 확인했다** — WA §2-2-1 이 «`n CHAR` 로 쓴다» 를 규약으로 두는 이유는
빠뜨려도 **테스트가 통과하기 때문**이다. 바이트였다면 한글 닉네임이 17자쯤에서 ORA-12899 로 터진다.
⚠ 검증 후 8083 은 **내렸다**(떠 있으면 다음 검증이 포트 충돌로 죽는다 — README 의 경고).

---

## 이월 (다음 문서로 반드시 옮길 것)

> WORKING-AGREEMENTS §4-1. 다음 날 문서가 그대로 물려받아 시작한다. 처리했으면 지운다.

- ✅ **디스크 — VM 가상 디스크 확장 종결**(§2). `/` **46G · 49% · 24G 남음**.
  BACKLOG `F-6` 을 「완료」로 옮겼다. ⚠ 캐시 1.9G 는 **일부러 안 지웠다**(§2-4) — 다시 찬다.
- ✅ **취소 시 적립금 환불 — 검증 종결**(§5, 커밋 `c724ae6`). `20260810-2651` 에서 잔액이
  **36,457 → 35,957 → 36,457** 로 기준값 복귀 · 원장에 `주문 취소 환불 500` · 폼 안내 문구 확인.
  **코드로 막힌 것 없음.** 이 항목은 이월에서 빠진다.
  - ✅ **대조군도 화면·원장 양쪽 다 닫았다**(§5-2-1) — `20260810-2652`(`used_point` 0):
    취소 폼에 적립금 줄 **없음** · 취소 후 `point_history` **0행**(음성) + `stock_history`
    **`CANCEL` +1 → 94** @ 09:36:19(양성) · 잔액 **36,457** 불변.
    **남는 것 없음.**
- 🔴 **B-25 관리자 주문 취소 — 코드는 끝났고 「배포 + 브라우저 검증」이 남았다**(§7, 커밋 `ec8c7e5`).
  테스트 657/153 통과 · 변형 6건 중 5건 잡히고 M6 은 단위로 덮음 · **빈 DB V43 검증 통과**(§7-5).
  ⚠ **V43 마이그레이션이 따라온다** — 배포하면 운영 DB 에 컬럼 2개 + 감사 CHECK 확장이 적용된다.
    넓히는 방향이라 구 jar 는 영향받지 않는다.
  브라우저로 볼 것:
  1. 관리자 `/admin/orders` → **ORDERED·PAID 행에만 「취소」 버튼**이 있다(발송완료 행엔 없다)
  2. 누르면 패널이 열리고 사유가 **필수** — 비운 채 「취소 처리」 하면 패널 안에 경고가 뜬다
  3. 사유를 넣고 처리 → 목록에서 **취소됨**으로 바뀌고 상태 건수가 즉시 갱신된다
  4. 🔴 **고객 계정으로** 그 주문 상세를 열면 *"고객센터에서 대신 취소했어요(관리자닉)"* 가 뜬다
  5. 🔴 **적립금을 쓴 주문**으로 하면 잔액이 돌아온다(본인 취소와 같은 경로인지 확인)
  ⚠ 대조군: **본인이 취소한 주문**의 상세에는 4번 줄이 **없어야** 맞다(`cancelledByName` 이 NULL).
  ⚠ 검증 잔재는 조건부 잔여 7번에 합류한다. **`ZZ` 를 붙일 것.**
  ⚠ 뒷받침할 로그: `Order cancelled by admin: … admin=… refundedPoint=…`
    ⚠ **로그로 판정하기 전에 `uptime -s` 를 볼 것**(WA §3-3-1 — 오늘 §6 이 그 사고다).
      돈이 움직이는 쪽은 `point_history` 원장이 더 낫다.
- ⚠ **배포 스크립트의 복사 실패 → `.bak` 되돌리기 갈래를 아직 안 밟아 봤다**(8/05 §5, 성공 경로만 탔다).
  - 앞으로: SSE 탭이 붙어 있으면 배포 종료가 **최대 30초** 걸릴 수 있다 — 고장이 아니라 되찾은 동작이다.
- ⚠ **SSE 「갱신도 실패 → 스트림만 멈춘다」 갈래의 화면을 못 봤다**(8/05 §6-4). 로그상 밟힌 것으로
  **보이지만** 그 시점 화면을 보지 않았다. 단위 테스트는 덮고 있다.
- ⚠ **PackageKit 의 선다운로드를 끄는 방법은 아직 안 찾았다**(08-07 §10). 시스템 동작을 바꾸는 일이라
  확인받고 해야 한다. **지금은 급하지 않다**(§2-4).
- ⚠ **`~/.npm/_cacache` 가 왜 되살아나는지 모른다**(108M). 급하지 않지만 **추측으로 적지 않는다.**

### 조건부 잔여

**8/07 §조건부 잔여를 그대로 물려받는다.** ⚠ 개수를 여기 적지 않는다 — 목록이 곧 개수다(WA §4-0).

1. `/tmp` WebP 잔재 — tmpfiles.d 10일 규칙으로 자동 삭제.
2. 운영자ID 계정 검증 적립 영구 잔존(SILVER·누적 204,880·잔액 97P) — 취소 불가가 설계.
3. 공유기 DHCP 예약 — ⚠ 전제가 사라졌다(NAT). **브리지로 되돌리면 되살아나는 항목**이라 지우지 않는다.
4. 인증서 SAN — `IP:192.168.50.14` 는 죽은 값. **당장 할 일 없음**, 재발급 시 IP 대신 DNS 로.
5. 8fter 의 미학(색·여백·타이포) — 사용자가 "무엇이 좋았는지" 한 줄 주면 DESIGN §2 보강.
6. 디자인 더 갈 여지(B-12 잔여) — 회원 상세 레이아웃 카드화, 상태 뱃지 pill 강화.
7. ⚠ **검증으로 생긴 주문은 지우지 않기로 결정했다**(2026-07-31). **다시 들추지 말 것.**
   실무 규칙: 데모·발표에서 **매출 숫자를 인용하지 않는다.**
   ⚠ 건수를 여기 적지 않는다(WA §4-0-2) — 검증할 때마다 늘어나는데 문장은 처음 쓴 상태로 남는다.
   세려면: `select count(*) from orders where lower(buyer_nickname) like 'zz%' or lower(ship_recipient) like 'zz%';`
   (⚠ 이 쿼리는 `ZZ` 를 안 붙인 잔재를 못 잡는다 — 2026-07-22 사고.
   ⚠ **`lower()` 는 2026-08-07 에 붙였다** — 그전엔 `like 'ZZ%'` 라 소문자 `zz-` 를 놓쳤다.)
8. **검증으로 생긴 문의 1건도 지우지 않기로 했다**(2026-08-06, 사용자 결정 — 8/06 §5-3).
   16:59 · 비밀글 · 작성자 `김기현팀3` · 제목 *"이거 자바아닌데?"* · 답변 있음(`ANSWERED`).
   ⚠ **`ZZ` 가 없어 세는 쿼리로는 못 찾는다** — 그래서 식별 문구를 여기 적어 둔다.
   7번과 성격이 같다: **다시 들추지 말 것.**
   ⚠ **V42 백필이 이 행도 건드렸다** — `inquiry_type` 이 `PRODUCT` 로 채워졌다(8/07 §7-3).
9. **일반 문의 2건도 지우지 않기로 했다**(2026-08-07, 사용자 결정 — 8/07 §7-4).
   `zz-제목`(WAITING, 14:23, `김기현팀`) · `zz-답답하다`(ANSWERED, 14:24, `김기현팀2`) — 둘 다 `DELIVERY`.
   ⚠ 7·8번과 달리 **이건 「들추면 안 되는」 것이 아니다** — 매출·통계에 안 얹히므로 나중에 지워도 된다.
   그래도 남긴다: **일반 문의가 실제로 들어온 유일한 표본**이라 화면·목록을 다시 볼 때 쓸 데가 있다.
   세려면: `select count(*) from inquiry where lower(title) like 'zz%';`
