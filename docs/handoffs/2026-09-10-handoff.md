# 2026-09-10 핸드오프 — 사흘 끊긴 자리를 잇고, **깨진 알림 링크 55건**을 찾았다

> 사용자: *"음 아마 도중에 끊겼을거야 핸드오프에 정리해줘"*.
> 🔴 **«끊겼다» 를 «안 됐다» 로 읽지 않고 먼저 실측했다 — 배포는 되어 있었다.**
> 09-07 세션은 **커밋(14:28) → 배포(14:32) → 끊김** 이었고, **없는 것은 기록뿐**이었다(§2).
>
> ⚠ **그리고 이 문서를 한 번 고쳤다.** 처음엔 브라우저 검증을 *"실측으로 못 가른다"* 로 닫았는데,
> 사용자가 *"그럼 뭐가 안 된 거지? 브라우저 테스트 아님?"* 이라고 되물어 다시 봤다.
> 🔴 **nginx 접근 로그가 디스크에 남아 있어 «안 했다» 가 확정됐다**(§2-1) —
> «못 가른다» 와 «안 했다» 는 **다음 세션이 할 일이 달라지는 차이**다.
>
> ✅ **그리고 그날 안에 «했다» 가 됐다**(§6) — 확정하고 나니 **할 일이 분명해져서** 바로 밟았다.
> 🔴 **숫자 여섯 자리가 전부 맞았고, B-26 이 완전히 닫혔다.**
>
> 🔴 **오늘의 수확은 그 확인이 아니라 그 김에 나온 것이다.** 아침 재계수에서 **`notification` 만
> 3 늘어** 있었고(§1), 그 셋을 쫓다가 **알림 링크가 가리키는 상품이 없는 행이 55건**이라는 것을
> 찾았다(§4). ⚠ **M 축의 ⑲ 가 정확히 그 증상(«누르면 깨진 페이지»)을 막으려고 만든 불변식인데
> 0 을 내고 있었다** — 링크에 «null» 이 박혔는지만 보고 **가리키는 대상이 있는지는 안 본다.**
>
> ⚠ **그리고 09-07 의 마감값이 틀려 있었다**(§3). 🔴 **그날 아침 §1-1 에서 09-02 의 바로 그
> 실패를 적어 놓고, 같은 날 오후에 그대로 반복했다.**

---

## 0. 오늘의 커밋

| 해시 | 무엇 |
|---|---|
| `36ac557` | docs: 끊긴 09-07 을 실측으로 닫고, 깨진 알림 링크 55건을 M-4 로 세운다 |
| `185b76d` | docs: 09-10 커밋 표를 만든다 (마감) |
| `9d4fbe5` | docs: 09-10 §2-1 을 정정한다 — 브라우저 검증은 「못 가른다」가 아니라 「안 했다」였다 |
| `ca259cf` | docs: 09-10 커밋 표를 다시 만든다 — 두 번째 (마감) |
| `622ad1c` | docs: 브라우저 검증을 마치고 B-26 을 완전히 닫는다 — 숫자 여섯 자리가 전부 맞았다 |
| `22940e3` | docs: 09-10 커밋 표를 다시 만든다 — 세 번째 (마감) |
| `640ceca` | docs: 09-10 핸드오프를 마감 정리한다 — 낡은 참조·번호 땜질·자기모순 셋 |
| `17a1aa3` | docs: 09-10 커밋 표를 다시 만든다 — 네 번째 (마감) |
| `c63511d` | fix: 09-07 을 마감 정리한다 — §5 가 사흘 동안 거짓이었고, 검사는 인용 표에 속고 있었다 |
| `7c266d7` | docs: 09-10 커밋 표를 다시 만든다 — 다섯 번째 (마감) |
| `931540f` | docs(BACKLOG): M-4 의 두 갈래를 가른다 — 감사 로그가 자를 대 줬다 |
| `442f541` | fix(notification): 상품이 영구 삭제되면 그 상품을 가리키던 알림을 치운다 (M-4) — 불변식 20개 |
| `312ef3b` | docs: M-4 완료를 BACKLOG·09-10 §7 에 적고 마감값을 고친다 (배포 대기) |
| `5788f05` | docs: 09-10 커밋 표를 다시 만든다 — 여섯 번째 (마감) |
| `31baa92` | docs: 09-10 §7-7 배포를 닫는다 — DB 가 앞서 있던 어긋남이 해소됐다 |

⚠ **이 표는 이어 붙이지 않고 통째로 다시 만든다**(WA §4-4):
`git log --reverse --since="2026-09-10 00:00" --until="2026-09-10 23:59:59" --pretty='| \`%h\` | %s |'`

---

## 1. 아침 기준값 재계수 — **한 열만 움직였다** (WA §3-5)

09-07 마감값과 대조:

| 무엇 | 09-07 마감 | 오늘 | |
|---|---|---|---|
| `member` / `point_account` / `member_coupon` | 10 / 10 / 9 | **10 / 10 / 9** | ✅ |
| `admin_audit_log` | 87행 · 최장 detail 64자 | **87 · 64자** | ✅ |
| product alive / trash | 8 / 0 | **8 / 0** | ✅ |
| `product_variant` | 9 | **9** | ✅ |
| `orders` | 87 | **87** | ✅ (최신 주문도 `09-02 08:09` 그대로) |
| `point_history` | 73 | **73** | ✅ |
| `stock_history` | 98 | **98** | ✅ |
| **`notification`** | **263** | 🔴 **266** | **+3** |
| 김기현팀 | 220,854 / 174,378 · SILVER | **같다** | ✅ |

✅ **원장 불변식 19개 전부 성립**(`check-money-invariants.sh`, 종료코드 0).
✅ 고아 javadoc 0 · infra 드리프트 없음 · 배포 브랜치 뒤처짐 없음.

⚠ **매출 둘은 오늘도 안 셌다** — 관리자 화면을 안 열었다. 🔴 **셌다고 적지 않는다.**

---

## 2. 🔴 09-07 은 **배포 직후 끊겼다** — 배포는 됐고 기록만 없었다

```
282cdbe  09-07 14:28  fix(docs): Transform 의 javadoc 두 블록을 합친다   ← 마지막 커밋
         09-07 14:32  /opt/glassvue-backend/glassvue-backend.jar        ← 배포된 시각
         (그 뒤 09-08·09-09 커밋 0건)
```

**실측으로 확인한 것**(자세히는 `2026-09-07-handoff.md` §10-6 에 채워 넣었다):

| 무엇 | 결과 |
|---|---|
| 백엔드 jar | **09-07 14:32** · 롤백용 `.bak` 도 같은 시각 |
| `GET /api/policy/today` | ✅ **200** · `{"today":"2026-09-10"}` — 🔴 **KST 경로가 운영에서 돈다** |
| 프론트 | nginx 의 `index.html` 이 **`index.8db1776d.js`**(09-07 빌드 산출물)를 가리킨다 |
| 그 번들 | `api/policy/today` · 「기간 해제」 · 「종료일」 **전부 들어 있다** |

⚠ **«배포 안 했겠지» 로 시작했으면 다시 배포했을 것이다.** 그러면 같은 jar 을 한 번 더 올리고
`.bak` 이 **새 jar 로 덮여 롤백 지점이 사라진다.** 🔴 **끊긴 세션을 이을 때 첫 동작은 재실행이
아니라 실측이다.**

### 2-1. 🔴 브라우저 검증은 **안 했다 — 로그로 확정됐다**

> ⚠ **이 절은 오늘 두 번 썼다.** 처음엔 *"«안 했다» 도 «했다» 도 실측으로 못 가른다"* 로 닫았다 —
> 기간 필터가 **읽기 전용**이라 DB 에 흔적이 안 남고, 서버가 오늘 09:42 에 재부팅돼
> 저널이 날아갔기 때문이다(TROUBLESHOOTING §2-4).
> 🔴 **그런데 그 결론이 틀렸다** — 사용자가 *"브라우저 테스트 아님?"* 이라고 되물어 다시 봤더니
> **nginx 접근 로그는 저널이 아니라 디스크에 있다.**

`/var/log/nginx/glassvue-access.log` — **07/15 ~ 09/10 이 끊김 없이 남아 있다**(4.6MB):

| 날 | 요청 수 |
|---|---|
| 09-01 | 875건 |
| 09-02 | 384건 |
| 🔴 **09-03** | **0건** |
| 🔴 **09-07** | **0건** |
| 09-08 · 09-09 | 0건 |
| 09-10 | **5건 — 전부 오늘 확인용 `curl`** |

**기간 선택 관련 요청은 전부 0건이다:**

```
GET /api/policy/today          0건
admin/orders?…from=            0건
admin/audit?…from=             0건
```

마지막 **브라우저** 요청은 **09-02 17:12**, `10.0.2.2`(VirtualBox NAT 호스트)다.

⚠ **09-03 이 0건인 것은 정상이다** — 그날은 주석·문서·스크립트뿐이라 볼 화면이 없었다.
🔴 **09-07 이 0건인 것이 문제다** — 화면 둘이 바뀌었는데 아무도 안 봤다.

#### 🔴 여기서 배운 것 — «로그가 없다» 를 **너무 넓게 읽었다**

TROUBLESHOOTING §2-4 는 *"이 VM 의 **저널**이 휘발성이다"* 라고 적어 뒀다.
⚠ **나는 그것을 «로그가 없다» 로 읽고 판정을 포기했다.** 실제로는:

| 로그 | 어디 | 재부팅에 살아남나 |
|---|---|---|
| `journalctl`(앱·서비스 로그) | 메모리 | ❌ 날아간다 |
| **nginx 접근 로그** | **디스크(`/var/log/nginx/`)** | ✅ **남는다** |

🔴 **«화면을 열었나» 는 앱 로그가 아니라 접근 로그가 답하는 질문이다.**
→ **TROUBLESHOOTING 에 옮길 후보**(이월 **6번**).

✅ **그리고 이 로그가 곧바로 두 번째 값을 했다** — 검증을 마친 뒤 **같은 명령으로 «했다» 를
확인**했다(§6). 🔴 **판정 수단이 하나 생기면 «안 했다» 와 «했다» 를 같은 자로 잰다.**

---

## 3. 🔴 09-07 마감값이 틀려 있었다 — **같은 날 그 실패를 적어 놓고 반복했다**

`notification` **263** 은 **아침 값**이었다. 그 뒤 `2026-09-07-handoff.md` §10-5 의 **전수(14:20~14:29)** 가 돌면서
**STOCK 알림 3건이 커밋**됐고(`09-07 14:23`), 아무도 다시 안 셌다. → **09-10 에 266 으로 고쳤다.**

⚠ **09-07 §1-1 이 바로 이 실패를 적은 절이다** — *"09-02 문서의 마감값이 자기 안에서 어긋나 있다 …
같은 문서가 위에서 세 번 정정한 값을 아래에서 되돌려 놓았다"*. **그것을 아침에 적고, 그날 오후에
같은 모양으로 틀렸다.**

🔴 **왜 안 걸렸나**: 그날 만든 `check-handoff.sh` ⑥ 은 **문서 안의 두 블록**을 대조한다 —
**문서와 DB 를 대조하지는 않는다.** ⚠ **막을 수 있는 자리이긴 하다**(마감값 블록을 파싱해 실제
계수와 맞춰 보면 된다). **다만 오늘 그것을 만들지는 않았다** — 아래 이월에 후보로 적어 둔다.

⚠ **«전수를 돌리면 값이 움직인다» 는 것 자체가 §4 의 실마리였다.** 롤백하는 통합 테스트가
**알림만 커밋하고 있다**는 뜻이라서다.

---

## 4. 🔴 그 3건을 쫓다가 — **알림 링크가 가리키는 상품이 없다 · 55건**

### 4-1. 실마리

09-07 에 늘어난 3건은 **STOCK 알림**이고 **관리자 셋에게 하나씩** 갔다(재고 부족 브로드캐스트).
그런데 **가리키는 상품이 DB 에 없다** — 소프트 삭제가 아니라 **행 자체가 없다.**
→ 테스트가 상품을 만들고 재고를 떨어뜨려 알림이 나갔는데, **트랜잭션이 롤백되며 상품만 사라지고
알림은 남았다.** ⚠ **알림 저장이 테스트 트랜잭션 밖에서 일어난다는 뜻**이다.

### 4-2. 그래서 전수로 셌다

| 링크 종류 | 전체 | 🔴 가리키는 대상이 **없는** 것 |
|---|---|---|
| `/products/{id}` | 103 | 🔴 **55** |
| `/orders/{id}` | 161 | ✅ **0** |

**날짜·종류별 분포:**

| 날짜 | 종류 | 건수 |
|---|---|---|
| 07-31 | INQUIRY | 3 |
| 08-06 | INQUIRY | 1 |
| 08-13 · 08-14 · 08-20 | STOCK | 각 3 |
| 🔴 **09-02** | STOCK | **39** |
| 09-07 | STOCK | 3 |

⚠ **STOCK 은 전부 3 의 배수다** — 수신자가 관리자 셋이라 **브로드캐스트 한 번에 3행**이다.
즉 STOCK 51건은 **17번의 유출**이다. 🔴 **09-02 에 39건(13번)이 몰린 것은 그날 전수를 여러 번
돌렸기 때문으로 보인다** — ⚠ **«보인다» 다. 그날 전수 횟수를 세지는 않았다.**

### 4-3. 🔴 M 축 ⑲ 의 사각지대

M-1 이 ⑲(*"알림 링크에 «null» 이 안 박혔다"*)를 만들며 이렇게 적었다:

> ⚠ ⑲ 는 다른 여덟과 성질이 다르다 — 나머지는 «두 값이 서로 맞는가» 인데 이건 **«한 값이 스스로
> 말이 되는가»** 다. TROUBLESHOOTING 「알림은 오는데 누르면 깨진 페이지」가 그 증상이고,
> 🔴 그 사고는 서버 로그에 아무것도 안 남긴다 — **이 각도 말고는 잡을 방법이 없다.**

🔴 **그 각도가 맞았는데 잣대가 좁았다.** ⑲ 는 **문자열 «null»** 만 본다 — 링크가 문법적으로
멀쩡하고 **UUID 도 제대로 박혀 있는데 그 UUID 가 아무것도 안 가리키는** 경우는 통과한다.
**증상은 같다: 누르면 깨진 페이지.** ⚠ **불변식이 0 을 내는데 실제로는 55건이 깨져 있었다.**

### 4-4. ⚠ 원인을 **두 갈래로 갈라 두고, 오늘은 안 갈랐다**

1. **테스트 잔재** — 롤백이 알림을 안 따라간다. **3의 배수와 날짜 분포가 이쪽을 가리킨다.**
2. **정상 수명주기** — F-7 이 상품에 유예를 주고 그 뒤 **purge 하면 행이 사라진다.** 그때도
   과거 알림은 남으므로 **같은 모양이 된다.**

🔴 **둘을 안 갈랐다.** ⚠ **처방이 정반대라 가르기 전에는 못 고친다** — ①이면 **테스트를 고치고
쌓인 것을 지운다**, ②면 **지우면 안 되고**(과거에 실제로 있었던 알림이다) **화면이 «없어진
상품입니다» 를 말하게** 해야 한다. **오늘은 세기만 했다.**

---

## 5. 오늘 안 한 것 — **일부러**

- ⚠ **깨진 링크를 안 지웠다.** §4-4 를 안 갈랐기 때문이다. 🔴 **세어 놓고 지우면 근거가 사라진다.**
- ⚠ **불변식 ⑳ 을 안 만들었다.** 위와 같은 이유 — **무엇을 위반으로 셀지가 아직 안 정해졌다**
  (테스트 잔재까지 위반으로 세면 전수를 돌릴 때마다 빨개진다).
- ⚠ **테스트를 안 돌렸다.** 운영 코드를 한 줄도 안 고쳤다(문서뿐).
- ⚠ **배포 안 했다.** 배포할 것이 없다 — 09-07 것이 이미 나가 있다(§2).
- ✅ ~~브라우저 검증~~ — **처음엔 «오늘 안 할 것» 으로 뒀는데 §2-1 이 «안 했다» 를 확정하자
  바로 밟았다.** §6 참조. 🔴 **판정이 되니 미룰 이유가 없어졌다.**

---

## 6. ✅ **그날 안에 브라우저 검증까지 했다** — B-26 이 완전히 닫혔다

§2-1 이 «안 했다» 를 확정하자 **할 일이 분명해졌다.** 🔴 **«못 가른다» 였다면 여기서 멈췄을 것이다** —
그 차이가 오늘의 실익이다.

### 6-1. 착수 전에 **대조표를 먼저 뽑았다**

⚠ **«기간을 걸어 보고 잘 되나 본다» 로는 판정이 안 된다** — 목록이 줄어드는 것만 보면
**필터가 과하게 걸러도 «된다» 로 읽힌다.** 그래서 DB 에서 **나와야 할 건수**를 먼저 셌다:

| 화면 | 기간 | 나와야 할 건수 | 결과 |
|---|---|---|---|
| 주문 관리(전체 탭) | 지난 달 | **31** | ✅ |
| 주문 관리(전체 탭) | 이번 달 | **5** | ✅ |
| 주문 관리(전체 탭) | 해제 | **87** | ✅ |
| 감사 로그 | 지난 달 | **73** | ✅ |
| 감사 로그 | 이번 달 | **3** | ✅ |
| 감사 로그 | 해제 | **87** | ✅ |

**여섯 자리가 전부 맞았다.**

### 6-2. 🔴 **표본 조건을 함께 넘긴 것이 값을 했다**

⚠ 주문 관리는 **기본 탭이 「발송 대기(PAID)」** 이고 **9월 PAID 는 0건**이다.
🔴 **그대로 「이번 달」을 눌렀으면 빈 목록이 나와 «고장» 으로 보였을 것이다.**
착수 전 실측에서 그것을 보고 **«먼저 「전체」 탭을 누를 것»** 을 함께 넘겼다.

⚠ **09-02 §5-5 가 남긴 규약과 같은 자리다** — *"이 자리를 다시 밟을 사람에게 표본 조건을 반드시
함께 준다(첫 검증이 그것 없이 나갔다가 판정 불가로 끝났다)"*. **이번엔 처음부터 줬다.**

### 6-3. 접근 로그가 뒷받침한다

검증 전엔 셋 다 **0건**이었다(§2-1). 검증 후:

```
GET /api/policy/today     2건    ← 화면 둘, 각 1회
admin/orders?…from=       4건
admin/audit?…from=        5건    모두 200
오늘 브라우저 요청        56건
```

🔴 **`policy/today` 가 2건인 것이 그 자체로 증거다** — 프리셋이 **브라우저 시계가 아니라
서버 날짜로** 계산됐다는 뜻이다(안 불렸으면 버튼이 잠겨 있었을 것이다).

⚠ **그중 하나가 `from=2026-08-12&to=2026-09-10` 이다** — **어느 프리셋도 만들지 않는 구간**이라
**날짜를 직접 고르는 경로도 밟혔다.** 프리셋만 눌렀으면 안 나올 조합이다.

### 6-4. ⚠ DB 는 안 움직였다

`orders` 87 · `admin_audit_log` 87 · `notification` 266 · `point_history` 73 · `stock_history` 98 ·
`member` 10 · `member_coupon` 9 · `product_variant` 9 — **아침값과 한 자리도 안 다르다.**
✅ 불변식 19개 성립. 🔴 **기간 필터는 읽기 전용이라 그래야 맞는다** — 움직였으면 그게 결함이다.

---

## 7. ✅ M-4 를 닫았다 — 갈래 둘을 **다 막고**, 치우고, 불변식을 세웠다

§4 가 «55건이 깨져 있다» 로 열어 두고 *"먼저 할 일은 고치는 것이 아니라 **가르는 것**"* 이라 했다.
🔴 **가르는 자를 감사 로그에서 찾았다** — 관리자가 지운 상품은 `PRODUCT_DELETE` 가 남고,
테스트가 만들었다 지운 상품은 **감사를 한 줄도 안 남긴다.**

| 갈래 | 규모 | 처방 |
|---|---|---|
| **정상 삭제** | 상품 **2개** | 🔴 `ProductPurgedEvent` — purge 가 알림을 치운다. ⚠ **상품 purge 로 다른 도메인을 정리하는 첫 경로다** |
| **테스트 잔재** | 상품 **14개** | `ConcurrentDeductionTest` 의 `@AfterEach` 가 자기 알림도 치운다 |

⚠ **운영 경로 수정은 테스트에 안 닿는다** — 그 테스트는 `productRepository.deleteById` 로
**서비스를 건너뛴다.** 🔴 **그래서 둘을 따로 막아야 했다.**

### 7-1. 지우는 기준은 **«누가 썼나»** 였다

리뷰·문의는 **사람이 쓴 것**이라 상품이 없어도 읽을 값이 있고 화면이 이름 없음을 이미 견딘다.
알림은 **시스템이 보낸 것**이고 내용이 「가서 보라」뿐이라 **갈 곳이 없으면 남는 것이 없다.**
⚠ 회원 탈퇴가 알림을 지우는 것(F-1)과 같은 판단이다.

⚠ **「링크만 NULL 로 죽이기」도 됐다** — 화면에 `if (n.link)` 가드가 이미 있다(실측).
🔴 **안 골랐다**: `Notification.link` 가 `updatable = false` 로 **일부러 불변**이다 —
「보낸 것은 안 바꾼다」를 한 칸 때문에 깨지 않는다.

### 7-2. 🔴 **«정리를 넣었다» 와 «정리가 닿았다» 는 다르다**

V61 이 55건을 치운 **바로 그 실행에서 1건이 새로 생겼다.**
리스너가 `@Async` 라 `@AfterEach` 가 **먼저** 돌 수 있고, 알림은 **그 뒤에** 태어나 고아로 남는다.
→ 예산 2초 폴링으로 바꿨고(하나라도 지우면 즉시 끝), 그 1건은 **V62** 로 치웠다.

⚠ **V61 을 고치지 않고 V62 를 새로 만들었다** — V61 은 이미 적용됐고(체크섬),
🔴 **되돌아가서 고치지 않고 앞으로 나아간다.** 쿼리가 같은 것은 **멱등이라 안전**하고,
각각이 «그 시점의 잔재» 를 치운 기록이다.

### 7-3. ⚠ 마이그레이션을 **배포가 아니라 전수 테스트가** 적용했다

`@SpringBootTest` 가 앱 컨텍스트를 띄우면 **Flyway 가 실 DB 에 돈다.**
V61 은 **09-10 10:36**, V62 는 **10:55** 에 그렇게 적용됐다(`flyway_schema_history` success=1).
🔴 **알림 계수가 266 → 211 로 떨어진 것을 보고서야 알았다** — 처음엔 «테스트가 운영 데이터를
지웠나» 로 의심했다. ⚠ **의도한 결과이긴 하나, «언제 적용되는가» 를 알고 있어야 놀라지 않는다.**

### 7-4. 🔴 실측이 내 식을 **두 번** 고쳤다

| 내가 적었던 것 | 실측 |
|---|---|
| *"누르면 **깨진 페이지**"* | ❌ **막다른 길**이었다 — 404 를 화면이 잡아 *"상품을 찾을 수 없습니다"* 를 그린다. **크래시가 아니다** |
| *(리뷰·문의도 깨졌을 것)* | ❌ **리뷰·문의·위시리스트·재입고 구독 전부 0건.** 알림만이다 |

⚠ **앞엣것이 처방을 바꿨다** — 급한 결함이면 서둘러 고치는데, **막다른 길**이라 «치우는 일» 로
크기를 잡았다. 🔴 **K-1·M-2·O-2 에 이어 네 번째다** — 첫 느낌을 실측이 계속 깎는다.

### 7-5. ⚠ 두 번 걸려 넘어진 자리

- 🔴 **`@Modifying` 에 `flushAutomatically`·`clearAutomatically` 가 둘 다 필요했다.**
  없이 썼다가 **테스트 5개 중 4개가 «안 지워졌다» 로 빨개졌다** — 벌크 JPQL 이 영속성 컨텍스트를
  지나친다. ⚠ 감사 CHECK 제약에서 겪은 것과 같은 계열이다.
  **운영에서는 조용히 «지웠다고 믿는» 상태가 됐을 자리다.**
- 🔴 **불변식 개수를 늘리며 `SEEN -ne 19` 를 못 고쳤다** — *"20개 중 20개만 읽혔다 — 판정 불가"*.
  ⚠ **바로 위 주석이 «개수를 늘릴 때 이 숫자도 함께 고친다» 고 경고한 자리**를 그대로 밟았다.

### 7-6. 검증

| 무엇 | 값 |
|---|---|
| 전수 백엔드 | **1060** · `skipped 0` · `failures 0` (9분 3초) |
| 새 테스트 | **5** (`ProductPurgedNotificationTest` — 접두사·앵커·대조군·경계·다수 수신자) |
| 불변식 | **20개 전부 성립**, 종료코드 0 |

🔴 **전수를 돌린 «직후» 에 ⑳ 이 0 이다** — 이것이 재발 방지가 실제로 먹었다는 증거다.
⚠ 고치기 전에는 같은 실행이 3건을 남겼다.

### 7-7. 배포 — ✅ **했다 (2026-09-10 11:09)**

| 무엇 | 실측 |
|---|---|
| 배포된 jar | `/opt/glassvue-backend/glassvue-backend.jar` **11:09** (`.bak` **11:08** = 09-07 것) |
| 서비스 재기동 | **11:10:07 KST** (`systemctl show … ActiveEnterTimestamp`) |
| jar 안의 새 클래스 | `ProductPurgedEvent` · `ProductPurgedEventListener` · `ProductPurgedNotificationHandler` **셋 다 있다** |
| health | UP · `GET /api/policy/today` **200** |
| Flyway | V61·V62 **success=1** — 재기동에서 새로 할 일이 없었다(전수 실행이 이미 적용) |

→ 🔴 **DB 가 앞서 있던 어긋남이 닫혔다.** 이제 다음 purge 부터 알림이 실제로 치워진다.

⚠ **브라우저 검증은 안 했고, 이번엔 그게 맞다** — 화면이 하나도 안 바뀌었다(백엔드 이벤트뿐).
🔴 **볼 수 있는 것은 «다음 purge 가 일어날 때» 뿐**이고, purge 는 유예 7일 뒤 배치가 돈다.
→ **§이월 3-1 로 넘긴다** — 그때 알림이 함께 사라지는지 보면 끝난다.
⚠ **B-26 과 다르다**: 거긴 화면이 바뀌어 브라우저가 유일한 판정 수단이었는데,
여기는 **불변식 ⑳ 이 판정 수단**이다(전수 직후 0을 이미 확인했다).

## 이월 (다음 문서로 반드시 옮길 것)

> WORKING-AGREEMENTS §4-1. 다음 날 문서가 그대로 물려받아 시작한다. 처리했으면 지운다.
> ⚠ 🔴 **물려받을 때 «아직 참인가» 를 확인한다** — 오늘 그 대조가 **배포가 이미 됐다는 것**과
> **09-07 마감값이 틀렸다는 것**을 잡았다(§2 · §3).

### 다음 세션에서 시작할 것

1. 🔴 **아침에 기준값을 다시 센다.** **마감값 (2026-09-10)**: `member` **10** ·
   `point_account` **10** · `member_coupon` **9** · `admin_audit_log` **87행 · 최장 detail 64자** ·
   product alive / trash **8 / 0** · `product_variant` **9** · `orders` **87** ·
   `point_history` **73** · `stock_history` **98** · `notification` **211** ·
   김기현팀 **220,854 / 174,378 · SILVER** · 김기현팀2 **150 / 15,000 · BRONZE** ·
   김기현팀3~7 **전부 0 / 0 · BRONZE**.
   ⚠ **매출 둘은 안 셌다** — 관리자 화면을 안 열었다. 🔴 **셌다고 적지 않는다.**
   🔴 **`notification` 만 아침(266)과 다르다 — 266 → 211.** §7 이 55건(V61)+1건(V62)을 치웠고
   전수 실행이 Flyway 로 적용했다. ⚠ **나머지 아홉은 아침값 그대로다.**
   ✅ **배포(11:09)·재기동(11:10) 뒤 다시 셌고 열 값 전부 위와 같다** — 배포는 데이터를 안 움직인다.
   ✅ **불변식 20개 전부 성립**(종료코드 0).

2. ⚠ **아침에 `scripts/check-money-invariants.sh` 를 함께 돌린다** — 🔴 **이제 20개다**(§7).
   🔴 **종료코드 2 는 «성립» 이 아니라 «못 셌다» 다.**
   🔴 **다만 19개가 «깨끗하다» 를 뜻하지 않는다는 것을 오늘 봤다**(§4-3) — ⑲ 가 0 인데 55건이 깨져 있었다.

3. ✅ **M-4 가 닫혔다 — 오늘 했다**(§7 · BACKLOG §M-4). 55건을 갈래 둘로 갈라 **둘 다 막고**
   치우고(V61·V62) **불변식 ⑳** 을 세웠다. 🔴 **전수 직후에도 ⑳ 이 0 이다.**

3-1. ⚠ **M-4 의 운영 경로는 «다음 purge» 에서만 눈으로 볼 수 있다**(§7-7).
   배포는 끝났고 불변식 ⑳ 이 판정 수단이지만, 🔴 **«purge 가 실제로 알림을 치우는 것» 을 본 적은
   아직 없다**(테스트로만 봤다). ⚠ **유예 7일 뒤 배치가 돌 때** 확인한다 —
   지금 삭제 대기 상품은 **0개**라 당장은 볼 것이 없다.

4. ✅ **알림이 롤백을 안 따라가는 문제도 같이 닫혔다**(§7-2). ⚠ **다만 «비동기라 순서가 없다» 는
   그대로 살아 있다** — `@AfterEach` 가 `@Async` 리스너보다 먼저 돌 수 있다.
   🔴 **커밋하는 테스트를 새로 쓰면 같은 자리를 다시 밟는다** — 예산 폴링이 그 처방이다.

5. ✅ **B-26 이 완전히 닫혔다 — 브라우저 검증까지 끝났다**(§6 · `2026-09-07-handoff.md` §10-6).
   매출(08-13) · 주문·감사(09-07) · 배포(09-07 14:32) · **검증(09-10)**.
   ⚠ **이 자리를 다시 밟을 사람에게 넘길 것 하나**: 주문 관리는 **기본 탭이 「발송 대기(PAID)」** 이고
   **9월 PAID 는 0건**이다 — 🔴 **「전체」 탭을 먼저 누르지 않으면 빈 목록이 나와 «고장» 으로 보인다.**
   (표본 조건을 함께 넘기는 것 — 09-02 §5-5 가 남긴 규약과 같은 자리다.)

6. ⚠ **«저널이 휘발성» 을 «로그가 없다» 로 읽지 말 것**(§2-1). 🔴 **nginx 접근 로그는
   디스크에 있고 재부팅에 살아남는다** — «누가 화면을 열었나» 는 그쪽이 답한다.
   → **TROUBLESHOOTING §2-4 옆에 한 줄로 옮길 후보**다(오늘은 안 옮겼다 — 관측이 한 건이라
   「문서 주장을 규약으로 승격하기 전에 확인」에 걸린다. **다음에 한 번 더 쓰이면 옮긴다**).

7. ⚠ **`check-handoff.sh` 가 «문서 ↔ DB» 는 안 본다**(§3). 마감값 블록을 파싱해 **실제 계수와
   대조**하면 오늘 같은 어긋남을 막는다. 🔴 **후보로만 적어 둔다 — 오늘 안 만들었다.**
   ⚠ **만들 때 함께 정해야 할 것**: 「전수를 돌려 값이 움직인 날」과 「마감값을 안 고친 날」을
   **어떻게 가를 것인가**(4번과 얽힌다).

8. 🔴 **끊긴 세션을 이을 때 첫 동작은 «재실행» 이 아니라 «실측» 이다**(§2).
   오늘 «배포 안 했겠지» 로 시작했으면 같은 jar 을 다시 올려 **`.bak`(롤백 지점)을 덮었을 것**이다.

9. 🔴 **오늘 09:42 에 재부팅됐다**(`uptime -s`). ⚠ **저널(앱·서비스 로그)은 날아갔다** —
   09-07~09-09 의 `journalctl` 은 없다(TROUBLESHOOTING §2-4). 로그로 판정할 생각이면
   **그 자리에서 건수·시각·예외 이름을 옮겨 적을 것.**
   ⚠ **다만 «로그가 전부 없다» 는 아니다**(§2-1 · 위 6번) — **nginx 접근 로그는 디스크에 남아
   07/15 부터 이어져 있다.** 🔴 **둘을 갈라서 말할 것**: 「앱이 무엇을 했나」는 저널이,
   **「누가 화면을 열었나」는 접근 로그**가 답한다.

10. ⚠ **O 축이 남긴 «다음 축의 씨앗» 셋은 그대로 살아 있다**(`2026-09-07-handoff.md` §9-4):
   ①프론트 라우터 가드 `meta.requiresAuth` ②`@Transactional` 이 빠진 쓰기 서비스
   ③`NOT NULL` 없이 만든 컬럼. 🔴 **§4 가 «④알림이 트랜잭션을 안 따라간다» 를 하나 더 얹었다** —
   ⚠ 이건 씨앗이 아니라 **이미 재현된 것**이다.

11. ⚠ **N 절(AI 연동)은 사용자가 보류했다.** 착수 전 물어야 할 것 넷이 그대로 남아 있다
    (①실호출인가 로컬 더미인가 — **답을 못 받았다** ②`global/ai` 위치 ③실패 시 화면 ④모델·키).

### 조건부 잔여

**`2026-09-07-handoff.md` §조건부 잔여를 그대로 물려받는다.** ⚠ 개수를 여기 적지 않는다 —
목록이 곧 개수다. 🔴 **오늘 운영 코드는 한 줄도 안 고쳤다**(문서뿐) — **더할 것도 지울 것도 없다.**

#### 오늘 더해진 것

- 🔴 **알림 링크의 «건강» 을 세는 SQL 이 생겼다**(§4-2). 링크 끝의 UUID 를 뜯어 대상 테이블과
  조인한다 — ⚠ **`hextoraw(replace(…,'-',''))`** 로 문자열 UUID 를 RAW(16) 로 되돌려야 붙는다
  (PK 가 RAW(16) 라서다).

```sql
select count(*) total, count(case when p.id is null then 1 end) dangling
  from notification n
  left join product p
    on p.id = hextoraw(replace(regexp_substr(n.link,'[0-9a-f-]{36}$'),'-',''))
 where n.link like '/products/%';
```

- ⚠ **`notification` 은 «전수를 돌리면 움직이는» 열이다** — 다른 아홉 열과 성질이 다르다.
  🔴 **기준값 표에서 이 열만 어긋났을 때 «누가 뭘 했나» 로 가기 전에 «전수를 돌렸나» 를 먼저 묻는다.**
- ⚠ **`sqlplus` 는 NLS 때문에 한글이 `????` 로 나온다** — 값은 정확하니 **닉네임은 순서로 읽는다.**
  컬럼 이름 함정 둘: 등급은 `point_account.grade`, 누적구매는 `total_purchase`(`total_earned` 아니다).
