# 2026-09-11 핸드오프 — R-7: 공개 목록의 작성자 이름을 가린다 · Q-6: 잘려 사라지는 쿠폰 몫을 알린다

> 사용자: *"오늘 할 일은 뭐지?"* → 후보를 뽑아 올렸고 *"1번 먼저 하거라"* (R-7).
> ⚠ **처음엔 N-1 을 1순위로 뽑았다가 뒤집었다**(WA §1-2) — 09-10 에 *"N절 연동은 지금 할 건 아니고"*
> 라고 했으니 착수 여부는 사용자 몫이다. 그래서 R 축의 남은 것(R-7)을 앞으로 올렸다.

---

## 0. 오늘의 커밋

| 해시 | 무엇 |
|---|---|
| `ea047ac` | feat(R-7): 공개 문의·리뷰 목록의 작성자 이름을 가린다 |
| `7102c41` | docs: R-7 을 BACKLOG·09-11 핸드오프에 적는다 (배포 대기) + 빠졌던 이월 다섯을 처분한다 |
| `a3f3f20` | docs: R-7 배포·브라우저 검증을 닫고 09-11 마감값을 적는다 |
| `3f5f5a7` | docs: 09-11 커밋 표를 만든다 (마감) |
| `5289e3e` | feat(Q-6): 주문서가 쿠폰이 잘려 사라지는 몫을 알린다 |
| `60fdd1a` | docs: Q-6 을 BACKLOG·DESIGN·09-11 핸드오프에 적는다 (배포 대기) — 항목이 반만 참이었다 |
| `d9f3a2f` | docs: Q-6 배포·브라우저 검증을 닫고, 치울 수 없던 검증 쿠폰을 Q-7 로 적는다 |
| `a123fc8` | docs: 09-11 커밋 표를 다시 만든다 — 두 번째 (마감) |
| `be0052f` | docs: Q-6 검증 쿠폰을 남기기로 한 결정을 09-11 에 적는다 |
| `6197889` | docs: 09-11 커밋 표를 다시 만든다 — 세 번째 (마감) |
| `d43955d` | docs(WA): §2-12 — 손목록은 «빠뜨리면 어느 쪽으로 넘어지나» 를 묻는다 + O 축 2차(O-5~O-8) |

⚠ **이 표는 이어 붙이지 않고 통째로 다시 만든다**(WA §4-4):
`git log --reverse --since="2026-09-11 00:00" --until="2026-09-11 23:59:59" --pretty='| \`%h\` | %s |'`

---

## 1. 아침 기준값 재계수 — **전부 그대로** (WA §3-5)

09-10 마감값과 대조:

| 무엇 | 09-10 마감 | 오늘 | |
|---|---|---|---|
| `member` / `point_account` / `member_coupon` | 10 / 10 / 9 | **10 / 10 / 9** | ✅ |
| `admin_audit_log` | 89행 · 최장 detail 64자 | **89 · 64자** | ✅ |
| product alive / trash | 8 / 0 | **8 / 0** | ✅ |
| `product_variant` | 9 | **9** | ✅ |
| `orders` | 87 | **87** | ✅ |
| `point_history` | 73 | **73** | ✅ |
| `stock_history` | 98 | **98** | ✅ |
| `notification` | 165 | **165** | ✅ |
| 김기현팀 / 김기현팀2 | 220,854 / 174,378 · SILVER / 150 / 15,000 · BRONZE | **같다** | ✅ |
| 김기현팀3~7 · 김철수 · 일반회원팀 · 홍길동 | 0 / 0 · BRONZE | **같다** | ✅ |

✅ **원장 불변식 20개 전부 성립**(`check-money-invariants.sh`, 종료코드 0).
⚠ **매출 둘은 오늘도 안 셌다** — 09-10 에도 안 세어 **대조할 값이 없다.**

🔴 **오늘 08:57:20 에 재부팅됐다**(`uptime -s`). `journalctl --list-boots` 는 이 부팅 하나뿐 —
**09-10 의 저널은 없다**(TROUBLESHOOTING §2-4). 스케줄러 회차도 08:57 기동부터 다시 잡혔다(WA §3-4-1).
⚠ 삭제 대기 상품이 **0개**라 purge 로 볼 것은 여전히 없다(이월 5).

---

## 2. ✅ R-7 — 공개 문의·리뷰 목록의 작성자 이름을 가린다

### 2-1. 착수 전 실측 (WA §1-3-1)

| 백로그의 주장 | 판정 |
|---|---|
| 공개 목록에 `author` 가 그대로 나간다 | **참** — `InquiryResponse.from`·`ReviewResponse.from` 둘 다 `getAuthor()` 원문 |
| 마스킹 자리는 `…Response.from` 한 곳이어야 한다 | **참** — 두 응답의 생성 지점은 각각 **상품별 목록 하나뿐**(`InquiryQueryService:49` · `ReviewQueryService:62`) |
| (R-5) 캐시된 응답엔 열람자별 칸을 못 단다 | **해당 없음** — 캐시는 `notices:list`·`products:list` 둘뿐, **문의·리뷰 목록은 캐시가 없다** |

⚠ 공개 응답 DTO 중 `String author` 를 가진 것은 이 둘과 `NoticeResponse` 뿐이다.
**공지는 대상에서 뺐다** — 관리자가 쓰고, 공지 목록에 **작성자 검색**이 걸려 있다.

### 2-2. 결정 (사용자)

| 질문 | 결정 | 버린 안 |
|---|---|---|
| 어디까지 가리나 | **공개·비밀 모두** | 비밀글만(R-2 구멍만 막기) |
| 형식 | **앞 1글자 + 고정 `**`** (`김기현팀3 → 김**`) | 앞 2글자 + `**`(백로그가 적은 관행) · 가운데만(`홍*동`, 길이가 샌다) |
| 누구에게 원문인가 | **본인·관리자** | (선택지로 올리지 않은 전제) |

⚠ **1글자라 이 DB 에서는 모두가 `김**` 가 된다** — 작성자 넷(김기현팀·김기현팀2·김기현팀3·김철수)이
전부 «김» 으로 시작한다. 목록에서 **누가 누구인지 구별이 안 되는 것**이 이 형식의 대가다.

### 2-3. 무엇을 고쳤나 (`ea047ac`)

- `global/common/AuthorNames` — `forViewer(author, mine, viewer)` · `mask(name)`.
  별 개수 **고정**(길이가 안 샌다) · 첫 글자는 **코드 포인트**로 자른다(이모지 반쪽 방지).
- `InquiryResponse.from` · `ReviewResponse.from` 이 `author` 를 이걸로 거친다.
  «내 글인가» 는 응답이 이미 계산한 `mine` 을 넘긴다 — 두 번 계산하지 않는다.
- **화면 변경 0** — `ProductInquiries.vue`·`ProductReviews.vue` 는 `author` 를 그리기만 한다.

### 2-4. 검증

- 새 테스트 **8건**: `AuthorNamesTest` 4 · `ReviewResponseTest` 2(신규 파일) · `InquiryResponseTest` +2.
- 관련 8개 클래스 **64건 · skipped 0 · failures 0**.
- **되돌려서 확인**(커밋 뒤, 한 번에 하나씩, 실행 건수 16 으로 매번 같음):

| 변형 | 잡은 테스트 |
|---|---|
| M1 `ReviewResponse` 가 원문을 싣는다 | `ReviewResponseTest` 가리기 1건 |
| M2 `InquiryResponse` 가 원문을 싣는다 | `InquiryResponseTest` 가리기 1건 |
| M3 관리자 예외를 뺀다 | 세 클래스의 «본인·관리자 원문» 3건 |
| M4 `substring(0,1)` (코드 포인트 무시) | `AuthorNamesTest` 이모지 1건 |
| M5 앞 2글자를 남긴다 | 가리기·형식 4건 |

원복 후 표식 `/* M[1-5] */` **0건** · `git status` 깨끗.

- `/code-review low` 를 돌렸다(WA §5) — 🔴 **발견 0건인데 7초·도구 호출 1번**이라 diff 를 실제로
  읽었는지 **판정 불가**다(WA §3-6). diff 가 작아 직접 다시 읽었고 걸린 곳은 없었다.
  ⚠ «리뷰가 0건» 을 «리뷰에서 문제 없음» 으로 적지 않는다.
- **전수**(`.env` 소싱 · `cleanTest`): **1,088건 · skipped 0 · failures 0 · errors 0** (09:28:16 → 09:38:03).
- 전수 **뒤** 재계수: §1 의 열 전부 **그대로**(`notification` 165 포함) · 불변식 20 성립 · `zz%` 회원 0.
  ⚠ 전수는 공유 espdb 를 쓰므로 이 재계수가 «전수가 흔적을 안 남겼다» 의 증거다(이월 15).

### 2-5. 배포 — ✅ **배포·브라우저 검증 끝 (10:18)**

백엔드만 올린다(프론트 변경 0). **구·신 jar 가 다르게 답하는 자리**는 비로그인 목록의 `author` 다
(WA §5 «구 jar 와 신 jar 가 다르게 답하는 지점»). 배포 **전** 실측(09-11, 구 jar):

```
GET /api/products/019f64ba-8be4-77cb-b4b2-e41455253583/inquiries   (지바)
  → 9건 · {김기현팀: 4, 김기현팀2: 2, 김기현팀3: 2, 김철수: 1}
GET /api/products/019f64ba-8be4-77cb-b4b2-e41455253583/reviews
  → 2건 · {김기현팀: 1, 김철수: 1}
```

**신 jar 기대값**: 비로그인이면 문의 `{김**: 9}` · 리뷰 `{김**: 2}`. 건수는 그대로여야 한다.

**배포 실측**(사용자가 `deploy-backend.sh` 실행):

| 무엇 | 값 |
|---|---|
| jar / `.bak` | `10:17:41` / `10:17:39` |
| `ActiveEnterTimestamp` | **10:18:45** · `/actuator/health` **UP** |
| 비로그인 문의(지바) | **9건 · `{김**: 9}`** ✅ 기대값 그대로 |
| 비로그인 리뷰(지바) | **2건 · `{김**: 2}`** ✅ |

**브라우저 검증** — 사용자: *"오 너가 말한대로 잘보여"*. 넘긴 절차는 셋이었다:
①로그아웃 → 전부 `김**` ②김기현팀3(USER) → **본인 2건만** 원문, 나머지 `김**` ③김기현팀(SUPER_ADMIN) → 전부 원문.
🔴 **신 jar 위에서 밟았다는 것을 접근 로그로 확인했다**(`/var/log/nginx/glassvue-access.log`, TROUBLESHOOTING §2-5) —
기동 **10:18:45 뒤**에 지바 목록 조회가 `10:19:38`(로그인 없이) → `10:19:48`(로그인 200) → `10:20:01`(로그인 200)
순으로 남아 절차 셋과 맞는다. ⚠ 로그로는 **어느 계정인지는 모른다** — 그건 사용자의 말이 근거다.
⚠ **②의 표본 조건**: 본인 글과 남의 글이 **한 목록에 섞여 있어야** «본인에게만 원문» 이 판정된다.
김기현팀3 은 지바에 문의 **2건**(비밀 1 · 공개 1)이 있고 나머지 7건은 남의 것이라 조건이 성립한다
(WA §3-3-3). ⚠ ③은 관리자 예외를 보는 표본이다 — 김기현팀은 SUPER_ADMIN 이고 `Role.isAdmin()` 이 포함한다.

---

## 2-A. Q-6 — 주문서가 쿠폰이 잘려 **사라지는 몫**을 알린다

> 사용자: *"Q-6 고"*. ⚠ **절 번호를 §3 앞에 끼워 넣었다** — §3(이월 대조)은 이미 커밋된 절이라
> 번호를 밀면 오늘 앞선 커밋이 가리키는 절이 틀어진다.

### 2-A-1. 착수 전 실측 — **항목이 반만 참이었다** (WA §1-3-1)

| 백로그의 주장 | 판정 |
|---|---|
| 주문서에서 «5,000원 할인» 이라고만 보인다 | 🔴 **거짓** — 주문서는 쿠폰 줄에 `−discountPreview`(**이 주문에 실제로 깎이는 금액**)를 그린다. 1,000원 주문이면 **«−1,000원»** 이 뜬다. `6ac47f4`(07-23, 쿠폰 V17)부터 그랬다 |
| 이 주문에는 1,000원만 쓰인다는 말이 없다 | **반만 참** — 금액은 1,000원으로 뜨지만 **그게 잘린 값이고 남은 4,000원이 사라진다**는 말은 없다 |
| 서버는 이미 그 값을 안다 | **참, 그리고 이미 내려준다**(`discountPreview` = `Coupon.discountFor`) |

⚠ **경위**: Q-6 은 09-10 Q 축 감사가 «만드는 자리» 를 보다 **곁가지로** 적은 것인데, **주문서 화면은
그때 안 열었다.** «서버가 안다» 까지 확인하고 «화면이 모른다» 를 가정했다.
→ 🔴 **해야 할 일의 종류가 바뀌었다**: «최대 N원을 알린다»(이미 됨)가 아니라 **«잘렸다는 사실을 알린다»**.
쿠폰 이름이 액면가를 안 담으면(«테스트 쿠폰») 고객은 5,000원짜리를 1,000원에 태운다는 것을 **모른다.**

### 2-A-2. 무엇을 고쳤나 (`5289e3e`)

- `Coupon.forfeitFor(itemsTotal)` = **규칙상 할인액 − 실제 할인액**. `discountFor` 의 식을
  `ruleAmount` 로 꺼내 둘이 **같은 식**을 쓴다(두 사본을 안 만든다).
  ⚠ **정률 상한(`maxDiscountAmount`)은 «잘린 것» 이 아니다** — 쿠폰의 규칙이라 `ruleAmount` 안에 둔다.
  정률은 100% 이하라(Q-5) 사실상 늘 0 이고, **«정액 > 상품합계» 에서만 생긴다.**
- `MemberCouponResponse.forfeitPreview` — **못 쓰는 쿠폰은 0**(`discountPreview` 와 같은 조건).
- 주문서: 쿠폰 줄 아래 `text-xs text-warning` 한 줄 — *«이 주문에는 N원만 쓰여요 · 남은 M원은 사라져요»*.
  **`forfeitPreview > 0` 일 때만.** ⚠ **화면은 액면가로 다시 빼지 않는다** — 빼면 정률 상한까지 «사라진다» 로 말한다.
- 쿠폰함(`CouponWallet`)은 `itemsTotal=0` 으로 부르므로 새 칸이 0 이다 — 손댈 것 없다.
- DESIGN.md 화면별 방침에 «주문서 쿠폰 선택» 줄을 더했다.

### 2-A-3. 검증

- 새 테스트: `CouponTest` +2 · `CouponFlowIntegrationTest` +1(HTTP — Q-4 모양 그대로 **5,000 / 최소 1,000**:
  `itemsTotal=1000 → 1000 / 4000` · `5000 → 5000 / 0` · `999 → 못 씀 / 0`) · `CheckoutView.test.js` +2.
  🔴 **화면 픽스처는 `forfeitPreview` 를 `discountValue − discountPreview`(4,000)와 일부러 다르게(3,500) 줬다** —
  같으면 화면이 다시 계산해도 통과해서 «서버 값을 그린다» 를 반증 못 한다(WA §5 의 09-02 리뷰 교훈).
- 관련 백엔드 6개 클래스 **46건 · skipped 0** · 주문서 화면 **16건**.
- **되돌려서 확인**(커밋 뒤, 하나씩 · 실행 건수 백엔드 15 / 프론트 16 으로 매번 같음):

| 변형 | 잡은 테스트 |
|---|---|
| M1 `forfeitFor` 가 늘 0 | `CouponTest` 정액 · `CouponFlowIntegrationTest` Q-6 (2건) |
| M2 정률 상한 **전** 값으로 뺀다 | `CouponTest` «상한은 잘린 것이 아니다» 1건 |
| M3 못 쓰는 쿠폰에도 `forfeitFor` | `CouponFlowIntegrationTest` Q-6 1건(`999` 줄) |
| M4 화면이 `discountValue − discountPreview` 로 그린다 | 화면 «서버 값 그대로» 1건 |
| M5 화면이 `forfeitPreview` 조건 없이 띄운다 | 화면 대조군 1건 |

원복 후 표식 **0건** · `git status` 깨끗.
- 프론트 전수: **47 파일 · 477건 통과**.
- 백엔드 전수: **1,091건 · skipped 0 · failures 0**(10:37:57 → 10:47:43) — 오전 1,088 + 오늘 새 백엔드 시험 3.
  전수 뒤 재계수 **전부 그대로**(`notification` 165 · `member_coupon` 9) · 불변식 20 성립.
  ⚠ `notification` 165 는 **10:28:45 알림 정리 예측 0건**과도 맞는다 — 🔴 다만 «돌았다» 가 아니라
  **«안 움직였다»** 만 확인된 것이다(0건이면 로그가 없다, 이월 12).

### 2-A-4. 배포 — ✅ **배포·브라우저 검증 끝 (백엔드 10:55 · 프론트)**

응답에 칸을 **더하는** 변경이라 순서가 틀려도 안 깨진다(구 백엔드면 `forfeitPreview` 가 없어 줄이 안 뜬다).
그래도 WA §5 대로 **백엔드 먼저**. 구·신이 갈리는 자리: `GET /api/coupons/me?itemsTotal=…` 에 `forfeitPreview` 칸이 있는가.

| 무엇 | 배포 전 | 배포 후 |
|---|---|---|
| OpenAPI `MemberCouponResponse.forfeitPreview` | **없음** | ✅ **있음** |
| 백엔드 `ActiveEnterTimestamp` | 10:18:45(R-7) | **10:55:02** · jar `10:54:02` · health 200 |
| 프론트 번들 | `index.1d93be22.js` | ✅ **`index.6f6b1e27.js`** — 안내 문구가 번들 안에 있다 |

**브라우저 검증** — 사용자: *"오 너가 말한대로 브라우저 테스트 잘 됐어 아주 성공임"*.
🔴 **표본을 새로 만들어야 했다** — 김기현팀의 쿠폰(5,000 · 3,999)이 전부 **구매 가능한 가장 싼 상품(몽쉘 6,800)보다 작아**
지금 데이터로는 «잘린 쿠폰» 이 생길 수 없었다(몽쉘 10개 5,000 은 재고 0). 그래서 절차 첫 줄에 **표본 조건**을 적었다
— *«장바구니 합계 < 쿠폰 액면(10,000)»* (WA §3-3-3). 판정은 `−6,800원` 이 아니라(고치기 전에도 같다)
**주황 한 줄의 유무**였고, 대조군은 같은 화면의 `테스트 쿠폰`·`5000` 줄이다.
**접근 로그로 순서를 확인했다**(`/var/log/nginx/glassvue-access.log`, 모두 백엔드 기동 10:55 뒤):

```
13:07:02  POST /api/admin/coupons                       200   ← ZZ-Q6잘림검증 생성
13:07:21  POST /api/admin/coupons/01a08ea5-…/issue      200   ← 김기현팀에게 발급
13:07:30  POST /api/cart/items                          200   (13:07:33~34 PATCH 수량 3번)
13:07:37  GET  /api/coupons/me?itemsTotal=6800          200   ← 🔴 표본 조건 성립(몽쉘 1개)
13:07:57  DELETE /api/cart/items/01a031f4-…             200   ← 장바구니 원복
```

⚠ 오늘 주문 **0건** — 결제는 안 했다.

**🔴 검증이 남긴 것 (정상 경로로 못 지운다 — 쿠폰 삭제 API 가 없다)**:

| 무엇 | 식별 |
|---|---|
| 쿠폰 정의 1 | `ZZ-Q6잘림검증` · 정액 10,000 · 최소 0 · 사용 `09-11 00:00 ~ 09-12 23:59 KST` · 상시 |
| 발급분 1 (미사용) | 김기현팀 · 위 쿠폰 — **09-12 가 지나면 «사용 기간이 아닙니다» 로 바뀐다** |
| 감사 2행 | `COUPON_CREATE` · `COUPON_ISSUE` (`13:07:02` · `13:07:21`) |

→ 기준값 `member_coupon` **9 → 10** · `admin_audit_log` **89 → 91** 이 전부 이것이다(아래 마감값).
⚠ **지울지는 사용자 결정이다 — 묻지 않고 지우지 않는다**(WA §3-1). ⚠ 이 쿠폰은 **09-12 까지 김기현팀 주문서에
«사용 가능» 으로 뜬다** — 실주문에 쓰면 10,000원이 깎인다.
✅ **결정 (사용자)**: *"지우진 말고 그대로 놔두자"* — **남긴다.** 09-12 가 지나면 «사용 기간이 아닙니다» 로 바뀐다.

---

## 3. 이월 대조 — **09-10 이월이 두 칸 더 빠져 있었다** (09-10 이월 15 의 지시)

09-02 → 09-07 → 09-10 을 목록 대 목록으로 맞췄다. 09-07 이월 13 이 *"09-02 이월 11·12·13·15 는
그대로 물려받는다"* 고 적었고, 09-07 이월 12 도 살아 있었는데 **09-10 이월에는 다섯 다 없다.**

| 원래 자리 | 무엇 | 오늘 처분 |
|---|---|---|
| 09-02 이월 11 | §L 판정표는 다시 세어야 하는 종류 — 클램프에 새 호출자가 생기면 재판정 | **되살린다**(아래 이월 13) |
| 09-02 이월 12 | 테스트를 새로 만든 날은 머지 전 `/code-review` | **닫는다** — WA §5 에 규약으로 있다. 오늘 실제로 돌렸다(§2-4) |
| 09-02 이월 13 | 픽스처를 베낄 때 «칸 이름» 확인 | **되살린다**(아래 이월 14) |
| 09-02 이월 15 | `ConcurrentDeductionTest` 는 롤백을 안 쓴다 | **되살린다**(아래 이월 15) |
| 09-07 이월 12 | `.env` 미소싱 → 통합이 조용히 SKIP | **되살린다**(아래 이월 16) — 09-07 이 *«규약으로 안 막히는 종류»* 라 적었다 |

⚠ **되살린 넷이 «아직 참인지» 는 오늘 확인하지 않았다** — 원문을 옮겼을 뿐이다.

---

## 4. 이월 18 — «결정을 강제하는 목록» 을 규약으로 올렸다 (WA §2-12)

> 사용자: *"AI는 나중에 해보자 일단 이거 이월에 넣고 다음 작업 고"* → 09-10 이 «다음 세션에서 판단할 것» 이라
> 적은 이월 18 을 골랐다.

### 4-1. 올리기 전에 센 것 (WA §1-3-1 · 메모리 «문서 주장을 규약으로 올리기 전에 검증»)

| 물음 | 실측 |
|---|---|
| 표본이 정말 셋인가 | 🔴 **넷이었다** — 09-10 이월이 **O-4 `routeGuardCoverage.test.js`(09-10)** 를 빠뜨렸다. 넷 다 **코드에 있는 관행**이다(문서 한 줄이 아니다) |
| WA 에 이미 있나 | **없다** — `빠뜨리면·default 가 없·결정을 강제·열리는 쪽` 으로 WA grep **0건**. 이웃은 §1-2-1(대조 테스트의 모양) · §3-6(세는 도구) — **«기본값의 방향» 을 묻는 절은 없다** |
| BACKLOG 에 이미 있나 | **O 축(09-07)이 같은 질문**으로 감사했다. 🔴 다만 «배운 것» 에만 있고 **«앞으로 이렇게 한다» 로는 안 올라가 있었다** |
| 🔴 **남은 자리가 있나** | **있다** — 아래 4-2 |

### 4-2. 남은 손목록 — 8갈래 탐색 + 무게 큰 넷 직접 확인

탐색(에이전트)이 8갈래를 훑었고, **옮겨 적기 전에 무게 큰 것을 직접 열었다**:

| 자리 | 탐색 | 직접 확인 |
|---|---|---|
| `ProductListView.vue:24` `role === 'ADMIN'` | SUPER_ADMIN 이 빠진다 | ✅ **맞다** — 줄 161·305 의 관리 버튼이 이 값에 걸려 있다. 🔴 **지금 틀려 있다**(→ O-5) |
| 마케팅 발송 감사 | 없음 | ✅ `MarketingCommandService` 에 `Audit` **0회** · «안 남기는 것» 목록에도 없음(→ O-6) |
| 관리자가 남의 리뷰 수정·삭제 | 감사 없음 | ✅ `findManageable` 이 `isAdmin()` 통과 · 발행 없음(→ O-6) |
| 영구삭제 ↔ 찜·재입고 구독 | 핸들러·FK 없음 | ✅ V19·V28 에 FK 없음 · 받는 쪽은 알림 하나. **고아 행은 지금 0**(찜 3 · 구독 0 전부 산 상품)(→ O-7) |

⚠ 나머지(라벨 드리프트 · 캐시 60초 · 택배사 기본값 등)는 **탐색 결과를 그대로 옮기되 «탐색 결과» 로 표시**했다(BACKLOG §O 2차).
⚠ **enum `switch` 에 `default` 가 있는 곳은 0** 이었다 — 🔴 **위험은 `switch` 가 아니라 `==`/`!=`/삼항이 만드는 암묵적 default** 에 있다.
이게 WA §2-12 첫 처방(«컴파일러에게 맡긴다»)이 **안 닿는 자리**라, 둘째 처방(덮개)을 함께 적었다.

### 4-3. 무엇을 바꿨나

- **WA §2-12 신설** — 질문 하나(«여기 안 적으면 무엇이 되나?»)와 처방 둘(컴파일러 · 덮개), 표본 넷.
  ⚠ **§1-2-1·§3-6 과 겹치는 것은 다시 적지 않고 경계만 적었다**(WA §1-3-1 끝의 «이미 있나부터 센다»).
- **BACKLOG §O 2차** — O-5(프론트 역할 비교 · 🔴 현재 결함) · O-6(관리자 감사 누락 + 덮개 없음) ·
  O-7(영구삭제 ↔ 찜·구독, `MemberPurgeIntegrationTest` 의 손목록) · O-8(프론트 라벨 드리프트) + «가볍거나 안전한 것» 표.
- **코드 변경 0 · 배포 불필요.**

---

## 조건부 잔여

**`2026-09-10-handoff.md` §조건부 잔여를 그대로 물려받는다**(그쪽이 09-07 → 09-02 → 09-01 것을 물려받고 있다).
⚠ 개수를 여기 적지 않는다 — 목록이 곧 개수다.

---

## 이월 (다음 문서로 반드시 옮길 것)

> WORKING-AGREEMENTS §4-1. 다음 날 문서가 그대로 물려받아 시작한다. 처리했으면 지운다.
> 🔴 **09-10 이월 24개를 하나씩 처분했다** — 번호 대응은 각 항목 끝 `(09-10 #n)`.
> 09-10 에서 ✅ 로 닫혀 **남은 행동이 없는 것**(3 · 8 · 9 · 10 · 14 · 17)은 지웠다.

### 다음 세션에서 시작할 것

1. 🔴 **아침에 기준값을 다시 센다.** **마감값 (2026-09-11)**: `member` **10** ·
   `point_account` **10** · `member_coupon` **10** · `admin_audit_log` **91행 · 최장 detail 64자** ·
   product alive / trash **8 / 0** · `product_variant` **9** · `orders` **87** ·
   `point_history` **73** · `stock_history` **98** · `notification` **165** ·
   김기현팀 **220,854 / 174,378 · SILVER** · 김기현팀2 **150 / 15,000 · BRONZE** ·
   김기현팀3~7 · 김철수 · 일반회원팀 · 홍길동 **전부 0 / 0 · BRONZE**. (쿠폰 정의는 8 → 9 개)
   🔴 **움직인 것은 둘이고 둘 다 Q-6 브라우저 검증이다**(§2-A-4) — `member_coupon` +1(ZZ-Q6잘림검증 발급) ·
   감사 +2(`COUPON_CREATE`·`COUPON_ISSUE`). **나머지는 아침값 그대로다**(R-7·Q-6 코드는 응답만 바꾼다).
   ⚠ **매출 둘은 안 셌다.** ⚠ **배포 재기동이 두 번이라 알림 정리도 두 번 더 돌았다**(10:28:45 · 11:05:02 쯤) —
   둘 다 예측 **0건**(가장 오래된 읽은 알림이 `08-12 13:07:50 KST` 라 그때는 30일 선을 안 넘었다).
   🔴 **내일 아침 `notification` 은 165 가 아닐 수 있다 — 그건 정상이다.** 30일 선을 넘는 읽은 알림이
   `09-12 09:00` 까지 **3건** 생긴다(첫 건 `09-11 13:07:50`). 다음 회차는 재기동이 없으면 `09-12 11:05` 쯤이라
   **아침 계수 때는 아직 165** 여야 하고, 🔴 **밤사이 재부팅되면 기동 10분 뒤 돌아 162** 가 된다(`uptime -s` 로 가른다).
   ⚠ 이 배치는 0건이면 로그를 안 남긴다(이월 12) — **«돌았나» 는 로그가 아니라 이 숫자로 판정한다.**

2. ⚠ **아침에 `scripts/check-money-invariants.sh` 를 함께 돌린다** — 20개. 🔴 **종료코드 2 는 «못 셌다» 다.** (09-10 #2)

3. 🔴 **비동기를 «시간» 으로 기다리지 말 것** — 기다릴 것이 있으면 그것의 상태를 물을 수 있게 만든다
   (`eventExecutor` 를 빈으로 꺼낸 것이 처방, 09-10 §10-3). **커밋하는 테스트를 새로 쓰면 같은 자리를
   다시 밟는다** — `@AfterEach` 가 `@Async` 리스너보다 먼저 돌 수 있다. (09-10 #4 · #6)

4. ⚠ **M-4 의 운영 경로(purge 가 알림을 치우는 것)는 아직 눈으로 본 적이 없다** — 유예 7일 뒤 배치가
   돌 때 확인한다. 지금 삭제 대기 상품 **0개**. (09-10 #5)

5. ⚠ **주문 관리는 기본 탭이 「발송 대기(PAID)」** 이고 9월 PAID 는 0건이다 — 「전체」 탭을 먼저 누르지
   않으면 빈 목록이 «고장» 으로 보인다. (09-10 #7 — B-26 은 닫혔고 이 표본 조건만 남긴다)

6. 🔴 **끊긴 세션을 이을 때 첫 동작은 «재실행» 이 아니라 «실측» 이다.** (09-10 #11)

7. 🔴 **오늘 08:57 에 또 재부팅됐다** — 09-10 저널은 없다. 로그로 판정할 생각이면 그 자리에서 옮겨 적는다.
   ⚠ nginx 접근 로그(«열었나»)와 `admin_audit_log`(«무엇을 했나», 관리자만)는 디스크에 남는다. (09-10 #12)

8. ⚠ **O 축의 씨앗 중 `NOT NULL` 없는 컬럼은 «각도가 안 선다»** — 좁힐 기준을 찾으면 다시 집는다. (09-10 #13)

9. 🔴 **이월이 또 빠질 수 있다** — 다음 문서를 쓸 때 «지난 이월에서 지운 이유가 없는 것» 을
   **목록 대 목록**으로 맞춘다. 오늘 두 칸 더 찾았다(§3). (09-10 #15)

10. ⚠ **N 절**: 방침 확정 · **착수 안 함**. 착수하면 **N-1 먼저**. 전환 시점이 «키가 생기면» 이라 그 전엔
    실호출 검증 0 — 테스트는 **실호출 절대 금지**. (09-10 #16)
    🔴 **09-11 에 다시 물었고 다시 미뤘다** — 사용자: *"AI는 나중에 해보자 일단 이거 이월에 넣고"*.
    N-1(문의 답변 초안)과 N-6(분석용 데이터 생성)을 나란히 설명한 뒤의 답이다. ⚠ **N-6 은 AI 가 아니다**
    (합성 주문 생성기 · N-00) — 다음에 후보를 올릴 때 **둘을 한 묶음으로 올리지 말 것**(오늘 한 묶음으로 올렸다).
    ⚠ **N-6(분석용 데이터 생성)** 이 N-4·N-5·S 의 선행 — 생성기는 **«오늘 근처» 에도** 주문을 만들어야 한다.
    **S-4-1(상품 조회수 집계)** 도 S 의 선행. (09-10 #24)

11. ✅ **Q-6 은 배포·브라우저 검증까지 끝났다**(§2-A). ⚠ **항목이 반만 참이었다** — 금액은 이미
    떴고 빠진 것은 «잘렸다» 는 말이었다. **남은 것이 없다** — 다음 문서에서 지운다. (09-10 #18)
    ✅ **검증 잔재 `ZZ-Q6잘림검증`(쿠폰·발급분·감사 2행)은 남기기로 했다**(사용자 결정). ⚠ 발급분은
    **09-12 까지 김기현팀 주문서에 «사용 가능» 으로 뜬다** — 그날 실주문을 하면 이 쿠폰이 목록에 있다.

12. 🔴 **`initialDelay` 배치는 배포·재부팅마다 한 번 더 돈다** — 멱등이 아닌 배치를 만들면 다시 볼 것. (09-10 #19)
    🔴 **세 스케줄러가 «0건이면 로그를 안 남긴다»** — «안 돌았다» 와 구별이 안 된다. 바꾸려면
    **셋을 한꺼번에**, 소음과 판정 가능성의 맞바꿈이라 **값을 정해야 한다.** (09-10 #20)

13. ⚠ **§L 의 판정표는 «다시 세어야 하는» 종류다**(09-02 §6-2) — (B)와 (C)는 코드가 같고 입력 경로만
    다르다. 🔴 **클램프에 새 호출자가 생기면 그 줄을 다시 판정한다.** (09-02 #11 — 09-10 에서 빠졌던 것)

14. 🔴 **픽스처를 베낄 때 «칸 이름» 을 확인한다** — 장바구니는 `name`·`thumbUrl`, 주문은
    `productName`·`productImageUrl`. 틀려도 금액 단언만 있으면 초록이다. (09-02 #13 — 빠졌던 것)

15. ⚠ **`ConcurrentDeductionTest` 는 롤백을 안 쓴다** — 실제 행이 커밋되고 `@AfterEach` 로 지운다.
    정리가 실패하면 다른 테스트까지 깨진다. **전수 뒤 계수가 어긋나면 여기를 먼저 의심한다.** (09-02 #15 — 빠졌던 것)

16. ⚠ **`.env` 를 안 실은 채 돌리면 통합이 조용히 SKIP 된다** — 판정은 `tests` 가 아니라 **`skipped`** 로.
    TROUBLESHOOTING 이 «다섯 번째» 라 적은, 규약으로 안 막히는 종류다. (09-07 #12 — 빠졌던 것)

17. 🔴 **캐시된 응답에는 «사람마다 다른 칸» 을 못 단다**(R-5) — 다음에 그럴 일이 생기면 «키에 열람자를
    넣나 / 캐시 밖에서 채우나» 를 **먼저** 정한다. ✅ **R-7 은 해당 없었다**(문의·리뷰 목록은 캐시 없음). (09-10 #22)

18. ✅ **규약으로 올렸다 — WA §2-12**(§4). 남은 자리를 세어 보니 위험한 쪽으로 넘어지는데 덮개가 없는 곳이
    여럿이었고 하나는 **지금 틀려 있었다.** 다음 문서에서 지운다. (09-10 #23)

20. 🔴 **O-5 가 다음 후보 1순위다** — SUPER_ADMIN(김기현팀)은 **상품 목록의 관리 버튼·「상품 등록」을 못 본다**
    (`ProductListView.vue:24` 가 `isAdminRole` 대신 `role === 'ADMIN'`). 크기 소 · 프론트만.
    그다음 **O-6**(관리자 감사 누락 — 마케팅 발송은 **되돌릴 수 없는데** 감사 0) · **O-7**(영구삭제 ↔ 찜·구독).
    ⚠ **Q-7**(쿠폰 회수)도 후보다 — §2-A-4.

19. ✅ **R-7 은 배포·브라우저 검증까지 끝났다**(§2-5). **남은 것이 없다** — 다음 문서에서 지운다. (09-10 #21)
