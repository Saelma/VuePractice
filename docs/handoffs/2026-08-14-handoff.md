# 2026-08-14 핸드오프 — 상품 조작 감사 로그 (감사 대상 확대)

> WORKING-AGREEMENTS §4-0-1: **배포할 때마다 닫는다.** 하루 끝에 몰지 않는다.
> 보조 기준은 시간이 아니라 **커밋 5개**.

---

## 0. 오늘의 커밋

| 커밋 | 내용 |
|---|---|
| `e82cbab` | feat(audit): 상품 삭제·복구를 감사 원장에 남긴다 (V50) |
| `9b0d909` | fix(test): 전수를 깨뜨린 둘 — 한쪽은 테스트의 결함, 한쪽은 전제 붕괴 |
| `58b7f51` | fix(signup): 정의되지 않은 토큰 `border-ink-200` 을 `border-line` 으로 |
| `08cb779` | docs: 08-14 — BACKLOG 완료 · TROUBLESHOOTING 증상 1행 · ARCHITECTURE 정정 둘 |
| `5874f85` | fix(catalog): 멱등 가드가 막는 것은 「두 번 누르기」가 아니라 「낡은 목록」이다 |
| (이 문서) | docs: 08-14 검증 실측 — 두 탭 결과 · 조건부 잔여 상태 변화 |

⚠ **표는 문서를 닫은 뒤 다시 센다**(WA §4-4).

---

## 1. 시작 — 이월이 지목한 둘은 **오늘 하지 말라고 적혀 있었다**

08-13 이월의 순서는 ①이벤트 쿠폰 D-day 확인 ②B-26 나머지 ③F-8 이었는데,
**②는 «지금 착수하지 않는다», ③은 «급하지 않다»** 로 이월 자신이 못 박아 뒀다.
그래서 오늘의 기능 슬롯은 **열린 선택**이었다(BACKLOG B절: *"다음 후보는 정해 두지 않았다"*).

후보로 올린 넷 중 사용자가 **상품 조작 감사 로그**를 골랐다. 고른 근거는 이월에 이미 있었다 —
**세 문서를 건너온 항목**이고, *"회원 정지·역할변경·삭제 · 주문 취소 · 리뷰 숨김 · 문의 숨김은 다
남는데 **상품만 빠져 있다**"* 는 2026-08-12 실측이 그대로 살아 있었다.

## 2. 🔴 착수 전 결정 — 백로그가 «값만 늘리면 된다» 고 했지만 그렇지 않았다

BACKLOG 「감사 대상 확대」는 *"각 도메인이 `AdminActionEvent` 를 발행하고 `AuditAction` 값을
늘리면 된다"* 로 적혀 있었다. **배선을 읽으니 그 문장이 상품에는 안 맞았다.**

`admin_audit_log.target_id` 는 **NOT NULL 이고 뜻이 「대상 회원 id」**다. 지금까지 붙은 값들은
전부 그 기준을 통과했다 — `ORDER_CANCEL` 은 **주문자**, `REVIEW_HIDE`·`INQUIRY_HIDE` 는 **작성자**.
`AuditAction` 주석이 그 기준을 이렇게 못 박아 뒀다:

> *"갈리는 기준은 **도메인이 아니라 «대상에 회원이 있느냐»** 다."*

🔴 **상품이 그 기준을 처음으로 못 넘는 대상이다.** 상품에는 회원이 없고, 열은 비울 수도 없다.

**정한 것 (사용자와 확정, 2026-08-14)**

| 물음 | 답 | 안 고른 것과 이유 |
|---|---|---|
| `target_id` 에 무엇을 넣나 | **상품 id.** 열의 뜻을 «대상의 id — 무엇인지는 `action` 이 말한다» 로 넓힌다 | `target_type` 열 신설: 가장 정직하지만 **회원 아닌 대상이 아직 하나뿐**이다(미리 만들지 않는다). `target_id` nullable: «어느 상품인가» 를 id 로 못 걸게 된다 — 여기서의 null 은 V45 와 달리 «사실» 이 아니라 «안 넣은 것» 이다 |
| 어디까지 남기나 | **삭제 + 복구** | 삭제만: «지우고 바로 되살렸다» 를 못 읽어 원장으로서 반쪽이다. 등록·수정까지: 빈도가 높아 원장이 빠르게 커지고(F-2 와 같은 압력) «무엇이 바뀌었나» 를 `detail` 에 적는 방식을 따로 정해야 한다 |

⚠ **대가를 알고 골랐다.** 감사 화면의 검색은 `target_login` 부분일치뿐이라
(`AdminAuditLogRepository.search`) **상품 행은 「조작 종류」 필터로만 걸린다.**

## 3. 🔴 멱등이 원장을 두 줄로 만들 뻔했다

`Product.softDelete()` 는 **이미 대기 중이면 아무것도 안 한다**(그래야 유예가 안 되돌아간다).
`restore()` 도 복구 화면에서 두 번 눌러도 **200 이 정상**이다(«실패» 로 보이면 안 된다).

그래서 **«서비스 메서드가 호출됐다» 는 «조작이 일어났다» 와 다르다.** 호출 자리에서 그냥
이벤트를 발행하면 **복구 버튼을 두 번 누른 관리자가 원장에 두 줄을 남긴다.**

→ 두 엔티티 메서드가 **`boolean`(이번 호출로 실제로 바뀌었나)** 을 돌려주고, 서비스는 그때만
발행한다. ⚠ **멱등이 200 인 것은 그대로 두고 «기록만» 갈랐다** — 화면 동작을 바꾸면 F-7 의 판단을
뒤집는 것이 된다.

⚠ **여기서 «복구를 두 번 누르면» 이라고 적었는데 그건 틀린 그림이었다** — 검증에서 드러났다(§7-1).
닿는 경로는 **낡은 목록**이다. 가드의 값어치는 그대로지만 **막는 대상이 다르다.**

⚠ 이 모양은 새로 만든 것이 아니다. `MemberCoupon.restore()` 가 **이미 boolean** 이고,
그 이유도 같았다(2026-08-11 변형 주입 M9 — «아무것도 안 하게» 되는 것을 잡으려고).

## 4. 구현

| 자리 | 무엇 |
|---|---|
| `AuditAction` | `PRODUCT_DELETE` · `PRODUCT_RESTORE` |
| `V50` | CHECK 제약 확장 + `target_id` 열 주석 재작성(넷을 다 적었다: 회원·주문자·작성자·상품) |
| `Product` | `softDelete`/`restore` → **boolean** |
| `ProductCommandService` | 상태가 바뀐 경우에만 `AdminActionEvent` 발행. 상품명은 **`detail` 에 스냅샷** |
| `ProductControllerImpl` | `delete` 가 닉네임 대신 **`AuthUser`** 를 넘긴다(`actor_id` 가 NOT NULL) |
| `AdminProductControllerImpl` | `restore` 에 `@LoginUser` 추가(+ 인터페이스) |
| `audit.js` | 라벨·뱃지 2종 |
| `AuditLogAdminView` | 「대상 아이디」 빈칸 → `—` |

**왜 상품명을 `detail` 에 박나**: 상품은 **유예가 지나면 진짜로 사라진다.** id 만 남기면
«무엇을 지웠는지» 를 영영 못 읽는다 — **감사는 대상보다 오래 산다**(V45 가 `target_login` 스냅샷을
두는 이유와 같다).

**❌ 영구 삭제(purge)는 안 남긴다**: 부르는 것이 `ProductPurgeScheduler` 라 **행위자가 사람이 아니다.**
`actor_id` 는 NOT NULL 이고 배치를 가리킬 UUID 는 **지어낼 값**이다. 그 시점은 앱 로그에만 남는다.

**🔴 과거 삭제는 백필하지 않는다**: 출처가 없다 — `deleted_by_name` 은 «누가» 만 알고 «언제» 는
`deleted_at` 인데 **복구하면 둘 다 지워진다.** 원장은 오늘부터 시작한다(V44·V37 과 같은 판단).

## 5. 테스트 — 🔴 **여기는 통합이 필수였다**

새 enum 값이 실제로 들어가려면 Oracle 의 **CHECK 제약이 그 값을 알아야** 하는데,
**목(mock)은 제약을 모른다.** 제약을 안 넓혔으면 **통합에서만** ORA-02290 으로 터진다.
그래서 `ProductSoftDeleteIntegrationTest` 에 감사 절을 붙였다(단위 4 + 통합 3).

**변형 주입 (WA §3)** — 커밋 전에 했다.

| 변형 | 무엇을 망가뜨렸나 | 결과 |
|---|---|---|
| M1 | `restore()` 의 멱등 판정 제거(항상 true) | ✅ 「두 번 눌러도 한 줄」 FAILED |
| M2 | 상품명 스냅샷을 `null` 로 | ✅ **단위·통합 둘 다** FAILED |

## 6. ⚠ 전수에서 **둘이 깨졌다** — 내 변경이 아니었고, **한쪽은 진짜 결함**이었다

전수 758건 중 2 실패. 둘 다 원인이 **어제 남긴 검증 잔재**였는데, **성격이 정반대**였다.

### 6-1. 🔴 `AdminLowStockIntegrationTest` — 운영 데이터가 **테스트의 결함을 드러냈다**

*"count 가 DB 직접 집계와 일치한다 — 조건이 코드와 SQL 양쪽에서 같은지"* 라는 이름의 테스트다.
그 대조 SQL 에 **`deleted_at IS NULL` 이 없었다.** 서비스에는 있다(F-7, 08-12).

즉 **정의가 다른 채로 초록이었다.** 두 정의가 갈리는 행 — **「삭제 대기이면서 저재고」** — 이
운영에 없었기 때문이다. 어제 17:09 에 `ZZ-정가텟트`(재고 3)가 삭제 대기로 가면서 **그 행이 처음
생겼고**, 오늘 아침 전수에서 드러났다.

🔴 **이건 «테스트가 운영 데이터에 깨졌다» 가 아니다.** F-7 을 붙일 때 이 자리를 놓친 것이고,
운영 데이터는 **그걸 드러낸 것뿐**이다. → 대조 SQL 에 조건을 더했다(**고쳤다**).

### 6-2. ⚠ `EventCouponIntegrationTest` — 08-13 §9-4 가 예고한 **그 근본**이 터졌다

*"발급 창이 닫혀 있으면 그 쿠폰은 안 나간다"* 가 **단언에 닿지도 못하고** 실패했다 —
테스트가 만드는 «그저께~어제» 발급 창이 **어제 검증 쿠폰 `5000`(발급 창 08-13)과 겹쳐**
쿠폰 생성 자체가 거부됐다(겹침 금지는 앱이 지킨다).

→ 닫힌 창을 **한 해 전**으로 옮겼다. ⚠ **이건 «아마 없겠지» 가 아니다** — 이벤트 쿠폰 기능은
**2026-08-13 에 생겼으므로**(G-8, V49) 그 이전 발급 창을 가진 쿠폰은 **구조적으로 있을 수 없다.**

⚠ **근본은 그대로 남는다**(08-13 이월과 같은 문장): 공유 DB 라 «운영에 그 상태가 없다» 를 전제하는
단언은 언젠가 깨진다. **테스트 전용 스키마가 생기기 전까지는.**

### 6-3. 🔴 여기서 배운 것 — **두 실패는 대응이 다르다**

같은 «운영 데이터 때문에 빨개졌다» 로 보이지만:

- **테스트의 정의가 틀렸다** → **고친다**(6-1). 견디게 만들면 결함이 그대로 남는다.
- **테스트의 전제가 운영에 깨졌다** → **견디게 만든다**(6-2). 고칠 것이 없다.

⚠ 둘을 같은 손짓으로 처리하면 6-1 은 **영영 안 고쳐진다** — «또 운영 데이터네» 로 넘어간다.

## 7. 배포 · 브라우저 검증 — 🔴 **한 스텝은 밟을 수 없는 것이었다**

배포 후 사용자가 관리자 화면으로 밟았다: 상품 삭제 → 감사에 「상품 삭제」 줄(「대상 아이디」 `—`,
「내용」에 상품명) → 복구 → 「상품 복구」 한 줄 더 → 조작 종류 필터. **여기까지 확인됐다.**

### 7-1. 🔴 **멱등 스텝은 화면에 존재하지 않는다** — 검증 지시가 틀렸었다

*"복구를 한 번 더 눌러 감사가 안 느는지 본다"* 로 안내했는데 **그 버튼이 없다.**
`ProductTrashAdminView.onRestore` 가 복구 뒤 `await load()` 로 목록을 다시 받고, 복구된 상품은
`GET /deleted` 에서 빠진다 — **그 줄이 곧바로 사라진다.**

⚠ **그러면 이 가드는 무엇을 막는가**를 다시 물어야 했다. 코드 주석(그리고 이 문서 §3)은
*"복구 화면에서 두 번 누르면"* 이라고 적고 있었는데 **그 경로는 한 탭에서 닿지 않는다.**
실제로 닿는 것은 **낡은 목록**이다 — 두 관리자(또는 두 탭)가 같은 줄을 열어 두고 차례로 복구하면
뒤엣것이 **이미 복구된 상품**을 친다. **관리자가 셋이라 있을 수 있는 일이고, 가드는 여전히 값이 있다.**
→ `Product.restore()` 주석과 단위 테스트 이름을 **닿는 경로로** 고쳤다.

⚠ **V50 은 안 고쳤다** — 그 문장이 마이그레이션 주석에도 있지만 **이미 적용된 스크립트다.**
고치면 체크섬이 어긋나 **기동이 통째로 막힌다**(V45 주석의 사고). 주석 한 줄을 위해 그럴 값어치가 없고,
정정은 이 문서와 엔티티 주석에 남는다.

### 7-2. 🔴 **두 탭으로 밟았다 — 그리고 원장이 안 늘었다**

«만들 수 있는데 안 만들었다» 로 두려던 자리를 **그 자리에서 만들어 밟았다**(08-12 §9-1 의 그 판단).
nginx 접근 로그가 **두 번의 요청**을 그대로 갖고 있다:

| 시각 | 요청 | HTTP | 감사 원장 | 백엔드 로그 |
|---|---|---|---|---|
| 10:16:57 | `POST /api/admin/products/019ffa29…/restore` | **200** | ✅ `PRODUCT_RESTORE` 1줄 | ✅ 「삭제 대기 해제」 |
| **10:17:01** | **같은 상품에 또** | **200** | 🔴 **안 늘었다** | 🔴 **안 남았다** |

**4초 간격**이 두 탭의 흔적이다. 🔴 **둘 다 200 인데 원장은 한 줄뿐** — 멱등은 화면에 그대로 두고
(«실패» 로 안 보인다) **기록만 갈렸다.** 가드가 설계대로 일했다.

⚠ **로그 세 곳이 서로를 확인한다**: nginx 는 «요청이 두 번 왔다», 백엔드 로그는 «한 번만 바뀌었다»,
`admin_audit_log` 는 «한 줄만 남았다». **어느 하나만 봤으면 «두 번째 클릭이 안 갔다» 와 구분 못 한다** —
접근 로그가 그 갈래를 닫았다.

### 7-3. 🔴 덤 — 「원장은 오늘부터 시작한다」가 **실제 데이터로 나타났다**

```
08-14 10:08:22  PRODUCT_DELETE   김기현팀  ZZ-재고알림테스트
08-14 10:09:43  PRODUCT_RESTORE  김기현팀  ZZ-재고알림테스트
08-14 10:16:57  PRODUCT_RESTORE  김기현팀  ZZ-정가텟트      ← DELETE 짝이 없다
```

`ZZ-정가텟트` 는 **어제 17:07 에 삭제 대기로 간 상품**이라 그 삭제는 **V50 이전**이다.
복구만 있고 삭제가 없는 이 줄이 §4 의 판단(*"백필할 출처가 없다 — 지어내지 않는다"*)이
**운영에서 어떻게 보이는지**다. 지어냈으면 여기에 **거짓 삭제 기록**이 붙었을 것이다.

⚠ **읽는 사람이 «기록이 빠졌다» 로 오해할 수 있는 모양**이다 — 그래서 여기 적어 둔다.
같은 모양은 **08-14 이전에 지워진 상품 전부**에서 나온다.

## 8. 곁다리 — 어제 이월의 5분짜리

`SignupView.vue` 의 `border-ink-200` → `border-line`. 정의되지 않은 토큰이라 테두리가 토큰이 아니라
상속색으로 그려지고 있었다. ⚠ 어제 홈 스트립과 **같은 쪽으로 갔다**(`@theme` 에 `--color-ink-200` 을
더하지 않는다) — `border-line` 은 이미 76곳에서 쓰인다.

---

## 이월 (다음 문서로 반드시 옮길 것)

> WORKING-AGREEMENTS §4-1. 다음 날 문서가 그대로 물려받아 시작한다. 처리했으면 지운다.

**다음 세션은 여기서 시작한다** — 순서는 이렇다:

1. **다음 기능은 정해 두지 않았다.** BACKLOG 에서 고른다 — 오늘 후보로 올렸다가 안 고른 셋이
   그대로 남아 있다: **F-8**(관리자 재고 편집 알림, 소) · **첫 뷰 테스트**(뷰 34개에 0개) ·
   **G-4 부분 취소 / G-5 타임세일**(중).
   ⚠ **B-26 나머지와 F-8 은 여전히 «조건이 갖춰지면»** 이다(08-13 이월의 판단 그대로).
2. ⚠ **검증 상품 둘이 살아 있다**(아래 조건부 잔여) — 치울 생각이면 **다시 삭제 대기로** 보낸다.

- ⚠ **`target_type` 열은 아직 없다**(§2). 회원 아닌 감사 대상이 **하나 더 늘면 그때가 세울 때**다.
  지금은 상품 하나뿐이라 안 만들었고, 대가는 «감사 검색에 상품 행이 안 걸린다» 하나다.
- ⚠ **감사에 아직 안 붙은 관리자 조작**: 주문 발송 · 반품 승인/거절 · 상품 등록·수정 · 쿠폰.
- 🔴 **「화면이 유일한 방어」가 세 번째로 나오면 WA 로 올린다**(08-13 이월). 오늘은 안 나왔다.
- 🔴 **「필드를 지우기 전에 이름을 저장소 전체에서 센다」도 두 번째까지 와 있다**(08-13 이월).
- ⚠ 08-13 이월의 나머지 항목은 **그대로 물려받는다** — 정가 칸 원인 미상 · 겹침 금지가 앱에만 있다 ·
  상품 삭제 감사(✅ **오늘 닫았다**) · 뷰 테스트 0개 · 배치 확인 수단 없음 · 막힌 것 셋 등.

### 조건부 잔여

**08-13 §조건부 잔여를 그대로 물려받는다.** ⚠ 개수를 여기 적지 않는다 — 목록이 곧 개수다(WA §4-0).

🔴 **다만 두 항목의 상태가 오늘 바뀌었다 — 08-13 문서의 처분을 그대로 베끼면 틀린다.**

| 상품 | 08-13 문서의 처분 | **오늘(08-14) 실제** |
|---|---|---|
| `ZZ-재고알림테스트` (16번) | *"삭제 대기로 보내면 08-20 경 배치가 지운다"* | 🔴 **살아 있다** — 10:08 삭제 → 10:09 복구(검증) |
| `ZZ-정가텟트` (17번) | *"삭제 대기로 가면 08-20 경 배치가 지운다"* | 🔴 **살아 있다** — 10:16 복구(검증) |

⚠ **둘 다 배치가 안 지운다** — 삭제 대기가 아니기 때문이다. 치우려면 **다시 삭제 대기로 보내야 한다**
(그러면 유예 7일이 그때부터 다시 센다). ⚠ **되돌릴 수 없는 것은 없다.**

⚠ 이 둘의 **감사 줄 셋은 남는다 — 지우지 않는다**(알림과 같은 이유로 **기록**이다).
그리고 그것이 이 기능의 **첫 실측 데이터**이기도 하다(§7-2·§7-3).

⚠ **16번의 미결제 주문 둘**(`20260813-4185`·`20260813-4186`)은 **그대로다** — 오늘 안 건드렸다.
