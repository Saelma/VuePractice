# 2026-08-21 핸드오프 — 감사 확대 4차(V56)로 `target_type` 의 첫 시험대를 통과했다

> 어제(08-20) 이월이 지목한 대로 시작했다. **아침 재계수를 먼저 하고**(그게 이월 1번의 지시였다),
> 그 다음 «다음 기능 후보» 중 **감사 확대 4차**를 잡아 배포·브라우저 검증까지 닫았다.
>
> 🔴 **오늘의 요점은 «셋을 더 붙였다» 가 아니라 «어제 세운 틀이 확장에 견뎠다» 다.** 백로그가
> *"이 자리가 V53 구조의 첫 시험대다"* 로 적어 둔 자리였고, 결과는 **구조를 안 고치고 값만 늘었다.**
>
> 규약도 하나 조건이 성립했다 — **관측 창은 09~18시**(08-20 의 «새벽» 지시가 재발하며 넓어졌다).

---

## 0. 오늘의 커밋

| 해시 | 무엇 |
|---|---|
| `e58c61d` | feat(audit): 카테고리·공지·문의 답변을 원장에 남긴다 (BACKLOG 감사 확대 4차, V56) |
| `73d20f6` | docs: BACKLOG 「감사 대상 확대 4차」를 완료로 옮긴다 — 시험 결과를 함께 적는다 |

---

## 1. 시작 — 이월 1번은 «손댈 것이 없고 볼 것만 있다» 였다 (09:2x)

핸드오프 2일치(08-20·08-19) + BACKLOG + ARCHITECTURE 를 대조했다. 08-20 이월 1번이 이렇게 시작한다:

> *«08-21 에 배치가 셋을 지운다 … ⚠ **그날 아침에 먼저 다시 센다** — 그 사이에 상품을 만들거나
> 지우면 또 흔들린다.»*

**그래서 세션의 첫 조작이 재계수였다.** 09:2x 실측:

| 무엇 | 08-20 마감 | **08-21 09:2x 실측** |
|---|---|---|
| `product_variant` | 13 | **13** |
| `stock_history` | 49 | **49** |
| product alive / trash | 7 / 4 | **7 / 4** |

✅ **밤사이 안 움직였다.** 08-20 마감값이 그대로 유효했다는 뜻인데, 🔴 **그건 재계수를 해 봐야
알 수 있는 사실이다** — 08-20 이 «하루에 네 번 움직였다» 를 겪고 규칙으로 세운 이유가 그것이다.

### 1-1. 삭제 대기 넷의 내역 — CASCADE 로 줄어들 양이 여기서 결정된다

🔴 **여기서 아침에 한 번 틀렸다.** 처음엔 이력을 `stock_history → product_variant → product` 로
**조인해서** 셌고, 그 표는 이랬다 — `ZZ-옵션삭제검증` **0** · `ZZ-정가텟트` 1 · `ZZ-재고알림테스트` **1**.
**11:13 실측이 그 표를 깼다**(§5-1 에 경위가 있다). `productId` 로 다시 센 값이 맞다:

| 상품 | 유예 만료(KST) | variant | history (`product_id` 기준) | ⚠ 조인으로 세면 |
|---|---|---|---|---|
| `ZZ-옵션삭제검증` | 11:09 | 1 | **3** | 0 |
| `ZZ-정가텟트` | 15:55 | 1 | 1 | 1 |
| `ZZ-재고알림테스트` | 15:55 | 1 | **7** | 1 |
| `ZZ-123123` | 08-27 (오늘 아님) | 2 | **3** | 0 |

→ **예상: variant 13 → 12 → 10 · stock_history 49 → 46 → 38.**

### 1-2. 배치가 도는 시각을 **계산했다** — 지켜보지 않기 위해서

`ProductPurgeScheduler` 는 `fixedDelay` 1시간 + `initialDelay` 5분이다. 즉 **기동 시각이 회차를 정한다.**
아침 기동이 08:26:18 이라 08:31 → 09:31 → … 였고, 🔴 **배포하면 리셋된다.**

배포가 10:07:29 에 끝나서 **오늘의 회차는 10:12 · 11:12 · … · 16:12** 로 다시 잡혔다.

| 볼 것 | 예상 회차 | 근무 창(09~18) 안? |
|---|---|---|
| `ZZ-옵션삭제검증` — **1건 회차** | ≈11:12 | ✅ |
| `ZZ-정가텟트` + `ZZ-재고알림테스트` — 🔴 **2건 한 회차** | ≈16:12 | ✅ |

⚠ **둘 다 15:55 만료라 배포를 언제 하든 같은 회차에 들어간다** — 08-19·08-20 이 두 번 못 본
「여러 건을 한 회차에」 갈래는 **배포 시각에 안 깨진다.** (반대로 11:09 건은 어떤 배치 주기로도
나머지 둘과 같은 회차에 못 들어간다 — 사이에 회차가 반드시 하나 이상 있다.)

---

## 2. 🔴 규약 조건이 성립했다 — **관측 창은 09~18시 KST** (사용자 지시, 두 번째)

> **사용자 (2026-08-21)**: *"애초에 내가 09~18시 근무라 그 시간대에 못 봐요, 09~18시 사이에 하는 게 아닌 이상"*

⚠ **08-20 이 조건을 걸어 뒀다**: *«아직 WA 로 올리지 않았다 — 한 번의 지시다. **또 실리면 그때 올린다.**»*
**오늘 재발했고, 범위가 넓어졌다** — 08-20 은 **새벽**만 짚었는데 오늘 것은 **18시 이후·09시 이전 전부**다.

🔴 **08-14 이월의 규율을 그대로 적용해 판단했다** — «한 번의 관찰을 여러 번으로 세지 않는다».
여기는 **다른 날 · 다른 맥락의 두 번째 지시**이고 **범위까지 달라졌으므로** 같은 관찰의 되풀이가 아니다.

**딸려 나온 것 하나**: 🔴 **배포가 `@Scheduled` 타이머를 리셋한다**(§1-2). 낮에 관측할 배치가 있으면
**배포 시각이 다음 회차를 18시 밖으로 밀지 않는지 먼저 계산해야 한다.** 오늘은 계산해서 안 밀었다.

⚠ **WA 승격은 마감 때 확정한다**(사용자 확인 필요 — 08-19 가 §2-10 을 «사용자 확정» 으로 올린 것과 같은 절차).

---

## 3. 감사 확대 4차 — 넷 중 **셋**을 붙였다 (V56)

08-20 §10 이 관리자 쓰기 엔드포인트를 전수로 뽑아 남긴 넷이 착수 목록이었다.

| 조작 | 처분 | 결과 |
|---|---|---|
| 카테고리 등록·삭제 | ✅ | `CATEGORY_CREATE`·`CATEGORY_DELETE` · 🔴 `target_type` 에 **`CATEGORY` 신설** |
| 공지 등록·수정·삭제 | ✅ | `NOTICE_CREATE`·`NOTICE_UPDATE`·`NOTICE_DELETE` · 🔴 **`NOTICE` 신설** |
| 문의 답변 | ✅ | `INQUIRY_ANSWER` · 대상은 **질문자**라 `target_type` 을 안 건드린다 |
| 이미지 파생 생성 | ❌ **안 붙인다** | 백로그 판단 그대로 — 운영 배치성 · 되돌릴 수 있다 · «누가» 를 물을 일이 없다 |

🔴 **넷을 다 붙이는 것이 목표가 아니었다.** 목록을 훑어 세는 것과 전부 처리하는 것은 다른 일이다.

### 3-1. 🔴 시험 결과 — **접을 수 있었는데 접지 않았다**

카테고리·공지는 `MEMBER`·`PRODUCT`·`COUPON` 어디에도 안 들어가는 **첫 대상**이다(그전 넷은 전부
기존 값으로 접혔다 — 할인→상품, 주문·리뷰·문의→회원). **접는 길이 둘 다 있었다:**

- **카테고리 → `PRODUCT`** (상품을 담는 그릇이니까)
  ❌ 접으면 `target_id` 는 카테고리 id 인데 종류는 «상품» 이라, 「대상 종류=상품」으로 훑을 때
  **상품 이력 사이에 상품 아닌 행이 섞인다.** 그 필터의 존재 이유가 «회원 아닌 행을 갈라 보는 것»
  인데 **접는 순간 그 쓸모를 스스로 무너뜨린다.**
- **공지 → `MEMBER`** (공지에도 `author_id` 가 있다)
  ❌ 리뷰·문의가 «대상=작성자» 로 통한 이유는 **고객이 쓴 것을 관리자가 조치**해서다.
  공지는 **관리자가 쓰고 관리자가 고친다** → actor 와 target 이 거의 늘 같은 사람이라
  **원장 한 줄이 아무것도 안 가른다.**

→ 🔴 **«대상은 id 를 물어볼 값어치가 있는 쪽»(V43·V53) 규칙 자체는 그대로 통했다.** 이번엔 그 답이
기존 셋 **밖**에 있었을 뿐이고, **틀을 고칠 일은 없었다. 그것이 시험의 결과다.**

### 3-2. 공지는 **어제 기능 하나가 닫히면서 비로소 뜻을 갖게 된 값**이다

E-4(2026-08-20) 전에는 공지를 **고객도 쓸 수 있었다.** 원장은 «관리자가 무엇을 했나» 라,
그 상태로는 `NOTICE_CREATE` 를 남길 자리가 **없었다** — 「관리자 조작」이 아니었으니까.

⚠ 08-20 이월이 이 선행 관계를 *«공지의 선행 조건(E-4)은 오늘 풀렸다»* 로 명시해 뒀다.
🔴 **그 한 줄이 없었으면 오늘 «공지는 왜 지금까지 빠져 있었지» 를 다시 추론해야 했다.**

### 3-3. detail 규칙 — V53 을 그대로 따랐고, 한 군데만 더 접었다

`PRODUCT_UPDATE` 의 규칙(바뀐 것만 «전→후» · 긴 필드는 «바뀜» 만 · 변경 없어도 줄은 남긴다)을
공지 수정에 그대로 적용했다. 🔴 **본문(`content`)은 «바뀜» 만 적는다** — 공지 본문은 상품 설명보다
길어서 전/후를 다 실으면 `detail`(1000자)을 **확실히** 넘긴다. **잘린 원장은 틀린 원장이다.**

**삭제는 둘 다 «지우기 전에» 읽는다.** 🔴 **카테고리·공지에는 soft delete 가 없다**(F-7 은 상품에만
있다) — 행이 진짜로 사라지므로 **detail 이 유일하게 남는 흔적**이다.

**문의 답변의 detail 에 «첫 답변 / 답변 수정» 을 적는다.** 🔴 되돌릴 수 없는 것은 답변이 아니라
**알림**이고(B-15 는 첫 답변에만 보낸다), **그 한 마디가 «알림이 나갔나» 의 답**이다.
⚠ 그래서 등록·수정을 **두 값으로 안 갈랐다** — 갈라도 얻는 것이 detail 에 이미 있다.

### 3-4. ⚠ 백필은 안 했다 — 여기서는 **지어내는 것이 된다**

V53 은 `target_type` 을 **백필했다**(action 을 보면 결정되므로 «계산» 이다). 오늘은 다르다 —
없는 것이 **«누가·언제»** 이고 그건 아무 데도 없다:

- 카테고리: `created_at` 만 있고 **행위자 컬럼이 없다.** 삭제된 것은 행 자체가 없다.
- 공지: `author`·`author_id` 는 **등록자**만 안다 — 수정·삭제는 «누가 언제» 를 안 남겼다.
- 답변: `answered_at` 은 있어도 **답변자를 모른다.** ⚠ V51 이 주문 넷에서 만난 그 모양이다 —
  **«언제는 남고 누가는 안 남았다».**

→ 원장은 오늘부터 시작한다(V44·V50·V51 과 같은 판단).

### 3-5. 🔴 새 `target_type` 두 값은 **통합 테스트에서만 밟힌다**

`AuditActionTargetTypeTest` 는 **DB 를 안 띄운다** — enum 끼리만 대조하므로
`CHECK (target_type IN (...))` 를 **한 번도 실행하지 않는다.** V56 이 그 제약을 안 넓혔다면
그 테스트는 **초록인 채로** 운영에서 터졌을 것이다(Oracle enum CHECK 트랩).

→ `ContentAuditIntegrationTest` 를 새로 만들었다. ⚠ **단언을 조회로 해야 한다** —
그래야 Hibernate auto-flush 가 INSERT 를 DB 까지 밀어 제약이 실제로 실행된다.
«이벤트가 발행됐다» 만 세면 롤백까지 **INSERT 가 DB 에 안 닿는다**(백로그가 못 박아 둔 함정).

### 3-6. 곁가지 — 어제 놓친 Swagger 한 줄

`NoticeController.create` 의 요약이 **«공지 등록 (로그인 필요, 작성자=로그인 유저)»** 인 채였다.
E-4 로 관리자 전용이 된 것이 **어제**인데 문서 문자열이 안 따라왔다. 바로잡았다.
⚠ WA §1-2-1(«남기는 쪽 ↔ 보는 쪽» 도 짝이다)의 **아주 작은 판본**이다 — 권한을 좁혔으면
그 권한을 **설명하는 쪽**도 같이 열어야 했다.

---

## 4. 배포·브라우저 검증 — **9줄이 설계대로 남았다** (11:00~11:03)

배포 10:06(프론트)·10:07:29(백엔드). 배포본 확인: jar 의 `AuditTargetType` 에 `CATEGORY`·`NOTICE`
가 있고, 프론트 번들(`index.1bda63bb.js`)에 새 라벨이 있다. V56 은 통합 테스트가 돌 때
이미 espdb 에 적용됐고(WA §5), 제약을 실측으로 확인했다 —
`target_type IN ('MEMBER','PRODUCT','COUPON','CATEGORY','NOTICE')`.

⚠ **검증 순서를 «상품을 안 건드리는» 것으로 짰다** — §1 의 기준값이 흔들리면 배치 관측이 무의미해진다.

| 시각 | action | target_type | actor | target_login | detail |
|---|---|---|---|---|---|
| 11:00:20 | `CATEGORY_CREATE` | **CATEGORY** | 김기현팀 | — | `ZZ-검증분류` |
| 11:00:35 | `CATEGORY_DELETE` | **CATEGORY** | 김기현팀 | — | `ZZ-검증분류` |
| 11:01:00 | `NOTICE_CREATE` | **NOTICE** | 김기현팀 | — | `ZZ-검증공지` |
| 11:01:10 | `NOTICE_UPDATE` | **NOTICE** | 김기현팀 | — | `제목 ZZ-검증공지→ZZ-검증공지-고침 · 상단 고정` |
| 11:01:14 | `NOTICE_UPDATE` | **NOTICE** | 김기현팀 | — | `본문 바뀜` |
| 11:01:22 | `NOTICE_UPDATE` | **NOTICE** | 김기현팀 | — | `변경 없음` |
| 11:01:25 | `NOTICE_DELETE` | **NOTICE** | 김기현팀 | — | `ZZ-검증공지-고침 · 고정` |
| 11:02:05 | `INQUIRY_ANSWER` | MEMBER | 김기현팀 | 김기현팀3 | `이거 자바아닌데? · 답변 수정` |
| 11:02:34 | `INQUIRY_ANSWER` | MEMBER | **김기현팀2** | 김기현팀 | `zz-제목 · 첫 답변` |

**갈린 것들:**

- 🔴 **`CATEGORY`·`NOTICE` 가 운영 DB 의 CHECK 를 통과했다.** V56 의 요점이 여기서 끝났다.
- ✅ **`전자기기` 삭제 시도(소속 상품 있음 → 막힘)는 한 줄도 안 남았다.** 「일어난 일」만 원장에
  적는다는 규칙이 실측으로 확인됐다 — 감사와 조작이 같은 트랜잭션이라 예외가 나가면 함께 롤백된다.
- 🔴 **`NOTICE_UPDATE` 가 세 줄인 것이 계획보다 나은 표본이 됐다.** 제목+고정을 먼저 저장하고
  본문을 따로 바꿔서 **`제목…→… · 상단 고정` / `본문 바뀜` / `변경 없음`** 이 갈라져 나왔다 —
  한 번에 저장했으면 한 줄에 뭉쳐 «정말 바뀐 것만 적는가» 가 덜 선명했을 것이다.
- ⚠ **`NOTICE_DELETE` 의 제목이 수정 뒤 값**(`-고침 · 고정`)이다. 지우기 **직전** 상태를 읽는다는
  설계 그대로다.
- 🔴 **답변 둘이 갈렸다** — `· 답변 수정`(알림 없음) / `· 첫 답변`(알림 나감). 행위자도 다르다:
  ⚠ **본인 문의엔 답변이 막히므로**(`INQUIRY_SELF_ANSWER`) 김기현팀이 쓴 `zz-제목` 은
  **김기현팀2(ADMIN)로 로그인해야** 밟을 수 있었다.
- ⚠ 카테고리·공지 행은 `target_login` 이 **빈칸**이다 — 「대상 아이디」로는 못 찾고
  **「대상 종류」로만** 걸린다. 그 필터에 «카테고리»·«공지» 가 새로 뜨는 것을 화면에서 확인했다.

### 4-1. `COUPON_WELCOME_SET` 은 오늘도 브라우저로 안 밟았다

08-20 이월이 남긴 항목이 그대로다 — 통합 테스트로만 덮여 있다.

---

## 5. 배치 관측 ① — **1건 회차** (11:12:2x~11:13:1x)

사람이 지켜보지 않았다. **45초 간격 폴링 스크립트**를 10:33 에 걸어 두고 값 변화를 기다렸다
(§2 의 «순간을 봐야 하면 기계로 본다» 를 그대로 적용했다 — 오늘은 근무 창 안이라 볼 수도
있었지만, **11:12 를 화면 앞에서 기다릴 이유가 없다**).

```
11:11:39  same     13|49|7|4|ZZ-옵션삭제검증,ZZ-정가텟트,ZZ-재고알림테스트,ZZ-123123
11:12:25  same     13|49|7|4|ZZ-옵션삭제검증,ZZ-정가텟트,ZZ-재고알림테스트,ZZ-123123
11:13:13  CHANGED  12|46|7|3|ZZ-정가텟트,ZZ-재고알림테스트,ZZ-123123
```

| 무엇 | 09:2x | **11:13** | 변화 |
|---|---|---|---|
| `product_variant` | 13 | **12** | −1 |
| `stock_history` | 49 | **46** | 🔴 **−3** |
| product trash | 4 | **3** | −1 |

✅ **CASCADE 가 증명됐다** — 08-19 §13-2 가 «지우기 전 값을 안 세어 둬서» 못 한 것이 오늘 닫혔다.
✅ **1건 회차**다(`ZZ-옵션삭제검증` 하나). 「여러 건을 한 회차에」는 16:12 회차에 본다.

### 5-1. 🔴 **예측이 틀렸고, 틀린 쪽은 코드가 아니라 세는 방법이었다**

아침에 «`stock_history` 는 안 줄어든다(0건)» 로 예측했는데 **3줄이 줄었다.**

원인: 상품별 이력을 **`stock_history → product_variant → product` 로 조인해서** 셌다.
그런데 **`stock_history.variant_id` 는 FK 가 아니고 끊길 수 있다.** 엔티티가 이미 그렇게 적어 뒀다:

> *"관리자가 상품을 한 번만 저장해도 **모든 옵션의 id 가 바뀐다.** `variantId` 로만 조회하면 …
> `productId` + `variantName` 이 **실제 키**이고, `variantId` 는 느슨한 참조로만 남긴다
> (**FK 아님, NULL 가능**)."* — `StockHistory` javadoc

`ZZ-옵션삭제검증`(이름 그대로 옵션 삭제 검증용)은 옵션이 갈아치워져 이력 3줄의 `variant_id` 가
**이미 끊겨 있었다** → 조인이 0을 냈다.

⚠ **애플리케이션은 정상이다** — `StockHistoryRepository` 는 처음부터 `productId` 로 읽고,
CASCADE 도 `fk_stock_history_product`(ON DELETE CASCADE)에 붙어 있어 제대로 돌았다.
🔴 **틀린 것은 «내가 급히 쓴 확인 쿼리» 하나뿐이었고, 그 쿼리가 코드의 명시적 주석을 안 읽고 짜였다.**

**교훈**: 🔴 **기준값을 세는 쿼리는 «앱이 그 관계를 어떻게 읽는가» 를 따라야 한다.**
스키마에 컬럼이 있다고 그게 조회 키인 것이 아니다 — 여기서는 **컬럼이 둘인데 하나만 진짜 키**였고,
엔티티 주석이 그걸 명시하고 있었다. ⚠ 값이 **작을 때는 안 드러난다**(1건 회차라 −3 이 눈에 띄었지,
0 이었으면 «CASCADE 가 안 도나» 로 엉뚱한 데를 팠을 것이다).

### 5-2. 남은 둘의 예상 — 다시 센 값으로

| 상품 | variant | history (`product_id`) |
|---|---|---|
| `ZZ-정가텟트` | 1 | 1 |
| `ZZ-재고알림테스트` | 1 | 🔴 **7** (조인으로는 1로 보였다) |

→ **≈16:12 회차 예상: variant 12 → 10 · stock_history 46 → 38 · trash 3 → 1.**
⚠ `ZZ-123123` 은 **08-27 경**이라 오늘 대상이 아니다.

## 6. 🔴 14:46 강제 재부팅 — 한 사건이 넷으로 보였다

세션이 11:15 에 끊겼고 14:50 에 돌아오니 **증상이 넷**이었다. 넷 다 **같은 원인**이었다 —
`uptime -s` 가 **`2026-08-21 14:46:50`** 이었다.

| 보인 것 | 실제 |
|---|---|
| 폴링·세션이 11:15 에 끊김 | 재부팅 |
| `git status` 가 `index file smaller than expected` | 재부팅이 git 쓰기를 잘랐다 |
| `systemctl show … ActiveEnterTimestamp=` 가 **빈 값** | 서비스가 아직 `activating` 이었다 |
| 백엔드 `deactivating (stop-sigterm) · Result: timeout` | 🔴 **기동에 성공했는데 9초 늦어서 죽었다** |

⚠ **진단·복구 절차는 `TROUBLESHOOTING.md` §3-7·§3-8 로 옮겼다.** 여기엔 경위와 실측만 남긴다.

### 6-1. 시간을 쓴 순서가 틀렸다

디스크(52% · 23G 여유) → inode(2%) → 메모리 → OOM 로그를 **먼저** 팠고 **넷 다 정상**이었다.
`uptime -s` 는 **다섯 번째**로 봤는데, **그 한 줄이 넷을 한꺼번에 설명했다.**

🔴 **교훈**: 여러 증상이 **같은 시각에** 나타나면 **공통 원인을 먼저 찾는다.** 증상마다 따로
파고들면 «각각 다른 고장» 이라는 그림이 그려지고, 그 그림이 틀렸다는 걸 늦게 안다.
⚠ WA §3-3-1 이 이미 이웃한 규율을 적어 뒀다 — *«로그가 0건이면 판정 전에 `uptime -s`»*.
**같은 명령이 여기서도 답이었는데 「로그가 0건일 때」로만 좁게 적혀 있었다.**

### 6-2. 백엔드 — 기동 129.298초 vs `TimeoutStartSec=120`

| 회차 | 소요 | 결과 |
|---|---|---|
| 1차 (14:48:57~) | **129.298초** | 🔴 systemd 가 120초에 SIGTERM → `Failed to start` |
| 2차 (14:51:23~, `Restart=on-failure`) | **57.352초** | ✅ `active` (14:52:30) |

**앱은 두 번 다 정상적으로 떴다.** 1차가 느린 이유는 같은 VM 의 Oracle 이 함께 뜨기 때문이고,
2차는 캐시가 더워져 절반 이하다. ⚠ **즉 재부팅마다 «한 번 실패하고 두 번째에 사는» 패턴**이라
**혼자 나아서 원인을 안 찾게 된다.** 오늘 처음으로 그 자리를 봤다.

🔴 **제안(sudo 라 적용은 사용자)**: `TimeoutStartSec=120` → **`300`**.
⚠ `ExecStartPost` 헬스체크는 **그대로 둔다** — 그게 «active = 요청 처리 준비 완료» 를 만드는
장치다(유닛 주석이 그 이유를 적어 뒀다). 늘려야 하는 건 **기다려 주는 시간**뿐이다.

### 6-3. git — 손상은 **마지막에 쓰인 것**에 몰렸다

| 무엇 | 상태 |
|---|---|
| `.git/index` | **0바이트** |
| `.git/refs/heads/main` | **0바이트** |
| loose object | 🔴 **5개가 0바이트** / 성한 것 **1053개** |
| `.git/logs/HEAD`(reflog) · packed-refs · pack 파일 | ✅ 무사 |

🔴 **`git push` 는 성공했는데 로컬만 날아간 상태였다.** 11:16:18 에 만든 핸드오프 커밋
(`027847d`)이 **GitHub 에는 있고** 로컬에는 **0바이트 object** 로만 있었다 —
그래서 `git ls-remote` 가 로컬 HEAD(`73d20f6`)보다 **한 커밋 앞선** 값을 답했다.

✅ **복구 완료**: 0바이트 object 5개 삭제 → 깨진 loose ref 삭제 후 `update-ref` → `git reset` 으로
인덱스 재생성 → `git fetch` 로 5개 복원 → `origin/main` 으로 ff.
**`git fsck` 깨끗 · 작업 트리 무손실**(핸드오프 파일이 커밋본과 `sha256` 까지 같은 것을 확인하고 ff 했다).

⚠ **잃은 것이 없는 이유가 «운이 좋아서» 가 아니다** — 커밋을 **작게 자주 하고 바로 push** 해서
크래시 시점에 «로컬에만 있는 것» 이 **한 커밋뿐**이었고, 그 한 커밋도 원격에 있었다.

### 6-4. 배치 회차가 **또** 밀렸다

재부팅으로 백엔드가 14:52:27 에 다시 떴다 → `initialDelay` 5분 → **회차가 ≈14:57 · 15:57 · 16:57** 이다.
`ZZ-정가텟트`·`ZZ-재고알림테스트` 는 **15:55 만료**라 **≈15:57 회차**에 함께 사라진다(근무 창 안).

🔴 **오늘 회차가 세 번 다시 잡혔다** — 08:26 기동 → 10:07 배포 → 14:52 재부팅.
⚠ §2 가 «배포가 타이머를 리셋한다» 로 적어 둔 그것인데, **재부팅도 같은 일을 한다.**
관측을 계획할 때 세는 것은 «배포» 가 아니라 **«마지막 기동 시각»** 이다.
