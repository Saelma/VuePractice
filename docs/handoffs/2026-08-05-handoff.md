# 2026-08-05 핸드오프

> WORKING-AGREEMENTS §4-0-1: **배포할 때마다 닫는다.** 하루 끝에 몰지 않는다.
> 보조 기준은 시간이 아니라 **커밋 5개**.

---

## 0. 오늘의 커밋

| 커밋 | 내용 |
|---|---|
| `8a8b56d` | fix(global): 없는 경로·메서드 불일치가 500이던 것을 404·405로 |

> ⚠ 하루를 닫을 때 **이어 붙이지 말고 통째로 다시 만든다**(WA §4-4):
> `git log --reverse --since="2026-08-05 00:00" --pretty='| \`%h\` | %s |'`

---

## 1. 오늘 무엇을 할 것인가 — 후보와 근거

**이월: 없음**(8/04 §이월). 8/04 의 B-19 · B-17 · B-18(최소안)이 전부 배포·브라우저 검증까지 닫혔다.

다만 사용자가 *"어제 안 한 게 있으면 그것부터"* 라고 정해서, **조건부 잔여 중 「확인 대기」인 둘**을
먼저 잡는다. 8/04 「다음 기능 후보」(G-3 묶음 등)는 그 뒤로 미룬다.

| 오늘 할 것 | 근거 |
|---|---|
| **조건부 잔여 9번** — SSE 수정이 실제로 도는지 | 배포는 됐는데 *경로를 한 번도 안 밟은 채* `ERROR 0건` 이었다 |
| **조건부 잔여 8번** — 취소 폼에 공백만 넣고 누르기 | 서버·프론트는 각각 덮였고 **그 사이 Vue 폼 배선 한 칸**이 미확인 |
| **곁가지(오늘 발견)** — 없는 경로가 500 | 9번을 **판정할 수단** 자체가 오염돼 있었다(§2) |

### 1-1. 착수 전 실측 — **9번은 어제 근거가 이미 사라져 있었다**

| 본 것 | 결과 |
|---|---|
| 운영 health | `{"status":"UP"}` |
| 배포된 jar | **08-04 17:00:16** (= 어제 마지막 배포본, SSE 수정 16:03 포함) |
| 서비스 기동 | **2026-08-05 09:37:56** — `who -b` 도 09:37, **VM 이 오늘 재부팅**됐다 |
| 🔴 저널 보존 | `journalctl --list-boots` = **boot 0 하나뿐**(오늘 09:37~) |
| nginx `glassvue-access.log` | 마지막 기록 **08-04 17:16** → **오늘 브라우저 트래픽 0** |

🔴 **어제 §3-9 가 근거로 쓴 「배포 후 ERROR 0건」은 지금 재확인이 불가능하다.** 저널이 부팅 단위로만
남아 어제 로그가 통째로 없다. 즉 9번은 *"어제 것을 다시 본다"* 가 아니라 **지금부터 새로 증거를
만들어야** 하는 항목이다.

**그리고 양성 증거를 로그로 만들 수 없다** — 어제 넣은 핸들러는 `log.debug` 인데 `application.yml` 에
`logging` 설정이 없어 운영은 INFO 다. actuator 도 `health,info` 만 노출돼(실측) **런타임으로 레벨을
못 바꾼다.** → 판정은 **①경로를 확실히 밟았다를 nginx 로 증명 → ②그 뒤 예외 0건** 순서로 간다.
어제 빠진 것이 정확히 ① 이다.

---

## 2. 곁가지 — **없는 경로가 500 이었다** (오늘 발견, 커밋 `8a8b56d`)

### 2-1. 어떻게 드러났나

위 §1-1 에서 *"로그 레벨을 잠깐 올릴 수 있나"* 하고 `/actuator/loggers/...` 를 찔러 본 것이 시작이다.
**500 이 돌아왔고 `ERROR "Unhandled exception"` 이 남았다.**

```
NoResourceFoundException: No static resource actuator/loggers/...
  → GlobalExceptionHandler#handleUnexpected → ERROR + 500
```

**이건 어제 §3-7(SSE)과 정확히 같은 계열이다** — 포괄 `Exception` 핸들러가 *받으면 안 되는 것*을 받아
①클라이언트 잘못을 서버 오류로 만들고 ②`ERROR 0건` 확인을 오염시킨다.
⚠ 즉 **9번을 판정하려던 수단 자체가 고장 나 있었다.** 그래서 9번보다 먼저 고쳤다.

### 2-2. 실측 — 구멍은 **두 종류**이고, 경로 모양에 따라 갈렸다

운영(:8080)에 직접 쏴서 전수로 봤다. 그 결과 `ERROR` 7건이 남았고 **전부 이 probe 가 만든 것**이다
(오늘 `ERROR` 총계도 7 — 기존 오류가 아니다).

| 요청 | 지금 | 나와야 할 것 | 예외 |
|---|---|---|---|
| `/api/zzz` · `/api/notices/zzz/deep/path` · `/` · `/actuator/loggers` | **500** | 404 | `NoResourceFoundException` (5건) |
| `DELETE /api/notices` · `PUT /api/products` | **500** | 405 | `HttpRequestMethodNotSupportedException` (2건) |
| `/api/products/nope-not-a-route` · `/api/notices/list-typo` | **400** ✅ | 400 | (`{id}` 패턴에 걸려 타입 변환 실패) |

🔴 **마지막 줄이 이 구멍이 오래 안 보인 이유다.** API 오타를 쳐 보면 대개 `{id}` 패턴에 걸려
**400 이 잘 나온다** — 500 이 되는 건 **어느 패턴에도 안 걸릴 때뿐**이라, 몇 번 찔러 보고
*"에러 처리 잘 되네"* 로 끝나기 쉬운 모양이었다.

### 2-3. 결정

| 물은 것 | 정한 것 | 근거 |
|---|---|---|
| 404 와 405 를 합칠까 | **가른다** | 답이 다르다 — 404 는 *"주소를 다시 봐라"*, 405 는 *"주소는 맞고 메서드가 틀렸다"*. 합치면 프론트가 원인을 못 좁힌다 |
| 에러 코드 | **`COMMON-404`·`COMMON-405` 신설** | 도메인별 404(`PRODUCT-404` 등)는 *"그 자원이 없다"* 이고 이건 *"그런 경로가 없다"* 라 **층이 다르다**. 도메인이 던지는 코드가 아니라 **스프링 예외를 옮겨 담는 자리**임을 enum 주석에 적어 뒀다 |
| 로그 레벨 | **`warn`** | 클라이언트 잘못이라 `ERROR` 는 아니지만(그게 이 수정의 목적), **프론트가 없는 경로를 부르고 있으면 알아야** 한다. 기존 `handleUnreadableBody`(400)와 같은 레벨 |
| `Allow` 헤더 | **준다** | HTTP 규약이고, *무엇이 되는지* 알려주는 게 405 의 값이다 |

**버린 안**: `ResponseEntityExceptionHandler` 를 상속해 스프링 기본 처리에 맡기는 것.
`NoResourceFoundException` 이 `ErrorResponse` 를 구현하고 있어 그 경로로도 404 가 나오지만,
**응답이 `ProblemDetail` 이 되어 우리 공통 래퍼 `ApiResponse` 와 형식이 갈린다**(CLAUDE.md 규칙).
전역 핸들러 하나를 바꾸면 **기존 400·500 응답 형식까지 전부 흔들리는** 크기라, 두 핸들러를 더하는
쪽이 훨씬 작다.

⚠ **정적 리소스 서빙과 충돌하지 않는지 확인했다** — 백엔드에 `resources/static` 이 **없고**,
nginx 가 프론트를 맡으며 `/api/` 와 `/actuator/health` 만 프록시한다(`infra/nginx/conf.d/glassvue.conf`).
SPA 딥링크는 nginx `try_files … /index.html` 이 처리하므로 **이 변경이 닿지 않는다.**

### 2-4. 테스트 · 변형 주입 — **6건 넣고 6건 다 잡혔다**

| | 결과 |
|---|---|
| 백엔드 | **613 tests · skipped 0 · failures 0 · errors 0** (8/04 마감 608 → **+5**) |
| 프론트 | **130 tests · 19 files** (변경 없음) |
| 커버리지(자로만 쓴다, WA §3-2) | 명령 **91%**(8/04 90%) · 분기 **80%**(동일) |

변형은 커밋 `8a8b56d` **뒤에** 넣었고, ⚠ **넣기 전에 `git status --short` 가 비었는지 먼저 봤다**
(§3-8 에서 어제 규약에 넣은 확인 동작 — 그때 안 비어 있어서 커밋을 먼저 했다).
기준선은 관련 2개 클래스 **11건 · 실패 0**. H-8 대로 **한 번에 하나씩**.

| # | 무엇을 망가뜨렸나 | 지킨다는 규칙 | 빨개진 것 |
|---|---|---|---|
| Q1 | `@ExceptionHandler(NoResourceFoundException)` 제거 | 없는 경로는 **전용 핸들러**가 받는다 | **2** |
| Q2 | `@ExceptionHandler(HttpRequestMethodNotSupported)` 제거 | 메서드 불일치도 그렇다 | **2** |
| Q3 | `Allow` 헤더만 제거 | 405 는 **무엇이 되는지** 알려준다 | **1** |
| Q4 | 404 핸들러가 `INVALID_INPUT` 을 쓰게 | 상태·코드가 **404** 여야 한다 | **1** |
| Q5 | 405 핸들러가 `ENDPOINT_NOT_FOUND` 를 쓰게 | 404 와 405 를 **안 뭉갠다** | **1** |
| Q6 | `MethodArgumentTypeMismatch` 핸들러 제거 | 🔴 `{id}` 오타는 **여전히 400** | **1** |

**Q6 이 이번의 요점**이다. 나머지는 *"새로 넣은 게 동작하나"* 를 보지만, Q6 은 **구멍이 어디까지였는지**
를 고정한다 — 400 이던 자리가 404 로 뭉개지면 *"형식이 틀렸다"* 와 *"그런 경로가 없다"* 가 합쳐져
프론트가 원인을 못 좁힌다. §2-2 의 마지막 줄이 글로만 남지 않게 **테스트로 박아 둔 것**이다.

원복 후 `git diff -- src/main` **비었고** `git status --short` 도 **비었다**.

---

## 이월 (다음 문서로 반드시 옮길 것)

> WORKING-AGREEMENTS §4-1. 다음 날 문서가 그대로 물려받아 시작한다. 처리했으면 지운다.

- **404·405 수정**(§2) — **배포 대기**. 코드·테스트·변형 주입까지 끝났고 마이그레이션은 없다.
- **조건부 잔여 9번**(SSE 수정이 도는지) — **검증 대기**. 오늘 재부팅으로 어제 로그가 사라져
  **증거를 새로 만들어야** 한다(§1-1).
- **조건부 잔여 8번**(취소 폼 공백) — **검증 대기**. 쓸 주문은 정해졌다(아래).

### 다음 기능 후보

8/04 것을 그대로 물려받는다(오늘 아직 하나도 착수 안 함).

| 후보 | 크기 | 근거 |
|---|---|---|
| **G-3 + 문의 관리 한 묶음** | 중 | 관리자 **문의** 목록이 없어 G-3 이 단독으로 안 된다. 8/04 실측 재확인: `InquiryControllerImpl` 전수 **5개**(`POST/GET /products/{productId}/inquiries` · `PUT/DELETE /inquiries/{id}` · `POST /inquiries/{id}/answer`) — **관리자 문의 목록 0개**. 어제 만든 `/admin/reviews` 패턴을 그대로 복제하면 된다 |
| **B-18 남은 것**(신고·문의 숨김·감사) | 소~중 | 문의 숨김은 G-3 과 함께 볼 자리다 |
| **B-25 관리자 주문 취소** | 중 | 결정 셋이 얹힌다(어느 상태까지 · 환불 · 취소자 기록) |
| **관리자 재고 목록 화면** | 소 | ⚠ `stock_history` 가 **아직 0행**(어제 검증 잔재는 상품 삭제 때 CASCADE 로 사라졌다) — 볼 게 없으면 값이 안 난다 |
| **기존 회원 재동의** | 소~중 | ⚠ 연습 프로젝트엔 과할 수 있다 |

### 조건부 잔여

**8/04 §조건부 잔여를 그대로 물려받는다.** ⚠ 개수를 여기 적지 않는다 — 목록이 곧 개수다(WA §4-0).

1. `/tmp` WebP 잔재 — tmpfiles.d 10일 규칙으로 자동 삭제.
2. 운영자ID 계정 검증 적립 영구 잔존(SILVER·누적 204,880·잔액 97P) — 취소 불가가 설계.
3. 공유기 DHCP 예약 — ⚠ 전제가 사라졌다(NAT). **브리지로 되돌리면 되살아나는 항목**이라 지우지 않는다.
4. 인증서 SAN — `IP:192.168.50.14` 는 죽은 값. **당장 할 일 없음**, 재발급 시 IP 대신 DNS 로.
5. 8fter 의 미학(색·여백·타이포) — 사용자가 "무엇이 좋았는지" 한 줄 주면 DESIGN §2 보강.
6. 디자인 더 갈 여지(B-12 잔여) — 회원 상세 레이아웃 카드화, 상태 뱃지 pill 강화.
7. ⚠ **검증으로 생긴 주문은 지우지 않기로 결정했다**(2026-07-31). **다시 들추지 말 것.**
   실무 규칙: 데모·발표에서 **매출 숫자를 인용하지 않는다.**
   ⚠ 건수를 여기 적지 않는다(WA §4-0-2) — 검증할 때마다 늘어나는데 문장은 처음 쓴 상태로 남는다.
   세려면: `select count(*) from orders where buyer_nickname like 'ZZ%' or ship_recipient like 'ZZ%';`
   (⚠ 이 쿼리는 `ZZ` 를 안 붙인 잔재를 못 잡는다 — 2026-07-22 사고. 오늘 8번으로 생기는 것도 그렇다.)
8. **취소 폼에 공백만 넣고 누르는 경로를 화면에서 안 봤다**(2026-08-04, §3-6).
   서버 쪽 공백→NULL 은 통합 테스트가, 프론트 `'' → {reason: null}` 은 단위 테스트가 덮는다.
   **안 덮인 것은 그 사이의 Vue 폼 배선 한 칸**이고, 이 프로젝트엔 컴포넌트 렌더링 테스트가 없어
   **구조적으로 못 덮는 자리**다(WA §3).
   - **오늘 전제가 하나 풀렸다**: *"취소 가능한 주문이 하나 필요하다"* 가 걸림돌이었는데, 실측하니
     **`김기현팀` 계정에 ORDERED 8건**이 이미 있다(`20260727-0291`·`0258`~`0253`·`20260715-0002`).
     **새 주문을 만들지 않기로** 했다(사용자 결정) — 대가는 그 주문이 영구히 CANCELLED 가 되고
     `stock_history` 에 `CANCEL` 줄이 처음 생기는 것.
9. **끊긴 클라이언트 수정이 실제로 도는지 못 봤다**(2026-08-04, §3-9).
   - 🔴 **오늘 더 나빠졌다** — VM 이 09:37 에 재부팅됐고 **저널이 부팅 단위로만 남아 어제 로그가 없다**(§1-1).
     *"어제 0건이었다"* 조차 이제 인용할 수 없다.
   - 남은 일: **①로그인해서 SSE 를 붙이고**(nginx 에 `/api/notifications/stream` **200** 이 남아야 한다 —
     어제는 401 만 108건이라 *붙지도 않았다*) **②끊고**(페이지 이동·탭 닫기, 또는 배포 재시작)
     **③`journalctl -u glassvue-backend | grep -c AsyncRequestNotUsableException` 이 0** 인지 본다.
     ⚠ ① 없이 나온 0 은 **어제와 똑같은 무의미한 0** 이다.
10. **만료 토큰 탭이 SSE 를 31초마다 무한 재시도한다**(2026-08-04, §3-9).
    ⚠ 고치려면 **재연결 정책과 토큰 갱신을 같이** 봐야 한다 — 한 줄 수정이 아니다.
