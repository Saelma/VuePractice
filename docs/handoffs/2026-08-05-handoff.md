# 2026-08-05 핸드오프

> WORKING-AGREEMENTS §4-0-1: **배포할 때마다 닫는다.** 하루 끝에 몰지 않는다.
> 보조 기준은 시간이 아니라 **커밋 5개**.

---

## 0. 오늘의 커밋

| 커밋 | 내용 |
|---|---|
| `8a8b56d` | fix(global): 없는 경로·메서드 불일치가 500이던 것을 404·405로 |
| `65a90ba` | docs: 08-05 문서 개시 — 이월 인수 · §1-1 착수 전 실측 · §2 404/405 곁가지 |
| `a14abf8` | docs: 08-05 §2-5 배포 확인 + §3 SSE 9번 경로 밟음 — 그리고 배포 결함 발견 |
| `34bfdc6` | docs: 08-05 마감 — §4 브라우저 검증(8·9번 종결) · §5 하루 총괄 · §0 표 전수 재작성(3건) |

> ⚠ 하루를 닫을 때 **이어 붙이지 말고 통째로 다시 만든다**(WA §4-4):
> `git log --reverse --since="2026-08-05 00:00" --pretty='| \`%h\` | %s |'`

---

## 1. 오늘 무엇을 할 것인가 — 후보와 근거

**이월: 없음**(8/04 §이월). 8/04 의 B-19 · B-17 · B-18(최소안)이 전부 배포·브라우저 검증까지 닫혔다.

다만 사용자가 *"어제 안 한 게 있으면 그것부터"* 라고 정해서, **조건부 잔여 중 「확인 대기」인 둘**을
먼저 잡는다. 8/04 「다음 기능 후보」(G-3 묶음 등)는 그 뒤로 미룬다.

| 오늘 할 것 | 근거 |
|---|---|
| **조건부 잔여 9번** — SSE 수정이 실제로 도는지 | 배포는 됐는데 *경로를 한 번도 안 밟은 채* `ERROR 0건` 이었다 |
| **조건부 잔여 8번** — 취소 폼에 공백만 넣고 누르기 | 서버·프론트는 각각 덮였고 **그 사이 Vue 폼 배선 한 칸**이 미확인 |
| **곁가지(오늘 발견)** — 없는 경로가 500 | 9번을 **판정할 수단** 자체가 오염돼 있었다(§2) |

### 1-1. 착수 전 실측 — **9번은 어제 근거가 이미 사라져 있었다**

| 본 것 | 결과 |
|---|---|
| 운영 health | `{"status":"UP"}` |
| 배포된 jar | **08-04 17:00:16** (= 어제 마지막 배포본, SSE 수정 16:03 포함) |
| 서비스 기동 | **2026-08-05 09:37:56** — `who -b` 도 09:37, **VM 이 오늘 재부팅**됐다 |
| 🔴 저널 보존 | `journalctl --list-boots` = **boot 0 하나뿐**(오늘 09:37~) |
| nginx `glassvue-access.log` | 마지막 기록 **08-04 17:16** → **오늘 브라우저 트래픽 0** |

🔴 **어제 §3-9 가 근거로 쓴 「배포 후 ERROR 0건」은 지금 재확인이 불가능하다.** 저널이 부팅 단위로만
남아 어제 로그가 통째로 없다. 즉 9번은 *"어제 것을 다시 본다"* 가 아니라 **지금부터 새로 증거를
만들어야** 하는 항목이다.

**그리고 양성 증거를 로그로 만들 수 없다** — 어제 넣은 핸들러는 `log.debug` 인데 `application.yml` 에
`logging` 설정이 없어 운영은 INFO 다. actuator 도 `health,info` 만 노출돼(실측) **런타임으로 레벨을
못 바꾼다.** → 판정은 **①경로를 확실히 밟았다를 nginx 로 증명 → ②그 뒤 예외 0건** 순서로 간다.
어제 빠진 것이 정확히 ① 이다.

---

## 2. 곁가지 — **없는 경로가 500 이었다** (오늘 발견, 커밋 `8a8b56d`)

### 2-1. 어떻게 드러났나

위 §1-1 에서 *"로그 레벨을 잠깐 올릴 수 있나"* 하고 `/actuator/loggers/...` 를 찔러 본 것이 시작이다.
**500 이 돌아왔고 `ERROR "Unhandled exception"` 이 남았다.**

```
NoResourceFoundException: No static resource actuator/loggers/...
  → GlobalExceptionHandler#handleUnexpected → ERROR + 500
```

**이건 어제 §3-7(SSE)과 정확히 같은 계열이다** — 포괄 `Exception` 핸들러가 *받으면 안 되는 것*을 받아
①클라이언트 잘못을 서버 오류로 만들고 ②`ERROR 0건` 확인을 오염시킨다.
⚠ 즉 **9번을 판정하려던 수단 자체가 고장 나 있었다.** 그래서 9번보다 먼저 고쳤다.

### 2-2. 실측 — 구멍은 **두 종류**이고, 경로 모양에 따라 갈렸다

운영(:8080)에 직접 쏴서 전수로 봤다. 배포 전까지 오늘 `ERROR` 는 **8건**이고 **전부 이 확인이 만든
것**이다(11:28:16 최초 probe 1건 + 13:02:1x~13:02:45 전수 probe 7건 — 시각으로 대조했다).
**기존 오류는 0건**이다. 예외 종류로 가르면 `NoResourceFoundException` **5** ·
`HttpRequestMethodNotSupportedException` **2**(전수 probe 구간 기준).

| 요청 | 지금 | 나와야 할 것 | 예외 |
|---|---|---|---|
| `/api/zzz` · `/api/notices/zzz/deep/path` · `/` · `/actuator/loggers` | **500** | 404 | `NoResourceFoundException` (5건) |
| `DELETE /api/notices` · `PUT /api/products` | **500** | 405 | `HttpRequestMethodNotSupportedException` (2건) |
| `/api/products/nope-not-a-route` · `/api/notices/list-typo` | **400** ✅ | 400 | (`{id}` 패턴에 걸려 타입 변환 실패) |

🔴 **마지막 줄이 이 구멍이 오래 안 보인 이유다.** API 오타를 쳐 보면 대개 `{id}` 패턴에 걸려
**400 이 잘 나온다** — 500 이 되는 건 **어느 패턴에도 안 걸릴 때뿐**이라, 몇 번 찔러 보고
*"에러 처리 잘 되네"* 로 끝나기 쉬운 모양이었다.

### 2-3. 결정

| 물은 것 | 정한 것 | 근거 |
|---|---|---|
| 404 와 405 를 합칠까 | **가른다** | 답이 다르다 — 404 는 *"주소를 다시 봐라"*, 405 는 *"주소는 맞고 메서드가 틀렸다"*. 합치면 프론트가 원인을 못 좁힌다 |
| 에러 코드 | **`COMMON-404`·`COMMON-405` 신설** | 도메인별 404(`PRODUCT-404` 등)는 *"그 자원이 없다"* 이고 이건 *"그런 경로가 없다"* 라 **층이 다르다**. 도메인이 던지는 코드가 아니라 **스프링 예외를 옮겨 담는 자리**임을 enum 주석에 적어 뒀다 |
| 로그 레벨 | **`warn`** | 클라이언트 잘못이라 `ERROR` 는 아니지만(그게 이 수정의 목적), **프론트가 없는 경로를 부르고 있으면 알아야** 한다. 기존 `handleUnreadableBody`(400)와 같은 레벨 |
| `Allow` 헤더 | **준다** | HTTP 규약이고, *무엇이 되는지* 알려주는 게 405 의 값이다 |

**버린 안**: `ResponseEntityExceptionHandler` 를 상속해 스프링 기본 처리에 맡기는 것.
`NoResourceFoundException` 이 `ErrorResponse` 를 구현하고 있어 그 경로로도 404 가 나오지만,
**응답이 `ProblemDetail` 이 되어 우리 공통 래퍼 `ApiResponse` 와 형식이 갈린다**(CLAUDE.md 규칙).
전역 핸들러 하나를 바꾸면 **기존 400·500 응답 형식까지 전부 흔들리는** 크기라, 두 핸들러를 더하는
쪽이 훨씬 작다.

⚠ **정적 리소스 서빙과 충돌하지 않는지 확인했다** — 백엔드에 `resources/static` 이 **없고**,
nginx 가 프론트를 맡으며 `/api/` 와 `/actuator/health` 만 프록시한다(`infra/nginx/conf.d/glassvue.conf`).
SPA 딥링크는 nginx `try_files … /index.html` 이 처리하므로 **이 변경이 닿지 않는다.**

### 2-4. 테스트 · 변형 주입 — **6건 넣고 6건 다 잡혔다**

| | 결과 |
|---|---|
| 백엔드 | **613 tests · skipped 0 · failures 0 · errors 0** (8/04 마감 608 → **+5**) |
| 프론트 | **130 tests · 19 files** (변경 없음) |
| 커버리지(자로만 쓴다, WA §3-2) | 명령 **91%**(8/04 90%) · 분기 **80%**(동일) |

변형은 커밋 `8a8b56d` **뒤에** 넣었고, ⚠ **넣기 전에 `git status --short` 가 비었는지 먼저 봤다**
(§3-8 에서 어제 규약에 넣은 확인 동작 — 그때 안 비어 있어서 커밋을 먼저 했다).
기준선은 관련 2개 클래스 **11건 · 실패 0**. H-8 대로 **한 번에 하나씩**.

| # | 무엇을 망가뜨렸나 | 지킨다는 규칙 | 빨개진 것 |
|---|---|---|---|
| Q1 | `@ExceptionHandler(NoResourceFoundException)` 제거 | 없는 경로는 **전용 핸들러**가 받는다 | **2** |
| Q2 | `@ExceptionHandler(HttpRequestMethodNotSupported)` 제거 | 메서드 불일치도 그렇다 | **2** |
| Q3 | `Allow` 헤더만 제거 | 405 는 **무엇이 되는지** 알려준다 | **1** |
| Q4 | 404 핸들러가 `INVALID_INPUT` 을 쓰게 | 상태·코드가 **404** 여야 한다 | **1** |
| Q5 | 405 핸들러가 `ENDPOINT_NOT_FOUND` 를 쓰게 | 404 와 405 를 **안 뭉갠다** | **1** |
| Q6 | `MethodArgumentTypeMismatch` 핸들러 제거 | 🔴 `{id}` 오타는 **여전히 400** | **1** |

**Q6 이 이번의 요점**이다. 나머지는 *"새로 넣은 게 동작하나"* 를 보지만, Q6 은 **구멍이 어디까지였는지**
를 고정한다 — 400 이던 자리가 404 로 뭉개지면 *"형식이 틀렸다"* 와 *"그런 경로가 없다"* 가 합쳐져
프론트가 원인을 못 좁힌다. §2-2 의 마지막 줄이 글로만 남지 않게 **테스트로 박아 둔 것**이다.

원복 후 `git diff -- src/main` **비었고** `git status --short` 도 **비었다**.

### 2-5. 배포 — 증거로 확인했다 (상태코드로 가리지 않았다)

jar **13:38:44** · 서비스 **13:38:49** · health `UP` · 프론트는 **변경 없어 배포 안 함**.
마이그레이션 없음. `check-deploy-branch.sh` 통과(`main` = `b19-stock-history` = `65a90ba`).

⚠ **이 변경은 OpenAPI 로 안 보인다** — 엔드포인트가 늘지 않고 **동작만 바뀌기 때문**이다
(WA §5: *"구 jar 와 신 jar 가 다르게 답하는 지점이 어디인가"*). 그래서 **행동으로** 확인했다:

| 요청 | 배포 전 | 지금 |
|---|---|---|
| `GET /api/zzz` | 500 | **404 · `COMMON-404`** |
| `DELETE /api/notices` | 500 | **405 · `COMMON-405` · `Allow: POST`** |
| `GET /api/notices/not-a-uuid` | 400 | **400** (경계 유지 — Q6 이 지키는 것) |

**그리고 이 확인을 하는 동안 `ERROR` 증가가 0 이다.** 아침에 같은 probe 가 `ERROR` **7건**을
만들던 것과 대비된다 — *확인 수단이 확인 대상을 오염시키던 것*이 사라졌다.

---

## 3. 조건부 잔여 9번 (SSE) — **경로는 밟았다. 판정은 §3-2 참조**

### 3-1. 증거 사슬 — 어제 없던 ①이 생겼다

배포 재시작을 **끊김 트리거로 썼다**(사용자가 로그인한 탭을 열어 둔 채로 배포).

| # | 무엇 | 실측 |
|---|---|---|
| ① **붙었다** | 배포 전 nginx 에 완료 기록 **0건** + 백엔드 8080 established 연결 **1개** | 13:37 시점 |
| ② **끊겼다** | nginx SSE **200** 이 기록됨(= 그 순간 응답이 끝났다) | **13:38:46** |
| ③ 재연결 시도 | 백엔드가 없던 순간 **502 5건** | 13:38:4x |
| ④ 두 번째 끊김 | **200** 한 건 더 — `SseEmitter` **30분 타임아웃**(`TIMEOUT_MS`) | **14:09:54** |
| ⑤ 판정 | `AsyncRequestNotUsableException` **0건** · `ERROR` **8**(= 오늘 probe 그대로, 증가 0) | |

어제는 ①이 없어 *"0 이 나올 이유가 두 개(고쳐졌다 / 안 밟았다)"* 였다. **오늘은 ①이 있다.**

### 3-2. ⚠ 그런데 이번 끊김은 **어제와 같은 종류가 아닐 수 있다**

재시작 로그를 보다 **배포 방식의 결함**이 드러났다(오늘 두 번째 곁가지):

```
WARN  Failed to stop bean 'webServerGracefulShutdown'
java.lang.NoClassDefFoundError: org/springframework/boot/web/server/GracefulShutdownCallback
```

**원인**: `deploy-backend.sh` 가 **구 프로세스가 살아 있는 채로 jar 를 덮어쓰고**(`cp -f`) 그다음
재시작한다. JVM 은 클래스를 **필요할 때 읽으므로**, 종료 경로에서만 쓰는 클래스는 **이미 바뀐 파일**
에서 찾게 되어 못 읽는다.

**따라서 graceful shutdown 이 실패했다** — 연결이 곱게 닫힌 게 아니라 **끊겨 나갔다.**
그러면 톰캣이 *"끊긴 클라이언트에 쓰려다 실패"* 하는 단계까지 못 갔을 수 있다.
즉 **`0건` 이 「핸들러가 잡았다」인지 「예외가 애초에 안 났다」인지 아직 못 가른다** —
어제와 **모양이 다른 0** 이지만, 여전히 두 해석이 남는다.

⚠ 양성 증거(핸들러 실행 로그)를 볼 방법이 없다 — 핸들러는 `debug` 인데 운영은 INFO 이고
actuator 로 레벨을 못 바꾼다(§1-1).

→ **남은 판정**: 어제 §3-7 이 명시한 **다른 경로**(*"브라우저가 페이지를 옮기면"*)로 한 번 더 끊는다.
그건 정상 종료 경로라 이번 결함과 무관하다. **8번 검증이 그 자리를 지나므로 함께 본다.**

### 3-3. 곁가지로 확정된 것 — 조건부 잔여 10번이 **재현됐다**

**14:09:57 부터 SSE 가 401 로 되돌아온다**(31초 간격). 그 직전 14:09:54 에 30분 타임아웃으로
스트림이 끝났는데, **access 토큰 유효시간도 30분**(`jwt.access-token-validity-ms=1800000`)이라
**둘이 같은 시각에 만료된다** — 재연결하려는 순간 토큰이 이미 죽어 있다.

⚠ 어제는 *"만료된 토큰을 든 탭이 재시도한다"* 까지만 적었는데, **두 수명이 같아서 반드시 겹친다**
는 것이 오늘 드러났다. 우연이 아니라 **구조**다(10번에 반영).

---

## 4. 브라우저 검증 — **8번·9번 둘 다 닫았다**

기대값을 먼저 뽑아 놓고 들어갔다(WA §3). ⚠ 그 과정에서 **내가 오늘 문서에 옮겨 적은 전제 하나가
틀린 것도 여기서 드러났다** — §이월 「다음 기능 후보」의 *"`stock_history` 아직 0행"* 은 어제 §2-9 를
그대로 베낀 것인데, 어제 §3-6 이 그 뒤에 늘려 **실제로는 8행**이었다(고쳤다).
*"어제 문서에 있으니 참"* 이 오늘도 한 번 깨졌다(WA §1-3).

### 4-1. 조건부 잔여 8번 (취소 폼 공백) — **종결**

대상은 **기존 `20260715-0002`**(2026-07-15 · 반팔티 × 1 · `ORDERED`). 새 주문을 만들지 않았다.

| 본 것 | 기대값 | 결과 |
|---|---|---|
| 취소 자체 | 된다 | ✅ `CANCELLED` · `cancelled_at` 14:22:15 |
| 🔴 **사유 저장 형태** | **`NULL`**(빈 문자열이면 실패) | ✅ **`NULL`** — 공백만 넣었는데 빈 문자열로 안 들어갔다 |
| 화면의 사유 줄 | 안 뜬다 | ✅ (사용자 확인) |
| 재고 복원 | 193 → **194** | ✅ **194** |
| `stock_history` | 8 → **9행**, `CANCEL +1` | ✅ **9행** · `CANCEL +1` · `actor_name` 비어 있음(주문 경로라 관리자가 아니다 — 설계대로) |

**이걸로 「그 사이의 Vue 폼 배선 한 칸」이 실측으로 덮였다.** 서버(공백→NULL)와 프론트(`'' → null`)는
각각 테스트가 덮고 있었지만, **둘을 잇는 폼이 실제로 그 값을 보내는지**는 화면으로만 확인된다 —
이 프로젝트에 컴포넌트 렌더링 테스트가 없어 **구조적으로 못 덮는 자리**였다(WA §3).

### 4-2. 조건부 잔여 9번 (SSE 수정) — **종결**

오늘 SSE 는 **세 가지 방식으로 끊겼고 세 번 다 예외가 0** 이다.

| # | 끊긴 방식 | 시각(200 기록) | 어제와 같은 경로인가 |
|---|---|---|---|
| ① | 배포 재시작 | 13:38:46 | ⚠ graceful shutdown 실패를 탄 비정상 종료(§3-2) |
| ② | `SseEmitter` 30분 타임아웃 | 14:09:54 | 정상 종료 경로 |
| ③ | **페이지 이동** | **14:22:45** | ✅ **어제 §3-7 이 명시한 바로 그 경로** |

- `AsyncRequestNotUsableException` **0건** · 배포(13:39) 이후 `ERROR` **0건**.
- ③ 이 결정적이다 — §3-2 가 남긴 *"이번 끊김은 어제와 다를 수 있다"* 는 의심이 여기서 풀린다.

**따라서 어제 못 적은 문장을 이제 적을 수 있다: 「배포 확인의 `ERROR 0건` 이 SSE 로 오염되지 않는다.」**

⚠ **정직하게 남기는 한계**: 이건 *"예외가 안 났다"* 이지 *"핸들러가 실행됐다"* 의 양성 증거는 아니다
(핸들러는 `debug` 인데 운영은 INFO, §1-1). 다만 **이 항목이 물은 것은 전자**였다 —
어제 §3-9 가 못 적은 이유는 *경로를 안 밟아서* 였고, 오늘은 밟았다.

### 4-3. 검증 잔재 — **INSERT 1 · UPDATE 2 · `ZZ` 신규 0**

WA §3-1 대로 *"INSERT 한 것은? **UPDATE 한 것은?**"* 을 둘 다 물었다(두 번째를 빼면 조용히 남는다).

| | |
|---|---|
| INSERT | `stock_history` **1행**(`CANCEL +1`) |
| UPDATE | 주문 `20260715-0002` **ORDERED → CANCELLED** · `반팔티/기본` 재고 **193 → 194** |
| `ZZ` 계정 | **0** ✅ (기존 계정 사용) |
| `ZZ` 상품 | **0** ✅ (새로 안 만듦) |
| 적립금 | **오늘 0행** — 결제 전(`ORDERED`) 주문이라 적립 자체가 없었다 |

⚠ **되돌릴 수 없는 것**: 그 주문은 영구히 `CANCELLED` 다(주문 삭제 API 없음). **사용자가 고르고
동의한 대가**이고, 새 주문을 안 만든 대신 치른 값이다. 재고 +1 은 *"영영 배송되지 않을 주문이
붙들고 있던 재고"* 라 오히려 맞는 방향이다.

---

## 5. 하루 총괄

| | |
|---|---|
| 종결 | **404·405 수정**(§2, 오늘 발견) · **조건부 잔여 8번**(§4-1) · **조건부 잔여 9번**(§4-2) |
| 새로 연 것 | 🔴 **배포가 구 프로세스 밑에서 jar 를 갈아치운다**(§3-2) · 10번의 **원인이 좁혀짐**(§3-3) |
| 마이그레이션 | **없음** |
| 배포 | 백엔드 **1회**(13:38) · 프론트 **0회**(변경 없음) |
| 테스트 | 백엔드 **613**(어제 608 → **+5**) · `skipped 0` · 프론트 **130**(변동 없음) |
| 커버리지(자로만) | 명령 **91%**(어제 90%) · 분기 **80%**(동일) |
| 변형 주입 | **6건 넣고 6건 다 잡힘**(§2-4) |
| 검증 잔재 | INSERT 1 · UPDATE 2 · `ZZ` 신규 **0**(§4-3) |

**오늘 세 번 나온 한 가지 — 「0 이라는 답에는 이유가 둘 있다」**

1. §1-1 어제의 `ERROR 0건` → 고쳐져서가 아니라 **경로를 안 밟아서** 0 이었다.
2. §3-1~§3-2 오늘의 0 → 밟긴 밟았는데 **끊김의 종류가 달라** 아직 못 닫았다(§4-2 ③으로 닫았다).
3. §4 기대값의 `stock_history` → *"0행이라 볼 게 없다"* 고 적었는데 **실제로 8행**이었다.

세 번 다 **숫자를 먼저 뽑아 놓고 봤기 때문에** 갈렸다. 1·2 는 어제 WA 에 *"확인 수단은 이 변경의
성질에서 찾는다"* 로 적혀 있던 것이고, 3 은 **문서를 베껴 적는 순간** 생겼다.

---

## 이월 (다음 문서로 반드시 옮길 것)

> WORKING-AGREEMENTS §4-1. 다음 날 문서가 그대로 물려받아 시작한다. 처리했으면 지운다.

- ~~**404·405 수정**(§2)~~ — **종결**. 613 tests(+5) · `skipped 0` · **변형 6건 전부 잡힘**(§2-4) ·
  마이그레이션 없음 · **배포·행동 확인 완료**(§2-5).
- ~~**조건부 잔여 9번**(SSE 수정이 도는지)~~ — **종결**(§4-2). 세 방식으로 끊고 세 번 다 예외 0건 ·
  그중 **페이지 이동**이 어제 §3-7 이 명시한 경로다.
- ~~**조건부 잔여 8번**(취소 폼 공백)~~ — **종결**(§4-1). 사유가 **`NULL`**(빈 문자열 아님) ·
  재고 193→194 · `stock_history` 8→9.
- 🔴 **배포가 구 프로세스 밑에서 jar 를 갈아치운다**(오늘 발견, §3-2) — **미착수**.
  `deploy-backend.sh` 가 `cp -f` 로 덮어쓴 뒤 재시작해서 **graceful shutdown 이 매번 실패**한다
  (즉 어제까지의 배포 4회도 전부 그랬다). in-flight 요청이 잘리고 종료 로그가 스택트레이스로 더러워진다.
  - 최소안: **`systemctl stop` → `cp` → `start`** 로 순서를 바꾼다(다운타임은 어차피 `restart` 로
    이미 생긴다 — 늘어나는 건 빌드 산출물 복사 시간뿐이다).
  - 더 나은 안: **버전 파일명 + 심볼릭 링크**(`cp` 새 이름 → `ln -sfn` → restart). 구 프로세스가
    **옛 inode 를 계속 붙들어** 종료가 깨끗하고 롤백도 링크 교체 한 번이다. 유닛의 `ExecStart` 가
    링크를 가리키면 되므로 systemd 변경은 없다.
  - ⚠ `infra/` 원본과 서버 사본을 함께 봐야 하는 항목이 아니다(스크립트는 레포에만 있다).

### 다음 기능 후보

8/04 것을 그대로 물려받는다(오늘 아직 하나도 착수 안 함).

| 후보 | 크기 | 근거 |
|---|---|---|
| **G-3 + 문의 관리 한 묶음** | 중 | 관리자 **문의** 목록이 없어 G-3 이 단독으로 안 된다. 8/04 실측 재확인: `InquiryControllerImpl` 전수 **5개**(`POST/GET /products/{productId}/inquiries` · `PUT/DELETE /inquiries/{id}` · `POST /inquiries/{id}/answer`) — **관리자 문의 목록 0개**. 어제 만든 `/admin/reviews` 패턴을 그대로 복제하면 된다 |
| **B-18 남은 것**(신고·문의 숨김·감사) | 소~중 | 문의 숨김은 G-3 과 함께 볼 자리다 |
| **B-25 관리자 주문 취소** | 중 | 결정 셋이 얹힌다(어느 상태까지 · 환불 · 취소자 기록) |
| **관리자 재고 목록 화면** | 소 | ⚠ **어제 문서(§2-9)의 「0행」을 그대로 옮겨 적었다가 실측에서 틀렸다** — `stock_history` 는 지금 **8행**이다(`ORDER`/`CANCEL` 각 3·4, `RETURN` 1 — 어제 §3-6 의 취소·반품이 실상품에 남긴 것). §2-9 는 **그 시점엔 참**이었고 그 뒤 §3-6 이 늘렸다. 즉 *"볼 게 없다"* 는 근거가 **조금 약해졌다** |
| **기존 회원 재동의** | 소~중 | ⚠ 연습 프로젝트엔 과할 수 있다 |

### 조건부 잔여

**8/04 §조건부 잔여를 그대로 물려받는다.** ⚠ 개수를 여기 적지 않는다 — 목록이 곧 개수다(WA §4-0).

1. `/tmp` WebP 잔재 — tmpfiles.d 10일 규칙으로 자동 삭제.
2. 운영자ID 계정 검증 적립 영구 잔존(SILVER·누적 204,880·잔액 97P) — 취소 불가가 설계.
3. 공유기 DHCP 예약 — ⚠ 전제가 사라졌다(NAT). **브리지로 되돌리면 되살아나는 항목**이라 지우지 않는다.
4. 인증서 SAN — `IP:192.168.50.14` 는 죽은 값. **당장 할 일 없음**, 재발급 시 IP 대신 DNS 로.
5. 8fter 의 미학(색·여백·타이포) — 사용자가 "무엇이 좋았는지" 한 줄 주면 DESIGN §2 보강.
6. 디자인 더 갈 여지(B-12 잔여) — 회원 상세 레이아웃 카드화, 상태 뱃지 pill 강화.
7. ⚠ **검증으로 생긴 주문은 지우지 않기로 결정했다**(2026-07-31). **다시 들추지 말 것.**
   실무 규칙: 데모·발표에서 **매출 숫자를 인용하지 않는다.**
   ⚠ 건수를 여기 적지 않는다(WA §4-0-2) — 검증할 때마다 늘어나는데 문장은 처음 쓴 상태로 남는다.
   세려면: `select count(*) from orders where buyer_nickname like 'ZZ%' or ship_recipient like 'ZZ%';`
   (⚠ 이 쿼리는 `ZZ` 를 안 붙인 잔재를 못 잡는다 — 2026-07-22 사고. 오늘 8번으로 생기는 것도 그렇다.)
8. **만료 토큰 탭이 SSE 를 31초마다 무한 재시도한다**(2026-08-04, §3-9).
   - **오늘 재현됐고 원인이 좁혀졌다**(§3-3): `SseEmitter` 타임아웃과 access 토큰 유효시간이
     **둘 다 30분**이라(`TIMEOUT_MS` · `jwt.access-token-validity-ms=1800000`)
     **재연결하는 바로 그 순간 토큰이 만료돼 있다.** 우연이 아니라 구조다.
   - ⚠ 고치려면 **재연결 정책과 토큰 갱신을 같이** 봐야 한다 — 한 줄 수정이 아니다.
     (곁들여: 두 수명을 어긋나게 두는 것만으로도 증상이 크게 준다.)

> ⚠ **어제의 8번(취소 폼 공백)·9번(SSE)은 오늘 종결했다**(§4-1·§4-2). 그래서 10번이 8번 자리로 당겨졌다 —
> **번호는 자리표지일 뿐 항목의 이름이 아니다**(8/04 이 같은 이유로 남긴 경고).
