# 2026-08-25 핸드오프 — 부분 반품(G-10)을 만들었고, **읽어 보는 것이 세 가지를 잡았다**

> 08-24 이월 넷을 아침에 닫고(재계수 · WA §1-2-1 승격) **G-10 부분 반품**으로 갔다.
>
> 🔴 **오늘의 요점은 «부분 반품을 만들었다» 가 아니라 «전수가 초록인 채로 셋이 숨어 있었고,
> 셋 다 코드나 데이터를 «세어 봐서» 나왔다» 다.** 브라우저가 잡은 08-24 와 갈리는 지점이다 —
> 오늘 것은 **브라우저로도 안 보였을** 종류가 둘이다.
>
> ✅ **배포와 브라우저 검증까지 끝냈고 회귀는 0건이다**(§8) — 08-24 가 같은 자리에서 넷을 만났다.
> **결정 셋이 전부 밟혔고 미검증 갈래가 없다.**
>
> ⚠ **아침에 올린 규약이 그날 오후에 값을 했다** — §1-2-1 의 새 줄(«읽는 값이 원본인지 남은
> 것인지 대조한다»)을 `approveReturn` 에 적용하다가 판매량 구멍이 나왔다.

---

## 0. 오늘의 커밋

| 해시 | 무엇 |
|---|---|
| `3961215` | docs(WA): §1-2-1 에 «목록이 맞아도 양이 틀릴 수 있다» 를 더한다 |
| `c836d2b` | feat(order): 주문의 일부만 반품한다 (BACKLOG G-10, V58) |
| `ed6d9c4` | test(order): 부분 반품 배분식을 고정하고, 부분 취소가 판매량을 안 되돌리던 자리를 막는다 |
| `4f0038e` | fix(order): 부분 반품 이전에 걸려 있던 반품 요청을 승인할 수 있게 채운다 (V59) |
| `b087d6d` | docs: 08-25 마감 — G-10 을 완료로 옮기고 핸드오프를 쓴다 |
| `cb11add` | docs: 08-25 마감 대조 — 커밋 표를 채우고, 이미 적힌 것을 지우고, 진단 한 줄을 옮긴다 |
| `d03f4d6` | docs(WA): §2-2-2 신설 — 새 컬럼의 DEFAULT 가 기존 행에 대해 참인지 본다 |
| `5a568cb` | docs: 08-25 커밋 표와 이월 push 항목을 실제와 맞춘다 (WA §4-4) |
| `a3792a6` | docs: G-10 브라우저 검증 결과를 적는다 — 회귀 0건 |
| `0af64d7` | docs(TS): 배포 파일 grep 이 「허가 거부」로 0건을 내는 자리를 적는다 |
| `8bb1e2c` | docs: 배송비 갈래까지 밟았다 — G-10 결정 셋 전부 검증 완료 |
| `aa01bfe` | docs: 되돌리는 흐름의 알림이 «부분» 을 모른다 — 셋을 이월 1순위로 적는다 |
| `c9e80d8` | docs(BACKLOG): §I 「부분」 축 감사 — 항목 열하나와 보류 근거를 적는다 |
| `03913f1` | docs: 커밋 표를 통째로 다시 만든다 (check-handoff.sh 지적) |
| `bd6a741` | fix(stats): 매출이 부분 반품을 안 빼던 자리를 고치고, 식을 한 곳으로 모은다 (BACKLOG I-1) |
| `972f205` | docs: §10 — I-1·I-2 를 닫은 경위와 커밋 하나에 둘을 담은 실수를 적는다 |
| `b7c4c14` | fix(order): 부분 반품한 고객이 돈 이야기를 볼 수 있게 한다 (BACKLOG I-3·I-4 + 알림 넷) |
| `8377b10` | docs: §11 — I-3·I-4 와 알림 넷을 닫은 경위 · 배포 뒤 밟을 값 |
| `b3682a7` | docs: I-1~I-4 배포·검증 완료 — 회귀 0건 |

⚠ 🔴 **이 표를 갱신하는 커밋 자체는 표에 없다** — 08-24 와 같은 이유(WA §4-4).

---

## 1. 아침 — 기준값 재계수 (09:3x)

| 무엇 | 08-24 아침 | **오늘 아침 실측** |
|---|---|---|
| `product_variant` | 10 | **10** |
| `stock_history` | 38 | 🔴 **53** (+15) |
| product alive / trash | 7 / 1 | **7 / 1** |

🔴 **`stock_history` 가 움직였다.** 날짜·사유별로 갈라 보니 **08-24 것이 정확히 15줄**이다
(`ORDER` 6 · `CANCEL` 8 · `ADMIN_EDIT` 1) — 즉 **밤사이 움직인 것은 없고** 08-24 검증의 흔적이다.

⚠ **덤으로 나온 것**: `CANCEL` 8 인데 `ORDER` 는 **6** 이다. 차이 2 가 08-24 사고 ②
(지바 +1 · 반팔티 +1)의 **원장 흔적**이다 — 「어긋난 채로 둔다」 결정과 값이 맞는다.

⚠ `ZZ-123123` 영구 삭제는 **08-27 13:16** 이라 오늘은 볼 것이 없었다.

### 1-1. 🔴 이월에 **틀린 항목**이 하나 있었다 — 5일간 실려 왔다

08-24 조건부 잔여의 *「`ProductDiscountAdminView` 에 뷰 테스트가 없다」* 는 **거짓**이다.

`src/views/ProductDiscountAdminView.test.js` — **246줄 / 테스트 14개**, G-5 커밋 `ed8e16a`(08-19)에
함께 들어왔다. 🔴 **08-19 핸드오프 §267 이 «오늘 `views/` 에 하나 늘었다 — `ProductDiscountAdminView`»
라고 직접 적어 놨는데**, 같은 날 이월이 「둘 다 없다」로 적혀 **08-20 → 08-21 → 08-24 까지 확인 없이
실려 왔다.**

⚠ **이것이 오늘 올린 규약과 같은 종류다** — 목록은 물려받았는데 그 안의 값이 틀렸다.
이월은 «지웠나» 만 보고 «아직 참인가» 는 안 본다.

---

## 2. WA §1-2-1 승격 — 08-24 이월 1번 (`3961215`)

**한 줄**: *«짝을 열었으면 목록만이 아니라 읽는 값이 «원본»인지 «남은 것»인지도 대조한다»*.

**전례를 다시 셌다** — 08-24 가 «검색어를 내가 골랐다» 며 다시 세라고 남겨 둔 절차다.
「두 번 돌려」·「이중」·「중복 환불」·「원본을 읽」·「스냅샷을 읽」·「합이 안 맞」·「전량」·
「두 번 계산」·「중복 차감」 등 **여러 갈래**로 `docs/` 를 훑었다.

| 히트 | 실제로 무엇이었나 |
|---|---|
| 「이중 진실」(07-24) | 같은 값을 두 곳에 두는 **설계** 문제 — 다른 종류 |
| 「이중 할인」(08-19·08-20) | 세일+쿠폰 중첩이라는 **가격 정책** — 다른 종류 |
| 「이중 마진」(07-28) | CSS |
| 「스냅샷을 읽」(07-30) | «지우기 전에 읽는다» 는 **순서** 문제 — 다른 종류 |

→ ✅ **여전히 0건. 08-24 가 첫 번째다.** 그래도 올렸다(08-24 사용자 결정) — 세는 이유는
«올릴지» 가 아니라 «몇 번째라고 적을지» 였다.

---

## 3. G-10 부분 반품 — 규칙 확정이 착수 조건이었다

⚠ **BACKLOG 에 항목이 없었다** — 08-24 이월의 포인터로만 존재했다. G-4·G-8 전례대로
**규칙을 먼저 정해 BACKLOG 에 적고** 착수했다.

**G-4 에서 그대로 따라오는 것**(다시 안 물었다): 배분식(비례·내림·잔돈은 주문에 남긴다) ·
쿠폰 최소금액 소급 안 함 · 무료배송 기준 소급 안 함.

**새로 정한 것 셋** (사용자 확정):

| # | 정한 것 | 갈린 이유 |
|---|---|---|
| 1 | `returned_quantity` **신설**(취소 칸 재사용 안 함) | 취소와 반품은 **돈이 다르게 움직인다** — 합치면 주문만 보고 못 가른다 |
| 2 | **고객이 요청 때 품목·수량을 고른다** | «승인은 요청한 대로 해 준다» 규약 유지. 관리자가 고르면 그게 깨진다 |
| 3 | **반품은 배송비를 안 돌려준다** | 취소와 갈라지는 유일한 지점 — 물건이 이미 나갔다. 🔴 **지금 코드가 이미 그렇다** → 바꾸는 게 아니라 **명문화** |

### 3-1. 🔴 규칙에서 «따라 나온» 것 — 취소와 헷갈리기 가장 쉬운 자리

```
환불액 = 반품금액 − 쿠폰 몫          ← 🔴 적립금 몫을 빼지 않는다
```

반품은 «현금결제분 + 사용적립금» 을 **함께** 적립금으로 돌려주므로 적립금 몫이 환불액 **안에**
이미 있다. ⚠ 그런데도 `returned_point` 를 누적한다 — `remainingUsedPoint()` 를 낮춰야 다음 회차의
분모가 맞기 때문이다.

**숫자로 보면 이렇다**(G-4 와 **같은 표본**을 일부러 썼다):

| 같은 B(15,000)를 뺄 때 | 환불 |
|---|---|
| **취소**(G-4) | **12,001** |
| **반품**(G-10) | **12,858** |

차이 **857** 이 적립금 몫이다. 🔴 **두 테스트 파일이 같은 표본을 쓰는 이유가 이 대비다.**

### 3-2. 🔴 `reversed_earned_point` 를 왜 신설했나

반품은 배송완료 적립을 회수하는데, 전량이던 시절엔 `earned_point` 전액이라 스냅샷 한 칸으로
충분했다. **부분이 생기면 «지금까지 얼마 회수했나» 를 알아야** 다음 회차를 계산할 수 있고,
내림 배분은 **경로 의존**이라 **유도할 수 없다** — V57 이 `cancelled_point` 를 둔 것과 같은 이유.

### 3-3. ⚠ 이름 하나를 고쳤다 — `hasNoRemainingItems` → `isFullyCancelledByItems`

그 값은 **배송비 환불 스위치**인데 실제로 세는 것은 **취소분뿐**이다. 반품이 생기는 순간
**이름이 거짓말을 시작한다** — «남은 게 없다» 로 읽히지만 반품으로 비워진 주문에는 거짓이다.
남은 것이 없는지는 `hasNothingLeft()` 가 답한다.

⚠ **미리 보기와 확정이 같은 코드를 탄다**(`settleRequestedReturns(boolean apply)`) — 08-24 가
부분 취소에서 배분식을 화면에 한 벌 더 두게 됐는데, **서버 안에서는 한 벌만** 갖게 했다.

---

## 4. 마이그레이션 검증 (V58) — WA §2-2 · §2-4-2 · §3-3

| 검증 | 결과 |
|---|---|
| 빈 DB 적용(`esptest`) | ✅ V1→V58 순서대로 · `validate` 통과 · **기동 100.42초** |
| 시각 컬럼 타입 | ✅ `returned_at` = `TIMESTAMP(9) WITH TIME ZONE`(V26 회피) |
| CHECK 양방향 | ✅ 위반 **7종** `ORA-02290` 거부 · **경계값 3종 통과** · rollback 후 0행 |
| 기존 데이터 위반 | ✅ 0건 (주문 72 · 품목 79) |

⚠ **경계값을 일부러 밟았다** — `cancelled + returned = quantity`(전량이 빠진 상태)와
`returned_* = 원본`(전량 반품 상태)이 **통과해야** 한다.

---

## 5. 🔴 오늘 «세어 봐서» 나온 것 셋

### 5-1. 부분 취소가 **판매량을 안 되돌리고 있었다** (G-4 가 두고 간 자리)

아침에 올린 §1-2-1 을 `approveReturn` 에 적용하다 나왔다.

`SoldLine.from(order)` 이 `OrderItem::getQuantity` — **원본 수량**을 읽는다. 그게 `sold_count`
되돌림의 유일한 입력인데, **부분 취소는 이벤트를 아예 안 낸다**(`applyItemCancellation` 은 전량이
빠질 때만 `OrderCancelledEvent` 를 낸다).

→ **부분 취소하고 그대로 두면 판매량이 취소된 만큼 안 줄어든다.** 인기순이 틀어진다.

🔴 **그런데 어긋난 값은 없었다**(실측: 전 상품에서 `sold_count` 가 원장과 일치). 08-24 검증의
**세 주문이 전부 `CANCELLED` 로 끝나** «+전량 / −전량» 이 상쇄됐기 때문이다.
🔴 **즉 우연히 맞았다** — 08-24 §5-4 의 매출 집계와 **같은 모양**이다.

**고친 자리 셋**: ①부분 취소·부분 반품이 **자기 몫만큼** 감소 이벤트를 낸다(`OrderItemCancelledEvent`
신설) ②`OrderCancelledEvent`·`OrderReturnedEvent` 의 줄은 **«남은 수량»** ③`OrderPlacedEvent` 는
그대로 **원본**(주문 시점엔 «원본 = 남은 것» 이다).

⚠ **이벤트를 새로 판 이유**: `OrderCancelledEvent` 의 구독자는 **둘**이다(판매량 + «주문이
취소되었습니다» 알림). 부분 취소에 그걸 그대로 내면 **1개만 뺐는데 «주문이 취소되었다» 고 알린다.**

### 5-2. 🔴 V58 이 **걸려 있던 반품 요청을 승인 불가로 만들었다** → V59

브라우저 검증 표본을 고르려고 운영 주문을 세다가 나왔다.

`return_requested_quantity` 가 `DEFAULT 0` 인데, **이미 `RETURN_REQUESTED` 이던 주문**의 요청은
«주문 전체» 였다(부분이라는 개념이 없던 시절이다). 새 코드는 그걸 **«아무 품목도 안 골랐다»** 로
읽어 승인이 `ORDER-400RI` 로 거부된다 — **고객은 요청해 놓고 영영 처리되지 않고, 관리자는 자기가
뭘 잘못 눌렀는지 알 수 없다.** 대상 **1건**(`20260724-0246`).

⚠ **테스트는 전부 초록이었다.** 테스트가 만드는 주문은 **새 코드로** 요청되므로
«마이그레이션 이전부터 그 상태로 있던 행» 을 **만들 수 없다.**

🔴 **WA §2-2 의 «기존 데이터 위반» 검사가 이걸 못 잡는다** — 그 검사는 «제약을 어기나» 만 보고
**«뜻이 맞나» 는 안 본다.** V58 의 구 jar 절도 `returned_*` 셋에 대해서만 «0 은 아는 값이다» 를
따져 놓고, **성격이 다른 넷째 칸**(«무엇을 요청했나»)에는 그 질문을 안 했다.

**V59 의 조건이 좁은 이유**: «그 주문의 품목이 **전부** 0 인 경우» 로만 좁혔다. 하나라도 0 이
아니면 새 방식으로 고른 요청이고, 0 인 품목은 **고객이 일부러 뺀** 것이다 — 거기까지 채우면
«안 고른 품목이 따라 반품되는» 사고를 마이그레이션이 저지른다.

⚠ **배포 순서가 이걸 완전하게 만든다** — Flyway 는 **새 jar 가 뜰 때** 돌고 그때 구 jar 는 이미
멈춰 있다. 구 jar 가 만든 마지막 요청까지 스냅샷에 들어 있다.

### 5-3. 이월 하나가 5일간 거짓이었다 (§1-1)

---

## 6. 되돌려서 확인했다 — 변형 **26개**

엔티티 11 · 서비스 6 · 화면 9. **전부 빨개진다.**
🔴 **그중 셋은 처음에 안 잡혔고, 그게 진짜 구멍이었다**:

| 안 잡힌 변형 | 왜 못 잡았나 | 새로 쓴 테스트 |
|---|---|---|
| 회차 안 누적(`dItems`)을 분모에서 안 뺀다 | 테스트가 전부 «한 회차에 품목 하나» — 실제 흐름은 여러 품목을 한 번에 고른다 | 한 회차에 두 품목 |
| 분모에서 «기존 반품분» 을 안 뺀다 | 픽스처가 전부 `returnedItemsTotal: 0` | 이미 반품한 이력이 있는 주문 |
| 기본 수량을 «원본» 으로 채운다 | 표본에서 원본과 남은 것이 **같았다** | 3개 중 1개 반품된 품목 |

⚠ 🔴 **두 번은 «0» 을 잘못 읽을 뻔했다** — 한 번은 치환이 제자리로 돌아간 **no-op** 이었고, 한 번은
**컴파일이 깨져** 테스트가 아예 안 돌았다. 둘 다 «빨개진 것 0개» 로 보였다.
**0 이라는 답에는 이유가 둘 있다**(WA §3-3) — 변형이 실제로 먹었는지 **파일을 열어** 확인하고 다시 셌다.

### 6-1. ⚠ 오늘 내가 낸 사고 하나 — 변형을 `git checkout` 으로 되돌렸다

**아직 커밋하지 않은 상태**였다. 첫 변형을 되돌리는 순간 `Order.java`·`OrderItem.java` 의
**그날 작업이 통째로 HEAD 로 날아갔다**(다른 21개 파일은 무사). 같은 변환을 다시 적용해 복구했다.

🔴 **교훈**: 변형 주입 전에 **커밋하거나 파일 사본을 뜬다.** `git checkout` 은 «변형만» 되돌리는
도구가 아니다 — **커밋되지 않은 전부**를 되돌린다.

---

## 7. 전수

| 무엇 | 08-24 | 오늘 |
|---|---|---|
| 백엔드 | 938 | **955** (skipped 0) |
| 프론트 | 278 | **287** |
| 프론트 빌드 | 통과 | **통과** |

⚠ **오늘 그 함정에 한 번 들어갔다** — 환경 없이 돌려 **463개가 조용히 skipped** 된 채
«BUILD SUCCESSFUL» 을 봤다. 🔴 **이미 적혀 있는 것이다** → `TROUBLESHOOTING.md` §1
(«`BUILD SUCCESSFUL` 인데 실제로 아무것도 안 돌았다»). 여기 다시 적지 않는다.
**전수를 셀 때는 `skipped 0` 을 함께 본다.**

⚠ 통합 테스트 실행이 **espdb 에 V58 을 적용했다**(10:12). 운영은 그때부터 배포 전까지 **구 jar +
새 스키마**로 돌았고 안 깨졌다 — V58 의 「구 jar 영향」 주석이 실측으로 확인된 셈이다.

---

## 8. 🔴 배포와 브라우저 검증 — **회귀 0건**

사용자가 백엔드·프론트를 **둘 다** 배포했다(`sudo` 라 내가 실행하지 않았다 — CLAUDE.md).

### 8-1. 배포 확인 — jar 시각으로 판단하지 않았다 (WA §5)

| 본 것 | 결과 |
|---|---|
| Flyway | **V59 08-25 10:36:47** · V58 10:12:44 (둘 다 success) |
| 🔴 V59 가 실제로 채웠나 | `20260724-0246` → `return_requested_quantity` **0 → 1** |
| 배포된 바이트코드 | `Order.class` 에 `applyRequestedReturns`·`previewRequestedReturns`·`hasNothingLeft`·`isFullyCancelledByItems` · `ReturnSettlement.class` · `OrderItemCancelledEvent.class` · `SalesEventListener.onOrderItemCancelled` |
| 배포된 API 계약 | `ReturnRequest.required = [items, reason]` · `items.minItems = 1` · 응답에 `returnableQuantity`·`returned*` |
| 프론트 번들 | 새 문구 전부 있음 · **옛 문구 0건** |

⚠ 🔴 **프론트 확인에서 «0» 을 잘못 읽을 뻔했다** — `/var/www/glassvue-frontend` 가 `root:nginx` 라
파일 grep 이 **「허가 거부」로 0건**을 냈다. **없는 것이 아니라 못 본 것**이었다(WA §3-3).
→ **nginx 가 서빙하는 번들을 `curl` 로 받아** 다시 셌다. 그 길이 «실제로 나가는 것» 을 보므로 더 맞다.
**→ `TROUBLESHOOTING.md` §1(배포·운영)에 한 줄로 옮겼다.**

### 8-2. 🔴 표본을 바꿔야 했다 — 어제 고른 것이 쓸 수 없었다

08-25 오전에 골라 둔 `20260728-0478` 은 **못 쓴다**: **구매자가 이미 탈퇴**했고(`point_account` 도 없다)
품목 하나의 **옵션도 지워져** 있었다. 로그인해서 반품을 요청할 방법이 없다.

⚠ **운영 주문을 다시 세어서 알았다** — 「품목 2개 · 쿠폰 있음」만 보고 골랐고 **«주문자가 살아
있나» 를 안 봤다.** 🔴 08-24 가 `20260820-5108` 을 «품목이 하나뿐이라 못 쓴다» 로 걸러낸 것과
**같은 자리에서 다른 이유**로 걸린 셈이다.

→ 살아 있는 주문 셋으로 다시 짰고, **재고·판매량까지 밟히는 표본은 새로 만들었다**(08-24 와 같은 판단).

### 8-3. 실측 — 예측값이 **전부 그대로** 나왔다

| 표본 | 밟은 것 | 결과 |
|---|---|---|
| `20260724-0246` | **V59** 검증 — 걸려 있던 요청의 승인 | ✅ `RETURNED` · 환불 8,000 · 적립 회수 160 |
| `20260724-0166` | 14개 중 **3개 → 4개** | ✅ 24,000 → 32,000 · 회수 480 → 640 · `DELIVERED` 유지 |
| `20260825-5484` | 🔴 쿠폰 10,000 + 적립금 10,000 + 2품목 × 2수량 | ✅ 아래 |

**`20260825-5484` 회차별** — 화면 「환불 예정」·원장 순변동·재고·판매량이 **넷 다 예측대로**:

| 회차 | 화면 | 원장 | 재고 | 판매량 |
|---|---|---|---|---|
| 지바 1개 | 8,000 | **+7,880** | 지바 987→**988** | 지바 14→**13** |
| 반팔티 1개 | 12,000 | **+11,820** | 반팔티 196→**197** | 반팔티 5→**4** |
| 🔴 **지바1+반팔티1 (한 회차)** | 20,000 | **+19,700** | 989 / 198 | **12** / **3** |

🔴 **수렴이 실측됐다** — `returned_items_total` **50,000**(=`total_price`) ·
`returned_coupon_discount` **10,000** · `returned_point` **10,000** · `reversed_earned_point` **600**.
**넷 다 원본과 정확히 같다.**

- ✅ **판매량 되돌림이 실제로 돌았다**(§5-1 에서 고친 자리). **예전 코드면 첫 두 회차에서 안 움직인다.**
- ✅ 재고 이력 **4줄**, 마지막 둘이 **같은 시각** — 한 회차에 두 품목이 돈 증거다.
- ✅ 감사 원장이 «무엇을 몇 개» 를 남긴다: `… / 지바 1개, 반팔티 1개 반품 / 환불 20000원`.
- ✅ **쿠폰은 전량이 빠질 때만** 복구됐다(마지막 회차 뒤 `used_at` NULL).
- ✅ **적립금 원장 불변식** `SUM(amount) == balance` — **165,005 == 165,005**.

### 8-4. ⚠ 내가 «어긋났다» 고 오독할 뻔한 자리

A·B 뒤 계정이 예측(135,005 / 148,880)과 달라 **125,605 / 178,880** 이었다.
🔴 **원인은 사고가 아니라 «그 사이에 C 주문이 끼어든» 것**이다 — 적립금 10,000 사용 · 적립 600 ·
누적구매 +30,000. 그걸 넣으면 **두 값 다 정확히 맞는다.**
⚠ **기준값은 «언제 잰 것인가» 가 값의 일부다.** 검증 도중에 다른 조작이 들어가면 앞서 적어 둔
기대값은 그대로 못 쓴다 — 08-20 의 «하루에 네 번 움직였다» 와 같은 교훈이다.

### 8-5. 🔴 배송비 갈래(결정 3) — **마지막 하나까지 밟았다**

`20260724-0167`(배송비 3,000 · 적립금 3,120 사용 · 적립 97P · 상품은 삭제됨).

| 확인 | 결과 |
|---|---|
| 🔴 **「결제 금액」** | 반품 전 **7,880** → 반품 뒤 **3,000** (남은 것 0 + 배송비) |
| 환불 | **8,000원** · 적립 회수 **97P** · 원장 순변동 **+7,903** |
| 계정 | 165,005 → **172,908** · 누적구매 148,880 → **144,000** |
| 불변식 | **172,908 == 172,908** |
| 감사 | `20260724-0167 / ZZP-전설의 몽쉘 1개 반품 / 환불 8000원` |

🔴 **이 한 값이 결정 3 의 전부다** — **취소였다면 0원**이다(물건이 안 나갔으니 배송비를 받을 이유가
없다, V57 이 08-24 에 그렇게 정했다). 반품은 물건이 이미 나갔으므로 운임을 안 돌려준다.
⚠ **같은 주문이 두 번째 갈림도 보여 줬다** — 사용했던 적립금 3,120 이 환불액 8,000 **안에** 들어
있다. 취소였다면 3,120 만 계정으로 돌아가고 환불액은 4,880 이다.
**즉 «취소 ↔ 반품» 이 갈리는 두 지점이 한 주문에 다 있었다.**

⚠ 재고·판매량은 안 움직였다 — **그 주문의 상품이 삭제돼 있어서이고, 그게 정상이다**
(`increaseStock` 이 옵션이 없으면 조용히 넘어간다).

✅ **G-10 의 결정 셋이 전부 브라우저로 밟혔다. 남은 미검증 갈래가 없다.**

### 8-6. 🔴 검증이 끝난 «뒤에» 하나 더 나왔다 — **되돌리는 흐름의 알림이 «부분» 을 모른다**

다음 할 일을 고르려고 알림 경로를 읽다가 나왔다. **세 자리가 같은 모양이다.**

**① 🔴 부분 반품인데 「반품이 완료되었어요」 — 오늘 내가 만들었다**

`OrderNotificationHandler.handle(OrderReturnedEvent)` 의 제목·문구가 **고정**이다.
오늘 검증에서 **네 번** 거짓으로 나갔다(실측):

| 시각 | 실제 | 나간 문구 |
|---|---|---|
| 01:56:30 | `0166` 14개 중 **3개**(11개 남음) | 「반품이 **완료**되었어요」 |
| 01:56:36 | `0166` **4개**(7개 남음) | 「반품이 **완료**되었어요」 |
| 02:04:40 | `5484` 지바 1개(3개 남음) | 「반품이 **완료**되었어요」 |
| 02:04:48 | `5484` 반팔티 1개(2개 남음) | 「반품이 **완료**되었어요」 |

🔴 ⚠ **아침에 내가 올린 WA §1-2-1 이 잡으라는 바로 그 종류다** — *«값을 늘렸으면 그 값을 표시·필터하는
쪽을 같이 연다»*. 흐름에 «부분» 을 넣고 **알림 문구를 안 열었다.**
**규약을 올린 당일에, 그 규약이 막으려던 것을 내가 했다.**
⚠ 그 짝이 «조용해서 오래 간다» 고 §1-2-1 이 경고한 그대로다 — **돈은 정확했고 아무것도 안 터졌다.**

**② ⚠ 부분 취소는 알림이 아예 없다** (G-4 가 두고 간 자리 — §5-1 에서 이미 지목했다)

관리자가 대신 부분 취소하면 고객에게 **아무 말이 없다.** 전체 취소는 알림이 간다.
🔴 **오늘 이후로 이 비대칭이 뒤집혔다** — 부분 **반품**은 알림이 가는데(문구는 틀렸어도)
부분 **취소**는 안 간다. 08-11 이 «취소엔 있고 반품엔 없다» 를 고친 것의 **거울상**이다.

**③ ⚠ 전체 취소 알림이 «원본 금액» 을 말한다** — 부분 취소 뒤면 틀리다

`OrderCancelledEvent.from` 이 `order.getTotalPrice()`(원본)를 싣는다. 08-24 데이터로 실측:

| 주문 | 원본 | 이미 빠진 것 | 그때 실제로 취소된 것 | 나간 알림 |
|---|---|---|---|---|
| `20260824-5296` | 35,000 | 25,000 | **10,000** | 「**35,000원** 취소」 |
| `20260824-5297` | 25,000 | 25,000 | **0** | 「**25,000원** 취소」 |

🔴 `5297` 은 그 시점에 취소된 것이 **0원어치**인데 25,000원이 취소됐다고 말한다.
⚠ **§5-1 의 판매량과 같은 뿌리다** — 이벤트가 «원본» 을 싣는다. 그때는 `lines` 를 고쳤고
**`totalPrice` 는 안 봤다.** 🔴 **같은 이벤트 안에서 한 필드만 고쳤다.**

→ **다음 세션의 1순위**(이월 1번). 셋이 한 덩어리이고 크기는 **소**다.

## 9. 문서 정리

- `BACKLOG.md` — **G-10 신설 후 「완료」로 옮겼다**(결정 셋 · 시험 결과 · 변형 표).
- `WORKING-AGREEMENTS.md` — **둘 고쳤다**: §1-2-1 에 한 줄(08-24 이월 1번, §2) ·
  **§2-2-2 신설**(«새 컬럼의 DEFAULT 가 기존 행에 대해 참인지 본다», §5-2 근거 · 사용자 확정).
  ⚠ 🔴 **하루에 규약 둘은 드물다.** 둘 다 «규약을 지켰는데도 났다» 는 같은 이유로 올라갔다 —
  §1-2-1 은 짝을 열고도 «양» 을 안 봤고, §2-2-2 는 «뜻» 을 묻고도 **칸 하나에만** 물었다.
- `BACKLOG.md` — 🔴 **§I 「부분」 축 감사(2026-08-25)를 신설했다** — 항목 열하나 + 보류 목록.
  ⚠ **감사가 보고한 것을 그대로 옮기지 않았다** — 실행 가능한 주장은 **코드·운영 데이터로 다시
  확인**했고, 확인 못 한 것은 §I-11 에 «논증이다» 라고 적어 분리했다.
- `TROUBLESHOOTING.md` — **두 줄 더했다.** ①(§1 배포·운영) *«배포된 파일을 직접 grep 했더니 0건
  → 웹루트가 `root:nginx` 라 못 읽은 것이다»*(§8-1 에서 실제로 걸렸다). ②(§1 DB·마이그레이션):
  *«새 컬럼을 넣은 뒤 **옛 행에서만** 조작이 막힌다 → `DEFAULT` 가 기존 행에 대해 거짓말을 한다»*.
  🔴 **§5-2 는 «증상 → 어디를 의심하나» 가 성립한다** — 다음에 같은 일이 나면 «새로 만든 건
  멀쩡한데 옛 것만 막힌다» 라는 **증상으로** 만나게 된다(오늘도 그 모양으로 만났다).
  ⚠ **예방 쪽(«만들 때 이걸 본다»)은 규약이라 여기 안 적고 WA §2-2-2 로 올렸다** — 자리가 다르다.
- ⚠ 🔴 **핸드오프 §7 에서 한 줄을 지웠다** — 「`BUILD SUCCESSFUL` 인데 skipped」는
  `TROUBLESHOOTING.md` §1 에 **이미 있다.** 같은 사실을 두 곳에 적으면 한쪽만 고쳐진다(CLAUDE.md).

---

## 10. §I 착수 — I-1(매출) · I-2(거절 버튼)

감사 직후에 «지금 값이 틀린 것» 둘만 골라 닫았다(사용자 선택).

### 10-1. 🔴 I-1 — 매출이 56,000원 부풀려져 있었다

**고친 방식이 요점이다.** 식의 사본이 **넷**이었다 — 쿼리 둘 + 클래스 주석 + 통합 테스트.
08-24 가 부분 취소분을 넣을 때 손으로 넣었고, 08-25 가 부분 반품분을 만들며 **넷을 다 안 열었다.**
🔴 **테스트 사본도 같이 틀려서 아무것도 안 빨개졌다.**

→ `OrderStatsRepository.ITEM_SALES` **상수 한 곳**으로 모았다. `@Query` 값은 컴파일 상수 식이어야
해서 텍스트 블록을 포기하고 `static final String` 연결로 조립했다.

⚠ 🔴 **상수는 «식이 옳은가» 를 안 지킨다 — «사본이 안 갈리는가» 만 지킨다.** 그래서
`matchesDirectAggregate` 는 이제 식을 독립 검증하지 못한다. **값으로 보는 테스트를 새로 썼다**:
`partialReturnReducesRevenue` — 3개 중 1개 반품 → 매출이 정확히 10,000 줄어들고
**주문은 여전히 `DELIVERED`** 인 것을 함께 단언한다(대조군이 없으면 «상태로 빠져서» 통과하는
다른 것을 재게 된다, WA §3-3).

**되돌려서 확인**: 08-24 상태로 변형하니 새 테스트가 빨개진다. ✅ 옛 사본 대조 테스트는
**안 빨개진다** — 상수를 공유하므로 설계대로다.

### 10-2. 🔴 I-2 — 목록의 「거절」이 2주간 항상 400

confirm → **사유 패널**. 🔴 **이 파일이 이미 답을 갖고 있었다** — 관리자 대행 취소(B-25)가
«사유가 필수면 confirm 이 아니라 패널» 로 돼 있다. **그걸 안 따른 것이 원인**이라 같은 패턴을 썼다.

✅ **이 화면의 첫 뷰 테스트**(5개)를 만들었다 — 반품 승인·거절 자리만. 화면 전체는 §I-8 이다.
**변형 넷 전부 빨개진다.** ⚠ 그중 하나는 처음에 **정규식이 안 걸린 no-op** 이라 0 으로 보였다 —
파일을 열어 확인하고 다시 셌다. 🔴 **오늘만 세 번째다**(§6 에 둘, 여기 하나).

⚠ **범위를 한 줄 넘겼다** — 같은 화면의 승인 confirm 이 «**결제금액을** 적립금으로 환불합니다»
였다(§I-7 의 일부). 방금 연 파일에 **관리자에게 거짓을 말하는 줄**을 남겨 둘 수 없어 고쳤다.

### 10-3. ⚠ 커밋 하나에 둘을 담았다 — 내 실수

`bd6a741` 의 메시지는 **I-1 만** 말하는데 **I-2 의 변경도 들어 있다**(`OrderAdminView.vue`·
`OrderAdminView.test.js`). `git add -A` 로 한꺼번에 담았다. 🔴 **이미 push 돼 있어 되쓰지 않았다** —
히스토리 재작성은 부탁받지 않은 조작이다. 여기 적어 두는 것으로 대신한다.

**전수**: 백엔드 **956** · 프론트 **292** · 프론트 빌드 통과 · 둘 다 skipped 0.

⚠ **이 시점엔 배포 전이었다** — I-3·I-4 와 한 배포에 묶었다(§11).

## 11. §I-3 · I-4 + 알림 넷 — 「부분 반품한 고객이 돈 이야기를 못 본다」

⚠ **사용자 판단으로 I-1·I-2 와 한 배포에 묶었다** — 같은 파일을 열고 브라우저 확인도 한 번에 끝난다.
🔴 대가는 «운영 매출이 그동안 계속 56,000원 틀린 채» 였다는 것이다. 연습 단계라 감수했다.

### 11-1. 화면 (I-3 · I-4)

- **반품 카드**: 렌더 조건에 `returnedItemsTotal > 0` 을 더했다.
  🔴 ⚠ **조건만 넓히면 함정이 있다** — `returnStatusText` 의 마지막 갈래가 «반품 요청이 거절됨» 이라
  **부분 반품된 주문이 거절됨으로 뜬다.** 갈래를 넷으로 늘리고 **순서를 규약으로 적었다**:
  `RETURNED → 요청중 → 거절 → 부분 반품`. 둘 다 참일 때 **거절이 이긴다**(대조군 테스트로 고정).
- **합계 카드**: «반품으로 빠진 것» 섹션 신설 + 총액 라벨이 반품도 본다.
  🔴 ⚠ **취소 섹션과 모양이 다르다** — «돌려받은 적립금» 줄을 **일부러 안 넣었다.** 반품 환불액에는
  쓴 적립금이 **이미 들어 있어** 따로 적으면 두 번 받은 것처럼 읽힌다. 그 «없음» 을 테스트가 단언한다.
- **적립 문구**: `reversedEarnedPoint` 를 응답에 실어 «준 것 − 회수한 것» 을 말한다.

### 11-2. 알림 넷

① **부분 반품 문구를 갈랐다** — «일부 반품이 완료되었어요» + «무엇이 몇 개».
② 🔴 **부분 취소 고객 알림을 신설했다** — 여기까지 **아예 없었다**(08-11 이 반품에 대해 고친 것의 거울상).
   ⚠ **전량이 빠지는 경우엔 알림을 안 낸다** — 뒤이어 `OrderCancelledEvent` 가 나가 **두 번 뜬다.**
   🔴 **이벤트 자체는 계속 낸다** — 판매량 되돌림이 이 회차 몫을 거기에만 싣기 때문이다.
   **«구독자마다 무엇을 하느냐를 가르는» 자리**이고, 이걸 안 봤으면 알림이 겹쳐 나갔을 것이다.
③ 전체 취소 알림이 **«이번에 취소된 금액»** 을 말한다(`totalPrice` → `cancelledAmount`).
   🔴 ⚠ **같은 이벤트의 `lines` 는 08-25 오전에 고쳤는데 이 필드는 안 봤다** —
   **한 이벤트 안에서도 필드마다 물어야 한다**(WA §2-2-2 «컬럼마다 따로 묻는다» 의 이벤트 판).
④ 관리자 목록 승인 confirm (§10-2 에서 함께).

### 11-3. 🔴 변형 아홉 — **셋이 처음에 «0» 으로 보였다**

| 처음 0 이었던 변형 | 진짜 이유 |
|---|---|
| 반품 카드 조건 · 총액 라벨 | `sed` 구분자 `|` 가 조건문의 `||` 와 충돌해 **아예 안 걸렸다** |
| 전체 취소 금액 | 핸들러 테스트가 이벤트를 **직접 만들어** `from()` 을 안 탔다 → 서비스 테스트에 단언을 더해 닫았다 |

⚠ 🔴 **오늘만 «0 을 잘못 읽을 뻔한» 것이 네 번째다**(§6 둘 · §10-2 하나 · 여기 하나).
**WA §3-3 «0 이라는 답에는 이유가 둘 있다» 를 하루에 네 번 썼다.**

**전수**: 백엔드 **960** · 프론트 **298** · 프론트 빌드 통과 · 둘 다 skipped 0.

### 11-4. 검증 **전에 박아 둔** 예상값

⚠ 🔴 **순서가 중요하다** — 아래는 배포 **전에** 적어 둔 값이고, §11-5 의 결과는 그 뒤에 잰 것이다.
나중에 맞춰 적은 것이 아니다. 08-24 가 «전수 초록인 채로 회귀 넷» 을 만난 뒤로 지키는 방식이다.
⚠ **A 의 «283,000» 은 «08-25 17:00 기준, 새 주문이 없다면» 이라는 전제가 붙은 값이다** —
그 전제를 안 적어서 §11-6 의 오독이 났다.

**A. 매출** — 관리자 통계 「전체」: **339,000 → 283,000** (차이 56,000, `20260724-0166` 한 건).

**B. `20260724-0166` 주문 상세** — 🔴 **부분 반품이 살아 있는 유일한 주문**이라 I-3·I-4 를 그대로 밟는다.
(112,000 · 쿠폰 0 · 적립금 0 · 적립 2,240 · 배송비 0 / 14개 중 **7개 반품** · 적립 회수 1,120)

| 볼 것 | 예상 |
|---|---|
| 🔴 **반품 카드가 뜬다** | 상태 «**일부 반품 완료 (남은 품목은 그대로)**» — 예전엔 **카드가 아예 없었다** |
| 합계 카드 「반품된 품목」 | **−56,000원** |
| 합계 카드 「환불 적립금」 | **56,000원** |
| 합계 카드 「회수된 적립」 | **−1,120원** |
| ⚠ 「돌려받은 적립금」 줄 | **없어야 한다**(반품 환불액에 이미 포함) |
| 총액 라벨 | «**남은 결제 금액**» **56,000원** |
| 적립 문구 | «이 주문으로 **1,120원** 적립되었어요 (반품으로 **1,120원** 회수)» — 예전엔 **2,240** 이라 했다 |

**C. 부분 취소 알림** — `20260727-0253`(김기현팀 · 몽쉘 10개 **6개** · 30,000 · `ORDERED`).
2개를 부분 취소하면 → 알림 «**주문 일부가 취소되었어요**» / «몽쉘 10개 2개 취소되었습니다.
**10000원**이 환불됩니다.» ⚠ 재고는 **안 움직이는 게 정상**(옵션 삭제됨). 판매량은 **10 → 8**.

**D. 목록의 「거절」 버튼** — `20260724-0166` 으로 반품을 하나 요청해 두고 관리자 **주문 목록**에서
「거절」을 누른다 → 🔴 **사유 패널이 뜨고**, 사유를 적으면 실제로 거절된다(예전엔 **항상 400**).

⚠ **D 를 하면 0166 의 `returnReason` 이 덮이고 거절 기록이 남는다** — B 를 **먼저** 볼 것.
### 11-5. ✅ 배포와 브라우저 검증 — **회귀 0건** (두 번째)

배포 확인(WA §5): jar **17:30:50** ↔ 서비스 기동 **17:31:58** · 프론트 번들에 새 문구 **여섯 다 있음**.
🔴 **배포된 매출 SQL 을 바이트코드에서 직접 읽었다** — `returned_*` 가 실제로 들어 있다.

⚠ 🔴 **여기서도 «0» 을 잘못 읽을 뻔했다**(오늘 다섯 번째). `javap -c` 로 클래스를 훑으니
`returned_items_total` 이 **0건**이었는데, **애노테이션 값을 안 찍기 때문**이었다 —
`@Query` 는 애노테이션이라 바이트코드 명령에 안 나온다. `strings` 로 상수 풀을 보니 **4건**이었고
SQL 전문이 그대로 나왔다.

| 밟은 것 | 결과 |
|---|---|
| **A. 매출** | ✅ **283,000** — §11-6 참고 |
| **B. `20260724-0166` 표시** | ✅ 반품 카드가 뜬다 · 반품금액 56,000 · 환불 적립금 56,000 · 적립 회수 1,120 · 남은 결제 금액 56,000 · 남은 적립 **1,120**(원본 2,240) |
| **C. 부분 취소 알림** | ✅ «**주문 일부가 취소되었어요** / 몽쉘 10개 2개 취소되었습니다. **10000원**이 환불됩니다.» · 판매량 **10 → 8** |
| **D. 목록의 「거절」** | ✅ 사유 패널로 실제 거절됐다(`20260825-5633`, 사유 «312») · **요청 수량도 0 으로 지워졌다** |

### 11-6. ⚠ 매출이 «293,000» 으로 보였다 — 또 기준값 문제였다

예상 283,000 인데 실측이 **293,000** 이었다. 🔴 **사고가 아니라 «검증 중에 새 주문이 끼어든» 것**이다 —
`20260825-5633`(상품 15,000 − 쿠폰 5,000 = **매출 기여 10,000**). 그 주문을 빼고 다시 세니
**정확히 283,000** 이었다.

⚠ 🔴 **오늘 이 실수가 두 번째다**(§8-4 에서 A·B 뒤 계정 값에 같은 일이 있었다).
**기준값은 «언제 잰 것인가» 가 값의 일부다** — 검증 도중에 다른 조작이 들어가면 앞서 적어 둔
기대값은 그대로 못 쓴다. → **다음부터는 예상값 옆에 «이 값을 잰 시각과 전제» 를 함께 적는다.**

### 11-7. 마감 기준값

`product_variant` **10** · `stock_history` **60** · `point_history` **44** · `notification` **149** ·
`orders` **74** · 매출(상품) **293,000**.


## 이월 (다음 문서로 반드시 옮길 것)

> WORKING-AGREEMENTS §4-1. 다음 날 문서가 그대로 물려받아 시작한다. 처리했으면 지운다.
> ⚠ 🔴 **물려받을 때 «아직 참인가» 를 확인한다** — 08-24 것 하나가 5일간 거짓이었다(§1-1).

### 다음 세션에서 시작할 것

1. ✅ **§I-1 ~ I-4 + 알림 넷은 배포·검증까지 끝났다** — **회귀 0건**(§11-5).
   🔴 **매출이 실제로 맞아졌다**(새 주문을 뺀 기준 283,000).

   **다음**: **I-5**(TOP N 이 원본 합산) · **I-6**(관리자 부분 취소로 주문을 비우면 행위자 미기록) ·
   **I-7**(관리자·고객 «목록» 이 부분을 못 본다) · **I-8**(부분 반품 HTTP 통합 테스트 · 권한 비대칭) ·
   **I-9**(반품 기한 — 🔴 **결정 표부터**, 코드보다 «N일» 이 본체다).
   ⚠ **I-11 의 보류 항목**(동시성 · 잔돈 음수)은 **재현 테스트를 먼저** 만든 뒤 판단한다.

2. ✅ **규약으로 올렸다 — WA §2-2-2** (2026-08-25 사용자 확정).
   *«새 컬럼의 `DEFAULT` 가 **기존 행에 대해 참인지** 본다 — 제약을 어기는지만 보지 말고
   **«뜻이 맞는지»** 를 본다.»* 근거는 §5-2 다.
   ⚠ **전례를 세어 봤다 — 이 질문은 이미 두 번 «제대로» 물어졌다**: V55(G-9)가 «모르는 값은 NULL»
   로 갔고, V57 이 «0 은 모르는 값이 아니라 아는 값이다» 를 따졌다.
   🔴 **오늘이 처음으로 «묻고서 틀린» 경우다** — V58 이 `returned_*` 셋에는 그 질문을 했는데
   **성격이 다른 넷째 칸**에는 안 했다. 그래서 규약의 첫 줄이 **«컬럼마다 따로 묻는다»** 다 —
   물을 줄 몰라서가 아니라 **한 번 묻고 넘어가서** 났기 때문이다.

3. ⚠ **아침에 기준값을 다시 센다.** 🔴 **검증 뒤 마감값**: `product_variant` **10** ·
   `stock_history` **60** · product alive / trash **7 / 1** · `point_history` **44** ·
   `notification` **149** · `orders` **74** · **매출(상품) 293,000**.
   ⚠ 이력은 `product_id` 로 센다.
   **김기현팀**: 적립금 **172,908** · 누적구매 **144,000** · **SILVER**.
   ⚠ 🔴 **누적구매가 212,880 → 144,000 으로 크게 줄었다** — 오늘 반품 일곱 회차의 결과이고
   **정상이다**(등급은 SILVER 를 유지했다).

4. 🔴 **`ZZ-123123` 이 08-27 13:16 경 영구 삭제된다** — 배치가 매시간 돈다. **낮 시각이라 볼 수 있다.**
   → 그때 `product_variant`·`stock_history` 를 다시 세면 **줄어든 만큼**이 CASCADE 다
   (옵션 2 · 이력 3 → variant 8 · history **57** 이 될 것이다 — 08-21 예측을 **검증 뒤 마감값** 기준으로 고쳤다).

5. 🔴 **다음 기능 후보** (오늘 처리한 것은 뺐다):
   ✅ **G-10 부분 반품은 오늘 닫았다**(배포·브라우저 검증까지 — §8).
   - **G-6 등급 혜택 / G-7 최근 검색어**(소).
   ✅ **부분 취소 고객 알림도 오늘 붙였다**(§11-2 ②) — G-4 가 두고 간 비대칭이었다.
   - **G-6 등급 혜택 / G-7 최근 검색어**(소) 외에는 **§I 의 남은 항목이 우선**이다(위 1번).
   ⚠ **뷰 테스트**: `OrderAdminView` 에 **씨앗 5개를 만들었다**(반품 승인·거절 자리만, §10-2).
   🔴 **화면 전체는 아직 아니다** — 발송·배송완료·대행 취소·필터가 비어 있다(§I-8).
   ⚠ 🔴 **`ProductDiscountAdminView` 는 이미 있다**(§1-1 — 이월이 틀렸다).
   ⚠ **마케팅 발송은 후보가 아니다**(08-20 §12 결론 유지) · **F-2 는 아직 아니다** ·
   **B-26 나머지는 «조건이 갖춰지면»** · **MSA 는 아직 아니다.**

6. ✅ **push 와 배포를 둘 다 했다.** push 는 `7066e75..b3682a7` 로 origin 과 동기화됐고,
   **배포도 두 번 했다**(G-10 → 10:43 · §I-1~I-4 → 17:30). 운영에 도는 것은 **오늘 코드**다.
   ⚠ **push 와 배포는 다른 일이다** — 오늘 그 사이가 벌어진 적이 있어 적어 둔다(오전에 push 만 한 채
   «배포됐다» 로 읽힐 뻔했다).

### 조건부 잔여

**08-24 §조건부 잔여를 물려받고, 오늘 것을 더한다.** ⚠ 개수를 여기 적지 않는다 — 목록이 곧 개수다.

#### 오늘 더해진 것

- ✅ **부분 반품·부분 취소를 운영에서 밟았다**(§8 · §11-5) — **두 번 다 회귀 0건**.
  🔴 **살아 있는 표본 다섯이 남는다**:

  | 주문 | 무엇을 밟는 자리인가 |
  |---|---|
  | `20260724-0166` | 🔴 **부분 반품이 살아 있는 유일한 주문**(14개 중 7개 반품 · **7개 남음** · `DELIVERED`). «부분 반품 뒤 다시 요청할 수 있나» 와 **부분 반품된 주문의 화면 표시**(§I-3·I-4)를 밟을 유일한 자리다 — **지우지 말 것** |
  | `20260727-0253` | **부분 취소가 살아 있는 유일한 주문**(6개 중 2개 취소 · `ORDERED`). ⚠ 옵션이 삭제돼 **재고는 안 움직인다**(정상) |
  | `20260825-5633` | **반품 거절 표본**(§I-2 로 고친 목록 거절로 거절됨 · 사유 «312»). 요청 수량이 0 으로 지워진 상태 |
  | `20260724-0246` | `RETURNED` — V59 가 채운 옛 요청 |
  | `20260825-5484` | `RETURNED` — 쿠폰 + 적립금 배분 표본(수렴이 실측된 자리) |

- ⚠ **`20260728-0478` 은 반품 표본으로 못 쓴다** — 구매자 탈퇴 · `point_account` 없음 · 옵션 하나 삭제됨(§8-2).
- ⚠ **김기현팀의 쿠폰 상태**(2026-08-25 실측): 「가입 쿠폰 10000원」·「ZZ-발급마감쿠폰테스트」·
  「테스트 쿠폰」·「5000」이 **사용 가능**, 「가입 쿠폰 낭낭」만 5633 이 썼다.
  🔴 「가입 쿠폰 10000원」은 5484 전량 반품으로 **복구된 것**이다 — 다음 검증에 쓸 수 있다.
- ⚠ **배분식이 서버와 화면 두 벌이다**(부분 취소와 같은 자리). **어긋나면 잡히게** 해 뒀다 —
  뷰 테스트가 서버 테스트와 **글자 그대로 같은 숫자**를 단언한다(12,858 · 17,143 · 8,333 · 2,142 · 2,857).
- ⚠ **`returnReason` 은 여전히 한 칸이라 회차마다 덮인다.** 부분 반품으로 **한 주문이 반품
  사이클을 여러 번** 돌 수 있게 됐으므로 이 손실이 08-11 때보다 커졌다. 🔴 **수량은 누적되지만
  «왜» 는 마지막 것만 남는다.** 필요해지면 별도 테이블이 답이다(컬럼이 아니다).
- ⚠ **`order_item.returned_at` 도 마지막 시각만** 남는다(`cancelled_at` 과 같다).
- ⚠ **매출 식이 상수 한 곳(`OrderStatsRepository.ITEM_SALES`)에 있다**(§10-1). 🔴 그래서
  `matchesDirectAggregate` 는 **식을 독립 검증하지 못한다** — «식이 옳은가» 는
  `partialReturnReducesRevenue` 하나가 지킨다. **그 테스트를 지우면 아무도 안 지킨다.**
- ⚠ 🔴 **`topProducts` 는 아직 원본을 합산한다**(§I-5) — 같은 파일의 세 쿼리 중 이것만 안 고쳤다.
  관리자 통계의 TOP 과 상점 인기순(`sold_count`)이 **서로 다른 말을 한다.**
- ⚠ **부분 취소 알림은 «전량이 빠지는 경우» 를 건너뛴다**(§11-2 ②). 그 판단이 이벤트의
  `orderFullyCancelled` 한 칸에 걸려 있다 — 그 칸이 틀리면 알림이 두 번 뜨거나 아예 안 뜬다.

#### ⚠ 그대로 물려받는 것

- 🔴 **`ZZ-123123`** — 유예 만료 **08-27 13:16**. 살아 있음(위 이월 4번).
- 🔴 **08-24 검증이 남긴 어긋난 값 셋** — `김기현팀` 적립금 **+1,428** · 지바 재고 **+1** ·
  반팔티 재고 **+1**. 「로컬이라 그냥 둔다」 결정. ⚠ 되돌리고 싶어지면 **적립금은 원장에 «보정»
  줄을 남기는 쪽**이 맞다(잔액만 고치면 `SUM(history) == 잔액` 이 깨진다).
- **세일 표본 `ZZ-세일검증`** — 세일 둘 다 `ENDED`. 🔴 `priceDelta ≠ 0` 인 옵션을 가진 **유일한
  상품**이라 곱 순서를 다시 밟을 대상이다. **정리는 아직 안 했다.**
- **G-9 표본 `20260820-5108`**(`ORDERED`) — 품목이 하나뿐이라 부분 취소가 곧 전체 취소가 되어
  표본이 사라진다. ⚠ 앞으로도 같다.
- **16번의 미결제 주문 둘**(`20260813-4185`·`20260813-4186`) · **V51 검증이 바꾼 옛 주문 둘**
  (`20260724-0249` `SHIPPED` · `20260727-0257` `RETURNED`) · **계정 `김기현팀4`**(USER) ·
  **08-24 검증 주문 셋**(`20260824-5295`·`5296`·`5297` — `5296` 은 **사고의 증거물**이다).
- 🔴 **`priceFilterUsesSalePrice` 의 간헐 실패 원인은 여전히 모른다**(08-19 §7). 구조를 바꿔
  **회피**했을 뿐이다. ⚠ 다음에 «가끔 빨개지는 테스트» 를 만나면 여기가 두 번째다.
- ⚠ **「캐시 60초 지연」은 끝내 실측하지 못했다.**
- ⚠ **`COUPON_WELCOME_SET` 은 아직 브라우저로 안 밟았다.**
- ⚠ **`recordAdmin` 은 `actor == null` 을 견디는데 감사는 못 견딘다**(08-20 §7-7). 그대로 뒀다.
- ⚠ **`recentlyViewed` 의 세일 값은 낡을 수 있다** — localStorage 스냅샷이다.
- ⚠ **`deleted_at` 오프셋이 행마다 섞여 있다**(08-19 §1-1). 값은 맞다. **원인 미상.**
- 🔴 **「화면이 유일한 방어」는 여전히 «두 종류에 두 번» 이다.** 오늘 새로 난 것 없다. **아직 안 올린다.**
- ⚠ **08-21 이월의 나머지는 그대로 물려받는다** — 정가 칸 원인 미상 · 겹침 금지가 앱에만 있다 ·
  `SignupView` 의 `border-ink-200` · 막힌 것 셋(배송완료 리뷰 · 탈퇴 회원 관리자 조작 ·
  탈퇴 시 장바구니 정리).
