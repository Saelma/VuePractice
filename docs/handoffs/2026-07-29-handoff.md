# 2026-07-29 작업 정리 (문서 개시 — 작업 착수 전)

> **7/28 핸드오프에서 이어서 진행한다.** 그 문서의 이월 미완은 **1건**(`GET /api/admin/coupons`
> 권한 테스트)이고, 「다음 기능 후보」로 회원관리 심화·D 대기 항목을 넘겨줬다.
>
> 착수 전 확인(2026-07-29): `main`(=`/home/ecstel/work`) HEAD == 작업 브랜치 `daily-status-review` HEAD
> == `74f3139`, 두 워크트리 워킹트리 **깨끗**. `git log main..daily-status-review` **비어 있음**
> (WORKING-AGREEMENTS §5 배포 전제 충족 상태). 7/28 작업분은 전부 배포·검증 종결.
>
> 2일치 대조(§1-1): 7/27 이월 4건(WebP 잔재·김기현팀 적립·플래키 테스트·DHCP 예약)은 7/28 문서가
> 전부 물려받았다 — **조용히 빠진 항목 없음**.

---

## 0. 오늘의 커밋

| 해시 | 메시지 |
|---|---|
| `659bc81` | docs: 07-29 핸드오프 개시 — 7/28 이월 인수 |
| `1b710ab` | docs: 07-29 핸드오프 §0 해시 확정 |
| `31ee367` | docs: 인프라·관측 도구 재검토(LGTM·상용APM·Kafka·k8s·MQTT) + B-13 이메일 수집 |
| `5a0a577` | docs: 07-29 핸드오프 §0 해시 확정 (2차) |
| `c9f35ad` | test: GET /api/admin/coupons 권한 테스트(401/403/200) — 7/28 이월 해소 |
| `7c294b3` | docs: 쿠폰 권한 테스트(§3) 반영 — 이월 미완 0건 |
| `e5ea7ab` | docs: 07-29 §0 표 최종 대조 + main ff머지 기록 |
| `491c141` | docs: 학습 트랙 신설 + 상용 APM 판단 정정(유료→볼 게 없어서) |
| `e2c5683` | docs: 07-29 §0 해시 확정 (3차) |
| `dd0f667` | feat: 이메일 수집(B-13) — 가입 필수 + 설정 화면 등록/변경 |
| (이 커밋) | docs: B-13(§5) 반영 + BACKLOG 완료 이동 |

> ⚠ §4-4: 이 표는 **작업을 끝낸 뒤 다시 대조한다** — `git log --oneline --since="2026-07-29 00:00"`.
> 문서를 닫는 커밋과 그 뒤 후속 커밋이 구조적으로 빠지는 자리다.

---

## 1. 오늘 무엇을 할 것인가 — 후보와 근거

7/28 에 BACKLOG 의 **표준 기능(A·B·C)이 전부 소진**됐다. 남은 건 성격이 다르다 — 규약 부채,
품질 정리, 그리고 조건 미충족으로 대기 중인 D 항목들. 우선순위 근거는 이렇다.

### 1-1. 먼저 할 것 — `GET /api/admin/coupons` 권한 테스트 (이월 1건)

**규약 부채라 다른 후보보다 앞선다.** WORKING-AGREEMENTS §2-4 는 새 관리자 엔드포인트에 권한
테스트(401/403/200)를 **"반드시"** 붙이라는 필수 규약인데, 7/28 쿠폰 관리 화면에서 빠뜨렸다.
사용자와 **다음 세션 처리로 합의**한 항목이다.

- 현황(실측): `CouponFlowIntegrationTest` 에 `POST /api/admin/coupons`(생성)·`/{id}/issue`(발급)의
  비관리자 403 은 **있다**. 빠진 건 **`GET /api/admin/coupons`(목록)** 하나 — 미인증 401 · 비관리자 403 · ADMIN 200.
- 붙일 자리: `CouponFlowIntegrationTest`(같은 도메인에 이미 admin 케이스가 있어 여기가 자연스럽다).
  패턴은 `AdminMemberIntegrationTest`.
- 크기: **소**(반나절 미만). 마이그레이션·배포 없음(테스트만 추가 — 프로덕션 코드 무변경).

### 1-2. 그다음 후보 (사용자 선택 필요 — 착수 시 확정)

| 후보 | 성격 | 크기 | 근거 |
|---|---|---|---|
| **플래키 테스트 정리** (`NoticeQueryServiceIntegrationTest`) | 품질 부채 | 소 | Redis 조회수 키가 테스트 간 격리 안 됨. 7/24부터 이월돼 온 항목 — "가끔 빨간불"은 회귀 판단을 흐린다 |
| **디자인 잔여 폴리싱** (회원 상세 카드화, 상태 뱃지 pill) | UI | 소~중 | 7/28 §14-3 이 남긴 "필요해지면" 항목. 로드맵 자체는 종결 |
| **감사 대상 확대** (주문 발송·반품 승인·상품 편집도 `AdminActionEvent`) | 기능 심화 | 중 | 지금은 회원 조작만 남는다. ⚠ `AuditAction` CHECK 제약도 함께 넓혀야 함(Oracle enum CHECK 트랩) |
| **정지 사유·기간** | 기능 심화 | 중 | 지금은 정지/해제 이진 상태뿐 |
| **이메일 수집 폼** (가입·설정) | D 선행 | 소~중 | `member.email`(V29)은 컬럼만 있고 **채우는 경로가 없어 전부 NULL**. SMTP 발송(D)의 선행 조건이고, 발송 채널 없이도 지금 할 수 있는 유일한 조각 |

> ⚠ **D 대기 항목은 오늘 착수 대상이 아니다** — SMTP 실발송·알림 실발송·PG 실결제는 조건
> (발송 채널·MSA·PG사)이 미충족이다. 단 위 표의 **이메일 수집 폼**은 조건 없이 가능한 선행분이다.

### 1-3. 후보의 문서 자리 정리 (사용자 요청)

위 후보들이 BACKLOG 에 있는지 대조했다. **거의 다 있었고, 빠진 건 하나**였다.

| 후보 | 문서 자리 | 조치 |
|---|---|---|
| 감사 대상 확대 · 정지 사유/기간 | BACKLOG 「회원 관리 심화」 | 이미 있음 |
| 디자인 잔여(회원상세 카드화·뱃지 pill) | BACKLOG B-12 「남은 여지」 | 이미 있음 |
| **이메일 수집 폼** | D 표 안에 **조건부로만** 묻혀 있었음 | ⚠ **B-13 으로 승격**(아래) |
| 플래키 테스트 정리 | — | **BACKLOG 에 넣지 않는다.** 제품 기능이 아니라 그날 작업에서 파생된 품질 부채라 **이월**이 자리다(BACKLOG 「쓰는 법」) |

> **B-13 이메일 수집을 D 에서 꺼낸 이유**: D 의 정의는 **"조건 대기"** 인데, 수집은 조건이 없다.
> 조건이 없는 일을 조건 대기 표에 두면 **영영 안 하게 된다.** 발송(SMTP)만 D 에 남긴다.
> 값의 근거: 수집은 시간이 지나야 쌓이므로 **발송 기능을 먼저 만들면 그날 발송 대상이 0명**이다.

---

## 2. 인프라·관측 도구 재검토 (사용자 제안 — LGTM · MaxGauge · Dynatrace · Jennifer · Kafka · k8s · MQTT)

사용자가 이후 도입 후보로 위 도구들을 제안했다. **코드 작업이 아니라 로드맵 판단**이라 자리는
`ARCHITECTURE.md §6`(미래 인프라 로드맵)이다 — BACKLOG 는 *제품 기능*, ARCHITECTURE 는 *설계·인프라*.

### 2-1. ⚠ 대조 결과 — 대부분 **이미 결정이 있었다**

| 제안 | 문서의 기존 상태 | 갱신 |
|---|---|---|
| **LGTM** | §6 관측 스택이 이미 **Alloy+Loki+Prometheus+Grafana(+Tempo)** | **새 도입이 아니라 같은 것의 다른 이름**임을 명시. 차이는 **M(Mimir) 하나** — Mimir 는 Prometheus 의 *장기보존·수평확장* 백엔드라 단일 VM 엔 비용만 는다 → **Prometheus 유지**, 보존기간·다중 인스턴스 시 remote_write 로 승격 |
| **Jennifer** | §7 에 이미 "상용/유료 ❌" | 유지 |
| **Dynatrace·MaxGauge** | **언급 없었음** | 신규 판단 추가. 둘 다 유료. ⚠ **MaxGauge 는 결이 다르다** — 앱 APM 이 아니라 **Oracle DB 전문**. 무료 대체는 P6SPY(완료)·`v$` 뷰·**Statspack**·`oracledb_exporter`. ⚠ **AWR/ASH 는 무료가 아니다**(Diagnostic Pack 유료 옵션) — 습관적으로 쓰기 전에 확인할 것 |
| **Kafka** | CLAUDE.md·§6 에 **"RabbitMQ 확정, Kafka 아님"**(2차 목표) | 유지 + **왜**를 보강. 우리 이벤트는 *주문완료→알림·적립* 같은 **작업 지시(task queue)** = RabbitMQ 결. Kafka 는 **replay·이벤트소싱·CDC·고스루풋**에서 값이 나는데 지금 셋 다 없다. **순서를 지키면 둘의 차이를 몸으로 알게 된다** |
| **Kubernetes** | §7 에 "단일 VM 엔 오버 ❌" | 유지하되 **학습 목적 예외 조건**을 명시 — 운영 VM 이 아니라 **별도 환경 + k3s/kind**, **Docker 화 이후**(이미지가 없으면 올릴 게 없다) |
| **MQTT** | **언급 없었음** | ❌ 도입 안 함. IoT 디바이스용 경량 pub/sub 이라 **이 도메인에 대응 문제가 없다**. 닮은 자리 둘은 이미 답이 있다 — 실시간 푸시=**SSE 완료**, 서비스 간=**RabbitMQ**. 뒤집히는 조건: 실제 디바이스(키오스크·POS·배송단말) |

### 2-2. 관통하는 판단 근거

- **볼 게 없는 상태에서 도구부터 깔지 않는다** — Sentry 를 보류한 것과 같은 판단(§1 "미리 만들지 않는다").
  관측 도구의 값은 **트래픽·서비스 수**에서 나오는데 지금은 단일 VM·단일 모놀리스·주문 수십 건이다.
- 도입 순서에 의미가 있다: **RabbitMQ → (필요하면) Kafka**, **Docker → (학습이면) k8s**,
  **Prometheus → (모자라면) Mimir**. 뒤에 것부터 깔면 앞의 것이 왜 부족했는지를 영영 모른다.

### 2-3. ⚠ 앞선 판단을 바꿈 — "유료라서 제외" 는 틀린 근거였다 (사용자 전제 확인)

위 2-1 을 적을 때 상용 APM 3종을 **"상용/유료 → ❌ 제외"** 로 닫았다. 사용자가 전제를 정정했다:
**이 프로젝트는 학습용이고 운영으로 쓰지 않으므로 비용은 제약이 아니다.** 나중에 도입할 수도 있으니
문서에 그렇게 남겨 달라는 요청. WA §1-2 대로 **"앞선 판단을 바꿉니다"** 를 밝히고 바꿨다.

**바꾼 것 / 안 바꾼 것을 갈라 적는다** — 비용은 제외 사유 **중 하나**였을 뿐이다.

| | 처음 판단 | 지금 |
|---|---|---|
| 비용·라이선스 | 제외 사유 | ❌ **철회** — 학습용이라 제약 아님 |
| **볼 게 없다** | 제외 사유 | ✅ **유지 — 이게 진짜 이유였다.** 단일 모놀리스·주문 수십 건이면 무엇을 깔아도 화면이 빈다 |
| 평가판 기간(15~30일) | (제외의 부속) | **순서 문제로 재해석** — 볼 게 준비된 뒤 짧게 태워야 한 번뿐인 기회를 안 버린다 |
| 구매 가능성 | 안 봤음 | **신규** — ⚠ 비용을 감수해도 **개인이 못 사는 제품일 수 있다**(국산 상용은 기업 영업 계약형). 평가판 제공 형태를 먼저 확인 |
| 외부 전송 | 안 봤음 | **신규** — Dynatrace SaaS 면 텔레메트리가 밖으로 나간다. 켜기 전에 무엇이 나가는지 확인(우리 원칙: 외부 전송 없이 로컬) |

→ 문서 표현도 **`❌ 제외` → `⏸ 보류(학습 트랙 후보)`** 로 바꿨다. **결론이 "안 쓴다"가 아니라 "순서가 있다"** 가 됐다.

### 2-4. 새 절 — ARCHITECTURE §6 「학습 트랙」

**더 근본적인 구멍이 드러났다.** 이 문서의 판단은 그동안 **"운영에 필요한가"** 한 축뿐이었는데,
**이 프로젝트는 운영하지 않는다.** 그 축만 쓰면 §1 "미리 만들지 않는다"가 **배우려고 넣는 것까지 전부 막는다** —
실제로 C-8(상품 옵션)은 그 예외를 의식적으로 선택하고 근거를 남긴 사례였다. 축이 하나 빠져 있었던 것이다.

그래서 **트랙을 둘로 나눠 명문화**했다(ARCHITECTURE §6 「학습 트랙」, §1 에도 연결).

| | 운영 트랙 | 학습 트랙 |
|---|---|---|
| 질문 | "지금 없어서 아픈가?" | "넣으면 **무엇을 겪게 되는가?**" |
| 통과 조건 | 실제 문제·요구 | **겪을 거리가 실재할 것** |
| 실패 모드 | 오버엔지니어링 | **no-op 설치**(깔았는데 화면이 비어 아무것도 안 배움) |

**3원칙**: ①**운영 VM 을 실험대로 쓰지 않는다**(별도 VM/포트/프로파일 — `esptest`·Redis 별도 db 와 같은 이유)
②**볼 게 먼저, 도구는 나중**(k6 가 관측·APM 의 선행) ③**"무엇을 배웠나"를 핸드오프에 남긴다**
(안 그러면 "깔았다"만 남고 지식은 안 남는다).

**권장 순서**: `k6 부하 → OSS 관측(LGTM) → 상용 평가판 비교 → Docker/compose → k3s·kind → RabbitMQ → Kafka`.
뒤엣것부터 하면 **앞의 것이 왜 부족했는지를 영영 모른다.**

> ⚠ **MQTT 만 이 순서에서 빠진다.** 나머지는 "지금은 실효가 없다"지만 MQTT 는 **대응 문제 자체가
> 이 도메인에 없다.** 배우고 싶으면 이커머스가 아니라 **디바이스가 있는 별도 토이 프로젝트**가 맞다.

> 반영 위치(2-3·2-4): `ARCHITECTURE.md` §1(학습 트랙 연결) · §6 상용 APM 절 전면 개정 ·
> §6 「학습 트랙」 신설 · §6 k8s 행(❌ → 학습 트랙 열림) · 「도입 조건」 표 · §7 문구 정밀화
> (`유료라서 ❌` → `볼 게 없어서 지금은 ❌`) / `BACKLOG.md` D 절에 **포인터만**(내용 이중 관리 금지).

> 반영 위치: `ARCHITECTURE.md` §6 관측(LGTM 관계·상용 APM 표) · §6 MSA 인프라(Kafka·k8s·MQTT 재검토 표) ·
> §6 「도입 조건」 표 4행 추가 · §7 「하지 않는 것」.

---

## 3. `GET /api/admin/coupons` 권한 테스트 — **완료** (이월 1건 해소)

7/28 이월의 유일한 미완. WORKING-AGREEMENTS §2-4(새 관리자 엔드포인트엔 401/403/200 필수)에서
**목록(GET)만** 빠져 있던 자리다.

### 3-1. 착수 전 확인 — 무엇이 이미 있었나

`CouponFlowIntegrationTest.permissions()` 에 **생성(POST)·발급(issue)의 USER→403 은 이미 있었다.**
`/api/coupons/me` 비로그인 401 도 있었다. **빠진 건 정확히 `GET /api/admin/coupons` 하나**였다.
→ 새 파일을 만들지 않고 **같은 파일에 테스트 1개 추가**(도메인·픽스처·`login()` 헬퍼를 그대로 재사용).

### 3-2. 무엇을 검증하나

`adminList_permissionAndContent()` — 비로그인 **401** / USER **403** / ADMIN **200**.

- ⚠ **200 을 상태코드로만 보지 않는다.** ADMIN 요청에서 **방금 만든 쿠폰 정의가 실제로 실려 오는지**까지
  본다(`content[0].name`·`discountType`·`discountValue`·`maxDiscountAmount`). 상태코드만 보면
  "권한은 통과했는데 목록이 비어 있다"를 못 잡는다 — 7/23 배포 확인에서 **401 을 근거로 엔드포인트 존재를
  판단했다가 틀렸던 것**과 같은 종류의 함정이다(WA §5).
- 첫 줄을 단정할 수 있는 근거: `CouponService.listAll` 이 정렬 미지정 시 **`createdAt DESC`** 를 넣는다(실측).
- 픽스처는 `PERCENT` + `maxDiscountAmount` 로 잡았다 — 기존 테스트가 `FIXED` 라 **다른 할인 방식**도 함께 덮인다.

### 3-3. 검증 (실측)

| 게이트 | 결과 |
|---|---|
| 백엔드 컴파일 | 통과 |
| **전체 백엔드 테스트** | **319 pass · skipped 0 · fail 0** (`.env` 소싱 후 실행 — WA §3) |
| 쿠폰 테스트 상세 | `CouponFlowIntegrationTest` 3건 전부 통과(신규 `adminList_permissionAndContent` 0.92s 포함) |
| 프론트 빌드 | **대상 아님** — 프론트 변경 0 |
| 마이그레이션 | **없음** — 테스트만 추가(프로덕션 코드 무변경) |
| infra drift | **대상 아님** — nginx/systemd 무관 |

> ⚠ **skipped=0 을 확인했다.** `BUILD SUCCESSFUL` 만 보고 통과라 하지 않는다(7/21 사고 — 환경변수 없이
> 돌아 통합 테스트 58건이 조용히 skip 됐다).
> 플래키 후보 `NoticeQueryServiceIntegrationTest` 도 이번 실행에선 통과했다(여전히 이월 유지 — 간헐이라 1회 통과가 해소는 아니다).

---

## 5. 이메일 수집 (B-13) — 구현 완료, **배포 대기**

BACKLOG D 의 「SMTP 실발송」에서 **수집만 떼어낸** 조각(§1-3에서 B-13으로 승격한 그것).
`member.email` 은 V29(7/28)로 컬럼만 있고 **채우는 경로가 없어 전 회원 NULL** 이었다.

### 5-1. 착수 전 결정 (사용자 확정)

| 질문 | 결정 | 이유 |
|---|---|---|
| 가입 시 필수인가 | **신규 가입만 필수** | 신규는 100% 확보되고, 기존 회원은 백필할 출처가 없다. ⚠ **DB 는 nullable 그대로** — "필수"는 **API 계층 규칙**(`SignupRequest @NotBlank`)이지 스키마 제약이 아니다 |
| 기존 회원 유도 | **설정 화면만** | 배너 유도는 노출 조건·닫기 상태 관리가 붙어 범위가 커진다. 필요해지면 그때 |

> ⚠ 백로그엔 *"선택 입력으로 둔다"* 고 적어 뒀었다. **스키마 제약(NOT NULL 불가)이 곧 API 규칙이라고
> 여긴 것**이 어림의 오류였다 — 둘을 갈라 보면 "컬럼은 nullable, 신규 가입만 필수"가 가능하다.

### 5-2. ⚠ 정규화 — 이게 이 작업의 핵심 판단

저장값을 **trim + 소문자**(`Member.normalizeEmail`, `Locale.ROOT`)로 정규화한다.

- **왜**: Oracle `UNIQUE` 는 **대소문자를 구분**한다. 정규화가 없으면 `A@b.com` 과 `a@b.com` 이
  **둘 다 들어간다** — 같은 사람에게 메일이 두 번 가거나, 재설정 링크가 엉뚱한 계정으로 간다.
  `uk_member_email` 이 있으니 안전하다고 믿기 쉬운데 **제약만으로는 안 막힌다.**
- **규칙을 한 곳에 둔 이유**: 가입(AuthService)과 변경(MemberService) 이 각자 정규화하면 언젠가 어긋난다.
- **검사와 저장이 같은 값을 써야 한다** — `existsByEmail` 에 원본을 넣고 저장은 정규화값으로 하면
  중복 검사가 통과해 버린다. 그래서 리포지토리 메서드에 "정규화된 값이 온다"를 주석으로 못박았다
  (여기서 `lower()` 를 또 걸면 인덱스를 못 탄다).
- ⚠ `Locale.ROOT` 명시: 기본 로케일이 터키어면 `"I".toLowerCase()` 가 `ı` 가 된다(터키 I 문제).

### 5-3. 구현

- **백엔드**: `SignupRequest.email`(@NotBlank @Email @Size 255) · `AuthService.signup` 중복검사 +
  저장 · `MemberService.changeEmail` · `EmailUpdateRequest` · `PATCH /api/members/me/email` ·
  `ErrorCode.DUPLICATE_EMAIL(MEMBER-409E)` · `MemberRepository.existsByEmail/existsByEmailAndIdNot` ·
  **`MemberResponse` 에 `email` 추가**(기존 회원은 null = "등록 안 함").
- **프론트**: `SignupView` 이메일 입력(느슨한 형식 검사 — 서버보다 엄격하면 정상 주소가 막힌다) ·
  `MyPageView`(/settings) 「이메일」 카드(미등록이면 안내 문구 + 버튼 라벨이 "등록"/"변경") ·
  `api/member.changeEmail`. ⚠ **화면은 입력값이 아니라 응답값을 되비춘다**(서버가 소문자로 바꾸므로).
- **마이그레이션 없음** — V29 컬럼 재사용, 엔티티 매핑도 무변경(`@Column(unique, length=255)` 그대로).
  그래서 빈 DB(esptest) 검증도 대상이 아니다.
- ⚠ **확인 메일이 없다 → 저장된 주소는 미검증이다.** SMTP 가 붙으면 "확인 메일 → 확인 전 pending"
  을 얹어야 한다(BACKLOG D 에 적어 뒀다).

### 5-4. ⚠ 컴파일러가 못 잡는 호출부 (WA §3 이 경고한 자리)

`SignupRequest` 에 필드를 더하자 **컴파일러가 Java 호출부 3곳을 즉시 잡아냈다**(AuthServiceTest).
그런데 **`mockMvc.perform(post("/api/auth/signup"))` 의 JSON 본문 3곳은 컴파일러가 모른다** —
`AuthFlowIntegrationTest` 2곳 · `PointFlowIntegrationTest` 1곳. 문자열이라 grep 으로 찾아 고쳤다.
규약이 예고한 함정에 실제로 걸릴 뻔한 자리라 기록해 둔다.

### 5-5. 검증 (실측)

| 게이트 | 결과 |
|---|---|
| 백엔드 컴파일 | 통과 |
| **전체 백엔드 테스트** | **328 pass · skipped 0 · fail 0** (319 → 328, B-13 신규 9건) |
| 신규 테스트 | `MemberEmailFlowIntegrationTest` 4(401 · 등록→/me 반영→**DB 실측** · 대소문자 중복 409 · 형식/빈값 400) · `MemberServiceTest` +3(정규화 · 중복 · 본인 재저장 허용) · `AuthFlowIntegrationTest` +2(가입 필수/형식 · 정규화+중복 409) |
| 프론트 빌드·테스트 | 통과 (108) |
| 마이그레이션 | **없음** (V29 재사용, 엔티티 매핑 무변경) |
| infra drift | 대상 아님 |

> 통합 테스트에서 **DB 값을 직접 다시 읽어** 확인했다 — 응답만 소문자이고 저장은 원본인 경우를 배제하려고.
> 단위 테스트는 리포지토리가 목이라 **진짜 UNIQUE 제약은 통합에서만** 드러난다.

### 5-6. ⚠ 배포 순서 주의 — 이번엔 "백엔드 먼저" 에 대가가 있다

**가입 API 계약이 좁아졌다**(email 필수). 그래서 배포 사이 창에서 무슨 일이 나는지 갈린다:

| 순서 | 그 사이에 벌어지는 일 |
|---|---|
| **백엔드 먼저**(WA §5 기본) | 구 프론트의 가입 요청에 email 이 없어 **400**. 회원가입만, 프론트 배포까지 몇 분 |
| 프론트 먼저 | 구 백엔드는 모르는 필드를 **조용히 버린다**(Spring 기본이 unknown 무시) → 가입은 되는데 **이메일이 유실**. `/settings` 저장은 404 |

→ **백엔드 먼저 + 곧바로 프론트**로 간다. 실패가 **눈에 보이는 쪽**(400)이 조용한 데이터 유실보다 낫다.
창이 짧고(LAN·단독 사용) 그 사이 가입 시도가 있을 가능성도 낮다.

---

## 6. 배포

### 6-1. 쿠폰 권한 테스트(§3) — 배포 대상 아님

이 변경은 **테스트 파일 하나**뿐이고 프로덕션 코드·스키마·프론트가 전부 무변경이다.
운영 jar 에 들어갈 것이 없으므로 `deploy-*.sh` 를 돌리지 않는다.

- ⚠ 그래도 **`main` ff 머지는 했다** — 두 워크트리가 어긋난 채로 다음 배포를 맞으면 7/23 사고
  (옛 코드가 조용히 배포됨)가 재현된다. **배포 요청 전에 머지를 끝내 둔다**(WA §5 사람 쪽 해법).
  실측: 작업 브랜치 push → `/home/ecstel/work` 에서 `git merge --ff-only` → `main` push →
  **`git log main..daily-status-review` 비어 있음**, 양쪽 HEAD `7c294b3` 동일.
- Flyway 무관(마이그레이션 0)이라 통합 테스트가 공유 espdb 를 건드린 것도 **기존 V32 상태 그대로**다.

### 6-2. 이메일 수집(§5) — **배포 필요, 대기 중**

백·프론트 **둘 다** 바뀌었고 응답 계약(`MemberResponse.email`)과 요청 계약(가입 email 필수)이
함께 움직였다. 순서와 그 대가는 **§5-6** 에 적어 뒀다 — **백엔드 먼저, 곧바로 프론트.**
마이그레이션이 없어 DB 선행 작업은 없다.

---

## 이월 (다음 문서로 반드시 옮길 것)

> WORKING-AGREEMENTS §4-1. 다음 날 문서가 그대로 물려받아 시작한다. 처리했으면 지운다.

### 미완 — 다음 세션이 이어받을 것

1. **이메일 수집(B-13) 배포 + 브라우저 검증** — 구현·테스트는 끝났고(§5, 328 pass) **배포만 남았다.**
   ⚠ 순서: **백엔드 먼저 → 곧바로 프론트**(§5-6 — 그 사이 구 프론트의 가입이 400 이 난다. 조용한
   이메일 유실보다 눈에 보이는 실패가 낫다는 판단). 확인할 것:
   ①신규 가입에 이메일 필수·형식 오류 반려 ②대문자로 넣어도 소문자로 저장 ③`/settings` 에서 기존
   회원(전원 NULL)이 등록 → 다시 열었을 때 값 유지 ④남이 쓰는 주소는 409.
   ⚠ 검증은 `ZZ` 접두 일회용 계정으로 하고 **끝나면 탈퇴**시킨다(WA §3-1).

여유 있을 때(급하지 않음):

2. 디자인 더 갈 여지: 회원 상세 레이아웃 카드화, 상태 뱃지 pill 강화(7/28 §14-3).
3. **D 대기 기능**: SMTP 실발송·알림 실발송·PG 실결제 — 조건(발송채널·MSA·PG사) 미충족.
   ⚠ SMTP 는 **남은 게 발송 하나**가 됐다 — 수집(B-13)이 오늘 끝나 보낼 주소가 생긴다.
   붙일 때 함께 볼 것: 기존 회원은 여전히 email 이 null 일 수 있다는 것, 지금 주소는 **미검증**이라는 것.

기능 검증 종결 기록(다음 문서로는 안 옮긴다):

- ~~`GET /api/admin/coupons` 권한 테스트(401/403/200)~~ — **종결**(2026-07-29). `CouponFlowIntegrationTest`
  에 추가, **319 pass · skipped 0 · fail 0**. 200 은 상태코드뿐 아니라 **목록 내용까지** 확인.
  배포 대상 아님(테스트만).

### 7/28 에서 넘어온 잔여 (전부 조건부 · 급하지 않음)

1. **`/tmp` WebP 잔재** — tmpfiles.d 10일 규칙으로 자동 삭제. 당길 거면 `rm -f /tmp/cwebp*binary /tmp/dwebp*binary`.
2. **김기현팀 계정 검증 적립 영구 잔존**(SILVER·누적 204,880·잔액 97P) — 취소 불가가 설계라 그대로. 데모 때 감안.
3. **플래키 테스트** `NoticeQueryServiceIntegrationTest`(Redis 조회수 격리) — 테스트마다 Redis 키 clear 로 고칠 수 있다.
4. **공유기 DHCP 예약**(MAC `08:00:27:df:8e:fd` → `.14`) — IP 충돌 겪으면 그때.
