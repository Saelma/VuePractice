# 2026-07-29 작업 정리 (문서 개시 — 작업 착수 전)

> **7/28 핸드오프에서 이어서 진행한다.** 그 문서의 이월 미완은 **1건**(`GET /api/admin/coupons`
> 권한 테스트)이고, 「다음 기능 후보」로 회원관리 심화·D 대기 항목을 넘겨줬다.
>
> 착수 전 확인(2026-07-29): `main`(=`/home/ecstel/work`) HEAD == 작업 브랜치 `daily-status-review` HEAD
> == `74f3139`, 두 워크트리 워킹트리 **깨끗**. `git log main..daily-status-review` **비어 있음**
> (WORKING-AGREEMENTS §5 배포 전제 충족 상태). 7/28 작업분은 전부 배포·검증 종결.
>
> 2일치 대조(§1-1): 7/27 이월 4건(WebP 잔재·김기현팀 적립·플래키 테스트·DHCP 예약)은 7/28 문서가
> 전부 물려받았다 — **조용히 빠진 항목 없음**.

---

## 0. 오늘의 커밋

| 해시 | 메시지 |
|---|---|
| `659bc81` | docs: 07-29 핸드오프 개시 — 7/28 이월 인수 |
| `1b710ab` | docs: 07-29 핸드오프 §0 해시 확정 |
| `31ee367` | docs: 인프라·관측 도구 재검토(LGTM·상용APM·Kafka·k8s·MQTT) + B-13 이메일 수집 |

> ⚠ §4-4: 이 표는 **작업을 끝낸 뒤 다시 대조한다** — `git log --oneline --since="2026-07-29 00:00"`.
> 문서를 닫는 커밋과 그 뒤 후속 커밋이 구조적으로 빠지는 자리다.

---

## 1. 오늘 무엇을 할 것인가 — 후보와 근거

7/28 에 BACKLOG 의 **표준 기능(A·B·C)이 전부 소진**됐다. 남은 건 성격이 다르다 — 규약 부채,
품질 정리, 그리고 조건 미충족으로 대기 중인 D 항목들. 우선순위 근거는 이렇다.

### 1-1. 먼저 할 것 — `GET /api/admin/coupons` 권한 테스트 (이월 1건)

**규약 부채라 다른 후보보다 앞선다.** WORKING-AGREEMENTS §2-4 는 새 관리자 엔드포인트에 권한
테스트(401/403/200)를 **"반드시"** 붙이라는 필수 규약인데, 7/28 쿠폰 관리 화면에서 빠뜨렸다.
사용자와 **다음 세션 처리로 합의**한 항목이다.

- 현황(실측): `CouponFlowIntegrationTest` 에 `POST /api/admin/coupons`(생성)·`/{id}/issue`(발급)의
  비관리자 403 은 **있다**. 빠진 건 **`GET /api/admin/coupons`(목록)** 하나 — 미인증 401 · 비관리자 403 · ADMIN 200.
- 붙일 자리: `CouponFlowIntegrationTest`(같은 도메인에 이미 admin 케이스가 있어 여기가 자연스럽다).
  패턴은 `AdminMemberIntegrationTest`.
- 크기: **소**(반나절 미만). 마이그레이션·배포 없음(테스트만 추가 — 프로덕션 코드 무변경).

### 1-2. 그다음 후보 (사용자 선택 필요 — 착수 시 확정)

| 후보 | 성격 | 크기 | 근거 |
|---|---|---|---|
| **플래키 테스트 정리** (`NoticeQueryServiceIntegrationTest`) | 품질 부채 | 소 | Redis 조회수 키가 테스트 간 격리 안 됨. 7/24부터 이월돼 온 항목 — "가끔 빨간불"은 회귀 판단을 흐린다 |
| **디자인 잔여 폴리싱** (회원 상세 카드화, 상태 뱃지 pill) | UI | 소~중 | 7/28 §14-3 이 남긴 "필요해지면" 항목. 로드맵 자체는 종결 |
| **감사 대상 확대** (주문 발송·반품 승인·상품 편집도 `AdminActionEvent`) | 기능 심화 | 중 | 지금은 회원 조작만 남는다. ⚠ `AuditAction` CHECK 제약도 함께 넓혀야 함(Oracle enum CHECK 트랩) |
| **정지 사유·기간** | 기능 심화 | 중 | 지금은 정지/해제 이진 상태뿐 |
| **이메일 수집 폼** (가입·설정) | D 선행 | 소~중 | `member.email`(V29)은 컬럼만 있고 **채우는 경로가 없어 전부 NULL**. SMTP 발송(D)의 선행 조건이고, 발송 채널 없이도 지금 할 수 있는 유일한 조각 |

> ⚠ **D 대기 항목은 오늘 착수 대상이 아니다** — SMTP 실발송·알림 실발송·PG 실결제는 조건
> (발송 채널·MSA·PG사)이 미충족이다. 단 위 표의 **이메일 수집 폼**은 조건 없이 가능한 선행분이다.

### 1-3. 후보의 문서 자리 정리 (사용자 요청)

위 후보들이 BACKLOG 에 있는지 대조했다. **거의 다 있었고, 빠진 건 하나**였다.

| 후보 | 문서 자리 | 조치 |
|---|---|---|
| 감사 대상 확대 · 정지 사유/기간 | BACKLOG 「회원 관리 심화」 | 이미 있음 |
| 디자인 잔여(회원상세 카드화·뱃지 pill) | BACKLOG B-12 「남은 여지」 | 이미 있음 |
| **이메일 수집 폼** | D 표 안에 **조건부로만** 묻혀 있었음 | ⚠ **B-13 으로 승격**(아래) |
| 플래키 테스트 정리 | — | **BACKLOG 에 넣지 않는다.** 제품 기능이 아니라 그날 작업에서 파생된 품질 부채라 **이월**이 자리다(BACKLOG 「쓰는 법」) |

> **B-13 이메일 수집을 D 에서 꺼낸 이유**: D 의 정의는 **"조건 대기"** 인데, 수집은 조건이 없다.
> 조건이 없는 일을 조건 대기 표에 두면 **영영 안 하게 된다.** 발송(SMTP)만 D 에 남긴다.
> 값의 근거: 수집은 시간이 지나야 쌓이므로 **발송 기능을 먼저 만들면 그날 발송 대상이 0명**이다.

---

## 2. 인프라·관측 도구 재검토 (사용자 제안 — LGTM · MaxGauge · Dynatrace · Jennifer · Kafka · k8s · MQTT)

사용자가 이후 도입 후보로 위 도구들을 제안했다. **코드 작업이 아니라 로드맵 판단**이라 자리는
`ARCHITECTURE.md §6`(미래 인프라 로드맵)이다 — BACKLOG 는 *제품 기능*, ARCHITECTURE 는 *설계·인프라*.

### 2-1. ⚠ 대조 결과 — 대부분 **이미 결정이 있었다**

| 제안 | 문서의 기존 상태 | 갱신 |
|---|---|---|
| **LGTM** | §6 관측 스택이 이미 **Alloy+Loki+Prometheus+Grafana(+Tempo)** | **새 도입이 아니라 같은 것의 다른 이름**임을 명시. 차이는 **M(Mimir) 하나** — Mimir 는 Prometheus 의 *장기보존·수평확장* 백엔드라 단일 VM 엔 비용만 는다 → **Prometheus 유지**, 보존기간·다중 인스턴스 시 remote_write 로 승격 |
| **Jennifer** | §7 에 이미 "상용/유료 ❌" | 유지 |
| **Dynatrace·MaxGauge** | **언급 없었음** | 신규 판단 추가. 둘 다 유료. ⚠ **MaxGauge 는 결이 다르다** — 앱 APM 이 아니라 **Oracle DB 전문**. 무료 대체는 P6SPY(완료)·`v$` 뷰·**Statspack**·`oracledb_exporter`. ⚠ **AWR/ASH 는 무료가 아니다**(Diagnostic Pack 유료 옵션) — 습관적으로 쓰기 전에 확인할 것 |
| **Kafka** | CLAUDE.md·§6 에 **"RabbitMQ 확정, Kafka 아님"**(2차 목표) | 유지 + **왜**를 보강. 우리 이벤트는 *주문완료→알림·적립* 같은 **작업 지시(task queue)** = RabbitMQ 결. Kafka 는 **replay·이벤트소싱·CDC·고스루풋**에서 값이 나는데 지금 셋 다 없다. **순서를 지키면 둘의 차이를 몸으로 알게 된다** |
| **Kubernetes** | §7 에 "단일 VM 엔 오버 ❌" | 유지하되 **학습 목적 예외 조건**을 명시 — 운영 VM 이 아니라 **별도 환경 + k3s/kind**, **Docker 화 이후**(이미지가 없으면 올릴 게 없다) |
| **MQTT** | **언급 없었음** | ❌ 도입 안 함. IoT 디바이스용 경량 pub/sub 이라 **이 도메인에 대응 문제가 없다**. 닮은 자리 둘은 이미 답이 있다 — 실시간 푸시=**SSE 완료**, 서비스 간=**RabbitMQ**. 뒤집히는 조건: 실제 디바이스(키오스크·POS·배송단말) |

### 2-2. 관통하는 판단 근거

- **볼 게 없는 상태에서 도구부터 깔지 않는다** — Sentry 를 보류한 것과 같은 판단(§1 "미리 만들지 않는다").
  관측 도구의 값은 **트래픽·서비스 수**에서 나오는데 지금은 단일 VM·단일 모놀리스·주문 수십 건이다.
- **상용 도구는 회사에서 배우고, 여기선 OSS 로 원리를 익힌다.** 업계 인지 목적이면 **문서로 남기는 것으로 충분**하다.
- 도입 순서에 의미가 있다: **RabbitMQ → (필요하면) Kafka**, **Docker → (학습이면) k8s**,
  **Prometheus → (모자라면) Mimir**. 뒤에 것부터 깔면 앞의 것이 왜 부족했는지를 영영 모른다.

> 반영 위치: `ARCHITECTURE.md` §6 관측(LGTM 관계·상용 APM 표) · §6 MSA 인프라(Kafka·k8s·MQTT 재검토 표) ·
> §6 「도입 조건」 표 4행 추가 · §7 「하지 않는 것」.

---

## 3. 검증 (실측)

*(작업 후 채운다. §4-3 — "동작 확인"이 아니라 건수·응답코드·로그를 적는다.)*

| 게이트 | 결과 |
|---|---|
| 백엔드 컴파일 | — |
| 전체 백엔드 테스트 | — (7/28 기준 315 pass · skipped 0 · fail 0 에서 출발) |
| 프론트 빌드 | — |
| 마이그레이션 | — |
| infra drift | — |

---

## 4. 배포

*(작업 후 채운다. 순서: 작업 브랜치 push → `main` ff머지·push(`main..branch`=0 확인) → 배포 스크립트.)*

---

## 이월 (다음 문서로 반드시 옮길 것)

> WORKING-AGREEMENTS §4-1. 다음 날 문서가 그대로 물려받아 시작한다. 처리했으면 지운다.

### 미완 — 다음 세션이 이어받을 것

1. **`GET /api/admin/coupons` 권한 테스트(401/403/200)** — ⚠ WA §2-4 필수 규약. 7/28 쿠폰 관리
   화면에서 빠뜨렸고 사용자와 **다음 세션 처리로 합의**(2026-07-28). 생성·발급의 403 은 이미
   `CouponFlowIntegrationTest` 에 있고 **목록(GET)만** 없다. 패턴은 `AdminMemberIntegrationTest`.

여유 있을 때(급하지 않음):

2. 디자인 더 갈 여지: 회원 상세 레이아웃 카드화, 상태 뱃지 pill 강화(7/28 §14-3).
3. **D 대기 기능**: SMTP 실발송(`member.email` 컬럼은 V29 로 준비, **값은 전부 NULL — 수집 폼 없음**)·
   알림 실발송·PG 실결제 — 조건(발송채널·MSA·PG사) 미충족.

### 7/28 에서 넘어온 잔여 (전부 조건부 · 급하지 않음)

1. **`/tmp` WebP 잔재** — tmpfiles.d 10일 규칙으로 자동 삭제. 당길 거면 `rm -f /tmp/cwebp*binary /tmp/dwebp*binary`.
2. **김기현팀 계정 검증 적립 영구 잔존**(SILVER·누적 204,880·잔액 97P) — 취소 불가가 설계라 그대로. 데모 때 감안.
3. **플래키 테스트** `NoticeQueryServiceIntegrationTest`(Redis 조회수 격리) — 테스트마다 Redis 키 clear 로 고칠 수 있다.
4. **공유기 DHCP 예약**(MAC `08:00:27:df:8e:fd` → `.14`) — IP 충돌 겪으면 그때.
