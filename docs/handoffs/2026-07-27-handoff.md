# 2026-07-27 작업 정리 (재입고 알림 — B-9)

> **7/24 에서 넘어온 이월은 두 가지였다**: ①인앱 알림 시스템(§11) 브라우저 검증, ②그다음 후보로 B-9 재입고 알림.
> 오늘 ①을 **종결**(사용자 6단계 확인)하고 ②를 **구현·검증·배포·브라우저 검증까지** 끝냈다. 이월 미완 0건.
>
> 7/24→오늘 사이 **3일 공백**(주말)이 있어 문서를 믿지 않고 착수 시점 상태를 실측했다(§1).

---

## 0. 오늘의 커밋

| 해시 | 메시지 |
|---|---|
| `1634aa0` | feat: 재입고 알림(B-9) — StockReplenishedEvent + restock 도메인 + V28 |
| `e795ef3` | docs: B-9 완료 반영 — ARCHITECTURE·BACKLOG·2026-07-27 핸드오프 |
| `0103728` | docs: B-9 배포·브라우저 검증 종결 반영 — 이월 미완 0건 |

세 커밋 모두 push·`main` ff머지 완료. **배포·브라우저 검증까지 종결**(2026-07-27).

---

## 1. 착수 시점 상태 (실측 — 3일 공백이라 믿지 않고 확인)

| 항목 | 확인 | 결과 |
|---|---|---|
| 알림 시스템(§11) 브라우저 검증 | 사용자 6단계 | **전부 통과** — 종결. 페이징만 "안 보인다" 했으나 **정상**(한 페이지 20개, `totalPages>1` 일 때만 버튼 노출 — 알림 20개 이하라 한 페이지) |
| git 동기화 | `main` HEAD == 작업 브랜치 | 동일(`8f56c45`). 워킹트리 깨끗 — B-8 때 겪은 "구 코드 배포" 재발 조건 없음 |
| 서버 | health · nginx | 백엔드 `UP` · nginx `https:200` |
| `/tmp` WebP 잔재 | `ls` | 11개(7/21~22 것 그대로) — 방치해도 tmpfiles.d 로 지워짐 |
| 디스크 | `df` | 84%(여유 4.4G). 7/24 82% 에서 소폭 상승 |

---

## 2. 무엇을 할 것인가 — B-9 재입고 알림

이월의 「그다음 작업 후보」이자 BACKLOG 다음 추천이었다. 인앱 알림 인프라(전달·설정·SSE·토스트)가 §11 로
이미 깔려 있어 **producer 하나만 얹으면 되는** 자리라고 적혀 있었다. 실제로도 그랬지만, 착수 전 코드를
읽다가 **제품 결정 두 개**가 나왔다(코드가 아니라 사용자가 정할 것이라 물었다).

### 2-1. ⚠ 착수 전 발견 두 가지

1. **`notification.type` 에는 CHECK 제약이 없다**(V26 확인). 그래서 `NotificationType.RESTOCK` 추가에
   **마이그레이션이 필요 없다** — Oracle enum CHECK 트랩(메모리 참조)이 여기엔 해당하지 않는다.
   V26 이 일부러 unconstrained VARCHAR2 로 둔 덕이다. (트랩은 `product.status`·`orders.status`·
   `point_history.type` 등 **CHECK 가 붙은** enum 컬럼에만 해당.)
2. **관리자 상품 편집이 옵션을 delete + 재삽입한다**(`ProductCommandService.update`) → **`variant.id` 가
   편집할 때마다 새로 생긴다.** 이게 두 결정을 다 좌우했다:
   - 관리자 재고 입력(restock 의 **주된** 실제 경로)은 `increaseStock` 을 안 타서 이벤트가 안 나간다.
   - variant_id 로 구독을 걸면 관리자가 편집하는 순간 **구독이 전부 고아**가 된다.

### 2-2. 결정 (사용자 확정)

| 질문 | 결정 | 이유 |
|---|---|---|
| 발송 트리거 | 주문취소·반품복원 **+ 관리자 재고편집** | 실제 이커머스의 주된 재입고는 "관리자가 재고를 채우는 것". 이걸 빼면 기능이 반쪽 |
| 구독 단위 | **상품 단위**(member+product) | variant.id 불안정 회피. 위시리스트와 같은 검증된 패턴. 멀티옵션 상품 false positive 는 감수 |

상품 단위로 정하니 관리자 편집 경로도 깔끔해졌다 — 옵션이 아니라 **상품 총재고 0→양수**만 감지하면 된다.

---

## 3. 구현

### 3-1. 이벤트 — `StockReplenishedEvent` (catalog 소유, `StockRunningLowEvent` 와 대칭)

재고는 catalog 가 소유하므로 재입고 이벤트도 catalog 가 낸다(주문 경로든 관리자 경로든 같은 이벤트).
페이로드는 `(productId, productName)` — 상품 단위라 옵션 정보를 담지 않는다.

### 3-2. ⚠ 3층 중 **핸들러를 restock 도메인에 뒀다** (재고부족과 다르다)

재고부족(`StockRunningLowEvent`)은 notification 도메인이 받는다 — 받아서 관리자에게 알리면 끝이라
알림이 곧 주체다. **재입고는 다르다** — 구독(신청) 데이터가 있고, 발송 후 그 구독을 **소진**해야 한다.
notify + 소진은 **구독 생명주기**라 restock 관심사다. 그래서:

- `catalog/event/StockReplenishedEvent` (발행: catalog)
- `restock/RestockEventListener` (어댑터, `@Async @TransactionalEventListener(AFTER_COMMIT)`)
- `restock/RestockNotificationHandler` (신청자 조회 → `NotificationCommandService.create(RESTOCK)` → 그 상품 구독 `deleteByProductId`)

의존 방향 `catalog →(event)→ restock → notification`, **순환 없음**. notification 은 restock 을 모른다.
알림 생성은 notification 의 공개 입구(`create`)로만 — 그 안에서 타입별 opt-out("재입고 알림" 끔)도 존중된다.

### 3-3. ⚠ 재입고 판정 — "상품 총재고 0→양수" 를 두 경로에서 (한 번만 발화)

`ProductVariantRepository.sumStockByProduct(productId)` 를 새로 추가해 상품 총재고를 스칼라로 읽는다.

- **`increaseStock(variantId, qty)`**(주문취소 §OrderService:261 · 반품승인 :296): 옵션 증가 후,
  `총재고 − 방금 더한 qty == 0` 이면 증가 직전이 0(=상품 전체 품절)이었다는 뜻 → 지금 양수면 발화.
  한 주문에 같은 상품 옵션이 여러 개여도 **첫 증가에서만** 조건이 참이라 발화는 1회.
- **`update(id, req)`**(관리자 편집): 옵션 통째 교체 **전** 총재고를 재두고(`stockBefore`),
  교체 후 총재고가 양수면서 `stockBefore==0` 이면 발화. `sumStockByProduct` 가 JPQL 이라 재삽입분을
  flush 한 뒤 값을 본다.

`increaseStock` 은 이제 반환행이 0(옵션 삭제됨)이면 조용히 무시하고, 아니면 판정한다.

### 3-4. 구독 도메인 + API (위시리스트 미러)

- `restock_subscription`(RAW(16) PK, member_id·product_id 느슨한 UUID, 감사 컬럼 `TIMESTAMP(9) WITH TIME ZONE`,
  `UNIQUE(member_id, product_id)`) — V28.
- ⚠ **위시리스트와 달리 `product_id` 인덱스를 따로 만들었다.** 조회 경로가 둘(내 신청=member_id / 신청자
  전원=product_id)인데, 후자가 재입고마다 도는 핫 경로인데 UNIQUE 선두가 member_id 라 못 탄다.
- 서비스: CQRS 분리(`RestockSubscriptionCommandService` subscribe/unsubscribe 멱등 · `...QueryService` myProductIds).
- API: `POST/DELETE /api/restock/{productId}`, `GET /api/restock/product-ids`. 전부 본인 것만(토큰).
- `SecurityConfig` 에 `/api/restock/**` authenticated 추가(기본 permitAll 이라 안 넣으면 남의 신청 열림 —
  위시리스트·쿠폰에서 겪은 자리).

### 3-5. 프론트 — 품절 상품에 "재입고되면 알림 받기"

`api/restock.js` · `stores/restock.js`(위시리스트 스토어 미러, 낙관적 토글) · `components/RestockButton.vue`.
`ProductDetailView` 는 **상품 전체 품절**(`totalStock === 0`)일 때만 버튼을 보여준다(부분 품절은 다른 옵션을
살 수 있으니 대상 아님 — 이벤트 기준과 일치). 로그아웃 시 `clearRestock()`(남의 신청 상태 누수 방지).
알림 설정 화면은 `NotificationType.values()` 를 순회하므로 **RESTOCK 토글이 자동 노출**된다(코드 무수정).

---

## 4. 검증 (실측)

| 게이트 | 결과 |
|---|---|
| 백엔드 컴파일 | 통과 |
| **전체 백엔드 테스트** | **280 pass · skipped 0 · fail 0** (재입고 13 신설 + 회귀 전부. 주문취소/반품/상품편집 경로 영향 없음 확인) |
| 재입고 테스트 상세 | `RestockEventListenerTest`(위임) · `RestockNotificationHandlerTest`(발송+소진/무신청 no-op) · `RestockFlowIntegrationTest`(API·401·멱등·격리·404 + **0→양수 발화**(취소경로·관리자편집) + **재고 있던 상품은 무발화** + 핸들러 발송·소진). `@RecordApplicationEvents` 로 발행을 검증(async+AFTER_COMMIT 라 트랜잭션 테스트에선 소비가 안 떠서) |
| 프론트 빌드 | 통과 |
| 프론트 테스트 | **108 pass**(restock 스토어 +10) |
| **빈 DB(esptest) V1→V28** | **통과** — `Successfully applied 28 migrations ... now at version v28` + `ddl-auto=validate` 통과(엔티티↔스키마 일치, restock_subscription 포함). 사용자가 bootRun 직접 실행(§배포 참고) |
| infra drift | 변경 없음(코드 변경만, nginx/systemd 무관) |

---

## 5. 배포 — 완료 (2026-07-27)

순서대로 진행(WORKING-AGREEMENTS §5): 작업 브랜치 push → `main` ff머지·push(main..branch 비움 확인) →
`deploy-backend.sh` → `deploy-frontend.sh`(백엔드 먼저, `/api/restock/*` 계약 신설이라 백·프론트 함께).
**배포·브라우저 검증 통과**(사용자 확인). V28 은 순수 추가(새 테이블)라 구 jar 무해 —
통합 테스트가 이미 공유 espdb 에 V28 을 적용했고 운영 정상. enum CHECK 트랩은 이번엔 무관(§2-1).

---

## 현재 상태 (7/24 대비 바뀐 것만)

- **알림 시스템(§11) 브라우저 검증 종결** — 사용자 6단계 통과. 페이징은 20개/페이지, `totalPages>1` 일 때만
  버튼 노출이라 "안 보인다"가 정상 동작.
- **재입고 알림(B-9) 신설** — `restock` 도메인 + `StockReplenishedEvent`(상품 총재고 0→양수: 주문취소·
  반품·관리자 재고편집) + 신청 API + 프론트 버튼. **구독은 상품 단위**(variant.id 불안정 회피).
  **배포·브라우저 검증 완료**(2026-07-27, 사용자 확인).
- **DB/스키마**: Flyway **V28**(`restock_subscription`). 빈 DB V1→V28 검증 통과. `NotificationType.RESTOCK`
  추가는 CHECK 없어 마이그레이션 불필요.
- **테스트**: 백엔드 280(+재입고 13) · 프론트 108(+restock 10). skipped 0.
- **운영**: 정상. 재입고 배포(백·프론트) 후 정상 확인.
- **git**: 작업 브랜치 `daily-status-review` == `main` == `0103728`. 커밋 3개(`1634aa0` feat · `e795ef3` docs ·
  `0103728` docs-배포검증종결) push·main ff머지 완료. 두 워크트리 동일.
- **인프라**: 변경 없음.
- **디스크**: 84%.

---

## 이월 (다음 문서로 반드시 옮길 것)

> WORKING-AGREEMENTS §4-1. 다음 날 문서가 그대로 물려받아 시작한다. 처리했으면 지운다.

### 미완 — 다음 세션이 이어받을 것

**없다.** B-9 는 배포·브라우저 검증까지 끝났다(아래 취소선). 다음 세션은 새 기능(B-10/B-11)에서 시작한다.

기능 검증 종결 기록(다음 문서로는 안 옮긴다):

- ~~재입고 알림(B-9) 배포 + 브라우저 검증~~ — **종결**(2026-07-27, 사용자 확인). 품절→"재입고되면 알림 받기"
  신청→**관리자 재고 채움→토스트+벨**(주 경로)→주문취소/반품 경로→일회성(소진)→opt-out→계정 격리까지 관통.

### 7/24 에서 넘어온 잔여 (전부 조건부 · 급하지 않음)

1. **`/tmp` WebP 잔재 11개(32MB)** — 재발은 막혔고 tmpfiles.d 10일 규칙으로 자동 삭제. 당길 거면
   `rm -f /tmp/cwebp*binary /tmp/dwebp*binary`(실행 중 JVM 없을 때).
2. **운영자ID 계정 검증 적립 영구 잔존**(SILVER·누적 204,880·잔액 97P) — 취소 불가가 설계라 그대로 둠(7/24 §이월).
   데모 때 감안.
3. **플래키 테스트** `NoticeQueryServiceIntegrationTest`(Redis 조회수 격리) — 이번 작업과 무관. 테스트마다
   Redis 키 clear 로 고칠 수 있다.
4. **공유기 DHCP 예약**(MAC `08:00:27:df:8e:fd` → `.14`) — IP 충돌 겪으면 그때.

### 다음 기능 후보 (B-9 배포·검증 종결 후)

BACKLOG 남은 표준: **B-10 비밀번호 재설정**(단 이메일 발송 stub 이라 반쪽) · **B-11 관리자 회원 관리**(조회 위주).
