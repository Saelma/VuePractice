# 기능 백로그 (이커머스)

> **이 문서가 답하는 질문: "무엇을 만들 것인가."**
> 설계·근거는 `ARCHITECTURE.md`, 일하는 방식은 `WORKING-AGREEMENTS.md`,
> 그날 한 일은 `YYYY-MM-DD-handoff.md`. 여기엔 **아직 없는 기능**만 적는다.
>
> **왜 따로 만들었나 (2026-07-23)**: 같은 갭 목록을 하루에 **두 번** 코드에서 다시 뽑았다.
> 재생성 비용이 반복해서 발생하면 문서로 만드는 게 맞다. 게다가 이 목록이 그날 핸드오프 안에
> 들어 있으면 **다음 날 문서가 생길 때 묻힌다** — WORKING-AGREEMENTS 가 존재하는 이유와 같은 문제다.

## 쓰는 법

- **핸드오프 `## 이월` 과 겹치지 않게 한다.** 이월은 *그날 작업에서 파생된 잔여*(수명 며칠),
  여기는 *제품 기능 후보*(수명 김)다. 이월에 제품 기능이 쌓이면 매일 복사돼 내려온다.
- 항목을 끝내면 **아래 「완료」로 옮기고 날짜·근거 문서를 남긴다.** 지우지 않는다 —
  "이건 왜 이렇게 돼 있지"를 나중에 되짚을 근거가 된다.
- **크기**는 어림값이다: 소=반나절 / 중=하루 / 대=며칠(도메인 신설·스키마 파급).

---

## A. 주문 흐름의 구멍 (이미 만든 것이 미완인 자리)

### A-3. 사람이 읽는 주문번호 — 크기 중

주문 식별자가 **UUID 뿐**이다. 화면은 `#019f8d59` 처럼 앞 8자만 잘라 보여주는데,
고객이 CS 에 불러주기 어렵고 잘린 값이라 **중복 가능성**도 있다.

- `orders` 에 `order_no`(예: `20260723-0001`) 컬럼 + 유니크 제약. 채번 방식을 정해야 한다
  (일자별 시퀀스 vs 전역 시퀀스 — 동시 주문에서 겹치지 않아야 한다).
- 기존 주문 25건은 **백필해야** 화면이 일관된다(UUID 를 그대로 노출할 수는 없다).
- 관리자 주문 검색에 주문번호 검색도 함께.

> ⚠ PK 를 바꾸는 게 아니다. PK 는 UUIDv7 그대로 두고 **표시·검색용 별도 컬럼**이다
> (ARCHITECTURE §3 PK 규칙 유지).

---

## B. 커머스 표준 기능

### B-7. 정가 / 할인가 — 크기 중  ← **다음 추천**

`Product.price` **하나뿐**이라 "20% 할인" 을 표현할 방법이 없다.

- `product` 에 정가(또는 할인율) 추가 → 목록·상세에 `~~39,000원~~ 31,200원 (20%)`.
- **주문 스냅샷에 파급**: 주문 시점의 정가·할인가를 모두 남길지 정해야 한다
  (지금은 `order_item.price` 하나 — 실제 판매가만 남는다).
- **B-4 쿠폰보다 먼저** 하는 게 좋다: 할인 표현이 없는 채로 쿠폰을 얹으면
  "정가 대비 얼마 할인" 을 보여줄 근거가 없어 가격 표시 로직을 다시 짜게 된다.
  (배송비를 쿠폰보다 먼저 넣은 것과 같은 이유 — 2026-07-23)

### B-4. 쿠폰 · 할인 — 크기 대

CLAUDE.md 가 도메인 예시로 든 `domain.coupon` 의 자리. 도메인 신설이라 가장 크다.

- `coupon`(정액/정률·최소주문금액·유효기간) + `member_coupon`(발급·사용여부·사용시각) 2테이블.
- 주문 금액 계산 순서: **상품합계 → 쿠폰할인 → 배송비 → 결제금액**.
  배송비(V14)가 이미 있으니 이제 얹을 토대는 있다.
- 쿠폰도 **부과된 값은 주문에 스냅샷**해야 한다(배송비와 같은 이유 — 정책이 바뀌어도 과거 주문은 그대로).

### B-5. 배송지 주소록 — 크기 중

현재 `member` 에 `ship_*` 5컬럼 = **기본 배송지 1개만**. 집/회사 구분이 안 된다.

- `member_address` 테이블 신설(별칭·기본배송지 플래그) + 주문서에서 선택.
- ⚠ **기존 데이터 이관이 필요하다** — `member.ship_*` 값을 새 테이블로 옮기고 컬럼을 정리해야 한다.
  지금까지의 마이그레이션은 대부분 순수 추가였는데 이건 다르다.

### B-6. 위시리스트(찜) — 크기 소~중

도메인 없음. 장바구니(Redis)와 구조가 비슷해 패턴을 재활용할 수 있다.
다만 장바구니와 달리 **영속이어야** 하므로(로그아웃해도 남아야 한다) Redis 가 아니라 테이블이 맞다.

---

## C. 규모가 커지면

### C-8. 상품 옵션(사이즈·색상) — 크기 대

**가장 큰 항목.** 지금 구조는 "상품 1 = 재고 1" 이라 옵션이 들어오면
`product`·`cart`·`order_item`·재고 차감/복원까지 **전부 파급**된다.
실제로 필요해지기 전에는 손대지 않는 게 맞다(ARCHITECTURE §1 "미리 만들지 않는다").

### C-9. 반품 · 교환 — 크기 대

`OrderStatus` 확장 + 환불 개념이 필요하다. **PG 없이는 반쪽**이라(돈을 돌려줄 수단이 없다)
PG 도입과 묶는 게 자연스럽다.

### C-10. 적립금 · 회원 등급 — 크기 중

`Member` 에 관련 필드 없음. 주문 완료 이벤트(`OrderPlacedEvent`)에 얹는 구조가 된다.

### C-11. 관리자 매출 통계 — 크기 중

관리자 API 가 **주문·이미지 둘뿐**이라 대시보드가 없다.
일/월 매출, 상품별 판매량 등. 집계 쿼리라 인덱스 확인이 함께 필요하다.

---

## D. 조건 대기 (조건이 갖춰지면)

| 항목 | 조건 |
|---|---|
| **PG 실결제** | MSA 단계 + PG사 확정. `OrderService.pay()` 가 seam 으로 남아 있다(ARCHITECTURE §5) |
| **HSTS / mkcert** | 도메인 + Let's Encrypt 도입 시. IP 접속엔 HSTS 가 애초에 무효 |
| **리뷰 목록 캐시** | P6SPY 로 실제 병목이 측정될 때. 도구는 이미 검증됨(2026-07-23) |
| **알림 Handler 실발송** | 임계치 재알림 스팸을 먼저 해결해야 한다 |
| **`DB_PASSWORD` 강화**(현재 7자) | 도메인·외부 노출 단계에서. `.env`·DB계정·백업(Notion) 셋을 함께 맞춰야 한다 |
| **Docker·RabbitMQ·관측 스택(Alloy/Loki/Prometheus/Grafana)·Spring Batch·OpenSearch** | MSA 단계 |

---

## 완료

| 항목 | 완료일 | 근거 |
|---|---|---|
| **A-1. 배송 추적** — 운송장(택배사·송장번호) + `DELIVERED` 상태 + 진행 스텝 4단계 | 2026-07-23 | V13 · `2026-07-23-handoff.md` §8 |
| **A-2. 배송비** — 정책은 설정(`glassvue.shipping`), 부과 금액은 주문 스냅샷 | 2026-07-23 | V14 · `2026-07-23-handoff.md` §10 |

> 두 항목의 순서에는 이유가 있었다: **A-1 은 "이미 만든 것이 미완"** 이라(배송지·진행 스텝·발송 처리를
> 다 만들어 놓고 추적만 비어 있었다) 새 기능보다 먼저였고, **A-2 는 금액 계산의 토대**라
> 쿠폰(B-4)보다 먼저여야 했다. 같은 논리로 지금은 **B-7(할인가)이 B-4(쿠폰)보다 앞**이다.
