# 2026-07-23 작업 정리 (오전: Oracle 자동기동 고장 — SELinux / 오후: 이월 소진 + 배송 추적·배송비 신설)

> 오전은 **이월 1번에 통째로 갔다.** 어제 문서는 이 항목을 *"`enable` 해뒀으니 내일 재부팅해서
> 검증할 것"* 으로 남겼는데, 아침 재부팅에서 **검증이 아니라 고장으로 판명**됐다.
> 그리고 원인을 찾기까지 **가설 4개를 연달아 헛짚었다.** 그 오진 과정을 전부 적는다 —
> 결론만 적으면 다음에 또 같은 순서로 헛짚는다(WORKING-AGREEMENTS §4-2).
>
> 실제로 오늘 **운영이 두 번 내려갔다.** 한 번은 아침 재부팅(자동기동 실패), 한 번은 검증 재부팅.
> 둘 다 원인은 같다.
>
> 오후에는 남은 이월을 전부 털었다 — `.env` 백업(§ 이월)·PDB saved state(§4)·`/tmp` 누수(§5)·
> `ItemThumb` 테스트(§6)·문의 N+1 실측(§7). **7/22 에서 넘어온 이월이 0 이 됐다.**
>
> 그리고 이커머스 기능 갭을 뽑아 **배송 추적(§8)** 과 **배송비(§10)** 를 신설했다.
>
> ⚠ 오늘 **내 판단을 세 번 정정했다**(SELinux 이전의 가설 4개, `/tmp` 누수의 "무한 증가" 전제,
> PDB saved state 의 위험도 과장). 각 자리에 정정 이유를 같이 적어 뒀다.

## 0. 오늘의 커밋

| 커밋 | 내용 |
|---|---|
| `627813c` | infra: Oracle 부팅 자동기동 수리 — hosts 핀 + network-online override |
| `ffb9fbc` | infra: Oracle 부팅 자동기동 진짜 원인 수정 — SELinux가 init_t의 su 실행을 거부 |
| `710b632` | docs: 2026-07-23 핸드오프 — Oracle 부팅 자동기동 SELinux 원인 규명 |
| `7b6a6d7` | docs: 7/23 커밋 표 보정 — 문서를 닫는 커밋 반영 (§4-4) |
| `0291d29` | docs: 7/23 재부팅 검증 통과 — Oracle 부팅 자동기동 이월 종결 |
| `1b6bd55` | docs: 7/23 커밋 표 최종 대조 (§4-4) |
| `18dffad` | docs: PDB saved state 확인 — 현 서버는 안전, 재구축 절차로 이관 |
| `73c6bcc` | docs: .env 실값 백업 완료 — 이월 미완 0건 |
| `bd53733` | docs: 7/23 커밋 표 대조 — 이후 작업 커밋 3건 반영 (§4-4) |
| `c90c8dc` | fix: scrimage WebP 바이너리를 전용 tmpdir로 — /tmp 누수 차단 |
| `340fabc` | test: ItemThumb 컴포넌트 테스트 (6건) — 순수 컴포넌트 전부 커버 |
| `d80df14` | docs: 문의 목록 이미지 N+1 실측 — N+1 없음 (P6SPY) |
| `5a5d05f` | docs: 7/23 커밋 표 대조 + 이월 정리 (§4-4, §4-1) |
| `55480be` | feat: 주문 배송 추적 — 운송장(택배사·송장번호) + 배송완료 상태 (V13) |
| `70d2c03` | docs: 배송 추적(V13) 핸드오프 §8 + ARCHITECTURE 주문 상태 갱신 |
| `d4fd043` | docs: 배포 사고 기록(§8-6) + 규약 2건 승격 (§5) |
| `f9bbc89` | refactor: 배송 조회 URL을 설정으로 — 실제 택배사 대신 앱 내 예시 페이지 |
| `c54dc87` | docs: 배송 조회 예시 페이지 배포 확인 + main ff 누락 재발 기록 |
| `c3bc7df` | build: 배포 스크립트가 브랜치 뒤처짐을 경고하도록 (check-deploy-branch.sh) |
| `12632c7` | docs: 배포 브랜치 경고 §9 + 이월 종결 (배송 추적 브라우저 검증 통과) |
| `a4e731f` | docs: 후속 후보에서 main ff 머지 제거 — 오늘 처리했고 자동 경고로 대체됨 |
| `2b38cd9` | feat: 주문 배송비 — 정책은 설정, 부과 금액은 스냅샷 (V14) |
| `c2c533c` | docs: 배송비(V14) 핸드오프 §10 + ARCHITECTURE 갱신 |
| `35eba58` | docs: 기능 백로그를 BACKLOG.md로 분리 — 이월에서 제품 기능 이관 |
| `ecc9165` | docs: 7/23 커밋 표 최종 대조 — 백로그 분리 커밋 반영 (§4-4) |
| `0796b20` | feat: 사람이 읽는 주문번호 (V15) — yyyyMMdd + 전역 시퀀스 |
| `a1aaf4d` | docs: 주문번호(V15) 핸드오프 §11 + 백로그 A-3 완료 처리 |
| `f5e9996` | docs: V15 빈 DB 검증 통과 — 이월 미완 0건 |
| `a3d1f76` | docs: 7/23 커밋 표 최종 대조 — git log에서 재생성 (§4-4) |
| `0c1349d` | feat: 정가/할인가 (V16) — price의 의미는 그대로, list_price를 더한다 |
| `e7d4ed3` | docs: 정가/할인가(V16) 핸드오프 §12 + 백로그 B-7 완료 |
| `12d1c5c` | docs: V16 빈 DB 검증 통과 — 이월 미완 0건 |

전체 목록은 `git log --oneline --since="2026-07-23 00:00"` 로 본다.

> §4-4 대로 작업을 멈출 때마다 `git log` 와 대조했다(오늘만 여섯 번). 뒤에 표를 채우는 커밋이
> 하나 붙으며, 구조상 자기 자신은 담을 수 없다.
>
> **⚠ 이 표에서 두 번 같은 실수를 했다** — `1b6bd55` 와 `bd53733` 에 각각 *"이 뒤에는 표를 채우는
> 커밋 하나만 붙는다"* 고 적었는데 **두 번 다 틀렸다.** 실제로는 그 뒤에 작업 커밋이
> 2건, 다시 4건 더 붙었다. **표가 작업의 끝을 선언할 수 없다** — 그건 사람이 정한다.
> 그러니 "이제 마지막"이라고 쓰지 말고, **멈출 때마다 대조**하는 것으로 족하다.

**오전(§1~§7)은 배포 대상이 아니었다** — 인프라 설정 · 빌드 설정(`build.gradle` 의 `java.io.tmpdir`) ·
테스트 · 문서만 바뀌었고 프론트 번들 해시도 그대로였다(§6).
**오후(§8)는 다르다** — 런타임 코드와 API 계약이 바뀌어 **백·프론트 모두 배포했다**(§8-6).

> ⚠ `627813c` 는 **원인을 잘못 짚은 커밋**이다. 되돌리지는 않았다 — 거기서 넣은 설정
> (hosts 핀·network-online 순서)은 그 자체로 타당해서 남겨둘 값어치가 있다. 다만
> **이 장애의 원인은 아니었고**, `ffb9fbc` 에서 README 의 원인 서술을 바로잡았다.

---

## 1. 발단 — 이월 1번은 "검증 대기"가 아니라 "이미 고장"이었다

어제 이월 1번은 이렇게 남아 있었다: *"`sudo systemctl enable oracledb_ESPDB-19c` 하나뿐이다(오늘 실행함).
내일 재부팅해서 확인할 것 — `enable` 만으로 SysV 스크립트가 실제로 뜬다는 보장은 없다."*

오늘 아침 **08:15:31 에 재부팅이 이미 일어나 있었고**, 그 경고가 그대로 적중했다.

| 실측 (08:15 부팅) | 값 |
|---|---|
| `oracledb_ESPDB-19c` | `enabled` 인데 상태 **`failed`** |
| 부팅 로그 08:15:44 | `Failed to start Oracle Net Listener ... status=1/FAILURE` |
| `ora_pmon_ESPCDB` | 떠 있음 · 리스너 `:3306` LISTEN |
| `glassvue-backend` | `active` |

DB 가 떠 있고 백엔드도 살아 있어서 **처음엔 자동기동이 성공한 것처럼 보였다.** 사용자에게 확인해
보니 **손으로 `lsnrctl start` + `startup` 을 한 것**이었다. 즉 자동기동은 실패했고 수동 복구된 상태였다.

> **교훈**: "지금 떠 있다"는 **자동기동 성공의 증거가 아니다.** 누가 손으로 올렸는지 반드시 확인한다.

---

## 2. 헛짚은 가설 4개 (기록해 두는 이유)

| # | 가설 | 왜 틀렸나 |
|---|---|---|
| 1 | **호스트명이 IPv6 링크로컬로 해석돼 리스너 바인드 실패** | `listener.ora` 가 `(HOST = ecstel)` 인데 `/etc/hosts` 에 핀이 없어 `getent hosts ecstel` 이 `fe80::…` 를 먼저 보여줬다. 그럴듯했지만 **프로그램이 실제로 쓰는 `getaddrinfo`(`getent ahosts`)는 `192.168.50.14` 하나만** 돌려준다. 해석은 원래 정상이었다 |
| 2 | **부팅 시 네트워크 미준비** | sysv-generator 유닛에 네트워크 의존성이 없어 부팅 13초 만에 실행되는 건 사실이었다. override 로 **13초 → 39초**까지 밀었는데 **똑같이 실패**했다 → 타이밍 문제가 아니다 |
| 3 | **sqlplus `startup` 실패** | 수동 실행하면 정상 기동한다. 게다가 `sqlplus -s` 는 내부 SQL 이 실패해도 `exit` 이 **0 을 반환**한다(`WHENEVER SQLERROR EXIT` 없음) → `RETVAL1` 은 사실상 항상 0이라 애초에 범인이 될 수 없었다 |
| 4 | **`su` 로 환경변수가 전파되지 않음** | 스크립트는 root 셸에서 `export ORACLE_HOME/ORACLE_SID` 후 `su` 로 넘긴다. 직접 확인: `SID=[ESPCDB] HOME=[/opt/oracle/product/19c/dbhome_1]` — **정상 전파** |

1·2 는 `627813c` 로 커밋까지 하고 재부팅했다가 **09:21:38 재부팅에서 동일하게 실패**하며 탈락했다.
이때 백엔드가 DB 를 못 잡아 `activating` 무한 재시작 — **운영이 다시 내려갔다.**

> **여기서 방향을 틀었어야 했다.** "고쳤는데 그대로 실패"는 **가설이 틀렸다는 신호**인데,
> 처음엔 "override 가 안 먹었나" 쪽을 의심했다. 실제로는 override 는 **잘 먹었고**(13→39초가 증거),
> 그 사실이 오히려 2번 가설을 반증하고 있었다.

---

## 3. 진짜 원인 — SELinux 가 `init_t` 의 `/bin/su` 실행을 거부한다

### 3-1. 왜 아무 에러도 안 보였나

부팅 로그를 성공 케이스와 나란히 놓자 결정적 차이가 보였다.

```
[실패, 08:15/09:22]                        [성공, 09:53]
Starting Oracle Net Listener.              Starting Oracle Net Listener.
                                           Oracle Net Listener started.        ← 이 줄이 없다
Starting Oracle Database instance ESPCDB.  Starting Oracle Database instance ESPCDB.
                                           Oracle Database instance ESPCDB started.  ← 이 줄도 없다
Failed to start ...                        Started SYSV: ...
```

**둘 다** 실패했고, 전부 **같은 1초 안**에 끝났다. 두 명령의 유일한 공통점은 **`su`** 다.

원인 추적이 어려웠던 이유가 세 겹이다:

1. 스크립트가 두 명령의 출력을 **`> /dev/null 2>&1` 로 통째로 버린다**(70·83행).
   그래서 `"Failed to start ..."` 한 줄만 남는다.
2. **Oracle 이 아예 실행되지 않았으므로** TNS/ORA 에러도, 리스너 로그도 **어디에도 없다.**
   "리스너 로그를 보자"가 계속 헛돌았다.
3. 셸에서 수동 실행하면 `unconfined_t` 라 **항상 성공**한다 → "수동은 되는데 부팅만 실패"로 보인다.

### 3-2. 증거

`journalctl | grep avc` 는 **빈손이었다.** AVC 거부는 저널이 아니라 **`/var/log/audit/audit.log`** 로만 간다.
(단서는 있었다 — 실패 다음 초에 `Starting SETroubleshoot daemon for processing new SELinux denial logs...`)

```
$ sudo ausearch -ts 09:22:10 -te 09:22:25

type=AVC msg=audit(...:64): avc: denied { execute } for pid=1077 comm="oracledb_ESPDB-" name="su"
  scontext=system_u:system_r:init_t:s0
  tcontext=system_u:object_r:su_exec_t:s0  tclass=file permissive=0
type=AVC msg=audit(...:66): avc: denied { execute } for pid=1087 ... (동일)
type=SYSCALL ... success=no exit=-13
type=SERVICE_START ... unit=oracledb_ESPDB-19c ... res=failed
```

**거부가 PID 1077·1087 두 개** — 리스너용 `su` 와 sqlplus 용 `su`, 정확히 두 번이다.
`exit=-13`(EACCES). SELinux 는 `Enforcing`.

### 3-3. 수정 — 원본 스크립트를 건드리지 않는다

```
/bin/su           → su_exec_t   (init_t 에서 거부)
/usr/sbin/runuser → bin_t       (init_t 가 실행 가능)
```

`runuser` 는 util-linux 가 **init 스크립트용으로 제공하는 `su` 대체품**이고 옵션(`-s`/`-c`)이 같다.
그리고 스크립트 47행에 훅이 있다:

```bash
if [ -z "$SU" ];then SU=/bin/su; fi
```

→ **환경변수 `SU` 만 넣으면 원본 스크립트 수정 없이 교체된다.** drop-in 에 한 줄:

```ini
[Service]
Environment=SU=/usr/sbin/runuser
```

원본(`/etc/init.d/oracledb_ESPDB-19c`)은 설치 프로그램 소유라 손대지 않는 방침을 지켰다
(`infra/README` 의 "여기 없는 것" 항목).

### 3-4. 검증 — **재부팅 없이** 부팅 조건을 재현했다

`systemctl start` 는 ExecStart 를 **`init_t` 컨텍스트로 실행**한다. 즉 SELinux 조건이 부팅과 같다.
DB 를 내린 뒤 `systemctl start` 가 성공하면 부팅에서도 성공한다.

```
$ sudo bash /tmp/ora-debug.sh stop          # cold 상태로
$ sudo systemctl start oracledb_ESPDB-19c   # ← init_t 로 실행됨
$ systemctl is-active oracledb_ESPDB-19c glassvue-backend
active
active
```

실측(09:53):

| 항목 | 값 |
|---|---|
| 기동 소요 | **30초** (09:53:02 → 09:53:32) — 실패할 땐 1초였다 |
| 유닛 로그 | `runuser[27272]: pam_unix(runuser:session): session opened for user oracle` ← `su` 아님 |
| | `Oracle Net Listener started.` · `Oracle Database instance ESPCDB started.` **둘 다 출력** |
| `avc denied su` | **0건** |
| health | `{"status":"UP"}` · `/api/products` **HTTP 200** |
| 드리프트 | `check-infra-drift.sh` **0** (4개 전부 일치) |

> **이 "1초 vs 30초"가 가장 빠른 구별점이다.** 1초 만에 끝났으면 Oracle 이 실행조차 안 된 것이다.

### 3-5. 재부팅 최종 확인 — **통과**

10:08 재부팅. **부팅 후 아무것도 손대지 않고** 상태만 확인했다(§6-1 — 손대면 그 부팅은 못 쓴다).

```
$ systemctl is-active oracledb_ESPDB-19c glassvue-backend
active
active
```

부팅 로그가 그대로 증명한다:

```
10:08:11  부팅
10:08:40  Starting Oracle Net Listener.
10:08:40  runuser[1103]: pam_unix(runuser:session): session opened for user oracle   ← su 아닌 runuser
10:08:42  Oracle Net Listener started.                    ← 실패할 땐 없던 줄
10:08:42  Starting Oracle Database instance ESPCDB.
10:09:39  Oracle Database instance ESPCDB started.        ← 이것도
10:09:39  Started SYSV: ...                               ← systemd 가 성공으로 판정
```

| 항목 | 값 |
|---|---|
| Oracle 시작 | 부팅 후 **29초**(10:08:11 → 10:08:40) — network-online 대기가 걸린 결과 |
| 스크립트 소요 | **59초**(10:08:40 → 10:09:39). 실패할 땐 1초였다 |
| `avc denied su` | **0건** |
| 검증 | health `{"status":"UP"}` · `/api/products` **200** · `:3306`·`:8080`·`:443` 리스닝 |

**이월 1번 종결.** 7/22 부터 이어진 "Oracle 이 부팅에 자동으로 안 뜬다" 는 이로써 끝났다.

---

## 4. 진단 중 발견한 별건 — 스크립트가 PDB 를 못 연다

디버그 실행(리다이렉트를 벗긴 사본)에서 드러났다.

```
bash: 경고: here-document가 줄 1에서 파일의 끝으로 구분됨 (`EOF'를 예상함)
ORA-00933: SQL 명령어가 올바르게 종료되지 않았습니다
SP2-0042: 알 수 없는 명령어 "EOF" - 무시됨
```

스크립트 78~83행이 두 군데 깨져 있다:

1. heredoc 종료자 `EOF` 가 **들여쓰기**돼 있다 — `<<` 는 들여쓴 종료자를 인식하지 않는다(`<<-` 가 아님).
2. `alter pluggable database all open` 에 **세미콜론이 없어** 다음 줄 `exit` 이 붙어버린다 → `ORA-00933`.

즉 **스크립트는 PDB 를 여는 데 매번 실패하고 있다.** 그런데도 `espdb` 는 열려 있다 — 확인해 보니
Oracle 19c 의 PDB **saved state** 가 대신 열고 있었다.

```
select name, open_mode from v$pdbs;               → ESPDB  READ WRITE
select con_name, state from dba_pdb_saved_states; → ESPDB  OPEN
```

**오늘 재부팅이 그대로 실증한다** — 10:08 부팅에서 스크립트의 `alter ...` 는 `ORA-00933` 으로 실패했는데
`ESPDB` 는 `READ WRITE` 로 올라왔다. saved state 가 실제로 동작한 것이다.

> **처음엔 "saved state 가 없어지면 터진다"고 적었는데 표현이 과했다.** saved state 는 CDB 에
> **영구 저장되는 정식 메커니즘**이라 저절로 사라지지 않는다(`discard state` 를 명시적으로 하거나
> PDB 를 재생성해야 없어진다). **현 서버에는 추가 조치가 필요 없다.**

→ 다만 **재구축 시에는 이 상태가 없다.** 새로 만든 PDB 는 saved state 가 비어 있어 부팅 후
`MOUNTED` 로 남는다. `infra/README` 재구축 절차에 "PDB 를 연 뒤 `save state` 한 번" 을 명시했다.

---

## 5. `/tmp` WebP 바이너리 누수 — 전용 tmpdir 로 해결 (7/21 부터 이월)

scrimage-webp 는 JVM 이 시작할 때 `cwebp`(4.6MB)·`dwebp`(1.9MB)를 `java.io.tmpdir` 에 추출하고
**종료할 때 정리하지 않는다.** 파일명이 JVM 마다 고유해서(`cwebp<난수>binary`) **덮어쓰지도 않는다.**
→ `gradle test`·`bootRun` 을 돌릴 때마다 `/tmp` 에 6.5MB 씩 쌓였다.

### 5-1. ⚠ 착수 전 판단이 틀렸다 — "무한히 커진다"가 아니었다

이 항목을 "시간이 지날수록 나빠지는 **유일한** 항목"이라며 1순위로 추천했는데, **근거가 틀렸다.**
착수해서 실측해 보니:

| 확인 | 결과 |
|---|---|
| `/usr/lib/tmpfiles.d/tmp.conf` | **`q /tmp 1777 root root 10d`** — 10일 경과분 자동 삭제 |
| `systemd-tmpfiles-clean.timer` | **active** (하루 1회, 실측 당시 33분 전 실행됨) |
| 그래서 | **무한 증가가 아니라 10일치에서 상한이 걸린다** |
| 규모 | 37MB = 디스크의 **0.14%**. `/tmp` 전체가 58MB |
| 루트 81% 의 실제 원인 | `/opt/oracle` 11G + `/usr` 5.3G + `/home` 3G (7/21 점검) — `/tmp` 와 무관 |
| 운영 서비스 | **누수 없음.** 유닛이 `PrivateTmp=yes` 라 격리 — private tmp 13개 총 **0바이트**, webp 바이너리 **0개** |

→ **디스크 압박의 원인이 아니고, 운영과도 무관하며, 자동 상한도 있다. 위험이 아니라 위생 문제였다.**
그래도 대응이 싸서(설정 몇 줄) 그대로 진행했다. **다만 "1순위"라는 판단은 철회한다.**

> **교훈**: "계속 커진다"고 단정하기 전에 **이미 걸려 있는 자동 정리부터 확인**한다.
> `tmpfiles.d` 규칙 한 줄이 전제를 통째로 바꿨다.

### 5-2. 선택한 안과 버린 안

**① 앱 전용 `java.io.tmpdir`** 을 골랐다 — `build/apptmp` 로 돌려 **`gradle clean` 이 함께 치우게** 한다.

- **버린 ②(`systemd-tmpfiles` 주기 정리)**: 이미 10일 규칙이 있어 중복이고, 기간을 줄이면
  **실행 중 JVM 이 쓰는 바이너리를 지울 위험**이 있다. scrimage 는 변환할 때마다 그 바이너리를
  `exec` 하므로, 지워지면 그 뒤 변환이 깨진다.
- **버린 ③(현행 유지)**: 자동 상한이 있으니 방치해도 되긴 하지만, 개발할 때마다 6.5MB 씩
  시스템 `/tmp` 에 남기는 건 정리 대상이 불분명하다. 프로젝트 `build/` 안이면 **경계가 명확하다.**

### 5-3. 검증 (실측)

`build.gradle` 에 `Test`·`JavaExec`(bootRun 포함) 대상으로 `java.io.tmpdir` 을 지정하고,
디렉토리가 없으면 임시파일 생성이 실패하므로 `doFirst` 에서 만든다.

| 확인 | 결과 |
|---|---|
| 테스트 실행 후 `/tmp` webp 개수 | **11 → 11** (새 파일이 `/tmp` 에 안 생김) |
| `build/apptmp/` | `cwebp3636765153898754775binary` — **여기로 감** |
| 테스트 | `tests 1  failures 0  errors 0  **skipped 0**` (§3 체크리스트대로 `.env` 소싱) |
| `gradle clean` 후 | `build/apptmp` **삭제됨** |

---

## 6. `ItemThumb` 컴포넌트 테스트 (7/21 부터 이월)

순수 컴포넌트 중 **유일하게 테스트가 없던 것**. 이걸로 DevExtreme 을 안 쓰는 컴포넌트는 전부 커버됐다.

### 무엇을 고정했나 — "로드 실패가 정상 흐름"이라는 계약

이 컴포넌트가 **존재하는 이유 자체**가 실패 처리다. 주문 품목의 이미지 URL 은 **주문 시점 스냅샷**이라,
이후 상품이 삭제되면 파일이 정리돼 **404 가 된다**(V9, 7/21 §12). 이름·가격이라는 본질 기록은 남으므로
화면은 깨지지 않고 대체 표시로 넘어가야 한다.

6건을 넣었고, 그중 **둘이 핵심**이다:

- **로드 실패 시 대체 표시로 넘어간다** — `img` 에 `error` 를 발생시키면 `img` 가 사라지고 `🖼️` 가 뜬다.
- **`src` 가 바뀌면 실패 상태를 초기화하고 다시 시도한다** — 목록에서 컴포넌트가 **재사용**되므로,
  실패 상태가 남아 있으면 **다음 품목까지 계속 대체 표시로 뜬다.** `watch(() => props.src)` 가 그걸 막는데,
  이건 없어도 화면이 "그럴싸하게" 동작해서 조용히 회귀하기 쉬운 종류다 — 그래서 테스트로 못박았다.

나머지 4건: `src` 있으면 이미지 / 없으면 대체 표시(배송지 도입 이전 주문처럼 스냅샷이 없는 경우) /
`size` 클래스 반영(목록과 상세의 썸네일 크기가 다르다) / 대체 표시는 `aria-hidden`(장식).

### 검증 (실측)

| 확인 | 결과 |
|---|---|
| `pnpm vitest run` | **52개 통과**(46 → +6), 테스트 파일 10개, 실패 0 |
| `pnpm build` | 성공. 번들 해시 `index.437a53f0.js` — **기존 배포본과 동일** |

→ 테스트만 추가라 **산출물이 안 바뀌었다. 배포 불필요.**

---

## 7. 문의 목록 이미지 N+1 실측 (7/21 부터 이월) — **N+1 없음**

`findByGroups` 배치로 회피했다고 적어만 뒀고 **실제 쿼리 수를 센 적이 없던** 항목.
P6SPY 는 이미 깔려 있었으므로(`application-dev.yml` 이 URL 에 `p6spy:` 접두사만 붙인다) 띄워서 셌다.

### 7-1. 측정 방법

- dev 인스턴스를 **8081** 에 띄우고 **Redis 는 db3 으로 격리**했다.
  격리한 이유가 둘이다 — ①운영 캐시(db0)를 안 건드리려고 ②`products:list` 가 캐시되므로
  **캐시 히트가 나면 쿼리가 0 이 되어 측정이 무의미**해진다.
- 요청 직전 로그 줄 수를 기록하고, 요청 후 **그 뒤에 붙은 줄만** 잘라 세는 방식(다른 요청과 섞이지 않게).
- 데이터를 만들지 않고 **읽기만** 했다 — 검증 잔여물 없음(§3).

### 7-2. 결과

**문의 목록** (`/api/products/{지바}/inquiries`, 문의 4건·이미지 그룹 1개) — **총 2회**

```
1  select ... from inquiry i1_0 where ...        ← 목록
2  select ... from image i1_0 where image_group_id in (...) order by sort_order
```

**상품 목록** (`/api/products?size=20`, 이미지 그룹 2개) — **총 3회**

```
1  select ... from product p1_0 ... fetch first 20 rows only
2  select ... from image i1_0 where i1_0.image_group_id in ('019F82DA…','019F64BA…') order by sort_order
3  select ... from category c1_0 where c1_0.id in ('019F647B…','019F647B…',NULL × 98)
```

### 7-3. 판정

- **이미지 N+1 없음.** 상품 목록에서 **그룹이 2개인데 이미지 쿼리는 1회**다 — N+1 이었다면 3회여야 한다.
- `ImageService.findByGroups` 의 `i.getImageGroup().getId()` 가 **LAZY 프록시를 초기화하지 않는다**는 것도
  같은 결과로 실증됐다(초기화됐다면 그룹 수만큼 `image_group` 조회가 붙는다).
- **문의 목록도 같은 `findByGroups` 를 호출**하므로 그 성질이 그대로 적용된다.

> ⚠ **측정의 한계를 정확히 적는다**: 문의 쪽은 **이미지 그룹이 1개뿐**이라, 그 요청만으로는
> 배치와 N+1 이 **구분되지 않는다**(둘 다 1회). 판별은 그룹이 2개인 **상품 목록이 해줬고**,
> 문의는 동일 코드를 호출한다는 근거로 이어 붙인 것이다. 그룹이 3개 이상인 문의 데이터를 만들면
> 문의 경로만으로도 직접 판별할 수 있으나, 그러려면 ZZ 검증 데이터(상품·이미지·문의) 생성이 필요해
> 오늘은 하지 않았다.

### 7-4. 부수로 확인한 것

- **카테고리 `IN` 에 붙은 `NULL` 98개는 버그가 아니다.** `application.yml` 의
  `hibernate.default_batch_fetch_size: 100` 이 동작한 결과다(주석에 *"지연로딩 연관(예: product.category)을
  페이지 단위로 일괄 로딩해 N+1 완화"* 라고 이미 적혀 있다). 즉 **카테고리도 N+1 이 아니라 배치**다.
  바인드 슬롯을 100개로 패딩하는 건 실행계획 재사용을 위한 Hibernate 동작이다.
- **문의 목록에 count 쿼리가 없다.** 첫 페이지이고 결과 수(4)가 페이지 크기(50)보다 작아
  Spring Data 가 count 를 생략한 것(`PageableExecutionUtils`). 정상이다.

### 7-5. 정리 확인

dev 종료 후 실측: 8081 응답 없음 · 운영 `active`/health `UP` · **운영 캐시 db0 253키 무손상** · dev db3 0키.

---

## 8. 기능 추가 — 주문 배송 추적 (V13)

이월을 다 털고 **이커머스 기준 기능 갭**을 뽑아 그중 하나를 구현했다. 후보를 A(주문 흐름의 구멍)·
B(커머스 표준)·C(규모 확장)로 나눴고, **A-1 배송 추적**을 골랐다 — 새 기능이라서가 아니라
**이미 만든 것이 미완이라서**다. 배송지(V11)·진행 스텝 UI·관리자 발송 처리를 다 만들어 놓고
"보냈고 어떻게 가고 있는지"만 비어 있었다.

> 참고로 뽑은 갭 목록: **A**-1 배송추적 / A-2 배송비 / A-3 사람이 읽는 주문번호,
> **B**-4 쿠폰 / B-5 배송지 주소록 / B-6 위시리스트 / B-7 정가·할인가,
> **C** 상품 옵션(사이즈·색상) · 반품/교환 · 적립금·등급 · 매출 통계.
> **A-2(배송비)는 A-1 다음으로 값어치가 크다** — 금액 계산의 토대라 쿠폰(B-4)보다 먼저 와야 한다.
> 배송비 없이 쿠폰을 넣으면 계산 로직을 다시 짜게 된다.

### 8-1. 만든 것

상태 흐름이 `ORDERED → PAID → SHIPPED → **DELIVERED**` 로 늘었다.

| 계층 | 내용 |
|---|---|
| DB | **V13** — `orders`에 `ship_carrier`·`ship_tracking_no`·`delivered_at`(전부 nullable, CHAR semantics) + `status` CHECK 교체 |
| enum | `DeliveryCarrier`(CJ·우체국·한진·롯데·로젠·기타) — 택배사별 **조회 URL 형식을 서버가 갖는다** |
| 엔티티 | `Order.ship(carrier, trackingNo)` · `deliver()` · `isDeliverable()` |
| DTO | `OrderShipRequest`(둘 다 `@NotNull`/`@NotBlank`) · `OrderResponse`에 `shipCarrier`·`shipCarrierName`·`shipTrackingNo`·`trackingUrl`·`deliveredAt` |
| API | `POST /api/orders/{id}/ship`이 **본문을 받게 변경** · `POST /api/orders/{id}/deliver` 신설(둘 다 ADMIN) |
| 프론트 | 주문 상세에 **배송 추적 카드**(조회 링크) + 진행 스텝 4단계, 관리자 화면·상세에 **운송장 입력 폼**, 배송완료 버튼, 상태 탭에 배송완료 |

### 8-2. 판단 근거 (버린 안 포함)

- **운송장을 필수로 했다.** 선택으로 두면 "발송됐는데 추적 정보가 없는" 주문이 생기는데,
  나중에 채워 넣을 경로가 없어 그 주문은 **영영 추적 불가**가 된다. 그래서 발송 전이와
  운송장 등록을 한 트랜잭션으로 묶었다("발송 처리 = 운송장 등록").
- **조회 URL을 서버가 만든다.** 택배사별 URL 형식을 프론트에 두면 화면마다 중복되고 서버는
  검증할 근거가 없다. 응답에 완성된 `trackingUrl`을 실어 주면 화면은 링크만 걸면 된다.
  링크가 없으면(null) 화면이 감추고 송장번호만 보여준다.
  > ⚠ **처음엔 실제 택배사 URL을 enum에 박았다가 되돌렸다 — §8-7 참고.**
- **`ship_carrier`에는 CHECK를 안 걸었다.** 택배사는 앞으로 늘어날 값이라 CHECK를 걸면
  하나 추가할 때마다 아래 §8-3의 제약 교체 작업을 반복해야 한다. 검증은 enum이 역직렬화 단계에서 한다.
  반대로 `status`는 상태 전이 규칙이라 **DB 레벨 보호를 유지**했다 — 둘의 성격이 다르다.
- **`isCancellable()`은 손대지 않았다.** `<> CANCELLED`가 아니라 **명시적 열거**(ORDERED·PAID)로
  돼 있어서 DELIVERED가 늘어도 자동으로 포함되지 않았다. 7/16에 상태를 확장하며 리뷰 구매인증
  범위가 조용히 어긋났던 사고(ARCHITECTURE §5)의 대비책이 실제로 작동한 사례다.

### 8-3. ⚠ Oracle CHECK 제약 교체 — 이름이 DB마다 다르다

`V1__init.sql`이 `status`를 **인라인 CHECK**로 선언해서 제약 이름이 자동 생성(`SYS_Cnnnnnn`)이고,
그 이름이 **espdb와 esptest에서 서로 다르다.** 이름을 하드코딩하면 한쪽에서만 동작하고
빈 DB 검증(§2-2)에서 깨진다. → PL/SQL로 **조건으로 찾아 드롭**한 뒤 `ck_orders_status`로
**이름을 붙여** 다시 만들었다. 다음 상태 추가부터는 `migration/README`의 예시대로 DROP/ADD 두 줄이면 된다.

- 못 찾아도 실패시키지 않게 했다(루프). **대신 그게 위험 지점이다** — 드롭이 조용히 실패하면
  옛 제약(4값)과 새 제약(5값)이 **공존**해 `DELIVERED` 저장이 ORA-02290으로 터진다.
  그래서 검증에서 **제약이 1개인지**를 직접 확인했다(§8-4).
- 구 jar 영향 없음: **넓히는 방향**이라 구 jar는 DELIVERED를 쓰지 않고 쓰던 4개 값은 그대로 허용된다.
  §5가 경계하는 건 좁히는 경우다. 사용자 확인 후 적용했다.

### 8-4. 검증 (실측)

| 확인 | 결과 |
|---|---|
| 백엔드 테스트 | **183개**(173 → +10) · failures 0 · errors 0 · **skipped 0** (`.env` 소싱, §3) |
| 빈 DB(esptest) | **V1→V13 순차 적용** + `ddl-auto=validate` 통과(앱 기동 56초) |
| 스키마 실측(esptest·espdb **양쪽**) | `CK_ORDERS_STATUS` **단일 제약**에 5개 값 → 동적 DROP이 실제로 옛 제약을 지웠다 |
| 컬럼 | `ship_carrier` 30 **CHAR** · `ship_tracking_no` 50 **CHAR** · `delivered_at` TIMESTAMP(9) WITH TIME ZONE |
| 프론트 | vitest **56개**(52 → +4) 통과 · `pnpm build` 성공 |
| 검증 잔여물 | `ZZ` 주문 **0건**(통합 테스트는 `@Transactional` 롤백) |

> **덤**: esptest 기동 로그에서 §5의 `/tmp` 수정도 실증됐다 —
> `Installing binary at .../build/apptmp/cwebp…binary` (시스템 `/tmp` 아님).

### 8-5. ⚠ HTTP 호출부 — 컴파일러가 안 잡아주는 자리

`POST /api/orders/{id}/ship`이 본문을 받게 바뀌었다. **7/21에 똑같은 걸로 배포가 막혔던** 자리라
(WORKING-AGREEMENTS §3) 착수 전에 호출부를 먼저 전수 조사했다.

- 컴파일러가 잡아준 것: `Order.ship()`·`OrderService.ship()` 호출 **5곳**(테스트).
- **컴파일러가 못 잡는 것**: `mockMvc.perform(post(".../ship"))` 2곳, 프론트 `shipOrder()` 2곳.
  → grep으로 찾아 전부 고쳤다. 이번엔 배포 전에 막히지 않았다.

### 8-6. ⚠ 배포 사고 — 옛 코드가 조용히 배포됐다

배포 스크립트를 돌렸는데 **백·프론트 둘 다 옛 코드가 나갔다.** 원인은 하나다:

> **배포 스크립트는 작업 브랜치가 아니라 `main` 워크트리(`/home/ecstel/work`)에서 빌드한다.**
> 그때 `main`은 배송 추적 커밋 2건을 아직 못 받은 상태였다.

**가장 나쁜 점은 조용했다는 것이다.** 스크립트는 정상 종료했고 jar 파일 시각(13:45)도 갱신돼서
겉보기엔 배포에 성공한 것 같았다. 실제로 나간 건 그 전 커밋이었다.

**게다가 내 확인 방법이 그 사실을 한 번 가렸다.** 신규 `/deliver` 가 있는지 보려고 미인증 요청을
날려 **401이 나오니 "엔드포인트 있음"으로 판단**했는데, `/api/orders/**` 는 인증이 필요해서
**없는 경로도 401**이다. 존재 여부를 상태코드로 가리려 한 게 잘못이었다.

→ 확실한 신호로 다시 확인했다:

| 신호 | 배포 전(옛 코드) | 배포 후 |
|---|---|---|
| OpenAPI 경로 | `/ship` 만 | **`/ship` + `/deliver`** |
| `/ship` requestBody | 없음 | **`OrderShipRequest`(`carrier`·`trackingNo` 둘 다 required)** |
| carrier enum | — | `CJ·KOREA_POST·HANJIN·LOTTE·LOGEN·ETC` |
| 프론트 번들 | `index.437a53f0.js` | **`index.2160b31c.js`**(로컬 빌드와 일치) |

**두 교훈 모두 WORKING-AGREEMENTS §5로 옮겼다** — ①배포 전 `main` ff 머지 ②배포 확인은
OpenAPI·번들 해시 같은 "코드가 실제로 바뀐 증거"로.

### 8-7. ⚠ 되돌린 결정 — 실제 택배사 URL을 코드에 박았다가 뺐다

처음 구현에서 `DeliveryCarrier` enum에 **실제 택배사 조회 URL**(CJ대한통운·우체국·한진·롯데·로젠)을
박았다. 그리고 그게 동작하는지 확인한다고 **그 사이트들에 curl GET을 보냈다.**
사용자가 *"실제 택배사 사이트에 보내면 어떡하냐, 돈 나가는 거 아니냐"* 며 중단시켰다.

**과금은 실제로 없었다** — 계약도 API 키도 없는 공개 조회 페이지에 대한 단순 GET 5건이고,
브라우저로 그 주소를 여는 것과 같다. 하지만 **지적 자체는 타당했고, 두 가지가 잘못이었다:**

1. **외부로 나가는 호출을 확인 없이 했다.** 결과가 무해했다는 건 사후 판단이고, 보내기 전에 물었어야 한다.
2. **더 근본적으로, 연습 단계에서 실제 택배사를 가리키는 설계 자체가 틀렸다.**
   - 주문이 전부 가짜라 송장번호도 가짜 → 링크를 눌러도 **"조회 결과 없음"** 만 나온다. 쓸모가 없다.
   - **통제할 수 없는 외부 의존**이 생긴다. 실제로 확인 중 CJ만 응답하지 않았는데(`000`),
     봇 차단인지 URL 변경인지 **알 방법이 없다는 게 문제의 핵심**이다 — 우리 화면의 링크가
     조용히 깨져 있어도 알 수 없다.
   - ARCHITECTURE §1 "미리 만들지 않는다"에도 어긋난다.

**고친 방식** — URL을 **설정으로 빼고 기본값은 앱 안의 예시 페이지**로:

| 계층 | 변경 |
|---|---|
| `DeliveryCarrier` | URL 제거, `displayName` + `isTrackable()`만 남김(ETC는 조회할 곳이 없다) |
| `DeliveryProperties` | `glassvue.delivery` 설정. 택배사별 URL 맵 + 기본 형식. 자리표시자 `{trackingNo}`·`{carrier}` |
| `application.yml` | 기본값 `/mock-tracking?carrier={carrier}&no={trackingNo}` |
| 프론트 | `MockTrackingView.vue` + `/mock-tracking` 라우트 — **"예시 페이지입니다"를 화면 맨 위에 명시**하고, 배송 단계의 시각은 **비워 둔다**(지어낸 값을 넣지 않는다) |

- **실서비스 전환은 설정만 채우면 된다** — 코드 변경 없음. PG를 seam으로 남겨둔 것과 같은 방식.
- `default-tracking-url`을 빈 문자열로 두면 **링크 자체를 안 만든다**(송장번호만 표시).
- 이 전환 경로가 실제로 되는지 `DeliveryPropertiesTest`로 고정했다(택배사별 설정이 기본값을 이긴다).

> **교훈**: 연습 단계 프로젝트에서 **외부 서비스로 실제 요청을 보내거나 링크를 걸지 않는다.**
> 필요하면 **앱 안에 예시 페이지를 만들어** 거기로 보내고, 외부 호출이 필요하면 **먼저 묻는다.**

**배포 확인(실측)**: jar에 `DeliveryProperties.class` 존재(14:28) · `DeliveryCarrier.class`가
2794→**1984바이트**로 줄어듦(URL이 빠진 부수 증거) · 프론트 번들 **`index.5c2faeef.js`**(로컬과 일치) ·
번들에 `/mock-tracking` 포함 · health `UP`.

> ⚠ **`/mock-tracking`에 HTTP 200이 온다는 건 배포 증거가 아니다** — SPA라 nginx가 모든 경로에
> `index.html`을 돌려준다. 실제로 이 신호에 한 번 속을 뻔했다. 번들 해시로 확인해야 한다.
> (§8-6의 "401이면 엔드포인트 있음"과 같은 종류의 착각이다.)

**⚠ 그리고 여기서 `main` ff 머지를 또 빠뜨려 옛 코드가 한 번 더 배포됐다 — 오늘 두 번째다.**
§8-6에서 규약(§5)으로 올려놨는데도 재발했다. **규약만으로는 안 걸러진다.**
→ 대응: ①앞으로 배포를 요청하기 **전에 내가 먼저 `main`을 ff 머지**한다(사람이 기억할 단계를 줄인다)
②배포 스크립트가 시작할 때 `main`이 뒤처졌으면 **경고**하도록 하는 게 구조적 해법이다
(이미 `check-infra-drift.sh`를 그 자리에서 돌리고 있으니 같은 방식 — 막지 않고 경고만).

### 8-8. 배포 후 브라우저 확인 순서

> `ZZ` 규약(§3-1): **실상품이 아니라 `ZZP-` 상품을 만들어서** 하고, 수령인에 `ZZ`를 남긴다.
> 실상품으로 하면 재고가 실제로 깎이고, `ZZ`가 없으면 나중에 정리 대상을 못 찾는다.
> 발송·배송완료는 **되돌릴 수 없으니**(취소 불가) 반드시 `ZZP-` 상품으로 할 것.

1. 관리자로 **`ZZP-` 상품 등록** → 일반 계정으로 장바구니 담기 → 주문서에서 **수령인 `ZZ수령인`** 으로 결제.
2. 주문 상세 → 진행 스텝이 **4단계**로 보이고 "배송 완료"가 마지막에 있는지(아직 미점등).
3. 구매자로 **결제하기** → `PAID`.
4. 관리자 주문 화면 → 그 주문의 **[발송]** → **운송장 입력 폼**이 뜨는지.
   **송장번호를 비운 채 [발송 처리]** → "송장번호를 입력해 주세요"로 **화면에서 먼저 막히는지**(서버 400 아니라).
5. 택배사 `CJ대한통운` + 송장번호 입력 → 발송 처리 → 목록에서 상태 `발송완료`, **[배송완료] 버튼**이 생기는지.
6. 주문 상세 → **배송 추적 카드**에 택배사·송장번호가 보이고 **[배송 조회 ↗]** 링크가 CJ 조회 페이지로 가는지.
   (⚠ 운영 서버는 LAN 내부지만 이 링크는 **브라우저가 여는 것**이라 클라이언트에 인터넷이 있으면 열린다.)
7. 택배사를 **기타**로 한 주문도 하나 만들어 → 송장번호는 보이는데 **조회 링크는 없어야** 한다.
8. 관리자 **[배송완료]** → 상태 `배송완료`, 진행 스텝 4단계 **전부 점등** + 수령 일시 표시.
9. **배송 추적 도입 이전 주문**을 열어 → 배송 추적 카드가 **아예 안 보여야** 한다(빈 칸이 아니라).
   진행 스텝도 깨지지 않아야 한다.
10. 주문 목록 상태 탭에 **배송완료**가 있고 건수가 맞는지. 관리자 화면 상태별 건수도 마찬가지.
11. **정리**: `ZZP-` 상품 삭제. 발송된 주문은 취소가 안 되므로 **주문 레코드는 남는다** —
    남는 주문을 사용자에게 보고한다(§3).

---

## 9. 배포 브랜치 뒤처짐 자동 경고 (`check-deploy-branch.sh`)

§8-6·§8-7에서 **같은 사고를 두 번** 겪었다 — `main` ff 머지를 잊고 배포해 **옛 코드가 조용히 나갔다.**
첫 번째 직후 WORKING-AGREEMENTS §5에 규약으로 올렸는데도 **두 번째가 났다.**
**규약만으로는 안 걸러진다는 게 그 재발로 증명됐다.**

### 9-1. 만든 것

`scripts/check-deploy-branch.sh` — 배포 워크트리의 HEAD로 **안 넘어온 커밋이 다른 로컬 브랜치에 있는지**
확인하고, 있으면 브랜치명·건수·커밋 목록·머지 명령까지 찍는다.

```
⚠ 배포 워크트리('main')가 다른 브랜치보다 뒤처져 있다 — 옛 코드가 배포될 수 있다:
    daily-status-review 에 main 로 안 넘어온 커밋 1건
      c3bc7df build: 배포 스크립트가 브랜치 뒤처짐을 경고하도록
  → 의도한 것이면 그대로 진행해도 된다. 아니면 먼저 머지할 것:
     git -C /home/ecstel/work merge --ff-only <브랜치> && git -C /home/ecstel/work push
```

`deploy-backend.sh`·`deploy-frontend.sh` 시작 시 자동 실행된다. **막지 않고 경고만** 한다 —
`check-infra-drift.sh` 와 같은 자리·같은 이유다(배포를 막으면 급할 때 스크립트를 우회하게 되어 더 나빠진다).
브랜치 검사를 드리프트 검사보다 **앞에** 뒀다: "무엇을 빌드하는가"가 "설정이 맞는가"보다 근본이다.

### 9-2. 설계에서 신경 쓴 것

- **root로 실행돼도 동작해야 한다** — 배포 스크립트는 sudo로 돌 수 있는데, root가 ecstel 소유 저장소에서
  git을 돌리면 *dubious ownership* 으로 거부당한다. → root면 `runuser -l ecstel` 로 실행한다.
- **판단이 안 서면 조용히 넘어간다**(git 실패 등 → exit 0). 근거 없는 경고는 신뢰를 깎아
  **다음의 진짜 경고를 무시하게** 만든다. 경고는 확실할 때만 낸다.

### 9-3. 검증 (실측)

동기화 상태에선 조용하고(exit 0), 뒤처지면 경고한다(exit 1)는 걸 **양쪽 다** 확인했다 —
"경고가 안 뜬다"만 보고 통과라 하면 아무것도 검증한 게 아니다.

| 상황 | 결과 |
|---|---|
| `main` == 작업 브랜치 | 출력 없음, exit 0 |
| `main` 이 1커밋 뒤처짐 | 위 경고 출력, exit 1 |
| 배포 스크립트 배선 | `deploy-backend.sh`·`deploy-frontend.sh` 각 1곳 |

> **덤으로 걸린 것**: 경고를 시험하려고 스크립트를 배포 워크트리에 **임시로 `cp`** 했더니,
> 그 뒤 `git merge --ff-only` 가 *"추적되지 않는 파일을 덮어쓴다"* 며 **중지**했다.
> 임시 파일이 머지를 막은 것 — 테스트하려고 만든 것이 그 다음 단계를 깨뜨리는 전형적인 모양이다.
> 지우고 재머지하니 정상.

---

## 10. 기능 추가 — 주문 배송비 (V14)

기능 갭 목록(§8)의 **A-2**. `total_price` 가 상품 합계뿐이라 커머스 화면인데 금액 구조가 비어 있었다 —
주문서 결제 요약에 배송비 줄이 없었다. 쿠폰(B-4)보다 먼저 해야 하는 항목이다: 금액 계산의 토대라
배송비 없이 쿠폰을 얹으면 계산 로직을 다시 짜게 된다.

### 10-1. 만든 것

| 계층 | 내용 |
|---|---|
| DB | **V14** — `orders.shipping_fee NUMBER(19,0) DEFAULT 0 NOT NULL` |
| 정책 | `global/policy/ShippingPolicy`(`glassvue.shipping`) — 기본 배송비 3,000원 / 30,000원 이상 무료 |
| 엔티티 | `Order.shippingFee`(스냅샷) + `getPayAmount()` = 상품합계 + 배송비 |
| 응답 | `CartResponse`에 `shippingFee`·`payAmount`·`amountUntilFree`(주문 전 미리보기), `OrderResponse`·`AdminOrderResponse`에 `shippingFee`·`payAmount` |
| 프론트 | 장바구니·주문서 결제 요약에 배송비 줄 + **"N원 더 담으면 무료배송"**, 주문 상세에 상품금액/배송비/결제금액, 관리자 목록 금액을 `payAmount`로 |

### 10-2. 판단 근거

- **`total_price` 의 의미를 바꾸지 않았다.** 여기에 배송비를 더하면 **이미 쌓인 25건**의 숫자가
  "상품 합계"인지 "결제 금액"인지 알 수 없어진다. 별도 컬럼으로 두고 결제 금액은 **계산**으로 낸다
  — 저장하면 두 값이 어긋날 여지가 생긴다.
- **정책은 설정, 부과된 금액은 스냅샷.** 정책은 언제든 바뀌지만 과거 주문에서 **실제로 받은 금액**은
  그대로여야 한다 — 배송지(V11)·구매자 닉네임(V5)·상품 이미지(V9)와 같은 판단이다.
- **`ShippingPolicy` 를 global 에 뒀다.** 장바구니(주문 전 미리보기)와 주문(실제 부과)이 둘 다 읽어야 하는데,
  이미 `order → cart` 의존이 있어서(주문이 장바구니에서 품목을 읽는다) order 에 두면 `cart → order` 가 생겨
  **순환**이 된다. 여러 도메인이 공유하는 상점 전체 정책이므로 global 이 맞다.
- **배송비는 서버가 계산한다.** 요청 본문으로 받으면 **0원으로 위조**할 수 있다 —
  품목·가격을 장바구니에서 읽는 것과 같은 이유. **부수 효과로 HTTP 호출부가 안 깨졌다**:
  요청 계약이 그대로라 §8-5 같은 "컴파일러가 못 잡는 호출부" 문제가 이번엔 없었다.
- **빈 장바구니엔 배송비를 붙이지 않는다.** 붙이면 화면에 "0원 + 배송비 3,000원"이 떠서
  **담지도 않은 값을 청구하는 것처럼** 보인다. 경계값(합계 0·음수)을 테스트로 못박았다.
- **무료까지 남은 금액을 서버가 준다**(`amountUntilFree`). "배송비 3,000원"보다
  "10,000원 더 담으면 무료배송"이 유용하고, 화면이 정책(기준 금액)을 알 필요가 없다.

### 10-3. 백필이 필요 없었던 이유

기존 주문은 배송비를 **받은 적이 없으므로 0이 사실**이다 — *모르는 값*이 아니다.
그래서 `DEFAULT 0 NOT NULL` **한 문장**으로 끝났고, WORKING-AGREEMENTS §2-1 의
3단계(nullable 추가 → 백필 → NOT NULL 확정)가 필요 없었다.
(값을 모르는 경우였다면 — 배송지·운송장처럼 — nullable 로 두고 화면에서 감췄을 것이다.)
Oracle 11g+ 는 DEFAULT 있는 NOT NULL 추가를 메타데이터만 갱신해 처리한다.

### 10-4. 검증 (실측)

| 확인 | 결과 |
|---|---|
| 백엔드 | **193개**(187 → +6) · failures 0 · errors 0 · **skipped 0** |
| 프론트 | **56개** 통과 · `pnpm build` 성공(번들 `index.6486a3d7.js`) |
| espdb 스키마 | `SHIPPING_FEE NUMBER NOT NULL DEFAULT 0` · **기존 주문 25건 전부 0, NULL 0건** |
| 빈 DB(esptest) | **V1→V14 순차 적용**(14개, 6.7초) + `validate` 통과(앱 기동 53초) |
| 정리 | dev 인스턴스 종료 · dev redis(db4) flush · 운영 캐시 db0 348키 무손상 |

> ⚠ 처음 테스트 실행에서 **7건 실패** — 전부 단위 테스트의 `ShippingPolicy` 미주입 NPE 였다.
> §8 에서 `DeliveryProperties` 로 **똑같은 실수를 했는데 또 했다.**
> → 교훈: **서비스에 협력자를 추가하면 `@InjectMocks` 단위 테스트가 조용히 NPE 로 깨진다.**
> 설정·정책처럼 순수 계산만 하는 객체는 목이 아니라 **`@Spy` + 실제 인스턴스**를 넣는다
> (목으로 두면 0/null 을 돌려줘 "값이 안 붙는" 경로만 검증하게 된다).

---

## 11. 기능 추가 — 사람이 읽는 주문번호 (V15)

백로그 **A-3**. 주문 식별자가 UUID 뿐이라 화면이 앞 8자만 잘라 보여줬다(`#019f8d59`).
고객이 CS 에 불러주기 어렵고, 잘린 값이라 중복 위험도 있었다.
→ `20260723-0025` 형식(`yyyyMMdd` + 전역 일련번호).

### 11-1. 판단 근거

- **PK 는 UUIDv7 그대로다.** CLAUDE.md 의 *"Long/SEQUENCE/IDENTITY 금지"* 는 **PK 규칙**이라
  표시·검색용 별도 컬럼에는 해당하지 않는다. PK 를 바꾸는 게 아니라 컬럼을 하나 더하는 것이다.
- **날짜별 1부터 리셋하지 않는다.** 리셋하려면 동시 주문이 같은 번호를 잡는 걸 막아야 해서
  카운터 테이블 락이나 유니크 충돌 재시도가 필요하다. **전역 시퀀스는 충돌이 원천적으로 불가능**하고
  재시도 로직도 없다. 번호가 날짜를 넘어 이어지는 게 조금 어색하지만 CS 식별에는 지장이 없다.
- **`NOCACHE`** — 캐시를 쓰면 인스턴스 재시작마다 최대 캐시 크기만큼 번호가 건너뛴다.
  주문번호가 띄엄띄엄하면 **"주문이 누락됐나?"** 하는 오해를 산다. 이 규모에선 성능 차이가 무의미하다.
- **날짜는 Asia/Seoul 기준.** `created_at` 이 UTC(+00:00)로 저장돼 있어서 그냥 `TO_CHAR` 하면
  **한국 시간 00:00~09:00 주문이 전날 날짜**로 찍힌다. 앱 채번과 마이그레이션 백필이 **같은 기준**을 쓰도록 맞췄다.

### 11-2. 백필 — V14 와 대조되는 지점

| | V14 배송비 | V15 주문번호 |
|---|---|---|
| 기존 행의 값 | **0 이 사실**(받은 적 없음) | **행마다 달라야** 함 |
| 방법 | `DEFAULT 0 NOT NULL` **한 문장** | **3단계**(nullable → 백필 → NOT NULL+UNIQUE) |
| 근거 | 모르는 값이 아니라 0 | DEFAULT 로 채울 수 없음 |

WORKING-AGREEMENTS §2-1 의 3단계가 **왜 있는지**를 두 마이그레이션이 나란히 보여준다 —
DEFAULT 로 될 때는 3단계가 과잉이고, 안 될 때는 우회할 수 없다.

백필은 **시퀀스를 주문 시각 순서로 소진**해서 했다. ROW_NUMBER 로 계산해 넣으면 시퀀스 현재값과
어긋나 **다음 주문이 유니크 충돌**을 낸다.

### 11-3. ⚠ 구 jar 동작이 바뀌는 첫 마이그레이션이었다

V13(CHECK 넓히기)·V14(DEFAULT 있는 NOT NULL)는 구 jar 가 그대로 돌았지만, V15 는 다르다:
**`order_no` 가 NOT NULL 인데 구 jar 는 이 컬럼을 모르고 DEFAULT 도 없어 주문 생성이 실패한다.**
7/21 닉네임 UNIQUE(V6) 사고와 같은 구조다.

→ §5 대로 **사용자에게 알리고 선택을 받았다.** 두 안을 제시:
①그대로 진행 후 즉시 배포(창 몇 분) ②마이그레이션을 둘로 쪼개 창을 없애기(배포 2회).
**단독 사용 환경이라 ①을 선택**했고, 테스트 → 커밋 → main 머지 → 배포를 끊지 않고 진행했다.

### 11-4. 검증 (실측)

| 확인 | 결과 |
|---|---|
| 백엔드 | **193개** · failures 0 · errors 0 · **skipped 0** |
| 프론트 | **56개** 통과 |
| 백필 | 25건 **전부 채워짐·전부 고유**, `20260715-0001` ~ `20260723-0025` 시간순, KST 날짜 일치 |
| 계약 고정 | 통합테스트에 형식 `yyyyMMdd-NNNN` 정규식 검증 추가 |
| 배포 | OpenAPI 에 `orderNo`(주문·관리자 응답) + 관리자 검색 파라미터 `orderNo` · 번들 `index.04e871da.js` |
| 빈 DB(esptest) | **V1→V15 순차 적용**(15개, 3.4초) + `validate` 통과 · `ORDER_NO` NOT NULL(CHAR) · `UK_ORDERS_ORDER_NO` · `SEQ_ORDER_NO` LAST_NUMBER=1 · CACHE_SIZE=0(NOCACHE 확인) |

> 빈 DB 검증을 굳이 한 이유: V15 에는 **PL/SQL 백필 블록**이 있어서 **대상이 0건일 때 조용히 넘어가는지**를
> 봐야 했다(운영 DB 에서는 25건이 있어 그 경로를 안 탄다). 실제로 통과했다.
> ⚠ 스키마를 비울 때 `migration/README` 의 스크립트는 **테이블만 지운다** — 시퀀스는 남는다.
> 이번엔 V15 가 esptest 에서 처음 도는 거라 문제없었지만, 다음에 다시 비울 땐 시퀀스도 확인해야
> `CREATE SEQUENCE` 가 ORA-00955 로 실패하지 않는다(실행 전 `user_objects` 로 0개인 것을 확인했다).

> ⚠ **번들 해시 대조에서 한 번 헛짚었다.** 배포본(`04e871da`)과 내 로컬 dist(`6486a3d7`)가 달라
> "옛 코드가 배포됐나" 했는데, **내 로컬 빌드가 낡은 것**이었다(주문번호 프론트 수정 후 `pnpm build` 를 안 돌렸다).
> 로컬을 다시 빌드하니 `04e871da` 로 일치했다.
> → **해시 대조는 로컬 빌드가 최신일 때만 유효하다.** 확실하게 하려면 배포본 내용을 직접 본다
> (실제로 `주문번호`·`orderNo` 문자열이 들어 있는지 확인해 먼저 결론을 냈다).

> 참고: 시퀀스에 **작은 공백**이 생긴다(롤백된 테스트 트랜잭션이 번호를 소비 + 확인하느라 `NEXTVAL` 을 한 번 씀).
> 시퀀스는 롤백되지 않으므로 정상 동작이다. 확인은 `user_sequences.last_number` 를 봤으면 더 깔끔했다.

---

## 12. 기능 추가 — 정가 / 할인가 (V16)

백로그 **B-7**. `Product.price` 하나뿐이라 **"20% 할인" 을 표현할 방법이 없었다.**

### 12-1. 핵심 판단 — `price` 의 의미를 바꾸지 않는다

정가를 넣는 방법은 둘이다:
①`price` 를 정가로 바꾸고 할인가를 새로 둔다 ②`price` 는 판매가로 두고 **정가**를 더한다.

**②를 골랐다.** `price` 는 장바구니 합계·주문 금액·**배송비 무료 기준**이 전부 쓰는 값이다.
여기를 정가로 바꾸면 그 계산들이 **한꺼번에 어긋난다** — 오늘 V14 에서 `orders.total_price` 의
의미를 지킨 것과 정확히 같은 판단이고, **오늘만 두 번째**다.

| 값 | 성격 | 저장? |
|---|---|---|
| `price` | 실제 판매가(청구되는 금액) | 저장 |
| `list_price` | 정가. null 이면 **할인 없음** | 저장 |
| 할인율 | 두 값에서 계산 | **저장 안 함** — 가격을 바꿀 때 어긋날 여지를 만들지 않는다 |

### 12-2. 할인율 계산은 서버가 아니라 화면에 뒀다

§8 에서는 배송 조회 URL 을 **서버가 완성**하게 했는데, 여기선 반대로 했다. 기준이 다르다:

- **조회 URL** — 택배사별 형식이라는 **외부 지식**이 필요하다. 화면에 두면 중복되고 서버는 검증할 근거가 없다.
- **할인율** — 화면이 이미 가진 **두 숫자의 산술**이다. 서버가 완성해 줄 이유가 없다.

대신 `api/product.js` **한 곳에만** 뒀다(`hasDiscount`·`discountRate`). 화면마다 계산이 갈리면
같은 상품이 목록에선 20%, 상세에선 21% 로 보인다 — 주문 상태 색을 한 곳에 모은 것과 같은 이유다.

### 12-3. 주문에도 정가를 스냅샷했다

`order_item.list_price` 를 함께 넣었다. **지금 안 넣으면 과거 주문은 영영 백필할 수 없다** —
그때 정가가 얼마였는지 알 방법이 없기 때문이다. **배송지(V11)·운송장(V13)에서 이미 두 번 겪은 구조**라
이번엔 결정을 미루지 않았다.

상품의 **현재** 정가를 조회해 쓰면 안 되는 이유도 같다: 가격은 나중에 바뀌므로
"그때 얼마에서 얼마로 할인받았는지"는 주문 시점 기록이어야 한다(상품명·가격·이미지와 같은 이유).

### 12-4. 백필이 필요 없었던 이유 (오늘 세 번째 사례)

정가가 없으면 **할인 없음**이라는 뜻이다. 기존 상품 5건·주문품목 26건은 할인한 적이 없으므로
**NULL 이 사실**이다 — 모르는 값이 아니다. V14 배송비의 "0 이 사실"과 같은 성격이다.

오늘 마이그레이션 4개가 백필 판단의 스펙트럼을 다 보여줬다:

| | 기존 행의 값 | 처리 |
|---|---|---|
| V13 운송장 | **모른다**(그때 안 받았다) | nullable, 화면이 감춤 |
| V14 배송비 | **0 이 사실** | `DEFAULT 0 NOT NULL` 한 문장 |
| V15 주문번호 | **행마다 달라야** 한다 | 3단계(nullable → 백필 → NOT NULL) |
| V16 정가 | **NULL 이 사실**(할인 없었음) | nullable, 백필 없음 |

### 12-5. 검증 (실측)

| 확인 | 결과 |
|---|---|
| 백엔드 | **194개**(193 → +1, 정가 스냅샷 계약) · failures 0 · errors 0 · **skipped 0** |
| 프론트 | **58개**(56 → +2, 할인 헬퍼 경계값: 정가=판매가, 정가<판매가, null) · 빌드 성공 |
| espdb | `product`·`order_item` 의 `LIST_PRICE` 둘 다 nullable · **기존 데이터 전부 NULL**(5건/26건) |
| 배포 | OpenAPI 4개 스키마에 `listPrice` · 번들 `index.f149ae99.js` 일치 · health `UP` · `/api/products` 200 |

> **빈 DB(esptest) 검증 통과**: V1→V16 순차 적용(16개, 4.7초) + `validate` 통과(기동 61초).
> `product`·`order_item` 의 `LIST_PRICE` 둘 다 nullable, `SEQ_ORDER_NO` LAST_NUMBER=1.
> ⚠ 이번엔 스키마를 비울 때 **시퀀스도 함께 지워야** 했다 — V15 가 만든 `seq_order_no` 가 남아 있으면
> `CREATE SEQUENCE` 가 ORA-00955 로 실패한다. `migration/README` 의 스크립트는 테이블만 지운다.

---

## 13. 기능 추가 — 쿠폰 (V17)

백로그 **B-4**. CLAUDE.md 가 도메인 예시로 든 `domain.coupon` 의 자리다.
금액 토대(배송비 V14 · 정가 V16)를 **먼저 깔아 둔 덕에** 그 위에 얹기만 하면 됐다.

```
상품합계 → 쿠폰할인 → 배송비 → 결제금액
```

### 13-1. ⚠ 크기를 잘못 어림했다

백로그에 **"대(며칠)"** 로 적어 뒀는데 **틀렸다.** 사용자가 *"쿠폰은 1시간 만에 끝나지 않아?"* 라고
되물어서 다시 분해해 보니 그 말이 맞았다.

- **"도메인 신설 = 크다"로 유추**했을 뿐, 실제로 분해해 보고 적은 게 아니었다.
- 핵심(스키마·엔티티·적용 로직·주문 통합·주문서 UI)은 **한 시간대**다.
- "대"로 보이게 한 건 **주변부**였다 — 관리자 쿠폰 관리 화면, 발급 정책 같은 것들.
  그건 스코프에서 빼고 **API 만** 만들었다(이미지 백필을 관리자 엔드포인트로만 둔 것과 같은 방식).

→ **크기를 적을 땐 핵심과 주변부를 갈라서 적는다.** 백로그의 「쓰는 법」에 규칙으로 넣었다.

### 13-2. 무료배송 기준을 "할인 전"으로 정한 것이 코드에서 값을 했다

착수 전에 물었던 유일한 결정이다:

```
상품합계 32,000 + 쿠폰 5,000 할인, 무료 기준 30,000
  A) 할인 전 32,000 기준 → 무료      ← 선택
  B) 할인 후 27,000 기준 → 3,000원
```

**A 를 골랐다** — 쿠폰을 썼다고 배송비가 붙으면 고객이 손해 본 기분이 든다(실제 커머스도 대부분 A).

**부수효과가 컸다**: `ShippingPolicy.feeFor(cart.totalPrice())` 를 **한 글자도 안 건드렸다.**
B 였다면 배송비 계산을 쿠폰 이후로 옮겨야 했고, 그러면 `checkout` 의 금액 계산 순서가 통째로 바뀐다.
**정책 결정 하나가 코드 변경 범위를 갈랐다.**

### 13-3. 설계 판단

- **`redeem()` 이 검증과 사용처리를 한 번에** 한다. 나누면 "검증했으니 이제 써도 되겠지" 사이에
  같은 쿠폰이 **두 번 쓰일 틈**이 생긴다. 주문 트랜잭션 안에서 호출되므로 주문이 롤백되면 함께 롤백된다.
- **order 는 coupon 엔티티·리포지토리를 직접 만지지 않는다** — `CouponService` 공개 API 로만(도메인 경계).
- **쿠폰도 주문에 스냅샷**(이름·할인액). 쿠폰 정의가 바뀌거나 삭제돼도 주문 내역은 그대로여야 한다.
  오늘만 여섯 번째로 적용한 원칙이다(닉네임 V5 · 이미지 V9 · 배송지 V11 · 배송비 V14 · 정가 V16 · 쿠폰 V17).
- **할인은 상품합계를 넘지 않는다** — 넘으면 결제 금액이 음수가 되고, 배송비만 남거나 환불해야 하는
  이상한 상태가 된다.
- **남의 쿠폰은 `COUPON_NOT_FOUND`** 로 답한다 — 존재 여부를 알려주지 않는다.
- **화면이 할인 규칙을 모른다.** 서버가 쿠폰마다 `discountPreview`(지금 얼마 깎이는지)·`usable`·
  `reason` 을 계산해 준다. "10% 할인"만 주면 고객이 직접 계산해야 하고 최소주문금액 미달도 알 수 없다.
- **CHECK 제약에 처음부터 이름을 붙였다**(`ck_coupon_discount_type`) — V13 에서 V1 의 인라인 CHECK
  자동 생성 이름을 PL/SQL 로 찾아 지워야 했던 걸 되풀이하지 않으려는 것이다. 같은 날 배운 걸 바로 적용했다.

### 13-4. ⚠ 보안 구멍을 하나 잡았다

`SecurityConfig` 의 기본이 **`anyRequest().permitAll()`** 이다. 그래서 `/api/coupons/**` 매처를
빠뜨리면 **남의 쿠폰 목록이 인증 없이 열린다.** (관리자 API 는 `/api/admin/**` 한 줄로 이미 막힌다 —
경로를 모아 둔 §2-4 규약이 여기서 값을 했다.)

→ 매처를 추가하고 **401 을 통합테스트 계약으로 고정**했다. 권한 규칙은 서비스 단위 테스트로 절대 안 잡히고
실제 요청을 보내야만 드러난다(§2-4). 배포 후 운영에서도 401 을 실측했다.

### 13-5. 검증 (실측)

| 확인 | 결과 |
|---|---|
| 백엔드 | **204개**(194 → +10: 할인 계산 경계값 6 · 관통 1 · 권한 1 · 주문 결제금액 2) · failures 0 · **skipped 0** |
| 프론트 | **58개** 통과 · 빌드 성공 |
| 배포 | OpenAPI 에 쿠폰 경로 3개 + `OrderResponse.couponName/couponDiscount` + `OrderCreateRequest.memberCouponId` |
| 보안 | 운영에서 인증 없이 `GET /api/coupons/me` → **401** |
| 번들 | `index.4e47c99c.js` 일치 · health `UP` |

> **빈 DB(esptest) 검증 통과**: V1→V17 순차 적용(17개, 6.1초) + `validate` 통과(기동 56초).
> `COUPON`·`MEMBER_COUPON` 생성, **`CK_COUPON_DISCOUNT_TYPE`**(이름 붙인 CHECK)·`FK_MEMBER_COUPON_COUPON`·
> `IDX_MEMBER_COUPON_MEMBER` 확인, `orders.COUPON_DISCOUNT` NOT NULL · `COUPON_NAME` nullable.
>
> **CHECK 에 이름이 붙은 게 실제로 확인됐다** — 다음에 `DiscountType` 에 값을 추가할 땐
> V13 처럼 자동 생성 이름을 PL/SQL 로 찾을 필요 없이 DROP/ADD 두 줄이면 된다.

---

## 현재 상태 (7/22 대비 바뀐 것만)

- **Oracle 부팅 자동기동**: **고쳐졌다.** `enable` + drop-in override, 핵심은 `Environment=SU=/usr/sbin/runuser`.
  `init_t` 재현 + **실제 재부팅(10:08)** 둘 다 통과 — 손 안 대고 `active`/`active`(§3-5).
- **`/etc/hosts`**: `192.168.50.14   ecstel` 핀 추가. 이 장애의 원인은 아니었지만 타당한 설정이라 유지.
- **`infra/`**: `systemd/oracledb_ESPDB-19c.service.d/override.conf` 신설(원인·근거를 파일 주석에 기록),
  `check-infra-drift.sh` PAIRS 에 등록(이제 4개 감시), `README` 재구축 체크리스트 5번 전면 개정.
- **운영**: 정상. 백엔드 `active` / health `UP` / `/api/products` 200 / DB `:3306` / nginx `:443`.
  오늘 **두 번 내려갔다가** 복구됨(둘 다 부팅 자동기동 실패가 원인).
- **주문 배송 추적 신설**(§8): 상태가 `ORDERED→PAID→SHIPPED→**DELIVERED**` 로 늘고, 발송 처리에
  **운송장(택배사·송장번호)이 필수**가 됐다. 주문 상세에 배송 추적 카드·조회 링크, 진행 스텝 4단계.
  **API 계약 변경** — `POST /api/orders/{id}/ship`이 본문을 받고, `/deliver`가 신설됐다.
- **주문 배송비 신설**(§10): `total_price`(상품 합계)와 **별도**로 `shipping_fee`를 두고
  결제 금액(`payAmount`)은 계산으로 낸다. 정책은 설정(`glassvue.shipping`, 3,000원/30,000원 무료),
  부과된 금액은 주문에 스냅샷. 장바구니·주문서·주문상세·관리자 목록 금액 표시 갱신.
- **주문번호 신설**(§11): `20260723-0025` 형식(`yyyyMMdd`(KST) + 전역 시퀀스). 화면이 UUID 앞자리를
  잘라 쓰던 걸 대체하고, 관리자 목록에 주문번호 컬럼·검색을 추가했다. 기존 25건 백필 완료.
- **DB/스키마**: Flyway **V13**(배송 추적 + `status` CHECK를 `ck_orders_status`로 교체)
  + **V14**(`orders.shipping_fee` DEFAULT 0 NOT NULL) + **V15**(`seq_order_no` + `orders.order_no`
  NOT NULL UNIQUE). espdb 적용·실측 확인(V15 는 esptest 검증 미실시 — 이월).
- **테스트**: 백엔드 173 → **193**(주문번호는 계약 검증만 추가)(배송 추적 +10, 배송비 +6, 예시 페이지 전환분 포함) · 프론트 46 → **56**.
  **순수 컴포넌트는 전부 커버됐다**(StarRating·ImageUploader·EmptyState·SkeletonList·ItemThumb).
- **빌드 설정**: `build.gradle` 의 `Test`·`JavaExec` 에 `java.io.tmpdir=build/apptmp` — scrimage 가
  `/tmp` 에 WebP 바이너리를 흘리던 것을 프로젝트 안으로 돌렸다(§5). `gradle clean` 이 함께 치운다.
- **성능**: 문의·상품 목록의 이미지 로딩에 **N+1 없음**을 P6SPY 로 실측(§7).
  카테고리도 `default_batch_fetch_size: 100` 으로 배치된다.
- **⚠ 배포 필요**: 오전 작업(인프라·문서·테스트)은 배포 대상이 아니었지만, **§8 배송 추적은 다르다** —
  런타임 코드와 API 계약이 바뀌었고 프론트 번들 해시도 `index.437a53f0` → **`index.2160b31c`** 로 바뀌었다.
  **아직 배포 안 했다**(이월 참고).
- **git**: `daily-status-review` 에 오늘 커밋 12건, 전부 push 됨.
  **`main` 에는 안 올렸다** — 배포 대상이 아니라 급하지 않다. 다음 배포 때 ff 하면 된다.
- **디스크**: 루트 **81%**(여유 5.0G). `/tmp` WebP 바이너리 **11개 37MB** — **재발은 막았고**(§5)
  이 11개는 7/21~22 잔재다. 방치해도 `tmpfiles.d` 의 10일 규칙으로 지워진다.

---

## 이월 (다음 문서로 반드시 옮길 것)

> 이 절은 규약이다(WORKING-AGREEMENTS §4-1). 다음 날 문서가 그대로 물려받아 시작한다. 처리했으면 그때 지운다.

### 미완 — 다음 세션이 이어받을 것

**없음.** 7/22 에서 넘어온 미완 2건도, 오늘 새로 만든 배송 추적도 전부 끝났다.

- ~~배송 추적(§8) 배포·검증~~ — **종결**. 배포 완료 후 **브라우저 시나리오(§8-8) 전 단계 통과**(사용자 확인).
  그 뒤 조회 URL을 설정으로 빼고 예시 페이지로 바꿔(§8-7) **재배포·재확인**까지 마쳤다.
  ⚠ 이 과정에서 `main` ff 머지 누락으로 옛 코드가 **두 번** 배포됐다 — §9에서 자동 경고로 막았다.

> **7/22 에서 넘어온 미완 2건은 오늘 전부 종결했다.** 아래 두 줄은 "왜 없어졌는지"를 남기려고
> 취소선으로만 둔다 — 다음 문서로는 옮기지 않는다.

- ~~Oracle 이 부팅에 자동으로 안 뜬다~~ — **종결**. 원인 규명(SELinux 가 `init_t` 의 `su` 실행 거부)·
   수정(`SU=runuser`)·`init_t` 재현·**실제 재부팅 검증**까지 마쳤다(§3-5).
   추가로 **13:20 예기치 않은 VM 재기동에서도 자동으로 떴다**(손 안 대고 둘 다 `active`) — 계획된
   검증이 아니라 실제 사고 상황이라 더 강한 증거다.
- ~~`.env` 실값 백업이 없다~~ — **종결**. **백업처: 사용자 개인 Notion**(2026-07-23).
   - 값은 여기에도, 저장소 어디에도 적지 않는다. **위치만** 남긴다 —
     다음 세션이 "백업이 있나 없나"를 다시 묻지 않게 하는 게 이 기록의 목적이다.
   - 대상은 `DB_PASSWORD`·`JWT_SECRET` 두 개다. 나머지 4개(`DB_HOST`·`DB_PORT`·`DB_USER`·`DB_SERVICE`)는
     비밀이 아니고 `infra/env.example` 에 형식이 있다.
   - ⚠ **`JWT_SECRET` 은 값을 잃으면 복구가 불가능**하다(새로 만들면 전 사용자 로그아웃).
     이게 이 항목이 계속 이월됐던 이유다.

### 후속 후보 · 조건부 보류 → **`docs/BACKLOG.md` 로 옮겼다**

> 여기 있던 제품 기능 항목(쿠폰·리뷰 캐시·알림 실발송·PG·`DB_PASSWORD` 강화 등)은
> **매일 이월로 복사돼 내려오고 있었다.** 수명이 긴 것들이라 이월에 둘 자리가 아니다.
> → **`docs/BACKLOG.md`** 신설(2026-07-23)해서 옮겼다. 이월에는 **그날 작업의 직접 잔여**만 남긴다.

이 문서에서 파생돼 남은 잔여는 아래 셋뿐이다.

1. **`/tmp` 잔재 11개 37MB** — 재발은 §5 로 막았고 7/21~22 에 쌓인 건 그대로다.
   방치해도 `tmpfiles.d` 10일 규칙으로 지워진다. 당길 거면
   `rm -f /tmp/cwebp*binary /tmp/dwebp*binary` (실행 중 JVM 없을 때).
   — "재발 방지"와 "기존 잔재 정리"는 별개다(WORKING-AGREEMENTS §2-5).
2. **문의 경로만으로 N+1 직접 판별**(§7-3) — 오늘은 이미지 그룹이 1개뿐이라 문의 요청 자체로는
   배치와 N+1 이 구분되지 않았다. 직접 판별하려면 이미지 그룹 3개 이상인 ZZ 문의가 필요하다.
   **결론이 바뀔 가능성은 낮다**(판별은 상품 목록이 했고 문의는 같은 코드를 탄다).
3. **공유기 DHCP 예약**(MAC `08:00:27:df:8e:fd` → `.14`) — IP 충돌을 겪으면 그때.
