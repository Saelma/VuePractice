/**
 * 화면 여러 곳이 **같은 상태를 같은 말로** 불러야 하는 문구 (2026-08-12).
 *
 * ⚠ 여기 있는 것은 «어느 화면에서 쓰나» 가 아니라 **«두 곳 이상이 쓰나»** 로 들어온다.
 * 한 화면만 쓰는 문구는 그 화면에 둔다 — 여기로 모으면 오히려 찾기 어려워진다.
 */

/**
 * 상품이 지워졌는데 그 상품을 가리키던 글(문의·리뷰)은 남아 있는 줄.
 *
 * 🔴 **문구가 갈리면 같은 상태가 화면마다 다르게 보인다** — 문의 관리와 리뷰 관리는 나란히 쓰는
 * 운영 화면이라(DESIGN §7) *"지워진 상품"* 과 *"삭제된 상품"* 이 섞이면 관리자는 **다른 상태**로 읽는다.
 * 2026-08-07 에 문의(관리자·고객)가 먼저 썼고 2026-08-12 에 리뷰 관리가 합류하면서 한 곳으로 모았다.
 *
 * ⚠ **빈칸으로 두지 않는 것이 요점**이다 — 빈칸은 «데이터가 잘못됐다» 로 읽힌다.
 * ⚠ 「무엇이 그 상태인가」의 **판정은 화면마다 다르다**(문의는 유형까지 봐야 하고, 리뷰는 서버가
 * {@code productDeleted} 로 알려준다). 여기 모으는 것은 **문구뿐**이다.
 */
export const DELETED_PRODUCT = '(지워진 상품)';

/**
 * 관리자 표 하단 페이저 문구 (2026-09-11).
 *
 * 🔴 **자리표시자 뜻을 거꾸로 읽고 있었다** — DevExtreme 의 `{0}` 은 **현재 쪽**, `{1}` 은 **전체 쪽 수**,
 * `{2}` 가 전체 건수다. 그런데 8곳이 `"{2}건 중 {0}-{1}"` 로 적어 91건이면 **«91건 중 1-5»** —
 * «1~5번째 행» 으로 읽히는데 실제로는 «5쪽 중 1쪽» 이었다. 한 줄을 복사해 퍼뜨린 사본이라 **여기 하나로 모은다.**
 * ⚠ 페이저는 기본값이 **한 쪽에 다 들어가면 숨는다**(`visible: 'auto'`) — 그때 건수도 같이 사라진다.
 *   건수가 늘 보여야 하는 화면은 표 위에 따로 적는다(감사 이력의 «총 N건»).
 */
export const PAGER_INFO = '총 {2}건 · {0}/{1}쪽';
export const PAGER_INFO_MEMBERS = '총 {2}명 · {0}/{1}쪽';
