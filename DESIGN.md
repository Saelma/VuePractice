# 디자인 시스템

이 문서는 **화면을 어떻게 만들지**를 정한다. 설계·아키텍처는 [`ARCHITECTURE.md`](./ARCHITECTURE.md),
작업 방식은 [`docs/WORKING-AGREEMENTS.md`](./docs/WORKING-AGREEMENTS.md).

- **스택**: Vue 3 + Tailwind CSS 4 + DevExtreme 23.1 (`dx.light`)
- **원칙 한 줄**: *데이터를 늘어놓지 않고, **읽는 순서**를 만든다.*

---

## 1. 왜 손대는가 (현재 진단)

지금 화면은 동작하지만 **데이터 나열**에 가깝다. 구체적으로:

| 증상 | 예 |
|---|---|
| 고객 화면인데 운영 툴처럼 보임 | 상품 목록이 `DxDataGrid`(표) — 이미지·가격·별점이 셀 안에 갇힘 |
| 위계가 없음 | 제목·라벨·값이 비슷한 크기·색. 눈이 어디부터 볼지 모름 |
| 토큰 없음 | 색·간격·radius를 화면마다 즉흥적으로(`slate-500`, `rounded`, `p-6` …) |
| 상태가 성의 없음 | 로딩 = "불러오는 중…", 빈 목록 = 회색 한 줄, 에러 = 빨간 박스 |
| 여백이 빡빡 | 카드 안팎 간격이 좁아 정보가 붙어 보임 |

→ 고칠 것은 **①토큰 ②위계 ③여백 ④상태 표현 ⑤고객/운영 화면 분리**다.

---

## 2. 지향점

**절제된 커머스 (잘크·미니멀).** 화려한 장식 대신, 여백과 타이포로 위계를 만든다.

- **강조색은 "거의 검정"** — 채도 있는 브랜드 컬러를 쓰지 않는다. 색을 빼면 남는 건 이미지·타이포·여백이고,
  그게 프로토타입 티를 지우는 가장 확실한 방법이다. 초록/주황/빨강은 **상태 표시에만** 쓴다.
- **거의 무채색 UI + 강조는 행동(CTA)에만.** 링크마다 파란색을 칠하지 않는다.
- **콘텐츠가 주인공.** 상품 이미지·이름·가격이 가장 먼저 읽히게.
- **얇은 경계, 옅은 그림자.** 두꺼운 보더나 진한 그림자는 낡아 보인다.
- **움직임은 거들 뿐.** 150~200ms 정도의 hover/focus 전환까지만.

### ⚠ 제약: 웹폰트는 CDN 금지 — 셀프호스팅만
운영 서버가 **LAN 내부**라 Google Fonts 같은 외부 CDN이 안 뜬다(뜨더라도 느리다).
→ 웹폰트는 **파일을 저장소에 넣어 셀프 호스팅**한다. **Pretendard 적용됨**(2026-07-28):
`src/assets/fonts/PretendardVariable.woff2`(Variable, 전 weight)를 번들이 `/static` 으로 fingerprint 해 emit,
`@font-face`(`src/index.css`)로 등록하고 `--font-sans` 최우선에 둔다. 없으면 시스템 스택으로 폴백.
⚠ **절대 public 경로(`/fonts/..`)로 참조하지 말 것** — rspack CSS 로더가 모듈로 resolve 하려다 빌드가 깨진다.
OFL 라이선스는 `public/fonts/OFL.txt`(공개 서빙)에 동봉.

### 레퍼런스 (벤치마크) — 왜 이 방향인가

위 "절제된 미니멀"을 아무 트렌디한 걸로 채우면 안 된다. **우리는 다품목 식품 카탈로그 커머스**(단일상품
DTC 랜딩이 아님 — 카테고리·옵션·검색·필터·쿠폰·적립금·리뷰가 있다). 그래서 **화려한 스낵 DTC 브랜드**
(Magic Spoon·Cob 류, 원색+굵은 폰트)는 우리 미학과 정반대라 **제외**하고, 아래를 앵커로 삼는다.
⚠ **패턴만 참고**한다 — 외부로 아무것도 보내지 않고 이미지·아이콘·서체는 우리 자산으로 재현(메모리 규약).
⚠ **남의 카피·문구를 원문 그대로 옮겨 적지 않는다**(2026-07-29 정정). 레퍼런스에서 가져올 것은
**패턴**이지 그 브랜드가 쓴 문장이 아니다 — 원문을 적으면 저장소가 남의 창작물을 실어 나르게 되고,
공개 저장소면 더 그렇다. **무엇을 하고 있는지 우리 말로 요약**하면 정보는 그대로 남는다.

- **미학 앵커 = MUJI** — 페이퍼화이트 + 흑연잉크 + 단 하나의 강조색(빨강, 그마저 1px 보더·작은 라벨에만),
  주요 버튼은 무채색. 위 지향점과 사실상 동일하고 MUJI 자체가 식품 포함 다품목 카탈로그라 구조도 맞는다.
  **Everlane**은 균일 정사각형 카드 + hover 이미지 5% 확대의 교과서.
- **구조 레퍼런스**(한국 식품 커머스 맥락):

| 화면 | 벤치마크 | 훔칠 패턴 |
|---|---|---|
| 상품 목록 | 29CM · 무신사 | 상단 필터 바 + 균일 카드(이미지·이름·가격·별점). 절제된 여백·타이포 위계 |
| 상품 상세 | 마켓컬리 · 무신사 | 큰 이미지 ↔ 우측 구매 박스, 아래로 리뷰 중심 긴 스크롤 + 스티키 서브탭 |
| 장바구니·주문서 | 마켓컬리 | 항목 + 우측 고정 요약(합계·배송비·무료배송까지·쿠폰·적립금) |
| 마이/혜택 | 마켓컬리 · 쿠팡 | 적립금·등급·쿠폰을 혜택 허브로 묶기 |
| 관리자 | Shopify admin · 토스 대시보드 | 밀도 있는 표(DataGrid) 유지 + 셸만 토큰(§6·§7) |
| 혜택 노출 · 카드 카피 | **8fter(엣프터)** | 아래 참조 — 신규가입 혜택을 **헤더/히어로에 전면 노출**, 상품 카드에 **한 줄 태그라인** |
| 모션(마이크로 인터랙션) | **토스** | 누르는 순간 **살짝 눌리고**, 끝나면 **조용히 확정된다**. 색으로 축하하지 않고 **버튼 자체가 상태를 말한다**(2026-08-13, G-8 — §5 「모션」) |

#### 8fter (엣프터) — 추가 레퍼런스 (2026-07-29, 사용자 제안)

`8fter.co.kr` — **Cafe24 기반 국내 홈웨어·이너웨어 브랜드샵.** 위 앵커들이 대형 플랫폼(무신사·컬리)이나
해외 브랜드인 데 비해, 이건 **우리와 규모·구조가 가장 비슷한 실전 커머스**다(카테고리·정렬·페이지네이션·
리뷰·쿠폰이 전부 있는 단일 브랜드샵). 그래서 "대형 플랫폼이라 되는 것"과 "작은 샵도 하는 것"을 갈라 볼 수 있다.

**훔칠 것 — 지금 우리에게 없는 것부터** (2026-07-29 **3건 전부 적용 완료**)

1. ✅ **신규가입 혜택을 첫 화면에 박는다** (적용 2026-07-29 — 핸드오프 §7-2). 8fter 는 **가입 혜택(쿠폰·무료배송)을
   헤더/히어로에 상시 노출**한다 — 로그인 전에도 "가입하면 뭐가 좋은지"가 첫 화면에서 읽힌다. ⚠ **우리는 쿠폰·적립금·무료배송 기준이 다 있는데 비로그인 첫 화면에서 그 사실을
   전혀 알리지 않는다** — 혜택 허브(`/benefits`)는 로그인 후에만 보인다. **기능이 아니라 노출의 문제**다.
2. ✅ **상품 카드의 한 줄 태그라인** (적용 2026-07-29, V33 — 핸드오프 §8). 카드에 **한글명 + 영문명 + 제품 특징을
   한 줄로 요약한 설명**이 함께 온다(착용감·용도 같은 "왜 좋은가"를 목록에서 바로 읽히게). 우리 `ProductCard` 는 이름·가격·별점뿐이라 **무엇이 좋은 상품인지 목록에서 안 읽힌다.**
   ⚠ `description` 을 잘라 쓰지 않고 **별도 필드**(`product.tagline`, 100자)로 만들었다 — 설명은 문단이라
   앞 N자를 자르면 문장이 끊긴다. **카드용 한 줄과 상세 설명은 목적이 다른 글**이다.
   화면 규칙: 없으면 줄을 그리지 않는다(`v-if`). ⚠ 단 **그것만으로는 카드 높이가 안 맞는다** —
   줄을 빼는 것도 높이를 바꾼다. 높이는 §5 「카드」의 구조 규칙(`h-full` + `mt-auto` + 배지 오버레이)이 맡는다.
3. ✅ **정렬 옵션의 실용적 조합** (리뷰 많은순 적용 2026-07-29 — 핸드오프 §7-1) — 신상품·낮은가격·높은가격·인기상품·**사용후기**. 특히 *리뷰 수 정렬*은
   우리에게 없고, `review_count` 비정규화가 이미 있어(2026-07-20 `RatingSyncHandler`) **거의 공짜로 붙는다.**

**안 가져올 것 (근거를 남긴다)**

- **컬렉션/드롭 중심 홈 서사**(시즌 컬렉션 이름을 건 섹션으로 홈을 구성하는 방식)는 **패션 브랜드의 문법**이다.
  우리는 다품목 카탈로그라 시즌 서사가 아니라 **탐색(카테고리·검색·필터)** 이 홈의 일이다.
- 룩북·브랜드 스토리 페이지도 같은 이유로 제외.

> ✅ **적용 시 지킨 것(2026-07-29)**: 혜택 스트립은 **무채색 원칙**을 지켰다 — 배경은 캔버스,
> 강조는 숫자(near-black)와 CTA 하나뿐. 그리고 **숫자를 화면에 적지 않고 공개 정책 API 에서 받는다**
> (`/api/policy/shipping`·`/api/policy/grades`) — 정책이 바뀌면 안내도 따라 바뀌게.
> ⚠ **쿠폰은 광고하지 않았다** — 8fter 는 가입 쿠폰을 내걸지만 우리 쿠폰은 관리자 지정 발급만이라
> 가입해도 안 생긴다. **없는 혜택은 적지 않는다.**

> ⚠ **미학(색·여백·타이포)은 이 문서에 적지 않았다.** 페이지 구조·문구는 확인했지만 **시각적 톤은 실제로
> 눈으로 봐야** 판단할 수 있고, 근거 없이 "미니멀하다"고 적으면 §2 앵커(MUJI)와 충돌하는지도 알 수 없다.
> 미학까지 앵커로 삼으려면 화면을 보고 **무엇이 좋았는지 한 줄** 남길 것 — 그때 이 절을 보강한다.

> 이 방향으로 2026-07-28 고객·관리자 화면을 전면 정리했다(적용 로드맵 §9). 출처: MUJI Design System
> (shadcn.io/design/muji)·Everlane(ecomm.design/site/everlane)·29CM(29cm.co.kr)·**8fter(8fter.co.kr)**·Baymard 등.

---

## 3. 디자인 토큰

Tailwind 4의 CSS-first 설정(`@theme`)에 정의한다. **화면에서 임의 색·간격을 쓰지 않고 토큰만 쓴다.**

> 아래 토큰은 `src/index.css`에 실제로 들어가 있다. Tailwind 4는 이 네임스페이스에서
> `bg-brand-600`·`text-ink-900`·`border-line`·`rounded-card`·`shadow-card` 같은 유틸리티를 자동 생성한다.

```css
/* src/index.css */
@theme {
  /* 강조 — 순수 무채색 near-black(파란기 없음, 무신사식 검/흰). CTA·활성·포커스에만.
     ink 그레이와 같은 계열이라 전체 톤이 통일된다(2026-07-28: 기존 gray-800/900 의 남색 틴트 제거). */
  --color-brand-50:  #f5f5f5;
  --color-brand-100: #e5e5e5;
  --color-brand-500: #262626;
  --color-brand-600: #171717;   /* 기본 CTA — 순수 near-black */
  --color-brand-700: #404040;   /* hover — 검정 버튼은 밝아지는 쪽이 자연스럽다(=ink-700) */

  /* 중립 — 대부분의 UI는 여기서 나온다 (순수 회색) */
  --color-ink-900: #0a0a0a;     /* 제목 */
  --color-ink-700: #404040;     /* 본문 */
  --color-ink-500: #737373;     /* 보조 설명 */
  --color-ink-400: #a3a3a3;     /* 비활성·플레이스홀더 */
  --color-line:    #e5e5e5;     /* 경계선 */
  --color-surface: #ffffff;     /* 카드 */
  --color-canvas:  #fafafa;     /* 페이지 배경 */

  /* 의미색 — 상태 표시 전용(장식으로 쓰지 않는다) */
  --color-success: #059669;
  --color-warning: #d97706;
  --color-danger:  #dc2626;

  /* 라운드 — 카드는 넉넉하게, 컨트롤은 절제 */
  --radius-card: 12px;
  --radius-control: 8px;

  /* 그림자 — 아주 옅게. 떠 있는 느낌만. 색도 순수 검정(slate 파란기 제거) */
  --shadow-card: 0 1px 2px rgb(0 0 0 / 0.05);
  --shadow-lift: 0 4px 12px rgb(0 0 0 / 0.08);

  /* 폰트 — Pretendard(셀프호스팅) 우선, 폴백은 시스템 스택(§2 참고) */
  --font-sans: "Pretendard Variable", -apple-system, BlinkMacSystemFont, "Segoe UI", "Pretendard",
               "Apple SD Gothic Neo", "Malgun Gothic", "Noto Sans KR", sans-serif;
}
```

### 타이포 스케일
숫자를 외우지 말고 **역할**로 고른다.

| 역할 | 클래스 | 용도 |
|---|---|---|
| 페이지 제목 | `text-2xl font-bold tracking-tight text-ink-900` | 화면당 1개 |
| 섹션 제목 | `text-lg font-semibold text-ink-900` | 카드/구획 머리 |
| 본문 | `text-sm text-ink-700` | 설명·내용 |
| 보조 | `text-xs text-ink-500` | 라벨·메타(날짜·작성자) |
| 가격/수치 | `text-lg font-semibold text-ink-900` (`tabular-nums`) | 정렬되게 |

> 한글은 같은 크기에서 라틴보다 커 보인다. **본문은 `text-sm`이 기본**이고 `text-base`는 강조용.

### 간격 리듬
- 페이지 여백: `p-6` (모바일 `p-4`)
- 카드 내부: `p-5`
- 섹션 사이: `mt-8`
- 관련 요소 사이: `gap-2`, 그룹 사이: `gap-4`

---

## 4. 레이아웃

```
┌ header (sticky, 높이 56px, 하단 1px line) ────────────┐
├ page  (max-w-6xl mx-auto, p-6) ───────────────────────┤
│   h1 제목 + 우측 액션                                  │
│   ── 필터/툴바 (있으면)                                │
│   ── 콘텐츠 (카드 그리드 또는 그리드/표)                │
└───────────────────────────────────────────────────────┘
```

- **최대폭**: 일반 `max-w-7xl`(1280px — 넓은 모니터의 양옆 휑함을 줄인다, 2026-07-28), 읽는 화면(상세·폼)
  `max-w-3xl`. 헤더·메인·푸터가 모두 같은 폭으로 정렬돼야 한다(App.vue).
- **헤더는 sticky** — 목록을 스크롤해도 이동이 항상 닿는다.

---

## 5. 컴포넌트 패턴

> **공용 클래스로 정의돼 있다** (`src/index.css`). 화면마다 유틸리티를 즉흥 조합하지 말고 이걸 쓴다:
> `page`·`page-narrow`·`page-title`·`section-title`·`muted` / `card`·`card-pad`·`card-link` /
> `btn`+`btn-primary|btn-secondary|btn-ghost|btn-danger` / `badge`+`badge-neutral|success|warning|danger` /
> `field`·`field-label`·`field-error` / `alert-error`·`alert-success` / `skeleton`
> (버튼·배지는 **베이스와 변형을 함께** 쓴다: `class="btn btn-primary"`)

### 버튼
| 종류 | 스타일 | 언제 |
|---|---|---|
| Primary | `bg-brand-600 text-white hover:bg-brand-700` | 화면당 **1개**의 주 행동 |
| Secondary | `border border-line bg-surface text-ink-700 hover:bg-canvas` | 보조 행동 |
| Ghost | `text-ink-500 hover:text-ink-900` | 목록 내 소소한 행동(수정·삭제) |
| Danger | `text-danger hover:bg-red-50` | 파괴적 행동 |

공통: `rounded-control px-3 py-2 text-sm font-medium transition-colors`
포커스: `focus-visible:outline-2 focus-visible:outline-brand-600 focus-visible:outline-offset-2`

### 카드
```html
<article class="rounded-card border border-line bg-surface p-5 shadow-card">
```
클릭 가능한 카드면 `transition hover:shadow-lift hover:-translate-y-0.5`.

#### ⚠ 그리드에 늘어놓는 카드는 **높이를 구조로 고정**한다 (2026-07-29)

카드 안에 **조건부 요소**(배지·태그라인·취소선…)가 하나라도 있으면 카드마다 세로 길이가 달라져
가격·별점 줄이 들쭉날쭉해진다. **조건부 렌더링을 어느 쪽으로 바꿔도 해결되지 않는다** —
줄을 그리면 늘어나고, 안 그리면 줄어들 뿐이다.

```html
<!-- 그리드 자식은 기본이 stretch — 카드가 h-full 이어야 그 높이를 받는다 -->
<div class="relative h-full">
  <button class="flex h-full w-full flex-col …">
    <div class="relative aspect-square shrink-0 …">
      <!-- 상태 배지는 여기(이미지 위 오버레이) — 텍스트 흐름에 넣지 않는다 -->
    </div>
    <div class="flex flex-1 flex-col p-4">
      … 조건부 줄들 …
      <div class="mt-auto …">가격 · 별점</div>   <!-- 항상 바닥 -->
    </div>
  </button>
</div>
```

세 가지가 세트다: **①카드 `h-full flex-col` ②기준선이 될 줄에 `mt-auto` ③상태 배지는 오버레이로.**

> **사고**: 상품 카드에 한 줄 태그라인을 넣으며 *"빈 줄을 남기면 높이가 달라지니 `v-if` 로 줄을 없앤다"*
> 고 적었는데 **줄을 빼는 것도 높이를 바꾼다.** 게다가 원인이 셋이었다 — 태그라인·품절 배지·정가
> 취소선. 뒤 둘은 그 전부터 있었으니 **품절 상품이 섞인 목록은 이미 어긋나 있었다.**
> 사용자가 목록을 눈으로 보고 *"반팔티 < 몽쉘10개 < 몽쉘 순으로 길어진다"* 고 알려 줘서 드러났다.
> ⚠ **정렬은 자동 테스트로 안 잡힌다**(jsdom 에 레이아웃이 없다) — 구조가 지켜지는지만 테스트로 고정하고,
> 실제 정렬은 **화면으로 확인**한다.

### 상태 배지 (주문 상태·문의 상태 등)
`rounded-full px-2 py-0.5 text-xs font-medium` + 의미색의 **연한 배경 + 진한 글자**
(예: 결제완료 `bg-emerald-50 text-emerald-700`). **채도 높은 단색 배경을 쓰지 않는다.**

### 빈 상태 (Empty)
회색 한 줄로 끝내지 않는다. **아이콘/이모지 + 한 줄 설명 + (가능하면) 행동**.
```html
<div class="flex flex-col items-center gap-2 py-12 text-center">
  <span class="text-3xl">🗂️</span>
  <p class="text-sm text-ink-500">아직 상품이 없어요.</p>
  <button …>상품 등록</button>
</div>
```
> 문구는 **상황에 맞게**. 7/20 교훈: 기본 필터 때문에 비어 있는 것과 사용자가 조건을 건 결과는 다른 문구를 쓴다.

### 로딩
"불러오는 중…" 텍스트 대신 **스켈레톤**(`animate-pulse` + 회색 블록)으로 레이아웃을 미리 잡는다.
목록은 카드 3~6개 분량의 스켈레톤.

### 폼
라벨은 `text-xs text-ink-500`, 필드는 세로 `gap-1`, 필드 그룹은 `gap-4`.

### 선택 입력 숫자 칸 — 「빈 값」으로 돌아갈 수 있어야 한다 (2026-08-13)

> **사고**: 상품 등록의 「정가(선택)」에 숫자를 한 번 넣으면 **지울 수가 없었다.** 입력기가 빈 입력을
> **0 으로 되돌리는데**, 0 은 저장 검증(*"정가는 판매가보다 커야 합니다"*)에 걸린다.
> → 🔴 **저장도 안 되고 비울 수도 없어서, 그 상품을 영영 저장 못 하는 상태**가 됐다.

**필수 칸에는 없는 문제다** — 0 이 유효한 값이거나(재고) 어차피 채워야 하니까(판매가).
**선택 칸에서만** 「없음」이라는 상태가 따로 필요하고, 숫자 입력기는 그걸 잘 표현하지 못한다.

지킬 것 셋:

1. **클리어 버튼을 단다**(`:show-clear-button="true"`). `ProductListView` 의 가격 필터가 선례다.
2. 🔴 **그래도 안 비워지면 「뜻을 넓힌다」** — 0 을 「없음」으로 읽고, **화면이 그렇게 말한다**
   (*"비우거나 0 이면 할인 없음"*). ⚠ **0 이 다른 뜻을 가질 수 없는 칸에서만** 쓸 수 있는 수다
   (정가 0원은 어떤 판매가에도 유효하지 않다). 재고 0 은 **품절**이라 이 수를 쓰면 안 된다.
3. ⚠ **「없음」의 저장 표현은 하나로 둔다** — 화면이 0 을 받아 주더라도 **서버로는 `null` 만** 보낸다.
   두 가지 철자를 허용하면 «0 인 행» 과 «null 인 행» 이 갈리고, 조회하는 쪽이 매번 둘 다 봐야 한다.

⚠ **원인은 못 찾았다.** `:min="0"` 을 떼 봤지만 그대로였다(2026-08-13 실측) — 입력기 내부를 더
파는 대신 위 2번으로 우회했다. **다른 선택 숫자 칸에도 같은 덫이 있을 수 있다.**

### 모션 (마이크로 인터랙션) — 2026-08-13 신설

> 🔴 **그전까지 이 제품에 연출용 모션은 하나도 없었다** — `transition-colors`, 카드 hover
> (`-translate-y-0.5`), 스켈레톤(`animate-pulse`)이 전부였다(실측). 이벤트 쿠폰 「받기」(G-8)가
> 첫 사례라, 여기서 정한 것이 **기준**이다. 구현은 `components/EventCouponBanner.vue`.

**앵커는 토스**(§2 레퍼런스) — *누르는 순간 살짝 눌리고, 끝나면 조용히 확정된다.* 크기는
**1~2px · 150ms** 수준으로 절제한다. 컨페티·색 폭죽은 무채색 원칙을 정면으로 깬다.

| # | 규칙 | 왜 |
|---|---|---|
| 1 | 연출은 기다리는 시간을 **설명**하는 것이지 **만드는** 것이 아니다 | 서버가 80ms 에 답하는데 연출이 800ms 면 사용자는 **720ms 를 기다린 것**이다. 응답을 기다리는 동안 돌고, 오면 완료로 전이한다. 「기대감」 몫으로 **최소 300~600ms** 는 정당하되 **그 이상 늘리지 않는다** ⚠ 같은 사람이 반복해서 누른다 — 두 번째부터 길게 느껴진다 |
| 2 | **실패 경로에도 모션이 있다** | 성공만 만들면 실패가 **「멈춘 것」처럼** 보인다. 되돌아오는 모션 + 이유 문구를 함께 |
| 3 | **연타를 잠근다** | 서버가 제약으로 막더라도 화면이 「완료」를 두 번 띄우면 **그게 거짓말**이다 |
| 4 | `prefers-reduced-motion: reduce` 면 **즉시 전환** | 상태는 라벨·색으로 그대로 전달되므로 연출만 건너뛰면 된다 |
| 5 | 확정은 **성공색이 아니라 「가라앉음」**으로 말한다 | 강조색은 CTA·상태에만(§2). 다 누른 버튼은 `ink-500` 으로 내려앉아 «더 누를 것이 없다»를 뜻한다 |

⚠ **버튼 자체가 상태를 바꾼다**(「받기」 → 스피너 → 체크가 그려지며 → 「받음」·비활성).
새 컴포넌트가 필요 없고 무채색 원칙과 맞는다. 🔴 **아이콘만으로 말하지 않는다** — 라벨이
함께 바뀐다(§8 접근성: 색·모양으로만 상태를 알리지 않는다).
에러는 필드 **바로 아래** `text-xs text-danger`. 상단 빨간 박스는 서버/전역 에러에만.

---

## 6. DevExtreme과의 공존 (중요)

DevExtreme은 **자체 테마(dx.light)** 를 들고 온다. Tailwind로 만든 셸과 색·radius가 어긋나면 그게 제일 촌스럽다.

**방침**
1. **운영(관리자) 화면은 DevExtreme 중심** — `DxDataGrid`의 정렬·페이징·필터는 직접 만들 이유가 없다.
2. **고객 화면은 Tailwind 중심** — 상품 목록·상세는 표가 아니라 카드로. DX는 입력 컨트롤 정도만.
3. **접점은 CSS 변수로 맞춘다** — DX의 accent/radius를 우리 토큰에 맞춰 덮어쓴다.

```css
/* dx.light 위에 우리 토큰을 덮어쓴다 */
.dx-widget { font-family: var(--font-sans); }
.dx-button-mode-contained.dx-button-default { background: var(--color-brand-600); }
.dx-texteditor, .dx-button { border-radius: var(--radius-control); }
.dx-datagrid { border-color: var(--color-line); }
```
> DX 내부 클래스를 과하게 파고들지 않는다. **색·radius·폰트 정도만** 맞추고, 나머지는 DX 기본을 존중한다.

---

## 7. 화면별 방침

| 화면 | 방향 |
|---|---|
| 상품 목록 | **DataGrid → 카드 그리드**(이미지·이름·가격·별점). 필터는 상단 바. 이게 "모던"의 체감 1순위 |
| 상품 상세 | 이미지 갤러리(큰 이미지 + 썸네일) ↔ 정보 2단. 구매 액션을 눈에 띄게. **연관 상품은 맨 아래**(B-23) — 리뷰·문의를 다 보고 *"그래서 다른 건 뭐가 있지"* 가 나오는 자리다. 위에 두면 상품 정보를 읽기도 전에 **이탈 경로를 먼저** 보여주는 셈. ⚠ **카드는 공유 `ProductCard` 를 쓴다** — 직접 그리면 높이 고정·할인·품절 규칙이 갈려 **같은 상품이 화면마다 다르게** 보인다 |
| 리뷰·문의 | 카드 리스트. 작성 폼은 접었다 펴기. **정렬·필터 줄**은 목록 위 한 줄(좌: 필터 체크박스 / 우: 정렬 select, 네이티브 컨트롤 + 토큰 색만 — §6 의 예외). ⚠ **리뷰가 0건이면 컨트롤을 감추지만, 필터로 걸러 0건이 될 때는 감추지 않는다**(필터를 풀 수 있어야 한다). ⚠ **빈 상태 문구를 필터 여부로 가른다**(7/20 교훈). ⚠ **정렬·필터를 바꾸면 1페이지로 되돌린다** — 3페이지에서 정렬만 바꾸면 사용자가 길을 잃는다 |
| 장바구니·주문내역 | 요약 카드 + 항목 리스트. 합계는 우측 고정 요약 |
| 주문 상세(`/orders/{id}`) | 고객·관리자가 **같은 화면**을 쓴다(역할로 버튼만 갈린다) — 갈라 두면 «관리자가 본 것»과 «고객이 본 것»이 어긋난다. ⚠ **사유를 받는 조작은 `confirm` 이 아니라 인라인 폼**이다(발송=운송장 · 반품 요청 · 관리자 취소 · 반품 거절). `window.confirm` 은 값을 못 받으므로 **값이 필요한 순간 폼으로 바뀐다**가 규칙이다. 🔴 **되돌아가는 조작은 화면에서 사라진다 — 흔적을 남길 값이 필요하다**(2026-08-11): 반품을 거절하면 상태가 `DELIVERED` 로 **돌아가서**, 반품 카드의 렌더 조건(`RETURN_REQUESTED`·`RETURNED`)에서 빠져 **카드째 사라졌다.** 고객은 «반품을 요청한 적 없는 주문» 과 구분할 수 없었다. → 상태로 못 그리는 사건은 **그 사건이 있었음을 나타내는 컬럼**(`returnRejectedAt`)을 렌더 조건에 함께 넣는다. 🔴 **안내는 가리키는 곳이 있어야 안내다** — 알림에 *"자세한 내용은 주문 상세에서 확인해 주세요"* 라고 쓰려면 **그 상세에 실제로 내용이 있어야 한다.** 값이 없으면 문장을 비울 게 아니라 **값을 만든다**(그날 V47 로 사유를 받았다). ⚠ 관리자 입력이 **고객에게 그대로 전달**되는 폼(거절 사유)은 폼 안에 그 사실을 적는다 — 관리자가 내부 메모처럼 쓰면 안 된다 |
| 관리자(주문·카테고리) | **DataGrid 유지**(밀도가 미덕). 셸만 새 토큰 적용 |
| 관리자 홈(대시보드) | ⚠ **DataGrid 를 쓰지 않는 유일한 관리 화면**이다. 목록이 아니라 **「지금 처리할 것」을 고르는 자리**라 밀도보다 훑기 쉬움이 이긴다 → 요약 카드 + 짧은 목록. 배치는 답할 질문 순서대로 **①처리 대기 → ②재고 부족 목록 → ③매출 한 줄 → ④바로가기**. ⚠ **다른 화면이 이미 그리는 것을 다시 그리지 않는다**(매출은 `/admin/stats` 로 링크 — 같은 숫자를 두 화면이 그리면 어느 쪽이 최신인지 헷갈린다). ⚠ **못 읽은 값을 `0` 으로 그리지 않는다** — 0 은 "할 일이 없다"로 읽힌다. 실패한 항목은 숫자 대신 `—` 를 쓰고 무엇을 못 읽었는지 상단에 알린다. 여러 출처를 모으는 화면이라 **하나가 죽어도 나머지는 보여준다**(전부 실패로 처리하면 "갈 곳이 없다"는 원래 문제로 돌아간다) |
| 공지 | 목록은 간결한 리스트, 상세는 읽기 폭(`max-w-3xl`) |
| 혜택 안내 문구(홈 스트립·가입 화면) | **서버가 값을 줄 때만 노출한다.** 무료배송 기준·적립률·**가입 쿠폰**(G-2, 2026-07-31)을 화면에 적어 두지 않고 API 응답으로 받아 `v-if` 로 감싼다. ⚠ 이유는 두 가지다 — ①설정만 바꿔도 **안내가 거짓말이 된다**(배송비 정책은 설정, 가입 쿠폰은 켜고 끄는 기능) ②없는 혜택을 광고하면 **가입 직후 사용자가 속았다고 느낀다**. 값이 하나도 없으면 **스트립 자체를 감춘다**(빈 껍데기를 남기지 않는다) |
| 홈 이벤트 쿠폰 배너(G-8, 2026-08-13) | **로그인한 사람에게만** 보인다 — 비로그인에게 「받기」를 보여주면 누른 순간 로그인으로 튕기고, 돌아왔을 때 이미 받은 상태라 **화면이 두 번 말을 바꾼다.** 비로그인에게는 혜택 스트립에 **가입 유도 한 줄**로 따로 말한다(대상·문구·CTA 가 전부 다른 **별개 배너**). 🔴 **배너가 말하는 것은 둘이고 섞지 않는다**: 오늘이 이벤트 날이면 「받기」(발급은 누를 때만), 앞에 있으면 *「다음 이벤트 D-3」* — **예고는 행동을 요구하지 않는다.** 🔴 **비로그인 줄에는 예고를 적지 않는다** — *"곧 나온다"* 를 보고 가입해도 **그 날 다시 와야 받는다**. 안 오면 못 받고, 그러면 *"가입해서 혜택을 누려라"* 가 **어긋난 약속**이 된다(오늘 진행 중일 때만 띄운다 — 가입하면 그 자리에서 받으므로 참이다). ⚠ **줄 게 없으면 자리를 만들지 않는다** — *"예정된 이벤트가 없습니다"* 는 자리만 먹는다. ⚠ **D-day 를 화면에서 계산하지 않는다** — 서버가 KST 로 센 `daysUntil` 을 준다(브라우저 시간대로 세면 **어떤 사람에게만 D-1 이 D-2 로 보인다**. 삭제 대기 화면의 `purgeAt` 과 같은 규칙). ⚠ 「이미 받음」(`COUPON-409I`)은 **에러가 아니라 상태 확정**이다 — 다른 탭에서 받았을 때 정확히 이 답이 오므로, 토스트가 아니라 버튼을 「받음」으로 굳힌다. 모션 규칙은 §5 「모션」 |
| 관리자 쿠폰(`/admin/coupons`) | 🔴 **발급 창과 사용 기간은 다른 값이고, 목록에서도 갈라 적는다** — 한 줄에 섞으면 둘이 같은 값처럼 읽히고, 그 오해가 곧 *"받자마자 만료되는 쿠폰"* 을 만든다. 「발급 마감일」 칸이 **비어 있으면 상시 쿠폰**이라 그 칸 하나가 기능의 on/off 다(가입 쿠폰의 *"지정 안 함 = 꺼짐"* 과 같은 방식). ⚠ **사용 기한 「한 달」은 코드가 아니라 화면 기본값**이다 — 발급 마감일을 넣으면 사용 종료일을 한 달 뒤로 채워 주고, 다른 이벤트는 관리자가 고친다. 정책을 서버에 박으면 이벤트마다 다르게 갈 수 없고 컬럼도 는다. ⚠ **이미 입력해 둔 값은 덮어쓰지 않는다**(관리자가 정한 것을 화면이 되돌리면 안 된다). ⚠ 배지는 무채색 테두리로 — 「가입 쿠폰」과 「이벤트」가 나란히 붙는다 |
| 관리자 상품 등록·수정(`ProductFormView`) | 입력 순서가 **답할 순서**다: 이름·카피 → 카테고리·상태 → 가격 → **옵션·재고** → 이미지. ⚠ **「정가」는 선택 칸이고 「없음」이 기본값**이다 — 비우거나 **0** 이면 할인 없음이고, 넣으면 **판매가보다 커야** 한다(§5 「선택 입력 숫자 칸」의 사고가 난 자리). 🔴 **그 규칙은 서버도 막는다**(`PRODUCT-400L`, 2026-08-13) — 2026-08-13 까지 **화면에만** 있어서 API 로 부르면 뜻 없는 정가가 저장됐고, 그러면 상세·목록이 **할인이 아닌데 취소선을 그린다**. ⚠ **재고 이력 카드는 저장 버튼 아래 별도 카드**로 두고 **등록 화면엔 두지 않는다**(위 별도 행 참조) |
| **가격 표시** (공통 규약, G-5 · 2026-08-19) | 🔴 **값은 셋인데 화면 자리는 둘이다** — 정가(`listPrice`, 「원래 이 값어치」) · 판매가(`regularPrice`) · 세일가(`price`). **세일 중이면 「판매가 취소선 + 세일가」를 보이고 정가는 숨긴다**(사용자 결정) — 고객이 답해야 할 질문은 «지금 얼마나 싼가» 이고, 정가까지 함께 띄우면 셋이 뒤엉켜 아무것도 안 읽힌다. 세일이 아니면 지금까지대로 「정가 취소선 + 판매가」. ⚠ **그 판단을 화면마다 하지 않는다** — `api/product.js` 의 `strikePrice()` 한 곳이 «무엇을 그을지» 를 정하고 카드·상세·찜이 그걸 부른다(주문 상태 색을 한 곳에 모은 것과 같은 이유). 🔴 **「세일 중인가」는 `discountRate != null` 로만 판정한다** — `price < regularPrice` 로 유추하면 1원짜리 1% 할인처럼 **반올림으로 두 값이 같아지는 경우에 배지가 사라진다**(그때도 세일인 것은 맞다). 그리고 「지금」은 **서버 시계**로 답해야 할 질문이라 화면이 날짜를 비교하지 않는다. ⚠ **긋는 값과 배지의 조건이 다르다** — 두 값이 같으면 취소선은 안 긋고(같은 숫자에 줄을 그으면 고장으로 보인다) 배지는 띄운다. 🔴 **관리자 상품 폼은 `regularPrice` 를 읽는다** — `price` 를 읽으면 세일 중 저장에 **세일가가 원래 판매가로 굳고 할인이 두 번 먹는다.** 폼은 세일 중이라는 사실도 칸 아래에서 말해 준다(안 그러면 관리자가 그 칸을 고쳐 맞추려 든다) |
| **네이티브 입력 `.ipt`** (공통, 2026-08-19) | 네이티브 `<input>`/`<select>` 를 토큰에 맞추는 클래스. **`index.css` 에 있다.** 🔴 **2026-08-19 에 여기로 올렸다** — 그전엔 `CouponAdminView` 의 `<style scoped>` 에만 있었고, **같은 클래스를 쓰는 `StatsAdminView` 의 입력칸이 스타일 없이 그려지고 있었다**(scoped 라 안 먹는다). ⚠ 아무것도 안 깨지고 콘솔에도 안 남아, **세 번째 화면(기간 할인)이 같은 것을 필요로 할 때까지 안 보였다** — 08-13 §8 의 `border-ink-200`(정의 없는 토큰)과 같은 종류다. 🔴 **위 「관리자 기간 선택」 규약이 이 클래스를 전제로 한다** — 규약이 공용인데 스타일이 한 화면에 갇혀 있으면 다음 화면이 또 같은 자리를 밟는다 |
| 관리자 기간 할인(`/admin/products/:id/discounts`, G-5 · 2026-08-19) | **상품 폼과 화면을 나눴다** — ①등록 시점엔 상품 id 가 없어 붙일 대상이 없고 ②할인은 상품 저장과 **다른 트랜잭션**이라 한 폼에 섞으면 「저장을 안 눌렀는데 세일이 이미 걸린」 상태가 생긴다. 맨 위에 **지금 얼마에 팔리는지**(세일 전 → 지금)를 두고, 폼은 **만들 것을 금액까지 문장으로 되읽어 준다**(「8월 22일부터 8월 24일까지 20% — 10,000원이 8,000원이 됩니다」). 🔴 그 되읽기는 G-8 에서 발급 마감일을 선택 칸으로만 둬 **상시 쿠폰이 조용히 만들어진** 사고 이후의 규칙이다. ⚠ **반올림 식을 서버와 똑같이 쓴다** — 미리보기가 1원 틀리면 관리자는 저장한 뒤에야 안다. **지난 것·진행 중·예정을 시간순으로 모두** 보여준다(「지금 세일 중인가」만 보이면 **다음 세일을 언제 걸어야 겹치지 않는지**를 알 수 없고, 겹침 거절 400 을 만난 뒤에야 찾게 된다). `status` 는 **서버가 정한다.** 🔴 **프리셋 버튼을 일부러 안 뒀다** — 위 「관리자 기간 선택」 규약의 핵심은 «「오늘」이 브라우저 시계가 아니다» 인데 이 API 는 오늘을 안 준다. 프리셋을 만들면 `new Date()` 가 기준이 되어 자정 근처에서 갈린다. 게다가 매출 기간과 달리 세일 기간은 매번 달라 「지난 달」 같은 되풀이되는 답이 없다. 필요해지면 응답에 `today`(KST)를 실어 그때 붙인다. ⚠ 배지는 **진행 중만 강조**(`badge-danger`)하고 예정·종료는 무채색 — 지금 돈이 달라지고 있는 것은 하나뿐이고, 「예정」에 amber 를 쓰면 경고처럼 읽힌다 |
| **관리자 기간 선택** (공통 규약, B-26 · 2026-08-13) | 🔴 **네이티브 `<input type="date">` 2칸 + 프리셋 버튼**으로 통일한다. ⚠ **정하지 않고 두면 갈린다** — 착수 시점에 쿠폰은 네이티브 2개, 공지는 `DxDateBox` 로 이미 둘로 나뉘어 있었고, 여기에 `DxDateRangeBox` 를 더하면 셋이 된다. 근거: ①매출 화면에는 **DevExtreme 이 하나도 안 쓰였는데** DX 컨트롤을 넣으면 그 테마까지 따라 들어온다 ②브라우저 기본이 라벨·포커스·스크린리더·모바일 달력을 이미 갖췄다(§6 의 예외 규칙 — 가입 동의 체크박스·리뷰 정렬 select 와 같은 판단) ③번들이 안 는다. ⚠ **프리셋이 먼저다**(오늘·7일·30일·이번 달·지난 달) — 실무에서 묻는 것 대부분이 「지난 달」·「30일」이라 달력을 두 번 찍는 것보다 빠르다. 🔴 **화면은 날짜만 보내고 경계(그 날의 00:00 이 언제인가)는 서버가 만든다** — 화면이 계산하면 KST 경계가 **두 곳**에 생기고 **하루가 어긋나도 화면은 멀쩡해 보인다**(F-4 가 «지금은 정상» 인 이유가 경계가 한 곳이라서다). 🔴 **「오늘」의 기준도 브라우저 시계가 아니다** — 파라미터 없이 부른 첫 응답의 `to` 가 곧 KST 오늘이고, 프리셋은 그걸 기준으로 센다(장부는 KST 인데 `new Date()` 는 보는 사람의 시간대를 따른다). ⚠ **종료일은 포함**이다(사람이 「7월 1~15일」이라고 말할 때의 뜻). ⚠ **서버가 실제로 집계한 구간을 응답에 되돌려 주고, 제목은 그 값을 쓴다** — 내가 보낸 값으로 제목을 쓰면 기본값일 때 무엇을 보고 있는지 못 적는다 |
| 관리자 매출(`/admin/stats`, C-11 · 기간 선택 B-26) | 답할 질문 순서대로 **①얼마 벌었나(요약) → ②흐름(일별 추이) → ③뭐가 팔리나(TOP)**. 🔴 **기간을 따르는 값과 안 따르는 값을 화면에 갈라 적는다** — 「지난 달」을 골라 놓고 「오늘」 카드가 지난달 어느 날을 가리키면 거짓말이라, 고정 카드에는 **「기간과 무관」**을 쓴다. ⚠ **제목이 기간을 따라가야 한다** — 「최근 30일」로 굳어 있으면 지난달을 보면서 그렇게 읽는다(**숫자보다 제목이 거짓말하기 쉽다**). ⚠ **빈 상태를 둘로 가른다**(§5 · 7/20 교훈): 전체 매출이 0이면 「아직 결제된 주문이 없어요」, 고른 기간만 0이면 **「이 기간에는」** 이라고 말한다 — 막대가 전부 바닥인 차트는 그냥 두면 고장으로 읽힌다. ⚠ **상품매출과 배송비를 합치지 않는다**(배송비는 그대로 택배비로 나간다). ⚠ TOP 판매액은 **쿠폰 할인 전**이라 요약과 일부러 다르고, 화면에 그렇게 적는다. ⚠ **기간에 상한(366일)이 있고 넘으면 거절한다** — 일별은 빈 날을 채워서 주므로 2년이면 막대 730개가 되어 읽을 수 없다. 🔴 **조용히 자르지 않는다**(잘린 줄 모르면 「그 기간 매출이 이만큼」으로 잘못 읽는다) |
| 관리자 프로모션 달력(`/admin/coupons/calendar`, B-27, 2026-08-13) | **DevExtreme `Scheduler` 를 쓰지 않는다** — 그건 *사람이 시간을 예약하는* 도메인(상담·예약제)의 컴포넌트다. 필요한 건 **기간 막대를 월 격자에 얹는 것**이라 격자를 직접 그린다. 🔴 **막대를 둘로 가른다**(발급 창 = 진한 막대 · 사용 기간 = 연한 막대) — 한 색으로 그리면 관리자가 **정상인 사용 기간 겹침을 사고로 읽는다.** 겹치면 안 되는 것은 발급 창뿐이다. 🔴 **상시 쿠폰은 격자 밖 스트립으로 뺀다**(2026-08-13, 사용자 지적) — 사용 기간이 한 달을 꽉 채워 **격자를 가로줄로 덮고**, 위치가 없는 값이라 격자에 있어 봐야 정보가 아니다. ⚠ 다만 **종료일은 스트립에 남긴다**(상시 쿠폰에서 유일하게 날짜가 뜻을 가지는 값이라 그것까지 없애면 「언제 끝나나」를 어디서도 못 본다). ⚠ **격자에 그릴지는 서버가 준 `event` 플래그로 가른다** — 「발급 창 막대가 있나」로 유추하면 **발급 창이 지난달인 이벤트**가 상시로 잘못 분류된다. 🔴 **날짜를 화면에서 계산하지 않는다** — 서버가 KST 로 잘라 「이 달의 며칠부터 며칠까지」를 정수로 준다(요일 격자는 순수 달력 산수라 화면이 해도 안 흔들린다 — **흔들리는 쪽만** 서버가 맡는다). ⚠ **관리자 메뉴에 넣지 않는다** — 메뉴는 「지금 처리할 것」을 여는 자리인데 달력은 답이 아니라 **조회 도구**다(AdminMenu 주석의 「길게 만들지 말라」). 쿠폰 화면에서 링크로 들어온다. ⚠ **빈 달이 정상**이고, 빈 상태 판정은 **격자 기준(이벤트 유무)** 이다 — 상시만 있는 달에 「쿠폰이 없다」고 하면 바로 위 스트립과 어긋난다 |
| 약관·개인정보 처리방침 | `page-narrow` + `max-w-3xl` 읽기 폭(공지 상세와 같다). **두 문서는 컴포넌트 하나**가 라우트 이름으로 갈린다 — 골격이 같아 파일을 나누면 **한쪽만 고쳐져 생김새가 갈린다**(인증 화면 셋과 같은 판단). ⚠ **문안이 「학습용 예시」라는 고지를 화면 맨 위 배지로 둔다** — 법률 문서를 그럴듯하게 지어내면 **나중에 그대로 쓰일 위험**이 있어, 경고를 주석이 아니라 **화면 구조**에 남긴다(B-21, 2026-08-03). ⚠ **공개 라우트여야 한다** — 가입 화면에서 여는 문서라 로그인 뒤에 두면 동의하기 전에 읽을 수가 없다 |
| 가입 동의 박스 | 「전체 동의」 + 「[필수] …」 + 「[선택] …」 세 줄. ⚠ **전체 동의는 필수 ∧ 선택**이다 — 필수만으로 켜지게 하면 **선택 동의를 한 적 없는 사람에게 동의 표시가 뜬다.** 체크박스는 **DevExtreme 대신 네이티브**를 쓰고 `accent-color` 만 토큰에 맞춘다(§6 의 예외 — 브라우저 기본이 이미 라벨 클릭·포커스·스크린리더를 갖췄고, DX 로 바꾸면 그걸 다시 만들어야 한다). 약관 링크는 **새 탭**(`target="_blank"`)으로 — 같은 탭이면 입력하던 값이 날아간다 |
| 되돌릴 수 없는 조작 화면 (마케팅 발송 등) | 화면이 지는 책임이 셋이다: ①누르기 **전에 규모를 보여준다**(대상 수) ②**확인 단계**를 둔다(주 버튼 → *"정말 보낼까요?"* → `btn-danger`) ③보낸 **뒤에 결과를 나눠서** 보여준다. ⚠ **화면이 서버보다 확신하지 않는다** — 대상 수는 *동의자 수*이고 실제 발송은 그보다 적을 수 있어 **"최대 N명"** 으로 적는다(혜택 문구를 서버가 줄 때만 노출하는 것과 같은 규칙). ⚠ 결과 숫자를 **합치지 않는다** — *"모집단이 적어서"* 와 *"다들 꺼서"* 는 대응이 완전히 다르다. 보낸 뒤 **입력을 비운다**(같은 내용 재발송 사고 방지) |
| 관리자 상품 수정 안의 「재고 변경 이력」 | **읽는 것을 입력 폼에 섞지 않는다** — 저장 버튼 **아래 별도 카드**로 둔다(폼 안에 넣으면 "고쳐야 할 칸"으로 보인다). **등록 화면엔 두지 않는다**(상품이 아직 없어 물어볼 대상이 없다). ⚠ **비어 있는 게 정상인 화면**이다 — V39(2026-08-04) 이전 변동은 기록이 없어 기존 상품은 이력이 없다. 빈 상태 문구가 **"아직 없다"가 아니라 "그 이전은 기록되지 않았다"까지** 말해 줘야 고장으로 안 읽힌다. ⚠ **실패를 빈 목록으로 위장하지 않는다** — 0건으로 그리면 "변동이 없었다"로 읽힌다(대시보드에서 배운 것과 같은 규칙). ⚠ **지금 옵션 목록에 없는 이름이 나온다** — 이력은 옵션 id 가 아니라 옵션명으로 이어지므로 삭제된 옵션의 과거 줄도 그대로 보인다. **"모르는 옵션"이라고 감추면 이 기능을 만든 이유가 사라진다.** ⚠ 변동량은 **부호를 항상** 붙이고(`+3`/`-3`), 사유는 배지로 — 줄어드는 것(주문)은 주의색, 느는 것(취소·반품)은 성공색, 관리자 조작은 중립. ⚠ 서버 enum 에 값이 늘어도 **칸이 비지 않게** 모르는 사유는 원문을 그대로 보여준다(빈칸은 "사유가 없다"로 읽힌다) |
| 관리자 리뷰 관리(`/admin/reviews`) | **DataGrid 유지**(운영 화면, 밀도가 미덕). 필터는 표 위 카드 한 줄. ⚠ **삭제 버튼을 두지 않는다** — 되돌릴 수 있는 「숨김」만 둔다. 상태는 `숨김`/`노출` 배지로 보이고 행마다 토글 버튼이 붙는다. ⚠ **확인 대화에 «무엇이 함께 움직이는지»를 적는다** — 관리자가 "글 하나 감추는 것"으로만 알면 **상품 별점이 왜 변했는지 못 짚는다**(숨기면 평균 별점·리뷰 수에서 빠진다). ⚠ **낮은 별점을 눈에 띄게**(1~2점 danger · 3점 warning) — 대개 손볼 대상이 거기 있다. ⚠ **상품이 지워진 리뷰는 상품 칸에 「(지워진 상품)」**(2026-08-12) — 리뷰는 상품을 느슨한 UUID 로만 가리켜 **상품을 지워도 리뷰는 남는다.** 빈칸으로 두면 «데이터가 잘못됐다»로 읽힌다(재고 이력에서 배운 것과 같다). **목록에서 빼지 않는다** — 빠지면 관리자는 **고칠 대상이 있다는 사실 자체를** 모른다. 🔴 **문구는 문의 관리와 같은 것을 쓴다**(`constants/labels.js` 의 `DELETED_PRODUCT` 하나) — 나란히 쓰는 화면이라 *"지워진"* 과 *"삭제된"* 이 갈리면 관리자는 **다른 상태**로 읽는다. ⚠ 다만 **판정은 화면마다 다르다**: 문의는 유형까지 봐야 하고(일반 문의도 이름이 빈다), 리뷰는 **서버가 `productDeleted` 로 알려준다**(이름이 빌 이유가 삭제 하나뿐이라 서버가 답할 수 있다). ⚠ **실패를 빈 목록으로 위장하지 않는다**(0건은 "볼 리뷰가 없다"로 읽힌다) |
| 관리자 삭제 대기 상품(`/admin/products/trash`) | **DataGrid 를 쓰지 않는다** — 줄이 몇 개뿐이고 정렬·필터가 필요 없다(유예가 7일이라 길어질 구조가 아니다). 카드 리스트로 두고 **한 줄에 셋만** 담는다: 무엇이 · 언제 사라지나 · 누가 지웠나. 🔴 **「남은 기간」이 이 화면의 요점이다** — 목록만 보여주면 관리자는 *"언제까지 되돌릴 수 있나"* 를 모르고, 그걸 모르면 **유예가 있으나 마나다.** 그래서 **D-day 를 배지로** 세우고 **하루 남으면 `badge-danger`** 로 바꾼다. ⚠ **D-0 을 쓰지 않는다** — 읽는 사람마다 «오늘까지» 와 «오늘 사라짐» 으로 갈린다. 하루 미만은 **「곧 사라짐」**이다. 🔴 **날짜 계산을 화면에서 하지 않는다** — 서버가 `purgeAt` 을 준다. 여기서 `deletedAt + 7일` 을 더하면 **유예 설정을 바꿨을 때 화면만 낡는다**(재고 부족이 임계값을 서버에서 받는 것과 같은 규칙). ⚠ **빈 상태가 정상인 화면**이다 — 「삭제 대기 중인 상품이 없습니다」에 *"삭제하면 여기 머물다 사라진다"* 까지 적어야 **고장으로 안 읽힌다**(재고 이력 카드에서 배운 것과 같다). ⚠ **실패를 빈 목록으로 위장하지 않는다**(0건은 «되돌릴 것이 없다»로 읽힌다). ⚠ 복구 확인 대화에 **«무엇이 함께 돌아오는지»** 를 적는다 — 목록·검색뿐 아니라 **장바구니 줄도 다시 살아난다**(리뷰 숨김 대화와 같은 규칙) |
| 관리자 문의 관리(`/admin/inquiries`) | **DataGrid 유지** + 필터 카드 한 줄(리뷰 관리와 같은 골격 — 나란히 쓰는 화면이라 생김새가 갈리면 안 된다). ⚠ **기본 필터가 「답변대기」다** — 이 화면을 여는 이유가 *"답할 게 뭐가 남았나"* 라서다. 「전체」도 고를 수 있어야 한다(선택지의 「전체」는 값이 **`null`** — 문자열이면 서버가 400 을 내고 화면엔 그게 "문의가 없다"로 보인다). 🔴 **비밀글 본문을 화면에서 가리지 않는다**(관리자는 열람 대상이다. 답을 쓰라면서 질문을 가릴 수 없다) — 대신 **「비밀」 배지**로 표시해 *답변이 공개되지 않는다*는 걸 알고 쓰게 한다. ⚠ **답변은 줄 안이 아니라 표 아래 카드**로 펼친다(본문·답변이 길어 셀 안에서 못 쓴다) — 원 문의(제목·본문)를 **함께 보여준 채로** 쓰게 한다. ⚠ **상품명이 비는 데는 뜻이 둘이라 화면이 갈라 준다**(2026-08-07): 유형이 `PRODUCT` 인데 이름이 없으면 **「(지워진 상품)」**, 일반 문의면 **`—`**. ⚠ **그 문구는 리뷰 관리와 공유한다**(2026-08-12, `constants/labels.js` 의 `DELETED_PRODUCT`) — 여기서만 고치면 두 화면이 갈린다. 둘 다 `—` 로 그리면 관리자가 *"상품이 지워졌나"* 를 매번 의심한다. **어느 쪽이든 목록에서 빼지 않는다**(빠지면 답할 대상이 있다는 걸 모른다). ⚠ 그래서 **유형 열이 필요하다** — 상품 칸이 빈 줄의 성격(배송·환불·기타)을 그 열이 답한다. ⚠ **실패를 빈 목록으로 위장하지 않는다**(0건은 "답할 문의가 없다"로 읽힌다) |
| 고객센터(`/support`) | **고객 화면이라 DataGrid 를 쓰지 않는다** — 문의하기 폼(위) + 내 문의 카드 리스트(아래) 한 화면. 🔴 **둘을 한 화면에 둔 것이 요점**이다: 「내 문의」는 편의가 아니라 **일반 문의의 주소**라 답변 알림이 `/support#inquiry-{id}` 로 착지한다. ⚠ **기본이 비밀글이다**(상품 문의는 공개 기본) — 고객센터 문의엔 주문번호·연락처가 들어가기 쉽다. ⚠ **상품 문의와 일반 문의를 가르지 않는다** — 사용자는 자기 질문이 어디에 달렸는지 기억하지 못한다. 대신 **줄마다 유형 배지**를 단다. ⚠ **유형 선택지에 「상품 문의」가 없다** — 그건 상품 페이지가 정하는 값이라, 넣으면 사용자가 **막다른 길**(400)을 만난다. ⚠ 알림으로 들어온 줄은 **잠깐 배경으로 강조**하고 그 줄까지 스크롤한다(`useAnchorScroll` — 위쪽 폼이 늦게 자라 한 번 계산하면 어긋난다). ⚠ **실패를 빈 목록으로 위장하지 않는다**(0건은 "문의한 적 없다"로 읽힌다) |
| 인증(로그인·아이디 찾기·비밀번호 재설정) | `page-narrow` + `max-w-sm` 카드 하나. 새 토큰·새 컴포넌트를 만들지 않는다 — 세 화면이 **같은 골격**이라 하나만 달라도 눈에 띈다. ⚠ **결과 문구는 단정하지 않는다**(*"보냈습니다"* 가 아니라 *"가입된 주소라면 보냈습니다"*) — 서버가 열거 방지로 응답을 똑같이 맞춰 놨는데 **화면 문구가 갈리면 거기서 새어 나간다**(G-1, 2026-07-31). 화면 사이 이동 링크를 서로 걸어 둔다(아이디를 잊은 사람이 재설정 화면에서 막히지 않게) |

---

## 8. 접근성 (최소선)

- 본문 대비 **4.5:1** 이상 — `ink-500`을 흰 배경 본문에 쓰지 않는다(보조 텍스트 전용).
- **포커스 링을 지우지 않는다**(`outline-none` 금지). `focus-visible`로 키보드일 때만 보이게.
- 아이콘만 있는 버튼엔 `aria-label` (예: ImageUploader의 삭제 버튼).
- 색으로만 상태를 알리지 않는다 — 배지에 **텍스트**를 함께 넣는다.

---

## 9. 적용 로드맵

한 번에 다 갈아엎지 않는다. **토큰 → 셸 → 고객 화면 → 운영 화면** 순.

1. **토큰 + 전역 CSS** (`index.css`) — 색·폰트·radius·그림자
2. **앱 셸** (`App.vue`) — sticky 헤더, 폭 제한, 내비 정리
3. **상품 목록 카드 그리드** — 체감이 가장 큰 화면
4. **상품 상세** — 갤러리 + 구매 액션
5. 리뷰·문의 카드, 장바구니·주문내역
6. 관리자 화면은 셸/토큰만 적용(그리드 구조 유지)
7. 공용 상태 컴포넌트(`EmptyState`, `Skeleton`)를 뽑아 재사용

> 각 단계는 **화면 하나씩** 올려 눈으로 확인하고 넘어간다(WORKING-AGREEMENTS §5 — 프론트는 하드 새로고침으로 확인).
